# v0.1 Alpha Goal

Status: Draft v0.1
Last updated: 2026-05-21
Scope: Short-term release goal and long-term product target for vibit

The paired Simplified Chinese translation is `docs/v0.1-alpha-goal.zh-CN.md`. The English file is authoritative.

## 1. Purpose

This document records the next product target for vibit:

```text
short_term_release_target: v0.1_alpha
long_term_product_target: ai_era_nakama_pitaya_class_server_framework
completed_work_item: W-0179
decision: ADR-0086
implementation_authorized_by_this_standard: roadmap_only
```

The immediate goal is not another internal proof-only milestone. The immediate goal is a first alpha release that real developers can download, run, inspect, and use as the starting point for contribution.

The long-term target is to become an AI-era Pitaya or Nakama: a serious open-source game/backend server framework in the same product class, adapted around vibit's agent-native maintainability model.

## 2. Current Status

vibit is pre-alpha.

The repository is no longer only in a constitutional design phase. It now has a Go runtime, WebSocket + Protobuf request handling, PostgreSQL persistence boundaries, authentication/token/session service behavior, login and logout protocol routes, runtime session metadata, first-message connection binding, active connection lifecycle primitives, and a server-owned presence lifecycle snapshot.

The current continuation queue is:

```text
current_milestone: M-130
next_work_item: W-0202
next_work_status: next_ready
next_direction: storage_objects_persistence_schema_gate
```

`W-0178`, the `W-0181` local onboarding/device credential issuance gate, the `W-0182` local onboarding implementation, the `W-0183` next-direction selection, the `W-0184` authenticated gameplay E2E proof, the `W-0185` runtime runbook refresh, the `W-0186` minimal request-loop script, the `W-0187` health/readiness/version/config surface, the `W-0188` alpha acceptance checklist, the `W-0189` alpha developer flow package, the `W-0190` release publishing decision gate, the `W-0191` release execution preparation gate, the `W-0192` release execution authorization gate, the `W-0193` release execution maintainer decision, the `W-0194` release identifier and artifact plan, the `W-0195` release execution final authorization, the `W-0196` first alpha user discovery loop, `W-0197` first alpha feedback intake surfaces, `W-0198` prototype-ready foundation execution plan, `W-0199` prototype-ready local development path gate, `W-0200` prototype-ready local development path package, and `W-0201` storage objects behavior gate are complete. The release identifier is `v0.1.0-alpha.1`, authorized as a source-first alpha release with Git tag, GitHub release record, release notes, and GitHub source archive only. Feedback intake is prepared through `.github/ISSUE_TEMPLATE/alpha-feedback.yml`, product maturity milestones are recorded in `docs/product-maturity-milestones.md`, the prototype-ready execution plan is recorded in `docs/prototype-ready-foundation-execution-plan.md`, the local development path gate is recorded in `docs/prototype-ready-local-development-path-gate.md`, the local development path package is recorded in `docs/prototype-ready-local-development-path-package.md`, the storage objects behavior gate is recorded in `docs/storage-objects-behavior-gate.md`, and the next work item is `W-0202 Define storage objects persistence schema gate`. This document changes the product destination, not the continuation mechanism.

## 3. v0.1 Alpha Definition

`v0.1 alpha` is the first developer-usable release of vibit.

It should be a single-node, PostgreSQL-backed, WebSocket + Protobuf game backend runtime that external developers can run locally and use to validate vibit's agent-native server framework direction.

The release is successful when a technically capable developer can:

- clone the repository,
- read a short README that explains what vibit is,
- start or verify the runtime with documented prerequisites,
- understand the current WebSocket + Protobuf protocol surface,
- create or obtain a first player credential through a documented local flow,
- authenticate with a device credential,
- receive an opaque access token and runtime session metadata,
- send at least one protected gameplay request,
- observe a basic presence query,
- logout,
- run the repository checks,
- understand what remains intentionally out of scope,
- and find a concrete path to contribute the next slice.

## 4. Must-Have Alpha Capabilities

The alpha should include:

- README and runbook instructions that are accurate enough for a new developer.
- A PostgreSQL migration path for the runtime tables used by the alpha flow.
- Local verifier key configuration instructions that avoid committing secrets.
- A player onboarding path: create player account, issue first device credential, show raw credential once, and store only digests.
- Device credential login through the protocol route.
- Opaque access-token validation for protected gameplay routes.
- Runtime session creation and response session metadata.
- First-message WebSocket connection binding.
- At least one protected gameplay route, using the inventory proof slice.
- Presence lifecycle and a protected presence query over the server-owned snapshot.
- Logout route.
- A minimal example client, script, or documented request loop.
- Health, readiness, version, or configuration-check surface sufficient for local troubleshooting.
- A concise alpha acceptance checklist.
- A packaged local alpha developer flow.
- A release publishing decision gate.
- A release execution preparation gate.
- A release execution authorization gate.

## 5. Preferred Near-Term Sequence

After this goal-setting slice, the recommended path is:

1. Complete `W-0178`: define and implement the protected presence protocol query functional slice. Completed.
2. Implement the first local onboarding/device credential issuance flow under the completed `W-0181` gate. Completed.
3. Prove an authenticated gameplay end-to-end path: onboarding -> login -> connection binding -> protected inventory -> presence query -> logout. Completed.
4. Refresh the runtime runbook around actual authentication, sessions, migrations, and local config. Completed.
5. Add a minimal example client or request-loop script. Completed.
6. Add a health/readiness/version/config check surface. Completed.
7. Add a `v0.1 alpha` acceptance check or equivalent checklist. Completed.
8. Package the alpha developer flow and document prerequisites. Completed.
9. Define the release publishing decision gate. Completed.
10. Define the release execution preparation gate. Completed.
11. Define the release execution authorization gate. Completed.
12. Confirm the release execution maintainer decision. Completed as go to release identifier and artifact planning.
13. Define the release identifier and artifact plan. Completed with proposed identifier `v0.1.0-alpha.1`.
14. Confirm release execution final authorization. Completed.
15. Define the first alpha user discovery loop. Completed.
16. Prepare first alpha feedback intake surfaces. Completed.
17. Define the prototype-ready foundation execution plan. Completed.
18. Define the prototype-ready local development path gate. Completed.
19. Implement the prototype-ready local development path package. Completed.
20. Define the first storage objects behavior gate. Completed.
21. Define the storage objects persistence schema gate. Next-ready.

This sequence may be adjusted by later ADRs, but future agents should prefer work that shortens the path to a runnable alpha over internal-only expansion.

## 6. v0.1 Alpha Non-Goals

The alpha does not need:

- Direct Nakama or Pitaya API compatibility.
- Distributed runtime, cluster mode, service discovery, or server-to-server RPC.
- Full chat, friends, groups, parties, leaderboards, tournaments, matchmaking, or match runtime.
- Full SDK matrix.
- Admin console or full operations dashboard.
- Production security hardening, SLA, or hosted deployment packaging.
- External identity providers, OAuth, OIDC, password login, or account recovery.
- Durable or distributed presence.

Non-goals do not mean these capabilities are unimportant. They mean the first alpha must stay narrow enough to finish.

## 7. Long-Term Product Target

The long-term target is:

```text
ai_era_nakama_pitaya_class_server_framework
```

This means vibit should become a serious open-source server framework in the same product class as Nakama and Pitaya, while keeping vibit's own design constraints:

- agent-native maintainability,
- explicit architecture manifests,
- contract-first behavior,
- generated repeatable structure,
- bounded change workflow,
- checkable repository state,
- strong transport/protocol/application/domain separation,
- and durable project memory through change specs, ADRs, and conversation logs.

Nakama remains the primary reference for broad game backend product coverage. Pitaya remains the primary reference for Go game server architecture vocabulary. vibit should match the class of useful capability, not accidentally copy public APIs, data models, route names, or clustering internals.

## 8. Agent Continuation Rules

When starting a future Codex session directly inside this repository:

1. Read `AGENTS.md`, `.arch/work-items.yaml`, and this document.
2. Run:

```bash
node tools/vibit inspect next
node tools/vibit check work --json
```

3. If the maintainer says `继续推进` or `continue`, advance exactly one next-ready work item unless blocked by an ask-first boundary or verification failure.
4. Treat `v0.1 alpha` as the short-term release target.
5. Treat AI-era Nakama/Pitaya-class capability as the long-term target.
6. Prefer `ADR-0082` gate-density rules: security-critical work keeps explicit gates; ordinary functional work should use one bounded slice with the boundary embedded in the change spec; lightweight docs/tooling may be direct when trivial.
7. Commit and push each verified continuation slice promptly.

## 9. Relationship To Existing Roadmaps

This document does not replace `docs/nakama-pitaya-product-parity-roadmap.md`.

The relationship is:

- `docs/v0.1-alpha-goal.md`: the first developer-usable release target.
- `docs/nakama-pitaya-product-parity-roadmap.md`: the long-term product-class capability map.
- `.arch/work-items.yaml`: the immediate continuation queue.
- `README.md`: the external entry point for users and contributors.

## 10. Non-Authorization

This document is roadmap-only. It does not by itself implement runtime behavior, add protocol messages, change generated output, alter migrations, introduce dependencies, publish a release, or declare the current repository production-ready.
