schema_version: 0.1
project: vibit
status: draft
standard: docs/workflow.md
current_milestone: M-186

release_targets:
  short_term:
    id: v0.1_alpha
    status: target
    standard: docs/v0.1-alpha-goal.md
    translation: docs/v0.1-alpha-goal.zh-CN.md
    decision: ADR-0086
    defined_by_work_item: W-0179
    goal: Make the first developer-usable alpha that external contributors can run, inspect, and build from.
    current_release_state: pitaya_aligned_cluster_safe_session_routing_source_first_map_implemented
    release_declared: true
    release_identifier: v0.1.0-alpha.1
    next_ready_work_item: W-0258
    preferred_after_current:
      - select_next_pitaya_aligned_direction_after_cluster_safe_session_routing_map
  long_term:
    id: ai_era_nakama_pitaya_class_server_framework
    standard: docs/nakama-pitaya-product-parity-roadmap.md
    maturity_standard: docs/product-maturity-milestones.md
    near_term_standard: docs/v0.1-alpha-goal.md
    goal: Become a Nakama-class open-source game/backend server framework optimized for AI-native development and AI-native testing, while deferring Pitaya as a future distributed architecture reference.
    primary_product_reference: Nakama
    pitaya_reference_status: deferred_future_architecture_reference
    ai_native_development_testing_goal: user_requirement_to_spec_tests_implementation_verification
    direct_api_compatibility_goal: false

continuation_semantics:
  default_step_unit: work_item
  one_step_request:
    phrases:
      - continue
      - 继续
    meaning: Advance one next_ready work item unless blocked or confirmation is required.
  multi_step_request:
    pattern: continue <n> steps
    meaning: Advance up to n next_ready work items in dependency order, stopping early if blocked or confirmation is required.
  stop_conditions:
    - no_next_ready_work_item
    - work_item_blocked
    - ask_first_boundary_reached
    - verification_failure
    - maintainer_redirect

milestones:
  - id: M-000
    title: Constitutional And Operating Foundation
    status: completed
    goal: Establish the constitution, bilingual documentation rule, change specs, ADRs, architecture manifests, conversation memory, and baseline verification tooling.
    completion_summary: The repository has a constitution, AGENTS guides, change spec workflow, ADRs, conversation logs, architecture checks, and initial runtime direction.

  - id: M-001
    title: First Go Runtime Request Loop
    status: completed
    goal: Build the first bounded WebSocket Protobuf application request loop without persistence.
    completion_criteria:
      - WebSocket transport accepts binary frames without owning protocol or domain behavior.
      - Protobuf adapter decodes envelopes and bridges inventory payloads.
      - Application dispatch routes command and query requests.
      - Composition connects WebSocket frames to Protobuf envelopes and application dispatch.
      - The loop is covered by Go tests and repository checks.
    non_goals:
      - PostgreSQL persistence
      - authentication
      - session validation
      - generated route registration
      - distributed runtime
    completion_summary: The first bounded WebSocket Protobuf request loop is implemented with transport, protocol, application dispatch, inventory runtime behavior, process wiring, request-loop test support, and repository checks.

  - id: M-002
    title: Durable Inventory Runtime
    status: completed
    goal: Add PostgreSQL-backed inventory persistence and transaction boundaries after the in-process request loop is proven.
    completion_criteria:
      - PostgreSQL runtime configuration and a pgx-backed transaction runner exist behind declared platform boundaries.
      - Migration apply and status tooling can execute SQL-first migration sources explicitly.
      - A disposable PostgreSQL verification environment is defined before live integration checks become mandatory.
      - Persistent inventory runtime composition can serve the existing WebSocket Protobuf request loop without moving persistence details into domain, protocol, or transport packages.
      - Durable inventory behavior is verified end to end or any unavailable external verification is explicitly recorded.
    non_goals:
      - object storage
      - cluster runtime
      - multi-module economy behavior
    completion_summary: The durable inventory runtime is implemented with PostgreSQL configuration, transaction runner, migration tooling, PostgreSQL repository adapter, explicit runtime composition, and live end-to-end PostgreSQL verification on Termux PostgreSQL 18.2.

  - id: M-003
    title: Player Identity And Session Boundary
    status: completed
    goal: Define the player identity, authentication boundary, and WebSocket session lifecycle before inventory, permissions, or future modules rely on ad hoc player context.
    completion_criteria:
      - Player identity, account ownership, authentication, and runtime session responsibilities are separated in manifests and standards.
      - The protocol session model is grounded in explicit player/session ownership without changing the existing envelope shape prematurely.
      - The first implementation work queue for player/session behavior is planned in bounded, verifiable steps.
      - Inventory permission policy has a documented path away from bootstrap static allow behavior.
      - No authentication provider, token format, or credential storage model is implemented before the boundary is ratified.
    non_goals:
      - external identity providers
      - production login security
      - token signing or cryptography dependencies
      - player database migrations
      - matchmaking or presence
    completion_summary: The player identity and session boundary is complete as a durable boundary milestone. Player identity, account lifecycle, authentication, runtime session validation, transport metadata, envelope session metadata, and request identity context are separated in standards and manifests; the application request identity and session validation hook exists; inventory permission handoff receives request identity without owning authentication or player accounts; metadata-only identity is guarded; and runtime.identity_boundary checks prevent common regressions. No authentication provider, token format, credential storage, player account persistence, Protobuf envelope change, or WebSocket handshake change was implemented.

  - id: M-004
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer confirmation point before choosing the next major product or architecture direction.
    completion_criteria:
      - The maintainer chooses the next milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for authentication, token, credential, player account schema, session persistence, Protobuf envelope, WebSocket handshake, major external framework dependency, and production permission decisions.
    non_goals:
      - implementing authentication before ratification
      - choosing token or credential design implicitly
      - adding player account migrations without schema decision
      - changing Protobuf envelope or WebSocket handshake without confirmation
      - declaring metadata-only identity sufficient for production permissions
    completion_summary: The maintainer selected ratify_player_account_and_session_contracts as the next direction, with Nakama and Pitaya remaining active references for capability coverage and server framework vocabulary. M-005 was created as the active contract-first milestone, and no authentication, token, credential, player account schema, session persistence, Protobuf envelope, WebSocket handshake, major dependency, or production permission decision was made.

  - id: M-005
    title: Player Account And Session Contracts
    status: completed
    goal: Ratify the first player account and session contract standards before implementing production authentication, player persistence, token behavior, session storage, or protocol handshake changes.
    completion_criteria:
      - Player account lifecycle commands, queries, events, errors, permissions, invariants, and ownership rules are declared before implementation.
      - Runtime session validation contracts are separated from authentication providers, token format, credential storage, WebSocket transport, Protobuf framing, and player account persistence.
      - Nakama account, user, authentication, and session capability coverage is reviewed and either adopted, adapted, deferred, or rejected in vibit terms.
      - Pitaya session binding, route handler, frontend/backend, and realtime server vocabulary is reviewed and either adopted, adapted, deferred, or rejected in vibit terms.
      - The first implementation queue after ratification remains bounded, verifiable, and does not rely on metadata-only identity as authenticated proof.
    non_goals:
      - implementing production authentication
      - choosing token signing, refresh, or expiration behavior
      - choosing credential storage or password hashing behavior
      - adding player account database migrations
      - adding session persistence
      - changing Protobuf envelope shape
      - changing WebSocket handshake authentication behavior
      - copying Nakama or Pitaya public APIs directly
      - adding a major external framework dependency
    completion_summary: M-005 completed the contract-first player account and runtime session validation milestone. The repository now has a player account/session contract standard with Nakama and Pitaya reference mapping, ratified player account semantic contracts, player account Protobuf wire shape and generated Go output, ratified runtime session validation semantic contracts, runtime-owned contract inspection support, and explicit guards that metadata-only identity is not authenticated proof. No authentication scheme, token behavior, credential storage, player account persistence, session persistence, Protobuf envelope change, WebSocket handshake authentication change, runtime player handler, WebSocket route, direct external API compatibility, or major external dependency was added.

  - id: M-006
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point before choosing the next major implementation or architecture direction after player account/session contract ratification.
    completion_criteria:
      - The maintainer chooses the next milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for authentication, token, credential, player account schema, session persistence, Protobuf envelope, WebSocket handshake, major external framework dependency, production permission, runtime player handler, and major game-domain expansion decisions.
    non_goals:
      - implementing authentication before ratification
      - choosing token or credential design implicitly
      - adding player account migrations without schema decision
      - changing Protobuf envelope or WebSocket handshake without confirmation
      - adding runtime player handlers or WebSocket routes without a chosen implementation milestone
      - declaring metadata-only identity sufficient for production permissions
    completion_summary: The maintainer accepted the recommended direction to improve generators, contract tooling, and architecture checks before adding more runtime features. M-007 was created as the active tooling hardening milestone without choosing authentication, token, credential, player account persistence, session persistence, Protobuf envelope, WebSocket handshake, runtime player handler, or major game-domain implementation behavior.

  - id: M-007
    title: Agent Tooling And Generator Hardening
    status: completed
    goal: Strengthen agent-facing inspection, generation, generated-output drift checks, and continuation intake before adding more runtime features.
    completion_criteria:
      - Agents can inspect the next work item, registered contracts, generated output, and reference planning context through narrow JSON commands.
      - Contract shape generation is reproducible from semantic contract sources.
      - Generated contract shape output is checked for source trace and drift.
      - Agent tooling guidance exists in English and Simplified Chinese.
      - Repository checks include the new tooling standard.
    non_goals:
      - implementing authentication
      - choosing token behavior
      - adding credential storage
      - adding player account persistence
      - adding session persistence
      - changing Protobuf envelope or WebSocket handshake behavior
      - adding runtime player handlers or WebSocket routes
      - adding new game-domain modules
    completion_summary: M-007 completed the agent tooling and generator hardening milestone. Agents can inspect next work, registered contracts, generated output, and reference planning context through narrow JSON commands; contract shape generation is reproducible from semantic contract sources; generated output checks cover source trace, drift, missing shapes, stale files, and compact status; agent tooling guidance exists in English and Simplified Chinese; and full repository verification passes. No authentication, token behavior, credential storage, player account persistence, session persistence, Protobuf envelope change, WebSocket handshake change, runtime player handler, or new game-domain module was implemented.

  - id: M-008
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point before choosing the next major implementation or architecture direction after generator and contract tooling hardening.
    completion_criteria:
      - The maintainer chooses the next milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for authentication, token, credential, player account schema, session persistence, Protobuf envelope, WebSocket handshake, major external framework dependency, production permission, runtime player handler, generated file conventions, and major game-domain expansion decisions.
    non_goals:
      - implementing authentication before ratification
      - choosing token or credential design implicitly
      - adding player account migrations without schema decision
      - changing Protobuf envelope or WebSocket handshake without confirmation
      - adding runtime player handlers or WebSocket routes without a chosen implementation milestone
      - adding a new game-domain module without a chosen milestone
      - declaring metadata-only identity sufficient for production permissions
    completion_summary: The maintainer accepted the professional recommendation to select ratify_player_account_postgresql_persistence_schema as the next direction. M-009 was created as the active schema-ratification milestone with a bounded first work item. No authentication, token behavior, credential storage, session persistence, Protobuf envelope change, WebSocket handshake change, runtime player handler, generated file convention change, external API compatibility, production permission decision, or new game-domain module was selected.

  - id: M-009
    title: Player Account PostgreSQL Persistence Schema
    status: completed
    goal: Ratify player account PostgreSQL persistence schema, migration ownership, repository boundary, adapter implementation, indexes, and verification path before adding runtime handlers, authentication, tokens, credentials, or session persistence.
    completion_criteria:
      - Player account PostgreSQL table ownership, lifecycle columns, event/audit rows, indexes, constraints, and cross-module data references are documented before migration source exists.
      - Player module, runtime manifests, persistence boundary standards, and repository checks agree that schema, migration source, repository interface, and PostgreSQL adapter implementation are separate traceable steps.
      - The first player account migration source follows the ratified schema and SQL-first migration rules.
      - Player account repository work has a module-owned interface, a PostgreSQL adapter boundary, and a focused PostgreSQL adapter implementation.
      - Verification records default static checks, fake-executor adapter checks, and any unavailable live PostgreSQL checks explicitly.
    non_goals:
      - production authentication
      - login method selection
      - token format, signing, refresh, expiration, or revocation behavior
      - credential storage, password hashing, OAuth, OIDC, or external identity providers
      - session persistence
      - Protobuf envelope changes
      - WebSocket handshake authentication changes
      - runtime player account handlers or WebSocket routes
      - direct Nakama or Pitaya public API compatibility
      - new game-domain modules
    completion_summary: M-009 completed the player account PostgreSQL persistence milestone. The repository has a ratified player account lifecycle schema boundary, SQL-first migration source, module-owned storage-neutral repository interface, PostgreSQL adapter boundary, focused PostgreSQL adapter implementation, UnitOfWork factory helper, fake-executor adapter tests, and repository checks. Runtime handlers, WebSocket routes, authentication, token behavior, credential storage, external identity linking, session persistence, Protobuf envelope behavior, WebSocket handshake behavior, direct Nakama/Pitaya API compatibility, and new game-domain modules remain deferred behind the next maintainer confirmation gate.

  - id: M-010
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point before choosing the next major implementation or architecture direction after player account PostgreSQL persistence.
    completion_criteria:
      - The maintainer chooses the next milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for authentication, token, credential, external identity, session persistence, Protobuf envelope, WebSocket handshake, major external framework dependency, production permission, runtime player handler, generated file conventions, operations posture, and major game-domain expansion decisions.
    non_goals:
      - implementing authentication before ratification
      - choosing token or credential design implicitly
      - adding external identity linking without a selected milestone
      - adding runtime player handlers or WebSocket routes without a selected implementation milestone
      - changing Protobuf envelope or WebSocket handshake without confirmation
      - adding a new game-domain module without a selected milestone
      - declaring metadata-only identity sufficient for production permissions
    completion_summary: The maintainer selected ratify_authentication_and_token_session_validation_design as the next direction after player account PostgreSQL persistence. M-011 was created as the active design milestone with a bounded first work item. No login method, token format, credential storage, external identity linking, session persistence, Protobuf envelope behavior, WebSocket handshake behavior, runtime player handler, WebSocket route, major dependency, generated file convention, production permission, or game-domain expansion decision was made.

  - id: M-011
    title: Authentication And Token Session Validation Design
    status: completed
    goal: Ratify authentication, token behavior, session validation semantics, and request identity trust boundaries before implementing production authentication, runtime player handlers, WebSocket routes, credential storage, external identity linking, or session persistence.
    completion_criteria:
      - Authentication, token/session validation, credential, external identity, session persistence, WebSocket handshake, and request-level validation responsibilities are separated before implementation.
      - Nakama account/auth/session token and realtime socket concepts are reviewed and mapped into adopted, adapted, deferred, or rejected vibit terms.
      - Pitaya session binding, route handler, frontend/backend, and realtime session vocabulary is reviewed and mapped into adopted, adapted, deferred, or rejected vibit terms.
      - The first implementation queue after design ratification remains bounded and does not treat metadata-only player_id or session_id as authenticated proof.
      - Any concrete token format, login method, credential storage, external identity linking, session persistence, Protobuf envelope, or WebSocket handshake choice is explicitly ratified before implementation.
    non_goals:
      - implementing production authentication
      - choosing concrete login methods in the direction-gate step
      - choosing JWT, opaque token, refresh token, signing, expiration, revocation, or token storage behavior in the direction-gate step
      - adding credential storage, password hashing, OAuth, OIDC, or external identity-provider dependencies before design ratification
      - adding external identity linking tables or behavior before design ratification
      - adding session persistence before design ratification
      - changing Protobuf envelope behavior
      - changing WebSocket handshake authentication behavior
      - adding runtime player account handlers or WebSocket routes
      - copying Nakama or Pitaya public APIs directly
    completion_summary: M-011 completed the authentication and token/session validation design milestone. Authentication proof, login methods, tokens, credentials, external identity links, runtime sessions, request identity, WebSocket handshake authentication, transport metadata, Protobuf envelope metadata, and player account lifecycle are separated in standards, manifests, checks, and agent guides. Nakama account/auth/session token/realtime socket concepts and Pitaya session binding/route/session vocabulary are mapped into vibit adopted, adapted, deferred, or rejected positions. Semantic dimensions, credential/external identity boundaries, and session persistence/WebSocket handshake gates are ratified as design boundaries only. Runtime authentication, token parsing, credential storage, external identity linking, session persistence, Protobuf envelope changes, WebSocket handshake authentication, runtime player handlers, WebSocket routes, and production permission reliance on metadata-only identity remain deferred behind the next confirmation gate.

  - id: M-012
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point before choosing the next major implementation or architecture direction after authentication and token/session validation design ratification.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for login methods, token format, credential storage, external identity linking, session persistence, Protobuf envelope behavior, WebSocket handshake authentication, runtime player handlers, WebSocket routes, major dependencies, production permission, operations posture, multiplayer/presence/matchmaking scope, and major game-domain expansion decisions.
    non_goals:
      - implementing production authentication before a direction is selected
      - choosing login methods, token format, credential schema, external identity linking, session persistence, Protobuf envelope behavior, or WebSocket handshake behavior implicitly
      - adding runtime player handlers or WebSocket routes without a selected implementation milestone
      - adding a new game-domain module without a selected milestone
      - declaring metadata-only identity sufficient for production permissions
    completion_summary: The maintainer authorized the agent's professional recommendation to choose `login_method_and_token_format_ratification` as the next major direction. M-013 was created as a bounded ratification milestone with W-0064 as the first design-standard work item. No production authentication implementation, concrete login handler, credential storage, external identity linking, session persistence, Protobuf envelope change, WebSocket handshake authentication, runtime player handler, WebSocket route, major dependency, production permission decision, or game-domain expansion was added.

  - id: M-013
    title: Login Method And Token Format Ratification
    status: completed
    goal: Ratify the first production authentication entry points, token model, proof carrier boundaries, token lifecycle semantics, and implementation queue before adding login handlers, token parsing, credential storage, session persistence, Protobuf envelope changes, WebSocket handshake authentication, runtime player handlers, or WebSocket routes.
    completion_criteria:
      - Candidate first login methods are compared against Nakama capability coverage and vibit security/agent-native constraints.
      - The first token model is ratified with issuer, verifier, subject, audience, expiration, refresh, revocation, rotation, replay, redaction, and storage implications recorded.
      - Token/proof carrier behavior is decided or explicitly deferred without changing the Protobuf envelope or WebSocket handshake implicitly.
      - Required contracts, errors, permissions, schemas, dependency adoption records, migration gates, tests, and repository checks are planned before implementation.
      - The first implementation queue remains bounded and does not treat metadata-only player_id or session_id as authenticated proof.
    non_goals:
      - implementing production authentication in the direction-selection step
      - adding credential storage tables, external identity tables, token tables, session tables, or migrations before schema ratification
      - adding password hashing, JWT, OAuth, OIDC, provider SDK, cryptography, key-management, Redis-like, or other major dependencies before adoption records
      - changing Protobuf envelope behavior before protocol impact is ratified
      - changing WebSocket handshake authentication behavior before handshake impact is ratified
      - adding runtime player account handlers or WebSocket routes
      - copying Nakama or Pitaya public APIs directly
    completion_summary: M-013 completed the login method and token format ratification milestone. It compared login-method candidates against Nakama capability coverage and vibit security constraints, ratified device_credential_login as the first login method, compared token format and carrier options, ratified opaque high-entropy access tokens with login-command issuance and explicit request proof payloads, defined token lifecycle and storage implications, defined runtime authentication semantic contract/error/permission/audit surfaces, defined credential/token/session schema gates, and added selected login/token repository checks. No runtime authentication, token behavior, credential storage, token storage, session persistence, external identity linking, generated authentication output, Protobuf messages, WebSocket routes, WebSocket handshake authentication, migrations, repositories, adapters, or major authentication dependencies were added.

  - id: M-014
    title: Credential And Token Verifier Schema Ratification
    status: completed
    goal: Ratify credential record and token verifier record schema boundaries for the selected device credential and opaque access-token posture before adding migrations, repositories, adapters, runtime lookup, generated authentication shapes, Protobuf messages, WebSocket proof carriers, handlers, routes, or authentication behavior.
    completion_criteria:
      - Credential record semantics, ownership, identifiers, verifier storage, uniqueness, lifecycle states, account relationship, rotation, revocation, disabled-credential behavior, and redaction rules are ratified before migration source exists.
      - Token verifier record semantics, ownership, non-plaintext verifier storage, statuses, expiration, revocation, credential-token linkage, retention, cleanup, replay-sensitive failure classes, and redaction rules are ratified before migration source exists.
      - PostgreSQL remains the default durable target unless a later explicit dependency or storage decision changes it.
      - Player account lifecycle tables remain credential-free, token-free, external-identity-free, session-free, WebSocket-state-free, and request-validation-free.
      - Future migration, repository, adapter, generated output, protocol, WebSocket carrier, runtime lookup, handler, route, and live verification work is split into later bounded work items.
    non_goals:
      - adding credential, token, external identity, runtime session, or audit migrations in the schema-ratification step
      - implementing credential lookup or token validation
      - adding runtime authentication handlers or WebSocket routes
      - changing Protobuf envelope or WebSocket handshake behavior
      - adding generated authentication contract shapes
      - adding password hashing, JWT, OAuth, OIDC, provider SDK, key-management, Redis-like, or other major authentication dependencies
      - copying Nakama or Pitaya public APIs directly
    completion_summary: M-014 completed the credential and token verifier schema ratification path. It ratified credential and token verifier record boundaries, added SQL-first PostgreSQL migration sources for authentication_device_credentials and authentication_access_tokens, added local static migration checks, added a storage-neutral authentication repository interface boundary, and defined the authentication PostgreSQL adapter boundary. Runtime authentication, credential lookup behavior, token issuance, token validation, logout execution, refresh, cleanup jobs, handlers, routes, generated authentication shapes, Protobuf messages, WebSocket proof carriers, WebSocket handshake authentication, authentication dependencies, external identity storage, runtime session persistence, and production authentication behavior remain deferred to later bounded milestones.

  - id: M-015
    title: Authentication PostgreSQL Adapter Implementation
    status: completed
    goal: Implement the PostgreSQL persistence adapter for the already-ratified authentication repository interface and migration sources without adding runtime authentication behavior, token generation, token validation, login handlers, WebSocket routes, Protobuf messages, generated authentication shapes, or authentication dependencies.
    completion_criteria:
      - Repository checks distinguish allowed authentication persistence-adapter vocabulary from forbidden runtime authentication behavior.
      - The PostgreSQL adapter implements authentication.Repository through a caller-supplied executor and does not own transaction lifetime.
      - Credential and token verifier rows map to module-owned records with redaction-safe error paths and no raw secret exposure.
      - Focused fake-executor tests cover credential storage/lookup, token storage/lookup, revocation, cleanup eligibility, error mapping, UTC normalization, and absence of transaction-control SQL.
      - Runtime authentication, protocol, WebSocket, generated output, login, token issuance, token validation, logout execution, refresh, cleanup jobs, and major dependencies remain deferred.
    non_goals:
      - implementing AuthenticateWithDeviceCredential
      - implementing ValidateAccessToken
      - implementing LogoutAccessToken runtime behavior
      - generating raw access tokens or credential material
      - comparing credential or token verifier digests
      - parsing bearer tokens
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding JWT, OAuth, OIDC, password-hashing, key-management, Redis-like, provider SDK, or cryptography dependencies
      - adding external identity, runtime session, audit, or refresh-token persistence
    completion_summary: M-015 completed the authentication PostgreSQL adapter implementation path. Repository checks now distinguish allowed persistence-adapter vocabulary from forbidden runtime authentication behavior, and the PostgreSQL adapter implements authentication.Repository through a caller-supplied executor with focused fake-executor tests for credential storage/lookup, token storage/lookup, revocation, cleanup eligibility, error mapping, UTC normalization, and absence of transaction-control SQL. Runtime authentication, token generation, token validation, login handlers, logout execution, refresh, cleanup jobs, Protobuf messages, WebSocket proof carriers, generated authentication shapes, major authentication dependencies, external identity storage, runtime session persistence, and production authentication behavior remain deferred.

  - id: M-016
    title: Runtime Authentication Implementation Boundary Planning
    status: completed
    goal: Plan the first runtime authentication implementation boundary after the persistence adapter without implementing login, token generation, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, generated authentication shapes, or authentication dependencies.
    completion_criteria:
      - Runtime authentication responsibilities are split between application services, module-owned persistence interfaces, PostgreSQL adapters, request identity handoff, protocol adapters, and transport boundaries before implementation.
      - The first runtime authentication implementation queue is ordered so token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, generated authentication shapes, and dependency adoption remain separately gated.
      - Required contracts, error mappings, permission surfaces, tests, and repository checks are identified before runtime behavior is added.
      - Nakama and Pitaya authentication/session concepts are referenced for capability coverage without copying public APIs or bypassing vibit boundaries.
      - No production authentication behavior is implemented in the planning step.
    non_goals:
      - implementing AuthenticateWithDeviceCredential
      - implementing ValidateAccessToken
      - implementing LogoutAccessToken runtime behavior
      - generating raw access tokens or credential material
      - comparing credential or token verifier digests
      - parsing bearer tokens
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding JWT, OAuth, OIDC, password-hashing, key-management, Redis-like, provider SDK, or cryptography dependencies
      - adding external identity, runtime session, audit, or refresh-token persistence
    completion_summary: M-016 completed the runtime authentication implementation boundary planning gate. The repository now has an English and Simplified Chinese runtime authentication implementation boundary standard, ADR-0036, manifest and guide references, repository checks for the runtime authentication implementation boundary, an application-owned future authentication orchestration boundary, persistence-only authentication PostgreSQL adapter rules, request identity handoff expectations, error/permission/audit mapping expectations, Nakama/Pitaya capability alignment, and a separately gated future implementation queue. Runtime authentication behavior, token generation, token validation, verifier comparison, login execution, logout execution, refresh, cleanup jobs, Protobuf authentication messages, WebSocket proof carriers, generated authentication shapes, authentication dependencies, authentication repository changes, and migration schema changes remain deferred.

  - id: M-017
    title: Authentication Generated Contract Shape Gate
    status: completed
    goal: Decide whether and when generated Go authentication contract shapes should be introduced before application service interfaces or runtime authentication behavior.
    completion_criteria:
      - Generated authentication contract shape timing is decided before runtime authentication service code starts.
      - The generated-shape decision references semantic authentication contracts and the generated-output standard.
      - If generated shapes are later authorized, their source trace, owner, output path, immutable status, and repository checks are defined before generation.
      - Runtime authentication behavior, token generation, token validation, verifier comparison, login execution, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, and migration schema changes remain deferred unless separately gated.
    non_goals:
      - generating authentication contract shapes in the direction step
      - implementing application authentication services
      - implementing token generation or verifier comparison
      - implementing login, token validation, logout, refresh, or cleanup
      - adding authentication Protobuf messages
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-017 completed the generated authentication contract shape gate. The repository now has runtime-family-aware generator, inspection, and drift-check support for `contracts/runtime/authentication/**`; metadata-only Go authentication contract shape files are generated under `runtime/internal/generated/contracts/runtime/authentication/`; and runtime boundary checks allow only source-traced generated metadata output while keeping runtime authentication behavior, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, and migration schema changes deferred.

  - id: M-018
    title: Application Authentication Service Interface Boundary
    status: completed
    goal: Define the application-owned runtime authentication service interface boundary before adding runtime authentication behavior, token material generation, verifier comparison, Protobuf messages, or WebSocket proof carriers.
    completion_criteria:
      - The future service interface owner, package boundary, input/output vocabulary, redaction expectations, and unit-of-work dependency shape are documented before implementation.
      - The interface boundary explains how generated authentication contract shapes inform service-level DTOs without being imported into domain modules or transport handlers.
      - Error, permission, audit, and request identity handoff expectations are mapped from existing semantic authentication contracts.
      - Runtime behavior, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, and migration schema changes remain deferred unless separately gated.
    non_goals:
      - implementing application authentication services
      - generating access tokens or credential material
      - comparing credential or token verifier digests
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-018 defined the application authentication service interface boundary. The repository now has an English and Simplified Chinese service-interface boundary standard, ADR-0039, a runtime check rule, manifest and guide references, generated authentication contract shape to service vocabulary mapping, redaction expectations, unit-of-work and authentication.Repository usage rules, error/permission/audit handoff mapping, and request identity handoff expectations. No application authentication service code, runtime authentication behavior, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, or migration schema changes were added.

  - id: M-019
    title: Token And Credential Verifier Algorithm Redaction Boundary
    status: completed
    goal: Define verifier algorithm posture and redaction test expectations before adding application authentication service code, token material generation, or verifier comparison.
    completion_criteria:
      - Credential verifier, token lookup digest, token verifier digest, entropy, encoding, key identifier, and constant-time comparison requirements are documented before implementation.
      - Redaction tests and secret-handling expectations are defined for credentials, raw access tokens, verifier inputs, application errors, audit-safe facts, and logs.
      - The boundary explains which future code may use Go standard cryptography packages or whether an external dependency adoption record is required.
      - Runtime behavior, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, and migration schema changes remain deferred unless separately gated.
    non_goals:
      - implementing token generation
      - implementing verifier comparison
      - implementing application authentication service code
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-019 defined the first token and credential verifier algorithm redaction boundary. The repository now has an English and Simplified Chinese verifier algorithm/redaction boundary standard, ADR-0040, a runtime check rule, manifest and guide references, `vibit_hmac_sha256_v1` as the first planned high-entropy HMAC-SHA-256 verifier algorithm family, digest classification for credential and token lookup/verifier material, purpose-label domain separation, minimum 256-bit raw credential and token entropy expectations, constant-time verifier comparison requirements, Go standard library dependency posture, and redaction test expectations. No token generation, credential generation, verifier comparison, application authentication service code, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, secret configuration loading, or production authentication behavior were added.

  - id: M-020
    title: Secret Configuration And Verifier Key Loading Boundary
    status: completed
    goal: Define verifier key configuration, key loading, key identifier, rotation, and redaction boundaries before implementing token or credential material generation and verifier comparison.
    completion_criteria:
      - Secret configuration ownership and future key loading posture are documented before implementation.
      - Verifier key identifiers, key set separation, fallback behavior, rotation posture, development defaults, and production requirements are defined without storing secret values in committed artifacts.
      - The boundary states whether the first local implementation can use process environment configuration or requires an external secret manager adoption record.
      - Token generation, credential generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, and production authentication behavior remain deferred unless separately gated.
    non_goals:
      - implementing secret loading
      - adding committed secrets or default production keys
      - implementing token generation
      - implementing verifier comparison
      - implementing application authentication service code
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-020 defined the secret configuration and verifier key loading boundary. The repository now has an English and Simplified Chinese secret configuration/key loading boundary standard, ADR-0041, a runtime check rule, manifest and guide references, application-owned future key loading posture under runtime/internal/app, first local process-environment or explicit runtime secret input posture, four separated logical verifier keys, verifier key id redaction rules, key-set rotation expectations, fail-closed production behavior, development/test key rules, and external KMS/secret-manager deferral behind dependency and operations gates. No secret loading, environment parsing, token generation, credential generation, verifier comparison, application authentication service code, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, KMS integration, secret-manager integration, or production authentication behavior were added.

  - id: M-021
    title: Token And Credential Material Generation Boundary
    status: completed
    goal: Define token and credential material generation ownership, return semantics, entropy enforcement, one-time presentation, redaction, and test expectations before implementing generation helpers or authentication service behavior.
    completion_criteria:
      - Token and credential material generation ownership and future helper boundaries are documented before implementation.
      - Raw material entropy, text encoding, one-time client presentation, non-storage, redaction, and test expectations are defined in a way that is consistent with the verifier algorithm and secret configuration boundaries.
      - The boundary states whether future generation helpers can use Go standard library randomness and base64 encoding without an external dependency adoption record.
      - Secret loading, verifier digest computation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, and production authentication behavior remain deferred unless separately gated.
    non_goals:
      - implementing token generation
      - implementing credential generation
      - implementing secret loading
      - implementing verifier digest computation
      - implementing verifier comparison
      - implementing application authentication service code
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-021 defined the token and credential material generation boundary. The repository now has an English and Simplified Chinese material generation boundary standard, ADR-0042, a runtime check rule, manifest and guide references, application-owned future generation posture under runtime/internal/app, server-issued application-generated first device credential and access-token sources, 32-byte and 256-bit raw material expectations, URL-safe text presentation, one-time client-visible presentation, raw material non-storage rules, repository digest-only handoff expectations, redaction requirements, generation test expectations, and Go standard library randomness/base64 dependency posture. No token generation, credential generation, secret loading, verifier digest computation, verifier comparison, application authentication service code, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior were added.

  - id: M-022
    title: Verifier Digest Computation And Comparison Boundary
    status: completed
    goal: Define verifier digest computation, canonical byte input, key use, lookup/verifier digest handoff, constant-time comparison, failure behavior, and redaction test expectations before implementing digest helpers or authentication service behavior.
    completion_criteria:
      - Verifier digest computation ownership, canonical input construction, key use, and output handoff are documented before implementation.
      - Lookup digest and verifier digest behavior remains separated for credentials and tokens.
      - Constant-time comparison, missing-record behavior, key-id selection behavior, and failure-class redaction are defined without adding code.
      - Token generation, credential generation, secret loading, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, and production authentication behavior remain deferred unless separately gated.
    non_goals:
      - implementing verifier digest computation
      - implementing verifier comparison
      - implementing token generation
      - implementing credential generation
      - implementing secret loading
      - implementing application authentication service code
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-022 defined the verifier digest computation and comparison boundary. The repository now has an English and Simplified Chinese verifier digest boundary standard, ADR-0043, a runtime check rule, manifest and guide references, application-owned future digest posture under runtime/internal/app, canonical HMAC input framing with version header and length prefixes, four digest class mappings, active and accepted-previous key-set lookup posture, lookup digest handoff rules, constant-time verifier digest comparison expectations, invalid-proof public failure collapse, redaction requirements, digest test expectations, and Go standard library HMAC/SHA-256/subtle dependency posture. No verifier digest computation, verifier comparison, token generation, credential generation, secret loading, application authentication service code, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior were added.

  - id: M-023
    title: Authentication Service Implementation Readiness Gate
    status: completed
    goal: Consolidate the implementation entry criteria, test expectations, and bounded first authentication service code queue before adding authentication service behavior.
    completion_criteria:
      - The first authentication service implementation slice has explicit entry criteria, owned packages, allowed files, required tests, and deferrals.
      - Secret loading, material generation, digest computation, verifier comparison, login execution, token validation, logout, cleanup, Protobuf messages, WebSocket proof carriers, repository changes, migration changes, and production behavior remain separately gated or explicitly sequenced.
      - The readiness gate maps Nakama and Pitaya authentication/session capability references into the concrete vibit implementation queue without copying their public APIs.
      - The next code or planning work item is bounded enough for an agent to implement without changing architecture implicitly.
    non_goals:
      - implementing authentication service code
      - implementing secret loading
      - implementing token generation
      - implementing credential generation
      - implementing verifier digest computation
      - implementing verifier comparison
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-023 defined the authentication service implementation readiness gate. The repository now has an English and Simplified Chinese readiness standard, ADR-0044, a runtime check rule, manifest and guide references, required prior-boundary list, recommended package candidate runtime/internal/app/authentication, forbidden first-slice write areas, recommended implementation queue, local verifier key configuration loader as the next recommended gate, service behavior entry criteria, required test classes, redaction and observability constraints, and Nakama/Pitaya capability mapping. No authentication service code, secret loading, token generation, credential generation, verifier digest computation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior were added.

  - id: M-024
    title: Local Verifier Key Configuration Loading Gate
    status: completed
    goal: Define the first bounded local verifier key configuration loading implementation gate before adding secret-loading code.
    completion_criteria:
      - The local verifier key configuration loading code gate declares exact owner package, input posture, validation rules, redaction rules, test cases, and file boundaries before implementation.
      - Environment parsing, explicit in-memory input, or both are sequenced without committing production-like secrets.
      - External KMS, cloud secret-manager, operations dependencies, token generation, credential generation, digest helpers, login execution, token validation, protocol carriers, repository changes, migration changes, and production authentication behavior remain deferred unless separately gated.
      - The next work item is either a narrow implementation slice or another explicit preparation gate.
    non_goals:
      - implementing secret loading
      - implementing token generation
      - implementing credential generation
      - implementing verifier digest computation
      - implementing verifier comparison
      - implementing authentication service code
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - changing the Protobuf envelope or WebSocket handshake
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-024 defined the local verifier key configuration loading gate. The repository now has an English and Simplified Chinese gate standard, ADR-0045, a runtime check rule, manifest and guide references, explicit in-memory verifier key set validation as the first future implementation slice, runtime/internal/app/authentication as the owner package, expected future files, validation requirements, redaction requirements, required unit tests, dependency posture, and Nakama/Pitaya capability mapping. Environment parsing, Base64 text decoding, startup wiring, KMS, cloud secret-manager integration, token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository changes, migration changes, and production authentication behavior remain deferred.

  - id: M-025
    title: Explicit In-Memory Verifier Key Set Validator
    status: completed
    goal: Implement the explicit in-memory verifier key set validator as the first narrow local verifier key configuration code slice.
    completion_criteria:
      - The validator is implemented under runtime/internal/app/authentication using the file boundaries defined by ADR-0045.
      - The validator accepts already-decoded explicit in-memory key material, copies input bytes, avoids mutable internal slice exposure, rejects invalid key sets, and keeps errors redacted.
      - Focused unit tests cover accepted complete input, copying, immutable accessors, missing key set id, missing keys, short keys, duplicate logical keys, all-zero keys, repeated single-byte keys, and redaction.
      - Environment parsing, Base64 text decoding, startup wiring, external KMS, cloud secret-manager integration, token generation, credential generation, digest helpers, verifier comparison, authentication service behavior, protocol carriers, repository changes, migration changes, and production authentication behavior remain deferred unless separately gated.
    non_goals:
      - parsing process environment variables
      - decoding Base64 key text
      - reading local secret files
      - wiring process startup
      - implementing token generation
      - implementing credential generation
      - implementing verifier digest computation
      - implementing verifier comparison
      - implementing authentication service behavior
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-025 implemented the explicit in-memory verifier key set validator under runtime/internal/app/authentication. The validator accepts already-decoded in-memory key material, requires a key set id and four distinct logical keys, enforces 32-byte minimum key length, rejects missing, short, duplicate, all-zero, and repeated single-byte keys, copies input key bytes, returns copies from accessors, and keeps validation errors redacted. Focused tests cover accepted input, key set id trimming, input copying, immutable accessors, missing key set id, missing keys, short keys, duplicate keys, all-zero keys, repeated-byte keys, and redaction. Environment parsing, Base64 text decoding, startup wiring, KMS, cloud secret-manager integration, token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository changes, migration changes, and production authentication behavior remain deferred.

  - id: M-026
    title: Environment Verifier Key Loader Gate
    status: completed
    goal: Define the environment verifier key loader gate before adding process environment parsing or Base64 key text decoding.
    completion_criteria:
      - The environment loader gate declares exact owner package, environment variable contract, decoding posture, validation handoff to the explicit in-memory validator, redaction behavior, test cases, and file boundaries before implementation.
      - The gate preserves deferrals for startup wiring, KMS, cloud secret-manager integration, token generation, credential generation, digest helpers, verifier comparison, authentication service behavior, protocol carriers, repository changes, migration changes, dependencies, and production authentication behavior.
      - The next work item is either a narrow environment loader implementation slice or another explicit preparation gate.
    non_goals:
      - implementing process environment parsing
      - decoding Base64 key text
      - reading local secret files
      - wiring process startup
      - implementing token generation
      - implementing credential generation
      - implementing verifier digest computation
      - implementing verifier comparison
      - implementing authentication service behavior
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-026 defined the environment verifier key loader gate in ADR-0046 and the paired gate standard. The future loader is application-owned under runtime/internal/app/authentication, uses verifier_key_env.go and verifier_key_env_test.go, declares the required process environment variable contract, prefers Base64URL unpadded key text while allowing standard padded Base64 for compatibility, requires handoff to NewVerifierKeySet, preserves redaction expectations, and keeps startup wiring, local secret files, dotenv behavior, CLI secret input, KMS, cloud secret-manager integration, token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, protocol carriers, repository changes, migration changes, dependencies, and production authentication behavior deferred. No Go code, process environment parsing, Base64 text decoding, secret loading, startup wiring, token behavior, digest helpers, service behavior, protocol carriers, repository changes, migration changes, external dependencies, or production authentication behavior were added.

  - id: M-027
    title: Environment Verifier Key Loader Implementation
    status: completed
    goal: Implement the narrow process environment verifier key loader adapter that decodes required environment values and hands them to the explicit in-memory validator.
    completion_criteria:
      - The loader is implemented under runtime/internal/app/authentication using the file boundaries defined by ADR-0046.
      - The loader accepts an explicit lookup function for tests and may expose a tiny process environment adapter.
      - The loader decodes supported Base64 key text, calls NewVerifierKeySet, preserves redaction, and adds focused tests.
      - Startup wiring, local secret files, dotenv behavior, CLI secret input, KMS, cloud secret-manager integration, token generation, credential generation, digest helpers, verifier comparison, authentication service behavior, protocol carriers, repository changes, migration changes, dependencies, and production authentication behavior remain deferred unless separately gated.
    non_goals:
      - wiring process startup
      - reading local secret files
      - parsing dotenv files
      - accepting CLI secret input
      - integrating KMS or cloud secret managers
      - implementing token generation
      - implementing credential generation
      - implementing verifier digest computation
      - implementing verifier comparison
      - implementing authentication service behavior
      - executing login, token validation, logout, refresh, or cleanup behavior
      - adding authentication Protobuf messages
      - adding WebSocket proof carriers or routes
      - adding authentication dependencies
      - changing authentication.Repository or migration schemas
    completion_summary: M-027 implemented the narrow process environment verifier key loader under runtime/internal/app/authentication. The loader accepts an explicit EnvLookup function, exposes a small process environment adapter using os.LookupEnv, reads the five required VIBIT_AUTH verifier key variables, decodes Base64URL unpadded and standard padded Base64 key text, calls NewVerifierKeySet for invariant validation, and returns redacted typed errors. Focused tests cover complete Base64URL and standard Base64 key sets, nil lookup, missing variables, invalid encoded keys, validator handoff failures for short, duplicate, all-zero, repeated-byte, and missing key-set-id inputs, redaction, and the process environment adapter. Startup wiring, local secret files, dotenv behavior, CLI secret input, KMS, cloud secret-manager integration, token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, protocol carriers, repository changes, migrations, authentication dependencies, and production authentication behavior remain deferred.

  - id: M-028
    title: Token And Credential Material Generation Implementation Gate
    status: completed
    goal: Define the next bounded implementation gate for token and credential material generation helpers before adding generation code.
    completion_criteria:
      - The gate names the exact helper ownership, file boundaries, entropy source, encoding posture, redaction requirements, tests, and deferrals.
      - The gate preserves deferrals for verifier digest computation, verifier comparison, authentication service behavior, protocol carriers, repository changes, migrations, startup wiring, authentication dependencies, and production authentication behavior.
      - No token or credential generation code is added by the gate itself.
    non_goals:
      - generating tokens or credentials
      - computing lookup or verifier digests
      - comparing verifier material
      - implementing authentication service behavior
      - wiring process startup
      - adding protocol carriers
      - changing repositories or migrations
    completion_summary: M-028 defined the token and credential material generation implementation gate in ADR-0047 and the paired gate standard. The future helper implementation is constrained to runtime/internal/app/authentication/material_generation.go and material_generation_test.go. It must use 32 random bytes, URL-safe unpadded Base64 presentation, explicit io.Reader entropy-source handoff for tests, copied raw bytes, redacted errors, all-zero and repeated-single-byte rejection, and focused tests. Digest helpers, verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior remain deferred. No token generation code or credential generation code was added by this gate.

  - id: M-029
    title: Token And Credential Material Generation Helper Implementation
    status: completed
    goal: Implement the narrow token and credential material generation helpers authorized by the implementation gate.
    completion_criteria:
      - The helpers are implemented only under runtime/internal/app/authentication/material_generation.go.
      - The helpers generate 32 bytes of random material through an explicit io.Reader handoff and encode URL-safe unpadded Base64 presentation text.
      - Focused tests cover raw length, encoding shape, round-trip, material kind, copying, nil/random error/short read failure, weak generated material rejection, non-constant output, and redaction.
      - Digest computation, verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior remain deferred.
    non_goals:
      - computing lookup or verifier digests
      - comparing verifier material
      - implementing authentication service behavior
      - executing login or token validation
      - wiring process startup
      - adding protocol carriers
      - changing repositories or migrations
      - adding authentication dependencies
    completion_summary: Implemented runtime/internal/app/authentication/material_generation.go and material_generation_test.go. The helpers expose distinct device credential and access-token material generation functions, accept an explicit io.Reader entropy source, read exactly 32 bytes with io.ReadFull, reject nil readers, read errors, short reads, all-zero material, and repeated-single-byte material, encode URL-safe unpadded Base64 presentation text, preserve MaterialKind, copy raw bytes on return, and use redacted typed errors. Tests cover the required material size, encoding shape, 43-character text length, round-trip decoding, kind preservation, copied raw accessors, fail-closed reader behavior, weak material rejection, non-constant progressing-source output, redaction, and the absence of digest/comparison behavior. Digest computation, verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior remain deferred.

  - id: M-030
    title: Verifier Digest Helper Implementation Gate
    status: completed
    goal: Define the next bounded implementation gate for verifier digest computation helpers before adding digest code.
    completion_criteria:
      - The gate names exact helper ownership, file boundaries, canonical input posture, logical key handoff, redaction requirements, tests, and deferrals.
      - The gate preserves deferrals for verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
      - No verifier digest computation code is added by the gate itself.
    non_goals:
      - computing lookup or verifier digests
      - comparing verifier material
      - implementing authentication service behavior
      - executing login or token validation
      - wiring process startup
      - adding protocol carriers
      - changing repositories or migrations
      - adding authentication dependencies
    completion_summary: Defined the verifier digest helper implementation gate in docs/verifier-digest-helper-implementation-gate.md, docs/verifier-digest-helper-implementation-gate.zh-CN.md, and ADR-0048. The gate names future helper files runtime/internal/app/authentication/verifier_digest.go and verifier_digest_test.go, defines the canonical input construction with versioned ASCII header, null separator, length-prefixed purpose label and raw material, requires HMAC-SHA-256 with the matching logical key from an already-validated VerifierKeySet, defines four digest classes with registered purpose labels and key mappings, requires copied 32-byte digest output, redacted errors, and focused tests. It preserves deferrals for verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior. No digest computation code was added.

  - id: M-031
    title: Verifier Digest Computation Helper Implementation
    status: completed
    goal: Implement the verifier digest computation helpers as a narrow application-owned code slice.
    completion_criteria:
      - The lookup and verifier digest computation helpers are implemented under runtime/internal/app/authentication/verifier_digest.go.
      - Focused unit tests are implemented under runtime/internal/app/authentication/verifier_digest_test.go.
      - The helpers build deterministic canonical input and compute HMAC-SHA-256 with the matching logical key.
      - The helpers return copied 32-byte digest bytes and expose redacted errors.
      - The helpers preserve deferrals for verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
    non_goals:
      - comparing verifier digests
      - implementing authentication service behavior
      - executing login or token validation
      - wiring process startup
      - adding protocol carriers
      - changing repositories or migrations
      - adding authentication dependencies
    completion_summary: Implemented runtime/internal/app/authentication/verifier_digest.go and verifier_digest_test.go. The helper-only slice adds DigestClass, ComputedDigest, VerifierDigestError, four registered purpose labels, deterministic canonical HMAC input with versioned ASCII header, null separator, uint16 big-endian purpose and raw material length prefixes, and four compute functions mapped to the matching logical key from VerifierKeySet. It returns copied 32-byte HMAC-SHA-256 digest bytes and redacted typed errors. Tests cover canonical input shape, purpose separation, key mapping, digest length, key/raw sensitivity, copy safety, fail-closed missing input, redaction, and the absence of verifier comparison. Verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior remain deferred.

  - id: M-032
    title: Verifier Digest Comparison Helper Gate
    status: completed
    goal: Define the next bounded gate for verifier digest comparison helpers before adding comparison code.
    completion_criteria:
      - The gate names exact comparison helper ownership, file boundaries, constant-time primitive, input redaction, failure posture, tests, and deferrals.
      - The gate preserves deferrals for authentication service behavior, login execution, token validation, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
      - No verifier comparison code is added by the gate itself.
    non_goals:
      - comparing verifier digests in this gate-only work item
      - implementing authentication service behavior
      - executing login or token validation
      - wiring process startup
      - adding protocol carriers
      - changing repositories or migrations
      - adding authentication dependencies
    completion_summary: Defined the verifier digest comparison helper gate in docs/verifier-digest-comparison-helper-gate.md, docs/verifier-digest-comparison-helper-gate.zh-CN.md, and ADR-0049. The gate names future helper files runtime/internal/app/authentication/verifier_comparison.go and verifier_comparison_test.go, keeps verifier_digest.go computation-only, requires constant-time comparison with crypto/hmac.Equal as the preferred primitive and crypto/subtle.ConstantTimeCompare as the acceptable alternative, allows comparison only between ComputedDigest verifier bytes and stored verifier digest bytes, forbids raw material, lookup digest, key id, database-only, and protocol metadata comparison, defines redacted failure posture, and requires focused tests. It preserves deferrals for authentication service behavior, login execution, token validation, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior. No verifier comparison code was added.

  - id: M-033
    title: Verifier Digest Comparison Helper Implementation
    status: completed
    goal: Implement the verifier digest comparison helpers as a narrow application-owned code slice.
    completion_criteria:
      - The verifier digest comparison helpers are implemented only under runtime/internal/app/authentication/verifier_comparison.go.
      - Focused unit tests are implemented under runtime/internal/app/authentication/verifier_comparison_test.go.
      - The helpers compare only computed verifier digest bytes to stored verifier digest bytes with a constant-time primitive.
      - The helpers reject lookup digest classes, wrong verifier classes, missing input, and malformed stored digest bytes with redacted errors.
      - The helpers preserve deferrals for authentication service behavior, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
    non_goals:
      - implementing authentication service behavior
      - executing login or token validation
      - wiring process startup
      - adding protocol carriers
      - changing repositories or migrations
      - adding authentication dependencies
    completion_summary: Implemented runtime/internal/app/authentication/verifier_comparison.go and verifier_comparison_test.go. The helper-only slice adds VerifierComparisonResult, VerifierComparisonError, class-specific CompareCredentialVerifierDigest and CompareTokenVerifierDigest helpers, and constant-time comparison with crypto/hmac.Equal. It compares only ComputedDigest verifier bytes to stored verifier digest bytes, rejects lookup classes, wrong verifier classes, missing input, malformed stored input, and malformed computed input with redacted typed errors, returns mismatch as a redacted non-match result, and keeps authentication service behavior, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior deferred.

  - id: M-034
    title: Authentication Service Behavior Implementation Gate
    status: completed
    goal: Define the bounded gate for future authentication service behavior before adding login execution, token validation, protocol carriers, repository orchestration, startup wiring, or production authentication behavior.
    completion_criteria:
      - The gate names exact service behavior ownership, input/output boundaries, repository handoff, proof validation flow, error collapse posture, tests, and deferrals.
      - The gate maps existing helper slices into a future service orchestration plan without adding service code.
      - The gate preserves deferrals for Protobuf authentication messages, WebSocket proof carriers, startup wiring, authentication dependencies, and production authentication behavior unless explicitly authorized later.
    non_goals:
      - implementing authentication service behavior in the gate-only work item
      - executing login or token validation
      - changing public contracts, repositories, migrations, or protocol carriers
      - wiring process startup
      - adding authentication dependencies
    completion_summary: Defined the authentication service behavior implementation gate in docs/authentication-service-behavior-implementation-gate.md, docs/authentication-service-behavior-implementation-gate.zh-CN.md, and ADR-0050. The gate names future service files runtime/internal/app/authentication/service.go and service_test.go, keeps service behavior application-owned, requires repository usage through the application unit-of-work boundary, maps existing verifier key, material generation, digest computation, and verifier comparison helpers into future login and access-token validation flows, defines public proof failure collapse, redaction requirements, request identity handoff, and focused tests, and preserves deferrals for login execution, token validation execution, logout, refresh, cleanup, protocol carriers, repository changes, migrations, startup wiring, dependencies, and production authentication behavior. No service code was added.

  - id: M-035
    title: Authentication Service Behavior Skeleton
    status: completed
    goal: Add a skeleton-only application authentication service shape that fails closed before login execution, token validation execution, protocol carriers, repository behavior, startup wiring, dependencies, or production authentication behavior are implemented.
    completion_criteria:
      - The skeleton defines typed service dependencies, request/result vocabulary, redacted internal failure classes, and fail-closed not-implemented behavior under runtime/internal/app/authentication.
      - Focused tests prove the skeleton does not execute login, validate tokens, call repositories for real behavior, expose protocol carriers, or leak raw proof material.
      - The skeleton preserves deferrals for real login execution, access-token validation execution, logout, refresh, cleanup, protocol carriers, repository changes, migrations, startup wiring, dependencies, and production authentication behavior.
    non_goals:
      - executing device credential login
      - validating access tokens
      - issuing tokens
      - calling PostgreSQL adapters directly
      - changing repository interfaces or migrations
      - exposing Protobuf or WebSocket authentication carriers
      - wiring process startup
      - adding authentication dependencies
    completion_summary: Implemented runtime/internal/app/authentication/service.go and service_test.go as a skeleton-only application authentication service shape. The skeleton defines Service, ServiceDependencies, UnitOfWorkRunner, ServiceError, reserved request/result vocabulary for AuthenticateWithDeviceCredential, ValidateAccessToken, LogoutAccessToken, and RefreshAccessToken, public authentication error codes, redacted internal failure classes, and fail-closed not-implemented or refresh-not-supported behavior. Focused tests prove nil dependency rejection, no unit-of-work calls from skeleton methods, no raw proof leakage, no token issuance, no identity proof population, and no revocation/refresh behavior. Real login execution, access-token validation execution, token issuance, repository lookup/mutation behavior, logout, refresh, cleanup, protocol carriers, repository changes, migrations, startup wiring, dependencies, and production authentication behavior remain deferred.

  - id: M-036
    title: Device Credential Login Service Behavior Gate
    status: completed
    goal: Define the bounded gate for future device credential login service behavior before adding real login execution, token issuance, repository orchestration, protocol carriers, startup wiring, dependencies, migrations, or production authentication behavior.
    completion_criteria:
      - The gate names exact login service behavior ownership, repository handoff sequence, helper composition order, public error collapse posture, redaction rules, and tests before login code is added.
      - The gate preserves the existing service skeleton while deferring real credential lookup, verifier comparison orchestration, token issuance, repository mutation, protocol carriers, startup wiring, dependencies, and production behavior.
      - Nakama account-authentication capability and Pitaya route identity context remain active references without copying their public APIs.
    non_goals:
      - executing device credential login
      - issuing access tokens
      - validating access tokens
      - changing Protobuf or WebSocket carriers
      - changing repository interfaces or migrations
      - wiring process startup
      - adding authentication dependencies
    completion_summary: Defined the device credential login service behavior gate in docs/device-credential-login-service-behavior-gate.md, docs/device-credential-login-service-behavior-gate.zh-CN.md, and ADR-0051. The gate fixes the future service-owned login sequence, proof input shape, unit-of-work repository handoff, helper order, player account active check, opaque access-token issuance posture, public error collapse, redaction requirements, and required tests before login execution code is added. It preserves deferrals for access-token validation, logout, refresh, cleanup, protocol carriers, WebSocket handshake authentication, startup wiring, repository changes, migrations, generated files, dependencies, and production authentication behavior.

  - id: M-037
    title: Device Credential Login Service Behavior Implementation
    status: completed
    goal: Implement the first bounded device credential login service behavior inside the existing application authentication service without adding access-token validation, protocol carriers, startup wiring, repository interface changes, migrations, dependencies, or production authentication behavior beyond the service method slice.
    completion_criteria:
      - `AuthenticateWithDeviceCredential` executes the gate-defined sequence using injected dependencies, verifier helpers, fake-testable unit-of-work repository capabilities, and existing repository interfaces.
      - Focused tests prove proof pre-validation, helper ordering, repository handoff, public error collapse, player account active checks, token digest-only storage, no raw proof leakage, and no token return on storage or commit failure.
      - Access-token validation, logout, refresh, cleanup, protocol carriers, startup wiring, repository changes, migrations, generated files, dependencies, and broader production authentication behavior remain deferred.
    non_goals:
      - validating access tokens
      - exposing Protobuf or WebSocket authentication carriers
      - changing service method signatures
      - changing repository interfaces or migrations
      - wiring process startup
      - adding external authentication dependencies
    completion_summary: Implemented device credential login inside runtime/internal/app/authentication/service.go as a service-only slice. The implementation validates Base64URL unpadded 32-byte proof before unit-of-work, computes credential lookup and verifier digests through application helpers, obtains authentication and player repositories through local unit-of-work capabilities, requires active credential and active player account state, generates opaque access-token material from an injected reader, stores token digests only, and returns raw access-token text only after unit-of-work success. Focused tests cover proof rejection, helper ordering, repository handoff, public error collapse, player account checks, digest-only token storage, redaction, and no token return on store or commit failure. Access-token validation, logout, refresh, cleanup, protocol carriers, startup wiring, repository interface changes, migrations, generated files, dependencies, and broader production behavior remain deferred.

  - id: M-038
    title: Access Token Validation Service Behavior Gate
    status: completed
    goal: Define the bounded gate for future access-token validation service behavior before validating tokens, producing RequestIdentity, touching protocol carriers, wiring startup, changing repositories, changing migrations, or adding production session behavior.
    completion_criteria:
      - The gate names the access-token validation proof shape, service ownership, repository handoff, helper composition order, token status checks, public error collapse posture, RequestIdentity handoff posture, redaction rules, and focused tests before token validation code is added.
      - The gate keeps WebSocket proof carriers, handshake authentication, route protection, session persistence, logout, refresh, cleanup jobs, repository interface changes, migrations, dependencies, and production authentication behavior deferred.
    non_goals:
      - implementing access-token validation
      - exposing Protobuf or WebSocket token carriers
      - changing repository interfaces or migrations
      - wiring process startup
      - adding session persistence
    completion_summary: Defined the access-token validation service behavior gate in docs/access-token-validation-service-behavior-gate.md, docs/access-token-validation-service-behavior-gate.zh-CN.md, and ADR-0052. The gate fixes the future service-owned validation sequence, proof input shape, dependency reuse, unit-of-work repository handoff, token lifecycle and audience checks, token verifier comparison, active player account requirement, request identity handoff with SessionValidated kept false until session persistence is ratified, public error collapse, redaction requirements, and required tests before validation execution code is added. It preserves deferrals for protocol carriers, WebSocket handshake authentication, route protection, session persistence, logout, refresh, cleanup, repository changes, migrations, generated files, dependencies, and production authentication behavior.

  - id: M-039
    title: Access Token Validation Service Behavior Implementation
    status: completed
    goal: Implement the first bounded access-token validation service behavior inside the existing application authentication service without adding protocol carriers, route protection, session persistence, startup wiring, repository interface changes, migrations, dependencies, or broader production authentication behavior.
    completion_criteria:
      - `ValidateAccessToken` executes the ADR-0052 sequence using injected dependencies, verifier helpers, fake-testable unit-of-work repository capabilities, and existing repository interfaces.
      - Focused tests prove proof pre-validation, helper ordering, repository handoff, public error collapse, token lifecycle checks, audience checks, player account active checks, request identity handoff, no raw token leakage, and no identity return on failed storage or commit boundary behavior.
      - Protocol carriers, WebSocket handshake authentication, route protection, session persistence, logout, refresh, cleanup, repository changes, migrations, generated files, dependencies, and broader production authentication behavior remain deferred.
    non_goals:
      - exposing Protobuf or WebSocket authentication carriers
      - changing service method signatures
      - changing repository interfaces or migrations
      - wiring process startup
      - adding session persistence
      - adding external authentication dependencies
    completion_summary: M-039 implemented the first service-local access-token validation behavior. `ValidateAccessToken` now rejects missing or malformed opaque Base64URL unpadded 32-byte proof before unit-of-work, computes token lookup and verifier digests with existing helpers, obtains authentication and player repositories through unit-of-work capabilities, checks token kind/status/verifier posture/audience/time window, compares verifier digest before request identity, requires active player account state, returns validated player identity only after unit-of-work success, and keeps `SessionValidated` false. Protocol carriers, WebSocket handshake authentication, route protection, session persistence, logout, refresh, cleanup, repository changes, migrations, generated files, dependencies, startup wiring, and broader production behavior remain deferred behind the next direction confirmation gate.

  - id: M-040
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after service-local device credential login and access-token validation are implemented.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for protocol carriers, route protection, session persistence, WebSocket handshake authentication, startup wiring, logout, refresh, cleanup, runtime composition, generated files, repository changes, migrations, major dependencies, and core game backend module expansion.
    non_goals:
      - exposing Protobuf or WebSocket authentication carriers without confirmation
      - adding route protection without confirmation
      - adding session persistence or WebSocket handshake authentication without confirmation
      - adding logout, refresh, cleanup, or token rotation without confirmation
      - wiring process startup or broader production authentication behavior without confirmation
    completion_summary: The maintainer authorized continued progress for five hours. The selected next direction is `expose_access_token_protocol_carrier_and_route_protection_gate`, starting with a gate-only milestone before any protocol carrier, route protection, session persistence, WebSocket handshake authentication, startup wiring, generated output, repository change, migration, dependency, logout, refresh, cleanup, or broader production behavior is implemented.

  - id: M-041
    title: Access Token Protocol Carrier And Route Protection Gate
    status: completed
    goal: Define the bounded gate for future request-level access-token proof carrier and route-protection work before adding protocol sources, generated output, adapter behavior, route protection, startup wiring, session persistence, or WebSocket handshake authentication.
    completion_criteria:
      - The gate selects a request-level validation posture and first proof-carrier posture.
      - The gate keeps the existing Protobuf envelope unchanged and WebSocket transport credential-neutral.
      - The gate names route policy, validation handoff, error mapping, redaction, tests, and deferrals before implementation.
    non_goals:
      - adding `.proto` sources or generated Protobuf files
      - changing the existing Protobuf envelope
      - implementing protocol adapter behavior
      - implementing route protection
      - wiring startup
      - adding session persistence or WebSocket handshake authentication
    completion_summary: Defined the access-token protocol carrier and route-protection gate in docs/access-token-protocol-carrier-route-protection-gate.md, docs/access-token-protocol-carrier-route-protection-gate.zh-CN.md, and ADR-0053. The gate selects request-level validation with an explicit Protobuf payload wrapper candidate, keeps the existing envelope unchanged, keeps WebSocket transport credential-neutral, requires application-owned route policy before protected domain dispatch, and preserves deferrals for `.proto` sources, generated output, protocol adapter behavior, route protection implementation, startup wiring, session persistence, WebSocket handshake authentication, repository changes, migrations, dependencies, logout, refresh, cleanup, and broader production behavior.

  - id: M-042
    title: Access Token Protocol Carrier And Route Protection Implementation
    status: completed
    goal: Implement the first bounded request-level access-token payload wrapper and application route-protection slice after the gate, without adding WebSocket handshake authentication, session persistence, startup wiring, repository changes, migrations, dependencies, logout, refresh, cleanup, or broader production behavior.
    completion_criteria:
      - Future implementation creates only the gate-authorized protocol source, generated output, protocol adapter tests, application route policy, and authentication validation handoff.
      - Protected routes reject metadata-only identity and missing, malformed, invalid, or unavailable token proof before domain dispatch.
      - The existing envelope remains unchanged and WebSocket transport remains credential-neutral.
    non_goals:
      - WebSocket handshake authentication
      - session persistence
      - startup wiring
      - repository or migration changes
      - logout, refresh, cleanup, or token rotation
    completion_summary: Implemented the bounded W-0114 request-level access-token payload wrapper and route-protection slice. The repository now has `vibit.authentication.v1.AuthenticatedRequest` as a generated Protobuf payload wrapper, Protobuf adapter handling that validates wrapped access-token proof before protected domain dispatch, application-owned route policy and validator handoff, focused tests for missing/malformed/invalid/unavailable proof and metadata-only identity rejection, and a WebSocket transport regression test that keeps handshake metadata credential-neutral. The existing Protobuf envelope remains unchanged; WebSocket handshake authentication, session persistence, startup wiring, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior remain deferred.

  - id: M-043
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after request-level access-token carrier and route protection are implemented.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session persistence, WebSocket handshake authentication, startup wiring, logout, refresh, cleanup, token rotation, repository changes, migrations, major dependencies, generated authentication messages beyond the wrapper, runtime composition, operations posture, and core game backend module expansion.
    non_goals:
      - selecting session persistence or WebSocket handshake authentication implicitly
      - wiring authentication into process startup implicitly
      - adding logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, or broader production authentication behavior without a selected milestone
      - expanding core game backend modules without a selected milestone
    completion_summary: The maintainer selected `wire_runtime_authentication_startup_composition` as the next direction and explicitly asked to keep Nakama and Pitaya as strong references. The selection creates a startup-composition gate before implementation, preserving deferrals for session persistence, WebSocket handshake authentication, logout, refresh, cleanup, token rotation, repository interface changes, migrations, dependencies, generated authentication messages beyond the existing wrapper, operations posture, and broader game backend expansion.

  - id: M-044
    title: Runtime Authentication Startup Composition Gate
    status: completed
    goal: Define the bounded startup composition gate for wiring the existing application authentication service, environment verifier key loader, request-level route protector, and Protobuf frame handler in process startup without adding session persistence, WebSocket handshake authentication, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, or broader production behavior.
    completion_criteria:
      - The gate names the process startup owner, environment configuration posture, authentication service dependency composition, route-protection handoff, tests, error redaction, Nakama/Pitaya reference mapping, and deferrals.
      - The gate keeps WebSocket transport credential-neutral and keeps the existing Protobuf envelope unchanged.
      - The gate limits first startup composition to the explicit PostgreSQL runtime store path; memory startup remains a bootstrap path without authentication repository capability.
    non_goals:
      - implementing startup composition in the gate step
      - adding session persistence or WebSocket handshake authentication
      - adding authentication command Protobuf messages or runtime login routes
      - changing repository interfaces, PostgreSQL adapters, migrations, dependencies, or generated files
    completion_summary: Defined the runtime authentication startup composition gate in docs/runtime-authentication-startup-composition-gate.md, docs/runtime-authentication-startup-composition-gate.zh-CN.md, and ADR-0054. The gate authorizes a later implementation slice to compose the existing environment verifier key loader, authentication service, route access-token validator, application route protector, and Protobuf frame handler only in the `VIBIT_RUNTIME_STORE=postgres` process path. It records default 1h token lifetime and `vibit_gameplay_runtime_requests` audience posture, redacted startup failure rules, focused tests, and Nakama/Pitaya reference mapping while preserving deferrals for session persistence, WebSocket handshake authentication, authentication command routes, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior.

  - id: M-045
    title: Runtime Authentication Startup Composition Implementation
    status: completed
    goal: Implement the bounded PostgreSQL startup composition slice that wires existing authentication service validation into request-level route protection while preserving transport neutrality and all deferred authentication lifecycle work.
    completion_criteria:
      - `runtime/cmd/vibit-server` loads verifier keys from explicit environment lookup for the PostgreSQL runtime path.
      - Startup creates the existing authentication service with PostgreSQL unit-of-work, standard-library entropy, system clock, token record id generator, positive token lifetime, and configured/default audience.
      - Startup passes `app.NewRouteProtector(authentication.NewRouteAccessTokenValidator(service))` into the Protobuf frame handler.
      - Default memory startup remains available without authentication repository capability.
      - Tests cover missing verifier configuration, lifetime parsing, route protector injection, and unchanged metadata-only bootstrap behavior for memory composition.
    non_goals:
      - WebSocket handshake authentication
      - session persistence
      - authentication command Protobuf messages or login route registration
      - repository interface changes, migrations, generated files, or new dependencies
      - logout, refresh, cleanup, token rotation, or token validation audit mutation
    completion_summary: Implemented the bounded runtime authentication startup composition slice under `runtime/cmd/vibit-server`. The PostgreSQL runtime path now loads verifier keys through the existing environment loader, builds the existing authentication service with PostgreSQL unit-of-work, `crypto/rand.Reader`, a system clock, a standard-library token record id generator, 1h/default or configured token lifetime, and `vibit_gameplay_runtime_requests`/default or configured audience, then wires the route access-token validator through `app.NewRouteProtector` into the Protobuf frame handler. The memory path remains a bootstrap path without authentication repository capability. WebSocket transport remains credential-neutral, the existing Protobuf envelope is unchanged, and session persistence, WebSocket handshake authentication, authentication command routes, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior remain deferred.

  - id: M-046
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after runtime authentication startup composition is implemented.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session persistence, WebSocket handshake authentication, authentication command routes, logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, operations posture, and core game backend module expansion.
    non_goals:
      - selecting session persistence or WebSocket handshake authentication implicitly
      - adding authentication command protocol messages or login route registration without a selected milestone
      - adding logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, or broader production authentication behavior without a selected milestone
      - expanding core game backend modules without a selected milestone
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to that recommendation. The selected next direction is `add_authentication_command_protocol_messages_and_login_route_registration`, starting with M-047/W-0119 as a gate-only milestone before protocol messages, generated output, route registration, startup composition, transaction wrapper bypass, or bridge behavior is implemented.

  - id: M-047
    title: Authentication Command Protocol And Login Route Gate
    status: completed
    goal: Define the bounded gate for exposing the existing device credential login service through a public Protobuf command route without adding session persistence, WebSocket handshake authentication, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, or broader production behavior.
    completion_criteria:
      - The gate names the public login route, planned Protobuf source, generated output, protocol bridge, route registration owner, startup composition path, transaction wrapper bypass, tests, redaction requirements, Nakama/Pitaya reference mapping, and deferrals.
      - The gate keeps WebSocket transport credential-neutral and keeps the existing Protobuf envelope unchanged.
      - The gate limits first composed login route behavior to the explicit PostgreSQL runtime store path; memory startup remains a bootstrap path without durable authentication repository capability.
    non_goals:
      - implementing Protobuf messages or generated output in the gate step
      - registering the login route in the gate step
      - adding session persistence or WebSocket handshake authentication
      - changing repository interfaces, PostgreSQL adapters, migrations, dependencies, or generated files outside the later authorized implementation
      - adding logout, refresh, cleanup, token rotation, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the authentication command protocol and login route gate in docs/authentication-command-protocol-login-route-gate.md, docs/authentication-command-protocol-login-route-gate.zh-CN.md, and ADR-0055. The gate authorizes a later implementation slice to expose the existing `AuthenticateWithDeviceCredential` service method through the public `runtime.authentication.AuthenticateWithDeviceCredential` Protobuf command route on the PostgreSQL runtime path, while keeping the envelope unchanged, WebSocket transport credential-neutral, and session persistence, WebSocket handshake authentication, logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, and direct Nakama/Pitaya API compatibility deferred.

  - id: M-048
    title: Authentication Command Protocol And Login Route Implementation
    status: completed
    goal: Implement the bounded public device credential login command route by adding gate-authorized Protobuf messages, generated output, protocol bridge behavior, application route registration, and PostgreSQL startup composition.
    completion_criteria:
      - `proto/vibit/authentication/v1/authentication.proto` defines the first login request and response messages.
      - Generated Go Protobuf output is produced through the accepted Buf path and not hand-edited.
      - Protocol bridge code maps login request/response payloads without changing the existing envelope.
      - Application bootstrap registers the explicit public login route only when the authentication service is composed.
      - The public authentication route bypasses the outer transactional dispatcher because the authentication service owns its own unit-of-work.
      - Tests cover success, public failure mapping, redaction, route publicness, protected-route behavior preservation, transport neutrality, and unchanged envelope behavior.
    non_goals:
      - session persistence
      - WebSocket handshake authentication
      - non-Protobuf credential carriers
      - repository interface changes, migrations, PostgreSQL adapter changes, or new dependencies
      - logout, refresh, cleanup, token rotation, token validation audit mutation, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the bounded W-0120 public device credential login command route. The repository now has `proto/vibit/authentication/v1/authentication.proto` and generated `runtime/internal/generated/proto/vibit/authentication/v1/authentication.pb.go` from Buf, protocol bridge behavior for `AuthenticateWithDeviceCredential`, application bootstrap route handlers, PostgreSQL startup route registration, and a transaction-wrapper bypass for the public authentication route because the authentication service owns its own unit-of-work. The existing Protobuf envelope remains unchanged; WebSocket transport remains credential-neutral; memory durable login remains unavailable; and session persistence, WebSocket handshake authentication, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, token validation audit mutation, and direct Nakama/Pitaya API compatibility remain deferred.

  - id: M-049
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the public authentication login command route is implemented.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session persistence, WebSocket handshake authentication, logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and core game backend module expansion.
    non_goals:
      - selecting session persistence or WebSocket handshake authentication implicitly
      - adding logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, or broader production authentication behavior without confirmation
      - adding memory durable authentication behavior without confirmation
      - expanding core game backend modules without confirmation
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to that recommendation. The selected direction is `ratify_session_persistence_and_websocket_handshake_authentication`, creating M-050/W-0122 as a gate-only ratification step before session persistence, WebSocket handshake authentication, first-message connection binding, session schema, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend expansion.

  - id: M-050
    title: Session Persistence And WebSocket Handshake Ratification
    status: completed
    goal: Ratify the first session persistence and WebSocket handshake authentication posture after public login route exposure without implementing session storage, handshake authentication, connection binding, migrations, dependencies, logout, refresh, cleanup, token rotation, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The current production-sensitive path remains request-level opaque access-token validation through the Protobuf authenticated request payload wrapper.
      - WebSocket transport remains credential-neutral and the existing Protobuf envelope remains unchanged.
      - Future connection-level binding is planned through a protocol/application first-message binding gate before implementation.
      - Future session persistence is planned as a later PostgreSQL-first schema/repository/migration gate before implementation.
      - Nakama and Pitaya reference mappings are recorded without adopting direct public API compatibility.
    non_goals:
      - implementing session persistence
      - implementing WebSocket handshake authentication
      - adding WebSocket Authorization, Bearer, cookie, query-string, or subprotocol proof carriers
      - changing the existing Protobuf envelope or adding generated Protobuf output
      - adding session tables, repositories, PostgreSQL adapters, migrations, dependencies, logout, refresh, cleanup, token rotation, or token validation audit mutation
    completion_summary: Ratified the first session and handshake posture in `docs/session-persistence-websocket-handshake-ratification.md`, `docs/session-persistence-websocket-handshake-ratification.zh-CN.md`, and ADR-0056. The current path remains request-level access-token validation through `vibit.authentication.v1.AuthenticatedRequest`; WebSocket transport remains credential-neutral; the existing Protobuf envelope remains unchanged; future connection binding is deferred to a first-message protocol/application gate; and future session persistence is deferred to a PostgreSQL-first schema/repository/migration gate. No runtime behavior, generated files, migrations, dependencies, logout, refresh, cleanup, token rotation, memory durable authentication behavior, or direct Nakama/Pitaya API compatibility was added.

  - id: M-051
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after session persistence and WebSocket handshake posture ratification.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for first-message connection binding, session persistence schema, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and core game backend module expansion.
    non_goals:
      - implementing first-message connection binding implicitly
      - adding session schema, repositories, migrations, dependencies, or cleanup jobs without confirmation
      - adding WebSocket handshake authentication or transport credential carriers without confirmation
      - adding logout, refresh, cleanup, token rotation, reconnect, presence, rooms, parties, match runtime, or broader game backend behavior without confirmation
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to that recommendation. The selected direction is `define_first_message_connection_binding_gate`, creating M-052/W-0124 as a gate-only step. Nakama guides the authenticate-then-realtime socket lifecycle; Pitaya guides the acceptor-session-route-handler separation. The selection keeps implementation of connection binding, session persistence schema, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend expansion deferred.

  - id: M-052
    title: First Message Connection Binding Gate
    status: completed
    goal: Define the future first-message connection binding posture before adding connection-bound identity behavior, Protobuf binding messages, generated output, route-policy use of bound identity, session persistence, reconnect behavior, logout-triggered invalidation, dependencies, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The gate selects a future protocol/application binding route and payload candidates.
      - The gate keeps WebSocket transport credential-neutral and the existing Protobuf envelope unchanged.
      - The gate preserves request-level access-token validation as the current protected-route path.
      - The gate records Nakama and Pitaya reference mappings without direct public API compatibility.
      - Implementation deferrals are explicit for Protobuf messages, generated output, connection registries, route-policy bound identity, session persistence, migrations, repositories, dependencies, logout/revocation, reconnect/epoch, and broader game backend behavior.
    non_goals:
      - implementing first-message connection binding in the gate step
      - adding new `.proto` messages or generated Protobuf output
      - changing WebSocket handshake authentication or adding transport credential carriers
      - changing the existing Protobuf envelope
      - adding connection binding registries, session tables, repositories, migrations, dependencies, logout/revocation, reconnect, presence, rooms, parties, match runtime, or group behavior
    completion_summary: Defined the first-message connection binding gate in `docs/first-message-connection-binding-gate.md`, `docs/first-message-connection-binding-gate.zh-CN.md`, and ADR-0057. The gate selects the future `runtime.authentication.BindConnection` system route with `vibit.authentication.v1.BindConnectionRequest` and `vibit.authentication.v1.BindConnectionResponse` payload candidates, keeps WebSocket transport credential-neutral, keeps the existing Protobuf envelope unchanged, keeps request-level access-token validation as the current path, and preserves deferrals for implementation, generated output, connection registries, route-policy bound identity, session persistence, migrations, repositories, dependencies, logout/revocation, reconnect/epoch, memory durable authentication behavior, and direct Nakama/Pitaya API compatibility.

  - id: M-053
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the first-message connection binding gate is defined.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for first-message connection binding implementation, PostgreSQL session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing connection binding implicitly
      - adding Protobuf messages, generated output, connection registries, startup wiring, or route-policy bound identity without confirmation
      - adding session schema, repositories, migrations, dependencies, or cleanup jobs without confirmation
      - adding logout, refresh, cleanup, token rotation, reconnect, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it. The selected direction is `define_first_message_connection_binding_implementation_gate`, creating M-054/W-0126 as a gate-only step. Nakama guides authenticated realtime socket lifecycle; Pitaya guides acceptor-session-handler separation. The selection keeps actual BindConnection implementation, Protobuf source changes, generated output, route-policy bound identity, session persistence, logout/revocation, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend expansion deferred.

  - id: M-054
    title: First Message Connection Binding Implementation Gate
    status: completed
    goal: Define the bounded implementation gate for future `runtime.authentication.BindConnection` before adding protocol messages, generated output, protocol adapter behavior, application binding registries, startup composition, route-policy bound identity, session persistence, migrations, dependencies, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The gate names future BindConnection Protobuf messages, generated output, protocol adapter owner, application binding owner, startup owner, tests, redaction requirements, and deferrals.
      - The gate keeps WebSocket transport credential-neutral and keeps the existing Protobuf envelope unchanged.
      - The gate preserves request-level access-token validation as the current protected-route path.
      - The gate records Nakama and Pitaya reference mappings without direct public API compatibility.
      - The gate does not add runtime behavior, protocol source, generated output, connection registries, route-policy bound identity, session persistence, repository changes, migrations, dependencies, logout/revocation, or reconnect/epoch behavior.
    non_goals:
      - implementing first-message connection binding in the gate step
      - adding new `.proto` messages or generated Protobuf output
      - changing WebSocket handshake authentication or adding transport credential carriers
      - changing the existing Protobuf envelope
      - adding connection binding registries, session tables, repositories, migrations, dependencies, logout/revocation, reconnect, presence, rooms, parties, match runtime, group behavior, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the first-message connection binding implementation gate in `docs/first-message-connection-binding-implementation-gate.md`, `docs/first-message-connection-binding-implementation-gate.zh-CN.md`, and ADR-0058. The gate defines the future bounded implementation slice for `runtime.authentication.BindConnection`, including planned Protobuf messages, generated output, protocol adapter handoff, application binding ownership, PostgreSQL startup composition, public error mapping, required tests, and Nakama/Pitaya reference alignment. No Go runtime behavior, Protobuf source, generated output, migration, dependency, connection registry, route-policy bound identity, session persistence, logout/revocation, reconnect/epoch behavior, memory durable authentication behavior, or broader game backend behavior was added.

  - id: M-055
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the first-message connection binding implementation gate is defined.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for implementing first-message connection binding, PostgreSQL session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing connection binding implicitly
      - adding Protobuf messages, generated output, connection registries, startup wiring, or route-policy bound identity without confirmation
      - adding session schema, repositories, migrations, dependencies, or cleanup jobs without confirmation
      - adding logout, refresh, cleanup, token rotation, reconnect, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it, with Nakama and Pitaya as key references. The selected direction is `implement_first_message_connection_binding`, creating M-056/W-0128 as a bounded implementation slice authorized by ADR-0058. Nakama guides authenticated session material before realtime socket use; Pitaya guides transport acceptor, session-like binding, and handler-context separation. The selection keeps PostgreSQL session persistence schema, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch policy, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend expansion deferred.

  - id: M-056
    title: First Message Connection Binding Implementation
    status: completed
    goal: Implement the bounded first-message `runtime.authentication.BindConnection` system route slice without adding session persistence, WebSocket handshake authentication, route-policy bound identity for ordinary protected routes, migrations, dependencies, logout/revocation, reconnect behavior, or direct Nakama/Pitaya public API compatibility.
    completion_criteria:
      - `BindConnectionRequest`, `BindConnectionResponse`, and `ConnectionBindingStatus` are added to the authentication Protobuf source and generated Go output is produced by Buf.
      - The Protobuf adapter recognizes `runtime.authentication.BindConnection` as a system route and hands it to application-owned binding logic before normal command/query route protection and dispatch.
      - Application-owned binding validates opaque access-token proof through the existing access-token validation boundary, uses server-observed connection metadata, returns no secret material, and keeps `SessionValidated` false.
      - PostgreSQL startup composition injects the binder only when the authentication service is composed, while memory bootstrap keeps binding unavailable.
      - WebSocket transport remains credential-neutral and the existing Protobuf envelope remains unchanged.
      - Tests cover success, public failure mapping, redaction, startup injection, transport neutrality, generated output, and request-level route-protection compatibility.
    non_goals:
      - durable session persistence
      - session tables, repositories, PostgreSQL adapters, migrations, or cleanup jobs
      - WebSocket handshake authentication or transport credential carriers
      - route-policy use of bound identity for ordinary protected routes
      - logout-triggered active connection invalidation
      - reconnect, resume, duplicate replacement, or connection epoch policy beyond metadata handoff
      - new dependencies
      - memory durable authentication behavior
      - direct Nakama or Pitaya public API compatibility
      - presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Implemented the W-0128 `runtime.authentication.BindConnection` slice. Authentication Protobuf source now includes `BindConnectionRequest`, `BindConnectionResponse`, and `ConnectionBindingStatus`; generated Go output was updated through Buf. Runtime application code owns `ConnectionBinder`; the Protobuf adapter intercepts the system route before normal dispatch; PostgreSQL startup injects the binder when authentication is composed; WebSocket transport carries server-observed connection epoch metadata without parsing credentials. The existing envelope and request-level `AuthenticatedRequest` route-protection path remain unchanged. No session persistence, migration, repository change, dependency, logout/revocation active-connection behavior, reconnect/epoch policy, memory durable authentication behavior, or direct Nakama/Pitaya API compatibility was added.

  - id: M-057
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after first-message connection binding is implemented.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for PostgreSQL session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding session schema, repositories, migrations, dependencies, or cleanup jobs without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
      - making first-message bound identity satisfy protected routes without an explicit future route-policy milestone
    completion_summary: The maintainer authorized continuing according to the agent's recommendation. The selected direction is `define_postgres_session_persistence_schema_gate`, creating M-058/W-0130 as a gate-only milestone. Nakama guides first-class session lifecycle and realtime socket identity expectations; Pitaya guides session-like context separation from acceptors and handlers. The selection keeps SQL migration source, session repositories, PostgreSQL adapters, runtime session validation, route-policy bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.

  - id: M-058
    title: PostgreSQL Session Persistence Schema Gate
    status: completed
    goal: Define the future first durable runtime session persistence schema boundary before adding session migrations, repositories, adapters, runtime validation behavior, route-policy bound identity, logout/revocation, or reconnect behavior.
    completion_criteria:
      - English and Simplified Chinese standards define the gate-only PostgreSQL session persistence schema boundary.
      - An ADR records PostgreSQL as the first durable session store target and `runtime_sessions` as the future logical table candidate.
      - Runtime, convention, contract, reference, work, module, AGENTS, rule, and tool artifacts reference the gate.
      - Repository checks verify that no session migration source, repository interface, PostgreSQL adapter, runtime session validation behavior, WebSocket handshake authentication, route-policy bound identity, logout/revocation, reconnect, dependency, or direct Nakama/Pitaya API compatibility is added by the gate.
    non_goals:
      - adding SQL migration source
      - creating `runtime_sessions` or `runtime_session_connections` tables
      - adding repository interfaces or PostgreSQL adapters
      - creating, validating, revoking, or cleaning up sessions at runtime
      - setting `RequestIdentity.SessionValidated` true
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope
      - adding Protobuf messages or generated output
      - making bound connection identity satisfy ordinary protected route policy
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, or durable epoch behavior
      - adding dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Defined the PostgreSQL session persistence schema gate in `docs/postgres-session-persistence-schema-gate.md`, `docs/postgres-session-persistence-schema-gate.zh-CN.md`, and ADR-0059. The gate selects PostgreSQL as the first durable runtime session store target, records `runtime_sessions` as the future logical table candidate, records `runtime/migrations/postgres/000005_create_runtime_sessions.sql` as the future migration source candidate, and preserves deferrals for migration source, repositories, adapters, runtime session validation, route-policy bound identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader realtime/game backend behavior.

  - id: M-059
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the PostgreSQL session persistence schema gate is defined.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session migration source, session repository boundary, session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding session migration source, repositories, adapters, dependencies, or cleanup jobs without confirmation
      - adding runtime session validation, logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
      - making first-message bound identity or future session identity satisfy protected routes without an explicit future route-policy milestone
    completion_summary: The maintainer asked the agent to recommend the next ten steps and continue, with Nakama and Pitaya as strong references. The selected direction is `implement_runtime_sessions_migration_source`, creating M-060/W-0132 as a migration-source-only slice. Nakama guides first-class authenticated session lifecycle records, and Pitaya guides keeping session context separate from transport acceptors and handlers. Session repositories, PostgreSQL adapters, runtime session validation, route-policy use of session or bound identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.

  - id: M-060
    title: Runtime Sessions Migration Source
    status: completed
    goal: Add only the PostgreSQL `runtime_sessions` migration source authorized by the prior schema gate.
    completion_criteria:
      - `runtime/migrations/postgres/000005_create_runtime_sessions.sql` exists with goose Up and Down markers.
      - The migration creates `runtime_sessions` with lifecycle, actor, player, status, issued, expiration, last-seen, creation, update, revocation, and optional access-token record linkage fields.
      - The migration forbids raw token, raw credential, token digest, credential digest, WebSocket connection state, and connection registry storage.
      - Runtime, convention, contract, reference, work, module, AGENTS, rule, and tool artifacts record the migration-source-only slice.
      - Repository checks verify that no session repository, PostgreSQL adapter behavior, runtime session validation behavior, WebSocket handshake authentication, route-policy use of session or bound identity, logout/revocation active-connection invalidation, reconnect/epoch behavior, dependency, or direct Nakama/Pitaya API compatibility is added.
    non_goals:
      - adding session repository interfaces or PostgreSQL adapters
      - creating, validating, revoking, or cleaning up sessions at runtime
      - setting `RequestIdentity.SessionValidated` true
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope or adding Protobuf session messages
      - creating `runtime_session_connections` or durable connection registry storage
      - making bound connection identity or persisted session identity satisfy ordinary protected route policy
      - adding logout/revocation active-connection invalidation, reconnect, resume, duplicate replacement, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Added the PostgreSQL migration source `runtime/migrations/postgres/000005_create_runtime_sessions.sql`. The table records durable runtime session lifecycle rows with actor/player identity, status, issued/expires/last_seen timestamps, optional revocation fields, and optional `authentication_access_tokens(token_record_id)` linkage. It deliberately does not store raw token text, raw credentials, token or credential digest bytes, WebSocket connection state, or connection registry rows. No Go repository, PostgreSQL adapter, runtime session validation behavior, WebSocket handshake authentication, route-policy change, logout/revocation active-connection behavior, reconnect/epoch behavior, dependency, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior was added.

  - id: M-061
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the runtime sessions migration source is added.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session repository boundary, session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding session repository interfaces, PostgreSQL adapters, dependencies, or cleanup jobs without confirmation
      - adding runtime session validation, logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
      - making first-message bound identity or persisted session identity satisfy protected routes without an explicit future route-policy milestone
    completion_summary: The maintainer asked the agent to recommend the next ten steps and continue, with Nakama and Pitaya as strong references. The selected direction is `define_session_repository_boundary`, creating M-062/W-0134 as a gate-only repository-boundary slice. Nakama guides the need for queryable session lifecycle capabilities, and Pitaya guides keeping session context separate from acceptors and transport. Go repository implementation, PostgreSQL adapters, runtime session validation, route-policy use of session or bound identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.

  - id: M-062
    title: Session Repository Boundary Gate
    status: completed
    goal: Define the future storage-neutral runtime session repository boundary before adding Go repository code, PostgreSQL adapters, runtime session validation, route-policy session identity, logout/revocation behavior, or reconnect behavior.
    completion_criteria:
      - English and Simplified Chinese standards define the gate-only session repository boundary.
      - An ADR records the future storage-neutral repository owner candidate and PostgreSQL adapter owner.
      - Runtime, convention, contract, reference, work, module, AGENTS, rule, and tool artifacts reference the boundary.
      - Repository checks verify that no Go session repository code, PostgreSQL adapter behavior, runtime session validation behavior, WebSocket handshake authentication, route-policy session identity, logout/revocation, reconnect, dependency, or direct Nakama/Pitaya API compatibility is added by the gate.
    non_goals:
      - adding `runtime/internal/app/session` or Go repository interfaces
      - adding PostgreSQL session adapters
      - creating, validating, revoking, or cleaning up sessions at runtime
      - setting `RequestIdentity.SessionValidated` true
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope or adding Protobuf session messages
      - making bound connection identity or persisted session identity satisfy ordinary protected route policy
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Defined the session repository boundary in `docs/session-repository-boundary.md`, `docs/session-repository-boundary.zh-CN.md`, and ADR-0061. The gate records `runtime/internal/app/session` as the future storage-neutral repository owner candidate, `runtime/internal/platform/persistence/postgres` as the future PostgreSQL adapter owner, candidate lifecycle capabilities such as `CreateRuntimeSession`, `FindActiveSessionByID`, `UpdateRuntimeSessionLastSeen`, `MarkRuntimeSessionExpired`, and `RevokeRuntimeSession`, and explicit separation from token validation, WebSocket transport, protocol carriers, route policy, logout/revocation active-connection behavior, reconnect behavior, dependencies, and direct Nakama/Pitaya API compatibility.

  - id: M-063
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the session repository boundary is defined.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session repository implementation, session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding session repository implementation, PostgreSQL adapters, dependencies, or cleanup jobs without confirmation
      - adding runtime session validation, logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
      - making first-message bound identity or persisted session identity satisfy protected routes without an explicit future route-policy milestone
    completion_summary: The maintainer asked the agent to recommend the next ten steps and continue, with Nakama and Pitaya as strong references. The selected direction is `implement_session_repository_interface`, creating M-064/W-0136 as a storage-neutral interface-only implementation slice. Nakama guides the need for first-class lifecycle lookup, expiration, revocation, logout, and management-ready session records, and Pitaya guides keeping session context separate from acceptors and transport. PostgreSQL adapters, runtime session validation, route-policy use of session or bound identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.

  - id: M-064
    title: Session Repository Interface Implementation
    status: completed
    goal: Implement the storage-neutral runtime session repository interface and lifecycle value types before adding PostgreSQL adapters, runtime session validation, route-policy session identity, logout/revocation behavior, or reconnect behavior.
    completion_criteria:
      - `runtime/internal/app/session/repository.go` defines storage-neutral value types, status vocabulary, lifecycle query and mutation types, and `Repository`.
      - `runtime/internal/app/session/repository_test.go` covers status vocabulary, normalization, UTC timestamp handling, lifecycle query and mutation validation, bounded active-session listing, and absence of secret material fields.
      - Runtime, convention, contract, reference, work, module, AGENTS, rule, and tool artifacts reference the implementation.
      - Repository checks verify that no PostgreSQL adapter, runtime session validation behavior, WebSocket handshake authentication, route-policy session identity, logout/revocation, reconnect, dependency, generated output, or direct Nakama/Pitaya API compatibility is added by the slice.
    non_goals:
      - adding PostgreSQL session adapters or unit-of-work factory wiring
      - creating, validating, revoking, expiring, or cleaning up sessions at runtime
      - setting `RequestIdentity.SessionValidated` true
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope or adding Protobuf session messages
      - making bound connection identity or persisted session identity satisfy ordinary protected route policy
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Implemented the storage-neutral session repository interface in `runtime/internal/app/session/repository.go` with focused tests in `runtime/internal/app/session/repository_test.go`. The package defines first-posture player session lifecycle records, active/expired/revoked status vocabulary, creation, lookup, active lookup, last-seen, expiration, revocation, and bounded player active-session listing shapes, plus normalization helpers. The slice adds no PostgreSQL adapter, SQL execution, runtime validation, session creation at login or BindConnection, route-policy session identity, WebSocket handshake authentication, transport credential carriers, Protobuf session messages, logout/revocation active-connection behavior, reconnect/epoch behavior, dependency, memory durable session behavior, or direct Nakama/Pitaya API compatibility.

  - id: M-065
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the session repository interface is implemented.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding session PostgreSQL adapters, runtime validation, dependencies, or cleanup jobs without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
      - making first-message bound identity or persisted session identity satisfy protected routes without an explicit future route-policy milestone
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it, with Nakama and Pitaya as key references. The selected direction is `define_session_postgresql_adapter_gate`, creating M-066/W-0138 as a gate-only adapter boundary slice. Nakama guides durable lifecycle lookup, expiration, revocation, logout, and listing pressure; Pitaya guides keeping session context and durable persistence out of acceptors, routing, and transport. PostgreSQL adapter implementation, runtime session validation, route-policy use of session or bound identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.

  - id: M-066
    title: Session PostgreSQL Adapter Gate
    status: completed
    goal: Define the future PostgreSQL adapter boundary for the storage-neutral runtime session repository before adding adapter code, unit-of-work factory wiring, runtime validation, route-policy session identity, logout/revocation behavior, or reconnect behavior.
    completion_criteria:
      - English and Simplified Chinese standards define the gate-only session PostgreSQL adapter boundary.
      - An ADR records platform persistence ownership, transaction handoff, SQL shape, error mapping, redaction expectations, and future adapter tests.
      - Runtime, convention, contract, reference, work, module, AGENTS, rule, and tool artifacts reference the gate.
      - Repository checks verify that no PostgreSQL session adapter code, unit-of-work factory wiring, runtime session validation behavior, WebSocket handshake authentication, route-policy session identity, logout/revocation, reconnect, dependency, or direct Nakama/Pitaya API compatibility is added by the gate.
    non_goals:
      - adding PostgreSQL session adapter Go files
      - adding unit-of-work factory wiring for session repositories
      - creating, validating, revoking, expiring, or cleaning up sessions at runtime
      - setting `RequestIdentity.SessionValidated` true
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope or adding Protobuf session messages
      - making bound connection identity or persisted session identity satisfy ordinary protected route policy
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Defined the session PostgreSQL adapter gate in `docs/session-postgresql-adapter-gate.md`, `docs/session-postgresql-adapter-gate.zh-CN.md`, and ADR-0063. The gate records `runtime/internal/platform/persistence/postgres` as the future adapter owner for `runtime/internal/app/session.Repository`, defines transaction handoff, SQL shape, error mapping, redaction, future adapter tests, and explicit separation from token validation, request identity construction, WebSocket transport, Protobuf carriers, route policy, logout/revocation active-connection behavior, reconnect behavior, dependencies, and direct Nakama/Pitaya API compatibility.

  - id: M-067
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the session PostgreSQL adapter gate is defined.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session PostgreSQL adapter implementation, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding session PostgreSQL adapter implementation, runtime validation, dependencies, or cleanup jobs without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
      - making first-message bound identity or persisted session identity satisfy protected routes without an explicit future route-policy milestone
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it, with Nakama and Pitaya as key references. The selected direction is `implement_session_postgresql_adapter`, creating M-068/W-0140 as a bounded persistence adapter implementation slice. Nakama guides durable lifecycle lookup, expiration, revocation, logout-support, and management-ready listing pressure; Pitaya guides keeping session context and durable persistence separate from acceptors, routing, and transport. Runtime session creation or validation, route-policy use of persisted session or bound identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.

  - id: M-068
    title: Session PostgreSQL Adapter Implementation
    status: completed
    goal: Implement the bounded PostgreSQL adapter for `runtime/internal/app/session.Repository` without adding runtime session validation, WebSocket authentication, route-policy session identity, logout/revocation active-connection behavior, or reconnect behavior.
    completion_criteria:
      - `runtime/internal/platform/persistence/postgres/session_repository.go` implements `session.Repository`.
      - `runtime/internal/platform/persistence/postgres/session_repository_test.go` covers SQL shape, row mapping, error mapping, nullable fields, unit-of-work factory wiring, and no live PostgreSQL requirement.
      - `UnitOfWork.NewSessionRepository()` creates the adapter inside the existing PostgreSQL unit-of-work boundary.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the implementation.
      - Repository checks verify that the slice remains persistence-only and does not add runtime validation, WebSocket credential carriers, Protobuf session messages, route-policy session identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, or direct Nakama/Pitaya API compatibility.
    non_goals:
      - creating sessions at login or BindConnection
      - validating runtime sessions or setting `RequestIdentity.SessionValidated` true
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope or adding Protobuf session messages
      - making bound connection identity or persisted session identity satisfy ordinary protected route policy
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Implemented the PostgreSQL session repository adapter in `runtime/internal/platform/persistence/postgres/session_repository.go` with focused fake-executor tests in `runtime/internal/platform/persistence/postgres/session_repository_test.go`, and added `UnitOfWork.NewSessionRepository()`. The adapter maps storage-neutral session repository calls to the existing `runtime_sessions` table, normalizes application inputs and returned rows, maps PostgreSQL errors to redacted typed errors, and stays transaction-neutral. The slice adds no runtime session creation or validation, `RequestIdentity.SessionValidated = true`, WebSocket handshake authentication, transport credential carriers, Protobuf session messages, route-policy session identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, or direct Nakama/Pitaya API compatibility.

  - id: M-069
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the session PostgreSQL adapter implementation is complete.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for runtime session validation behavior, session creation composition, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding runtime validation, session creation composition, dependencies, or cleanup jobs without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
      - making first-message bound identity or persisted session identity satisfy protected routes without an explicit future route-policy milestone
    completion_summary: The maintainer authorized continuing with the agent's recommendation, with Nakama and Pitaya as key references. The selected direction is `define_runtime_session_validation_gate`, creating M-070/W-0142 as a gate-only slice. Nakama guides session lifetime, expiration, revocation, logout, and active socket separation; Pitaya guides acceptor/session/handler separation. Runtime validation implementation, session creation composition, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.

  - id: M-070
    title: Runtime Session Validation Gate
    status: completed
    goal: Define the future application-owned runtime session validation boundary after the PostgreSQL session adapter without adding validation behavior.
    completion_criteria:
      - `docs/runtime-session-validation-gate.md` and `docs/runtime-session-validation-gate.zh-CN.md` define the future validation boundary.
      - The gate defines validation ownership, candidate input, validation order, request identity handoff, public error collapse, redaction, future tests, and Nakama/Pitaya reference mapping.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the gate.
      - Repository checks verify that no runtime validation implementation, `RequestIdentity.SessionValidated = true`, route-policy session identity, WebSocket authentication, transport credential carrier, Protobuf session message, logout/revocation active-connection behavior, reconnect/epoch behavior, dependency, memory durable session behavior, or direct Nakama/Pitaya API compatibility is added.
    non_goals:
      - implementing runtime session validation
      - creating sessions at login or BindConnection
      - setting `RequestIdentity.SessionValidated` true in runtime behavior
      - changing route policy
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope or adding Protobuf session messages
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Defined the runtime session validation gate in `docs/runtime-session-validation-gate.md`, `docs/runtime-session-validation-gate.zh-CN.md`, and ADR-0065. The gate makes future validation application-owned, requires already-validated actor identity before trusting persisted session rows, defines durable session lookup/status/expiration/actor-match semantics, collapses public invalid-session failures, preserves WebSocket and Protobuf neutrality, and keeps session creation composition, route policy, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, and direct Nakama/Pitaya API compatibility deferred.

  - id: M-071
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the runtime session validation gate is defined.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for runtime session validation implementation, session creation composition, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing runtime validation, session creation composition, dependencies, or cleanup jobs without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
      - making first-message bound identity or persisted session identity satisfy protected routes without an explicit future route-policy milestone
    completion_summary: The maintainer authorized continuing with the agent's recommendation, with Nakama and Pitaya as key references. The selected direction is `implement_runtime_session_validation`, creating M-072/W-0144 as a bounded implementation slice. Nakama guides first-class session lifecycle validity, expiration, refresh, logout, and socket separation; Pitaya guides keeping transport acceptors, session context, and handler routing separate. Session creation composition, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.

  - id: M-072
    title: Runtime Session Validation Implementation
    status: completed
    goal: Implement the application-owned persistent runtime session validator without wiring it into route policy, transport, protocol, logout, reconnect, or session creation behavior.
    completion_criteria:
      - `runtime/internal/app/runtime_session_validator.go` implements a persistent session validator.
      - `runtime/internal/app/runtime_session_validator_test.go` covers active-session success, missing or malformed session id, metadata-only identity, unvalidated identity, lookup failures, repository errors, record mismatches, nil repository, redaction, and no repository mutations.
      - The validator uses `runtime/internal/app/session.Repository.FindActiveSessionByID`.
      - `RequestIdentity.SessionValidated` is set true only after already-validated actor identity and durable active-session validation both pass.
      - Public failures collapse to a stable invalid-session class.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the implementation.
      - Repository checks verify that the slice does not add session creation composition, route-policy session identity, WebSocket authentication, transport credential carriers, Protobuf session messages, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, or direct Nakama/Pitaya API compatibility.
    non_goals:
      - creating runtime sessions at login or BindConnection
      - wiring the validator into startup or route policy
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope or adding Protobuf session messages
      - updating `last_seen_at` during validation
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Implemented the application-owned `PersistentSessionValidator` in `runtime/internal/app/runtime_session_validator.go` with focused fake-repository tests in `runtime/internal/app/runtime_session_validator_test.go`. The validator requires already validated player identity, resolves a normalized session id, looks up an active persisted runtime session through `session.Repository.FindActiveSessionByID`, checks active/unexpired actor-player match, and sets `RequestIdentity.SessionValidated = true` only after durable validation succeeds. It is not wired into route policy or startup and does not create sessions, update `last_seen_at`, change WebSocket or Protobuf behavior, invalidate active connections, add reconnect behavior, add dependencies, or add direct Nakama/Pitaya API compatibility.

  - id: M-073
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the runtime session validation implementation is complete.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session creation composition, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - creating sessions at login or BindConnection without confirmation
      - wiring session-validated identity into ordinary protected route policy without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
    completion_summary: The maintainer authorized continuing with the agent's recommendation, with Nakama and Pitaya as key references. The selected direction is `define_session_creation_composition_gate`, creating M-074/W-0146 as a gate-only composition slice. Nakama guides login/session lifecycle pressure, token/session expiration, refresh, logout, and management concerns; Pitaya guides keeping acceptors, session context, and handlers separated. Session creation implementation, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.

  - id: M-074
    title: Session Creation Composition Gate
    status: completed
    goal: Define the future application-owned composition boundary for creating durable runtime sessions without adding session creation code or changing route, transport, protocol, logout, or reconnect behavior.
    completion_criteria:
      - `docs/session-creation-composition-gate.md` and `docs/session-creation-composition-gate.zh-CN.md` define the gate.
      - `ADR-0067` records the decision.
      - The future composition owner is `runtime/internal/app`.
      - The future login-time composition candidate is `AuthenticateWithDeviceCredential`.
      - The future session repository method is `CreateRuntimeSession` through unit-of-work capabilities.
      - Session id, lifetime, token linkage, redaction, and future test expectations are recorded.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the gate.
      - Repository checks verify that the slice does not add session creation implementation, authentication service code changes, session id generation, route-policy session identity, WebSocket authentication, transport credential carriers, Protobuf session messages, generated output, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, or direct Nakama/Pitaya API compatibility.
    non_goals:
      - implementing session creation
      - changing `AuthenticateWithDeviceCredential`
      - generating session ids
      - creating sessions at login or BindConnection
      - changing runtime session validation or route policy
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope, adding Protobuf session messages, or adding generated output
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Defined the gate-only session creation composition standard. The gate records future application-owned login-time session creation composition, transaction ordering with token storage, private `access_token_record_id` linkage, session id and lifetime posture, redaction expectations, test requirements, and Nakama/Pitaya mapping. It does not add runtime session creation behavior, authentication service changes, route-policy changes, WebSocket or Protobuf changes, logout/revocation active-connection behavior, reconnect behavior, dependencies, memory durable session behavior, or direct Nakama/Pitaya API compatibility.

  - id: M-075
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the session creation composition gate is complete.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for session creation implementation, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing session creation without confirmation
      - wiring session-validated identity into ordinary protected route policy without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_session_creation_composition`, because durable runtime session validation and storage now exist but successful login still needed to create the corresponding session lifecycle row. Nakama informs login/session lifecycle pressure, expiration, logout, refresh, and management implications; Pitaya informs separation of acceptors, session context, handlers, and durable application composition.

  - id: M-076
    title: Session Creation Composition Implementation
    status: completed
    goal: Compose successful device-credential login with durable runtime session creation in the same application unit of work as access-token storage.
    completion_criteria:
      - `AuthenticateWithDeviceCredential` creates a durable active runtime session after token storage and before unit-of-work commit.
      - Runtime session creation uses `runtime/internal/app/session.Repository.CreateRuntimeSession` through a unit-of-work capability.
      - The first runtime session id is generated by an application-owned startup dependency and is not client supplied.
      - The runtime session links to `access_token_record_id` as private server metadata.
      - Raw credential material, raw access-token material, lookup digests, verifier digests, and verifier key ids are not copied into runtime session records.
      - Session creation failure and commit failure prevent successful token/session material return.
      - Route policy, WebSocket handshake authentication, Protobuf session carriers, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.
    non_goals:
      - exposing session ids through Protobuf login responses
      - changing the existing Protobuf envelope
      - changing request-level route protection to use session-validated or bound identity
      - changing access-token validation semantics
      - setting `RequestIdentity.SessionValidated = true` during login or token validation
      - adding WebSocket handshake authentication or transport credential carriers
      - implementing logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior
    completion_summary: Implemented login-time durable runtime session creation in the authentication service. Successful device-credential login now stores the access-token verifier record and creates one active runtime session linked to the stored token record in the same unit of work. Startup composition provides a random runtime session id generator. Tests cover success, repository acquisition failure, session id failure, session creation failure, commit failure, and unchanged access-token validation. WebSocket, Protobuf, route policy, logout, reconnect, generated output, dependencies, memory durable sessions, and direct Nakama/Pitaya compatibility remain unchanged.

  - id: M-077
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after session creation composition implementation.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - wiring session-validated or bound identity into ordinary protected route policy without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, or broader game backend behavior without confirmation
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_bound_identity_route_policy_gate`, because request-level token validation, first-message binding, durable session validation, and login-created sessions now exist but route policy has not defined how those identity sources may authorize routes. Nakama informs session-backed gameplay access and lifecycle pressure; Pitaya informs keeping acceptors, sessions, and handlers separated.

  - id: M-078
    title: Bound Identity Route Policy Gate
    status: completed
    goal: Define the future application-owned route-policy boundary for request-token, bound-connection, session-validated, and bound-session identity without changing production route behavior.
    completion_criteria:
      - `docs/bound-identity-route-policy-gate.md` and `docs/bound-identity-route-policy-gate.zh-CN.md` define the gate.
      - `ADR-0069` records the decision.
      - Future route policy ownership is `runtime/internal/app`.
      - Candidate route policy families are recorded for public, request-token, bound-connection, session-validated, and bound-session routes.
      - The recommended first implementation posture keeps ordinary protected domain routes on request-level access-token proof.
      - Nakama and Pitaya mappings are recorded in vibit terms.
      - WebSocket, Protobuf, logout, reconnect, operations, memory durable session behavior, direct Nakama/Pitaya compatibility, and broader game backend module expansion remain deferred.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the gate.
    non_goals:
      - implementing route-policy use of bound identity
      - implementing route-policy use of session-validated identity
      - removing per-request access-token proof from ordinary protected routes
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope, adding Protobuf session messages, or adding generated output
      - exposing session ids in login responses
      - adding logout/revocation active-connection invalidation
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Defined the gate-only bound identity route policy standard. The gate records future application-owned policy families and a conservative first implementation posture: ordinary protected routes continue using request-level access-token proof until a later implementation slice explicitly reclassifies routes. Bound connection identity and session-validated identity remain explicit route-scoped future policy families.

  - id: M-079
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the bound identity route policy gate.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for route-policy implementation, logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing route-policy use of bound or session-validated identity without confirmation
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, protocol session carriers, or broader game backend behavior without confirmation
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_bound_identity_route_policy`, because the route-policy gate already defined explicit policy families and the next safest step is to implement application-owned classification and fail-closed identity agreement without changing ordinary protected domain routes. Nakama informs session-backed gameplay access while keeping logout and refresh separate; Pitaya informs keeping acceptors, sessions, and handlers separated.

  - id: M-080
    title: Bound Identity Route Policy Implementation
    status: completed
    goal: Implement explicit application-owned route policy families for request-token, bound-connection, session-validated, and bound-session routes while keeping ordinary protected routes on request-level access-token proof.
    completion_criteria:
      - `runtime/internal/app/route_authentication.go` defines explicit route protection requirement vocabulary and route-scoped classification.
      - The default route policy keeps `runtime.authentication.AuthenticateWithDeviceCredential` public and all other routes `request_token_required` unless explicitly classified.
      - Ordinary protected domain routes still require request-level access-token proof and still clear `SessionValidated` after token validation.
      - Explicit bound-connection routes require a validated player identity bound to the observed connection id and epoch.
      - Explicit session-validated routes require an already session-validated player identity with matching session id.
      - Explicit bound-session routes require bound and session-validated identity sources to agree and fail closed on mismatch.
      - Metadata-only identity is rejected for every protected policy family.
      - WebSocket, Protobuf, startup route registration, logout/revocation, reconnect/epoch, protocol session carriers, dependencies, generated output, and direct Nakama/Pitaya compatibility remain unchanged.
      - Focused Go tests cover classification, unchanged token behavior, metadata-only rejection, explicit bound/session/bound-session policy success and failure, and identity mismatch failure.
      - ADR, manifests, guides, check rules, change specs, and conversation memory record the implementation.
    non_goals:
      - configuring any production domain route to stop requiring request-level access-token proof by default
      - adding a connection binding registry or durable active-connection store
      - wiring persisted runtime session validation into frame handling or startup route policy
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope, adding protocol session carriers, or exposing session ids through Protobuf
      - adding logout/revocation active-connection invalidation, reconnect, resume, duplicate replacement, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Implemented explicit route protection policy vocabulary and route-scoped classification in the application layer. The first posture remains conservative: public authentication stays explicit, default protected domain routes still require request-level token proof, and bound/session/bound-session identity can satisfy only routes explicitly configured for those policy families. The implementation is fail-closed, rejects metadata-only identity, preserves WebSocket and Protobuf behavior, and keeps logout, reconnect, protocol carriers, operations, and direct external API compatibility deferred.

  - id: M-081
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after bound identity route policy implementation.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, route-policy production reclassification, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding logout, refresh, cleanup, token rotation, active-connection invalidation, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, protocol session carriers, or broader game backend behavior without confirmation
      - configuring ordinary protected routes to rely on bound or session-validated identity without a separate route-scoped work item
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_logout_revocation_active_connection_gate`, because route policy now distinguishes request-token, bound-connection, session-validated, and bound-session identity, but logout and revocation have not defined how they affect active WebSocket connections. Nakama informs session lifecycle and realtime socket pressure; Pitaya informs keeping acceptors, sessions, handlers, and connection management separated.

  - id: M-082
    title: Logout Revocation Active Connection Gate
    status: completed
    goal: Define the future application-owned boundary for logout, token revocation, runtime session revocation, and active WebSocket connection invalidation without implementing revocation or socket close behavior.
    completion_criteria:
      - `docs/logout-revocation-active-connection-gate.md` and `docs/logout-revocation-active-connection-gate.zh-CN.md` define the gate.
      - `ADR-0071` records the decision.
      - Future policy ownership remains `runtime/internal/app`.
      - Future token revocation, runtime session revocation, and active-connection invalidation are recorded as separate state transitions.
      - The recommended future first posture is presented-token logout only until a later implementation gate broadens scope.
      - A future connection registry is required before active sockets can be targeted.
      - Nakama and Pitaya mappings are recorded in vibit terms.
      - WebSocket close policy, Protobuf logout routes, protocol session carriers, reconnect/epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya compatibility, and broader game backend module expansion remain deferred.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the gate.
    non_goals:
      - implementing `LogoutAccessToken`
      - revoking access tokens or runtime sessions
      - closing WebSocket connections on logout or revocation
      - adding active connection registries or transport close policy
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope, adding Protobuf logout routes, adding session carriers, or adding generated output
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Defined the gate-only logout/revocation active-connection standard. The gate keeps token revocation, runtime session revocation, and active socket invalidation as separate future decisions, recommends presented-token logout as the first future scope, requires an explicit connection registry before targeting open sockets, and preserves WebSocket, Protobuf, reconnect, operations, dependency, and direct external API compatibility deferrals.

  - id: M-083
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the logout/revocation active-connection gate.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for logout execution, active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing logout, token revocation, runtime session revocation, WebSocket close behavior, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, protocol session carriers, or broader game backend behavior without confirmation
      - making active socket invalidation best-effort hidden behavior without explicit policy
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_logout_access_token_behavior_gate`, because the previous gate recommended presented-token logout as the first future scope and logout execution needs proof validation, verifier comparison, transaction ordering, public error collapse, and explicit session/socket/protocol deferrals before code changes. Nakama informs session lifecycle pressure; Pitaya informs keeping session/connection infrastructure separate from handlers.

  - id: M-084
    title: Logout Access Token Behavior Gate
    status: completed
    goal: Define the gate-only boundary for future presented opaque access-token logout behavior before implementing token revocation.
    completion_criteria:
      - Future logout behavior is application-owned under `runtime/internal/app/authentication`.
      - The first future logout scope is `presented_access_token_only`.
      - Future proof validation, token lookup, verifier comparison, token revocation, transaction ordering, public errors, and redaction rules are defined.
      - Runtime session revocation, active connection invalidation, connection registries, WebSocket close behavior, Protobuf logout routes, protocol session carriers, refresh, logout-all, admin revocation, cleanup jobs, dependencies, memory durable session behavior, and direct Nakama/Pitaya API compatibility remain deferred.
    non_goals:
      - implementing `LogoutAccessToken`, revoking tokens, revoking runtime sessions, closing sockets, adding connection registries, exposing protocol logout routes, adding refresh/logout-all/admin revocation behavior, or copying Nakama/Pitaya APIs
    completion_summary: Added a gate-only logout access-token behavior standard. The gate records the future presented-token-only logout sequence, transaction boundary, verifier comparison-before-revocation rule, commit-before-success rule, public error collapse, and Nakama/Pitaya mapping without changing runtime behavior.

  - id: M-085
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the logout access-token behavior gate.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for logout implementation, active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing logout, token revocation, runtime session revocation, WebSocket close behavior, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, protocol session carriers, or broader game backend behavior without confirmation
      - making active socket invalidation best-effort hidden behavior without explicit policy
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_logout_access_token_behavior`, because the previous gate precisely defined presented-token-only logout behavior and the next safest implementation is service-local token revocation without session, socket, or protocol side effects. Nakama informs token/session lifecycle pressure; Pitaya informs keeping connection and session infrastructure separate from credential parsing.

  - id: M-086
    title: Logout Access Token Behavior Implementation
    status: completed
    goal: Implement the first application-owned presented opaque access-token logout behavior.
    completion_criteria:
      - `runtime/internal/app/authentication.Service.LogoutAccessToken` rejects missing and malformed access-token proof before unit of work.
      - The service computes token lookup digest and finds the token record through the authentication repository.
      - The service requires active access-token posture and compares verifier digest before revocation.
      - The service calls `RevokeToken` once with `logout_presented_access_token`.
      - The service returns `revoked` only after unit-of-work commit.
      - Public invalid-token and unavailable-token errors remain collapsed and redacted.
      - Tests cover success, missing/malformed proof, lookup miss, token posture failures, verifier mismatch, dependency failures, commit failure, no raw-token leakage, and no player/session repository side effects.
      - Runtime session revocation, active connection invalidation, connection registries, WebSocket close behavior, Protobuf logout routes, protocol session carriers, refresh, logout-all, admin revocation, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.
    non_goals:
      - revoking runtime sessions
      - closing WebSocket connections on logout or revocation
      - adding active connection registries or transport close policy
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope, adding Protobuf logout routes, adding session carriers, or adding generated output
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Implemented `LogoutAccessToken` as a narrow application service behavior. The service validates opaque access-token proof, uses lookup and verifier digests, verifies token posture, revokes only the presented token record, and returns success only after commit. Tests verify rejection, revocation ordering, unavailable dependencies, redaction, and no session/player side effects.

  - id: M-087
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after logout access-token behavior implementation.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding runtime session revocation, active WebSocket close behavior, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, protocol session carriers, or broader game backend behavior without confirmation
      - making active socket invalidation best-effort hidden behavior without explicit policy
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_active_connection_registry_gate`, because logout can now revoke the presented access-token record but future socket targeting still needs an application-owned active connection registry boundary. Nakama informs realtime socket and session lifecycle pressure; Pitaya informs keeping acceptors, sessions, handlers, and connection management separated.

  - id: M-088
    title: Active Connection Registry Gate
    status: completed
    goal: Define the future application-owned active WebSocket connection registry boundary without implementing registry, socket close, reconnect, or protocol behavior.
    completion_criteria:
      - `docs/active-connection-registry-gate.md` and `docs/active-connection-registry-gate.zh-CN.md` define the gate.
      - `ADR-0074` records the decision.
      - Future registry ownership is application-owned under `runtime/internal/app/connection`.
      - The first future registry posture is single-process, in-memory, and non-durable.
      - Registry records are defined as server-observed active connection state with validated identity linkage, not client proof.
      - WebSocket transport remains credential-neutral and does not own authentication state.
      - Registry lookup, invalidation, and concrete socket close remain separate future policy choices.
      - Nakama and Pitaya mappings are recorded in vibit terms.
      - WebSocket close policy, kick/disconnect behavior, reconnect/epoch behavior, Protobuf logout routes, protocol session carriers, durable/distributed registry storage, dependencies, memory durable session behavior, direct Nakama/Pitaya compatibility, and broader game backend behavior remain deferred.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the gate.
    non_goals:
      - implementing an active connection registry
      - closing WebSocket connections on logout or revocation
      - adding kick or disconnect behavior
      - revoking runtime sessions
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope, adding Protobuf logout routes, adding session carriers, or adding generated output
      - adding reconnect, resume, duplicate replacement, durable epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, presence, rooms, parties, match runtime, or broader game backend behavior
    completion_summary: Defined the gate-only active connection registry standard. The gate records application ownership, a single-process in-memory first posture, server-observed connection record vocabulary, validated identity linkage rules, redaction expectations, and Nakama/Pitaya mapping without changing runtime behavior.

  - id: M-089
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the active connection registry gate.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for active connection registry implementation, WebSocket close policy, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing active connection registries, WebSocket close behavior, kick/disconnect behavior, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, protocol session carriers, or broader game backend behavior without confirmation
      - making active socket invalidation best-effort hidden behavior without explicit policy
    completion_summary: The maintainer authorized the recommended `implement_active_connection_registry_single_process` direction. The choice follows Nakama's lesson that realtime sockets and authenticated session material need coordinated lifecycle state, and Pitaya's lesson that acceptors, sessions, handlers, and connection management should remain separate. M-090/W-0162 was created for a bounded application-owned, in-memory, non-durable registry implementation.

  - id: M-090
    title: Active Connection Registry Single Process Implementation
    status: completed
    goal: Implement the first application-owned in-memory active connection registry without WebSocket close policy, reconnect behavior, protocol carriers, durable storage, or direct external API compatibility.
    completion_criteria:
      - The registry implementation lives under `runtime/internal/app/connection`.
      - It can register server-observed open connections by connection id and epoch.
      - It can bind only validated player identity linkage, with optional runtime session id and access-token record id.
      - It can mark active records closed or invalidated without closing sockets.
      - It can find records by connection id/epoch and list active bound records by player, runtime session, or access-token record id.
      - Tests prove validation, terminal-state exclusion from active lists, copy semantics, no raw proof storage, and no WebSocket/Protobuf/PostgreSQL dependency.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the implementation.
    non_goals:
      - closing WebSocket connections
      - adding kick or disconnect behavior
      - revoking runtime sessions
      - replacing duplicate connections automatically
      - adding reconnect, resume, connection-epoch protocol behavior, or durable epoch behavior
      - changing WebSocket handshake authentication or transport credential carriers
      - changing the existing Protobuf envelope, adding Protobuf logout routes, adding session carriers, or adding generated output
      - adding durable/distributed registry storage, cleanup jobs, operations dependencies, memory durable session behavior, presence, rooms, parties, match runtime, broader game backend behavior, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented a bounded single-process in-memory active connection registry in `runtime/internal/app/connection`. It tracks server-observed connection id and epoch, validated player/session/token linkage, active and terminal record states, lifecycle timestamps, and policy-neutral invalidation markers without owning transport socket close behavior.

  - id: M-091
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the active connection registry implementation.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for WebSocket close policy, protocol logout routes, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - adding WebSocket close behavior, kick/disconnect behavior, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, protocol session carriers, or broader game backend behavior without confirmation
      - making active socket invalidation close concrete sockets without explicit policy
    completion_summary: The maintainer authorized the recommended `define_websocket_close_policy_gate` direction. The choice follows Nakama's lesson that realtime socket lifecycle policy must be explicit around session/logout state, and Pitaya's lesson that connection/session lifecycle policy should stay separated from acceptors, route handlers, and domain modules. M-092/W-0164 was created for a gate-only WebSocket close policy boundary.

  - id: M-092
    title: WebSocket Close Policy Gate
    status: completed
    goal: Define the future application-owned WebSocket close policy boundary before any active connection invalidation closes concrete sockets or sends close reasons.
    completion_criteria:
      - WebSocket close policy ownership, transport handoff, registry interaction, and application policy boundaries are documented.
      - First-posture close reason classes, close intent vocabulary, redaction rules, and failure semantics are defined as future choices only.
      - Nakama and Pitaya reference lessons are mapped into vibit-native policy without direct public API compatibility.
      - Socket close implementation, kick/disconnect behavior, protocol logout routes, reconnect/epoch behavior, protocol session carriers, transport credential carriers, durable/distributed registry storage, dependencies, and broader game backend behavior remain deferred.
      - Manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory record the gate.
    non_goals:
      - closing WebSocket connections
      - adding close codes, close reasons, kick messages, disconnect commands, or transport close handoff code
      - changing logout behavior, runtime session revocation, active connection registry implementation, or route policy
      - adding duplicate replacement, reconnect, resume, durable epoch behavior, or protocol session carriers
      - changing the existing Protobuf envelope, adding Protobuf logout routes, adding generated output, changing WebSocket handshake authentication, adding transport credential carriers, or adding dependencies
      - adding durable/distributed registry storage, cleanup jobs, operations dependencies, memory durable session behavior, presence, rooms, parties, match runtime, broader game backend behavior, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the gate-only WebSocket close policy standard. The gate records future application-owned close policy, policy-neutral transport handoff, registry interaction rules, redacted close reason classes, failure semantics, and Nakama/Pitaya mapping without changing runtime behavior.

  - id: M-093
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the WebSocket close policy gate.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for WebSocket close policy implementation, protocol logout routes, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing WebSocket close behavior, kick/disconnect behavior, reconnect, duplicate connection replacement, presence, rooms, parties, match runtime, group behavior, protocol session carriers, or broader game backend behavior without confirmation
      - making logout close concrete sockets without explicit implementation authorization
    completion_summary: The maintainer authorized the recommended `implement_websocket_close_policy_single_process` direction. M-094/W-0166 was created to implement only the application-owned single-process close policy primitive, preserving concrete transport close handoff, close codes, close reason text, logout protocol routes, reconnect/epoch behavior, protocol session carriers, operations, durable/distributed storage, direct Nakama/Pitaya API compatibility, and broader game backend behavior behind later gates.

  - id: M-094
    title: WebSocket Close Policy Single Process Implementation
    status: completed
    goal: Implement the first application-owned single-process WebSocket close policy primitive without concrete socket close handoff.
    completion_criteria:
      - Close policy lives under `runtime/internal/app/connection`.
      - The policy targets only active bound registry records by connection id and epoch, player id, runtime session id, or access-token record id.
      - The policy marks matched registry records invalidated and emits redacted close intents with `mark_invalidated_only` transport action.
      - The policy keeps concrete WebSocket close handoff, close codes, close reason text, protocol close messages, logout route behavior, reconnect/epoch behavior, protocol session carriers, durable/distributed storage, dependencies, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Focused tests verify target resolution, invalidation, redaction, skipped target behavior, and deferrals.
    non_goals:
      - closing WebSocket connections
      - adding transport close handoff code
      - selecting close codes or player-visible close reason strings
      - changing logout service behavior or protocol routes
      - adding runtime session revocation close behavior, duplicate connection replacement, reconnect/resume/epoch behavior, protocol session carriers, operations/admin disconnect, durable/distributed registry storage, dependencies, direct Nakama/Pitaya API compatibility, or broader game backend behavior
    completion_summary: Implemented an application-owned single-process close policy primitive that resolves active bound registry records, marks them invalidated, and returns redacted close intents. It adapts Nakama's explicit realtime lifecycle separation and Pitaya's session/connection-management layering without closing concrete sockets or copying external APIs.

  - id: M-095
    title: Next Direction Confirmation Gate
    status: completed
    goal: Hold the work queue at an explicit maintainer decision point after the WebSocket close policy implementation.
    completion_criteria:
      - The maintainer chooses the next major milestone direction.
      - The chosen direction is recorded as a bounded milestone with at least one next_ready work item.
      - Ask-first boundaries are preserved for protocol logout routes, reconnect/epoch behavior, protocol session carriers, concrete transport close handoff, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend module expansion.
    non_goals:
      - implementing protocol logout routes, reconnect, duplicate connection replacement, concrete WebSocket close handoff, protocol session carriers, presence, rooms, parties, match runtime, group behavior, operations/admin disconnect, or broader game backend behavior without confirmation
      - making close intents close concrete sockets without an explicit transport handoff gate
    completion_summary: The maintainer clarified that vibit must target Nakama/Pitaya-class product coverage and authorized replanning the development route and method. The selected direction is `expand_core_game_backend_modules_after_nakama_pitaya_review`, implemented as a precise `ratify_nakama_pitaya_product_parity_roadmap` slice before feature expansion.

  - id: M-096
    title: Nakama Pitaya Product Parity Roadmap
    status: completed
    goal: Ratify a product roadmap that makes Nakama/Pitaya-class common capability coverage an explicit vibit target while preserving agent-native boundaries.
    completion_criteria:
      - The roadmap defines parity as common capability coverage, not direct API compatibility.
      - Nakama product capability families and Pitaya Go game server architecture vocabulary are mapped into vibit roadmap families.
      - The near-term development method prioritizes lifecycle closure before presence, chat, social, matchmaking, and match runtime expansion.
      - Repository manifests, docs, ADRs, checks, and work queue state record the new target.
      - No runtime behavior, protocol route, dependency, generated output, data migration, direct Nakama/Pitaya API compatibility, or broad game module implementation is added by the roadmap slice.
    non_goals:
      - implementing protocol logout routes
      - implementing concrete WebSocket close handoff
      - implementing reconnect, protocol session carriers, presence, chat, friends, groups, parties, leaderboards, tournaments, matchmaking, match runtime, admin console, SDKs, cluster, RPC, or service discovery
      - copying Nakama or Pitaya APIs
    completion_summary: Added `docs/nakama-pitaya-product-parity-roadmap.md`, its Simplified Chinese translation, and `ADR-0078`. The roadmap makes same-class common capability coverage explicit, defines capability families and phases, and selects lifecycle closure as the next execution focus.

  - id: M-097
    title: Protocol Logout Route Gate
    status: completed
    goal: Define client-facing protocol logout route semantics after product parity roadmap ratification, without yet changing wire behavior or closing sockets.
    completion_criteria:
      - The gate defines ownership for future `runtime.authentication.LogoutAccessToken` protocol route behavior.
      - The gate records request/response semantics, public error mapping, proof carrier behavior, interaction with existing service logout, and close-policy interaction.
      - The gate keeps concrete WebSocket close handoff, close codes, reconnect/epoch behavior, protocol session carriers, presence, chat, social modules, match runtime, admin disconnect, dependencies, generated output, and direct Nakama/Pitaya API compatibility deferred.
      - The gate maps the decision to the Nakama/Pitaya product parity lifecycle-closure phase.
    non_goals:
      - adding Protobuf logout messages or generated output in the gate itself
      - registering a runtime logout route
      - making logout close concrete sockets
      - adding reconnect, duplicate replacement, protocol session carriers, presence, chat, friends, groups, parties, leaderboards, tournaments, matchmaking, match runtime, cluster, or SDK behavior
    completion_summary: Defined the gate-only protocol logout route standard in `docs/protocol-logout-route-gate.md`, its Simplified Chinese translation, and `ADR-0079`. The gate authorizes a later bounded implementation slice for `runtime.authentication.LogoutAccessToken` while preserving WebSocket transport neutrality, existing envelope shape, socket close/session/reconnect deferrals, and no direct Nakama/Pitaya API compatibility.

  - id: M-098
    title: Protocol Logout Route Implementation
    status: completed
    goal: Expose the existing `LogoutAccessToken` service behavior through the WebSocket Protobuf route surface without changing socket, session, reconnect, or direct compatibility behavior.
    completion_criteria:
      - Protobuf logout request/response messages are added through `proto/vibit/authentication/v1/authentication.proto`.
      - Generated Go Protobuf output is produced through the accepted Buf path, not hand-edited.
      - Protocol bridge behavior maps logout request/response payloads without leaking access-token proof.
      - Application bootstrap registers `runtime.authentication.LogoutAccessToken` only when the authentication service exists.
      - The route uses the explicit service-validated token lifecycle posture and bypasses the outer transactional dispatcher.
      - Focused tests verify route registration, bridge mapping, error redaction, transport neutrality, unchanged envelope, and no socket/session/reconnect side effects.
    non_goals:
      - closing WebSocket sockets or adding transport close handoff
      - revoking runtime sessions or invalidating active connection registry records
      - adding reconnect, duplicate replacement, protocol session carriers, presence, chat, friends, groups, matchmaking, match runtime, cluster, SDK, dependencies, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the bounded `runtime.authentication.LogoutAccessToken` protocol route. The slice added logout request/response Protobuf messages, regenerated Go Protobuf output through Buf, added Protobuf bridge mapping, application bootstrap route registration and handler behavior, PostgreSQL startup route composition, transaction bypass, focused tests, and repository check coverage while preserving WebSocket transport neutrality, unchanged envelope shape, and socket/session/reconnect deferrals.

  - id: M-099
    title: Next Direction Confirmation After Protocol Logout Route Implementation
    status: completed
    goal: Select the next lifecycle-closure direction after client-facing protocol logout route exposure.
    completion_criteria:
      - The next direction is selected from the Nakama/Pitaya product parity roadmap lifecycle phase.
      - The selected direction preserves socket close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, operations/admin behavior, memory durable behavior, dependencies, broad product modules, and direct Nakama/Pitaya API compatibility behind explicit gates.
      - The resulting next ready work item is bounded and checkable.
    non_goals:
      - implementing concrete socket close handoff in the confirmation step
      - revoking runtime sessions or invalidating active connection registry records in the confirmation step
      - adding reconnect, duplicate replacement, protocol session carriers, presence, chat, friends, groups, matchmaking, match runtime, cluster, SDK, dependencies, or direct Nakama/Pitaya API compatibility
    completion_summary: Selected `define_transport_close_handoff_gate` as the next lifecycle-closure direction after protocol logout route implementation. The selection follows the Nakama/Pitaya product parity roadmap by closing the application close policy to concrete WebSocket transport handoff gap before reconnect, protocol session carrier, presence, chat, social, matchmaking, match runtime, SDK, or distributed runtime work.

  - id: M-100
    title: Transport Close Handoff Gate
    status: completed
    goal: Define the boundary for future narrow handoff from application-owned close policy intents to concrete WebSocket socket close mechanics.
    completion_criteria:
      - The gate defines ownership between application close policy, active connection registry, WebSocket transport, and protocol adapter.
      - The gate selects the future handoff shape without implementing concrete socket close behavior.
      - The gate preserves close code mapping, close reason text, logout-triggered close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, and broad product modules behind later gates unless explicitly authorized.
      - The gate maps Nakama lifecycle expectations and Pitaya connection-management layering into vibit-native boundaries.
    non_goals:
      - implementing concrete socket close handoff in the gate
      - selecting close codes or player-visible close reason text
      - making logout automatically close sockets
      - revoking runtime sessions or changing active connection invalidation semantics
      - adding reconnect, duplicate replacement, protocol session carriers, presence, chat, friends, groups, matchmaking, match runtime, cluster, SDK, dependencies, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the gate-only transport close handoff boundary. The gate keeps close decisions application-owned, keeps WebSocket transport policy-neutral, selects server-observed `connection_id + epoch` as the first future handoff target, rejects client-supplied metadata as close authority, and preserves concrete close implementation, close code/reason mapping, logout-triggered close, session revocation, reconnect, protocol carriers, dependencies, and direct Nakama/Pitaya API compatibility for later gates.

  - id: M-101
    title: Next Direction Confirmation After Transport Close Handoff Gate
    status: completed
    goal: Select the next lifecycle-closure direction after defining the transport close handoff gate.
    completion_criteria:
      - The next direction is selected from the lifecycle closure sequence.
      - The selected direction preserves close implementation, close code mapping, close reason text, logout-triggered close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, presence, operations/admin behavior, dependencies, direct Nakama/Pitaya API compatibility, and broad product modules behind explicit gates unless selected.
      - The resulting next ready work item is bounded and checkable.
    non_goals:
      - implementing concrete socket close handoff in the confirmation step
      - selecting close codes or close reason text in the confirmation step
      - adding reconnect, duplicate replacement, protocol session carriers, presence, chat, friends, groups, matchmaking, match runtime, cluster, SDK, dependencies, or direct Nakama/Pitaya API compatibility
    completion_summary: Selected `implement_transport_close_handoff_single_process` as the next lifecycle-closure direction after the transport close handoff gate. The selection follows ADR-0080 by implementing the narrow server-observed `connection_id + epoch` WebSocket transport handoff before reconnect, protocol session carrier, presence, operations/admin disconnect, or broad product module work.

  - id: M-102
    title: Transport Close Handoff Single Process Implementation
    status: completed
    goal: Implement the first single-process application-to-WebSocket concrete close handoff while preserving application-owned close policy and transport credential neutrality.
    completion_criteria:
      - WebSocket transport exposes a narrow in-process handoff keyed only by server-observed connection id and epoch.
      - The handoff can request concrete socket close for an already accepted active socket without parsing credentials or owning lifecycle policy.
      - Stale epoch, missing socket, already closed socket, and close failure outcomes are redacted and policy-neutral.
      - Application close policy and active connection registry remain the owners of close decisions and registry lifecycle markers.
      - Focused tests cover target matching, stale epoch protection, redaction, transport neutrality, and no protocol envelope/authentication side effects.
    non_goals:
      - selecting WebSocket close codes or player-visible close reason text
      - making logout automatically close sockets
      - revoking runtime sessions
      - changing reconnect, duplicate replacement, protocol session carriers, presence, chat, friends, groups, matchmaking, match runtime, cluster, SDK, dependencies, operations/admin disconnect, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented a WebSocket transport-owned single-process close handoff. The implementation registers accepted sockets by server-observed connection id and epoch, exposes `RequestClose` with redacted outcomes, uses concrete socket mechanics without selecting close codes or reason text, protects stale epochs, and keeps application close policy, active connection registry policy, authentication logout behavior, runtime sessions, reconnect behavior, protocol carriers, dependencies, and direct Nakama/Pitaya API compatibility deferred.

  - id: M-103
    title: Reconnect Connection Epoch Functional Slice
    status: completed
    goal: Define and implement the first bounded reconnect/connection epoch behavior as a Tier 2 functional slice after the concrete transport close handoff.
    completion_criteria:
      - The slice records the reconnect/connection epoch boundary in its change spec before implementation.
      - The implementation, if added in the slice, remains bounded to server-observed connection epoch semantics and focused tests.
      - Close code mapping, close reason text, logout-triggered close, runtime session revocation, protocol session carriers, presence, operations/admin behavior, dependencies, direct Nakama/Pitaya API compatibility, and broad product modules remain behind explicit ask-first boundaries.
      - The resulting state is checkable through focused tests and repository checks.
    non_goals:
      - selecting close code mapping or player-visible close reason text
      - making logout automatically close sockets
      - revoking runtime sessions
      - adding protocol session carriers, presence, chat, friends, groups, matchmaking, match runtime, cluster, SDK, dependencies, operations/admin disconnect, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the first application-owned connection epoch progression primitive as a Tier 2 functional slice. The active connection registry now marks older active epochs for the same server-observed connection id as `superseded`, records `superseded_at` and `superseded_by_epoch`, rejects stale or repeated epochs after a newer epoch is observed with `connection_epoch_stale`, keeps superseded records available for lifecycle inspection, and excludes them from active target lists. No Protobuf, reconnect token, resume behavior, logout-triggered close, runtime session revocation, protocol session carrier, presence, operations/admin disconnect, dependency, direct Nakama/Pitaya API compatibility, or broad product behavior was added.

  - id: M-104
    title: Protocol Session Carrier Functional Slice
    status: completed
    goal: Define and implement the first bounded protocol-visible runtime session carrier behavior after logout, close handoff, and connection epoch lifecycle primitives are in place.
    completion_criteria:
      - The slice records the protocol session carrier boundary in its change spec before implementation.
      - Any implementation remains bounded to the smallest session carrier behavior needed for lifecycle closure and focused tests.
      - Reconnect tokens, resume tokens, close code mapping, close reason text, logout-triggered close, runtime session revocation, presence, operations/admin behavior, dependencies, direct Nakama/Pitaya API compatibility, and broad product modules remain behind explicit ask-first boundaries.
      - The resulting state is checkable through focused tests and repository checks.
    non_goals:
      - adding reconnect or resume tokens
      - selecting close code mapping or player-visible close reason text
      - making logout automatically close sockets
      - revoking runtime sessions
      - adding presence, chat, friends, groups, matchmaking, match runtime, cluster, SDK, dependencies, operations/admin disconnect, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the first bounded protocol session carrier by reusing existing `Envelope.Session` metadata. Successful login responses now carry the server-created runtime session id and authenticated player id without changing Protobuf sources or generated output. The Protobuf response builder can also derive response session metadata from already validated application identity, while metadata-only identity remains metadata-only and session id remains non-proof.

  - id: M-105
    title: Presence Lifecycle Functional Slice
    status: completed
    goal: Define and implement the first bounded presence lifecycle behavior after protocol-visible session metadata exists.
    completion_criteria:
      - The slice records the presence lifecycle boundary in its change spec before implementation.
      - Any implementation remains bounded to the smallest presence/status primitive needed for Nakama/Pitaya-class lifecycle closure and focused tests.
      - Chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, reconnect/resume tokens, logout-triggered close, runtime session revocation, dependencies, direct Nakama/Pitaya API compatibility, and broad product modules remain behind explicit ask-first boundaries.
      - The resulting state is checkable through focused tests and repository checks.
    non_goals:
      - adding chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, dependencies, or direct Nakama/Pitaya API compatibility
      - adding reconnect or resume tokens
      - making logout automatically close sockets
      - revoking runtime sessions
    completion_summary: Implemented the first server-owned presence lifecycle primitive as a Tier 2 functional slice. WebSocket transport now exposes a credential-neutral connection lifecycle observer, PostgreSQL startup composition wires open/close lifecycle and successful first-message binding into the application-owned active connection registry, and the registry can derive player online/offline presence snapshots from active bound records. No Protobuf presence route, generated output, subscription, broadcast, chat, social module, matchmaking, match runtime, durable/distributed presence, dependency, direct compatibility, logout-triggered close, or runtime session revocation was added.

  - id: M-106
    title: Presence Protocol Query Functional Slice
    status: completed
    goal: Expose the server-owned presence lifecycle snapshot through the first bounded protected protocol query.
    completion_criteria:
      - The slice records the presence protocol query boundary in its change spec before implementation.
      - Any implementation remains bounded to a protected query over the existing server-owned presence snapshot with focused tests.
      - Presence subscriptions, broadcasts, chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, reconnect/resume tokens, logout-triggered close, runtime session revocation, durable/distributed presence, dependencies, direct Nakama/Pitaya API compatibility, and broad product modules remain behind explicit ask-first boundaries.
      - The resulting state is checkable through focused tests and repository checks.
    non_goals:
      - adding presence subscriptions or broadcasts
      - adding chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, dependencies, or direct Nakama/Pitaya API compatibility
      - adding reconnect or resume tokens
      - making logout automatically close sockets
      - revoking runtime sessions
      - adding durable or distributed presence
    completion_summary: Implemented the first protected presence protocol query as a Tier 2 functional slice. `runtime.presence.GetPlayerPresence` now exposes the existing server-owned registry-backed player presence snapshot through `vibit.presence.v1` Protobuf messages and generated Go output. The route uses the existing request-token wrapper, is self-only for the authenticated player, returns bounded online/offline connection metadata and runtime session ids, and does not expose access-token record ids. No subscription, broadcast, durable/distributed presence, chat, social, matchmaking, match runtime, dependency, direct compatibility, logout-triggered close, or runtime session revocation was added.

  - id: M-107
    title: v0.1 Alpha Goal And Long Term Product Target
    status: completed
    goal: Record the short-term v0.1 alpha target and long-term AI-era Nakama/Pitaya-class product target so future sessions can continue without chat context.
    completion_criteria:
      - The short-term alpha target is documented in English and Simplified Chinese.
      - The long-term AI-era Nakama/Pitaya-class target is recorded without adding direct API compatibility.
      - README files become usable as external user and contributor entry points.
      - Work queue continuation remains active at M-106/W-0178.
      - No runtime behavior, protocol shape, generated output, migration, dependency, or release artifact is changed by this documentation slice.
    non_goals:
      - publishing v0.1 alpha
      - adding onboarding or credential issuance code
      - adding presence protocol query code
      - adding example client code
      - adding health/readiness/version endpoints
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added `docs/v0.1-alpha-goal.md`, its Simplified Chinese translation, `ADR-0086`, a conversation log, README updates, and manifest/guide pointers. The repository now records `v0.1 alpha` as the short-term release target and an AI-era Nakama/Pitaya-class server framework as the long-term product target while keeping `M-106/W-0178` as the immediate next ready work item.

  - id: M-108
    title: Next Alpha Direction Selection
    status: completed
    goal: Select the next bounded alpha-enabling work item after the protected presence protocol query.
    completion_criteria:
      - The next alpha-enabling direction is selected from `docs/v0.1-alpha-goal.md` or a maintainer redirect.
      - The chosen direction is recorded with a bounded next-ready work item.
      - Ask-first boundaries are preserved for release publishing, broad product modules, dependencies, operations posture, direct Nakama/Pitaya API compatibility, durable/distributed runtime behavior, and security-sensitive authentication/session changes.
    non_goals:
      - publishing v0.1 alpha
      - adding unbounded runtime behavior
      - adding dependencies without adoption records
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Selected `define_local_onboarding_device_credential_issuance_gate` as the next alpha-enabling direction after the protected presence protocol query. Because local onboarding/device credential issuance is security-sensitive credential behavior, the next step is a gate-only work item, not immediate implementation.

  - id: M-109
    title: Local Onboarding Device Credential Issuance Gate
    status: completed
    goal: Define the first local onboarding and device credential issuance boundary before adding any runtime behavior.
    completion_criteria:
      - The gate defines the first local onboarding surface candidate, ownership, and allowed implementation area.
      - The gate defines how player account creation, raw device credential generation, verifier digest storage, one-time presentation, redaction, and unit-of-work ordering must behave in a later implementation.
      - The gate preserves deferrals for production signup, external identity providers, password login, account recovery, multi-device linking, protocol routes, generated output, migrations, dependencies, release publishing, and direct Nakama/Pitaya API compatibility unless explicitly authorized.
      - The resulting future implementation work item is bounded and checkable.
    non_goals:
      - implementing onboarding or credential issuance
      - generating or displaying raw credentials
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - publishing v0.1 alpha
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the first local onboarding/device credential issuance gate in `docs/local-onboarding-device-credential-issuance-gate.md` and `ADR-0089`. The gate selects a future local-only application service under `runtime/internal/app/authentication`, requires player account creation and digest-only credential storage in one unit of work, requires one-time raw credential presentation only after commit, and preserves public signup, protocol route, generated output, migration, dependency, release, and direct Nakama/Pitaya compatibility deferrals.

  - id: M-110
    title: Local Onboarding Device Credential Issuance Implementation
    status: completed
    goal: Implement the first local-only application service slice that creates a player account and issues a device credential under the W-0181 gate.
    completion_criteria:
      - The implementation stays under the gate-authorized local application service boundary.
      - The service creates an active player account and active device credential record in the same application-owned unit of work.
      - The service generates server-issued device credential material, computes credential lookup and verifier digests, stores only digest material and metadata, and returns raw credential text only once after commit.
      - Focused tests cover ordering, digest-only storage, redaction, failure handling, and no login-route account creation.
      - Public signup, protocol routes, generated output, migrations, dependencies, access-token issuance from onboarding, runtime session creation from onboarding, production identity providers, account recovery, multi-device linking, release publishing, and direct Nakama/Pitaya compatibility remain deferred.
    non_goals:
      - adding public signup or production account creation
      - adding protocol routes, Protobuf sources, or generated output
      - adding migrations, repository interface changes, or dependencies
      - issuing access tokens or runtime sessions from onboarding
      - changing existing login route account-creation behavior
      - adding external identity providers, password login, account recovery, account merge, or multi-device linking
      - publishing v0.1 alpha
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the local-only `OnboardLocalPlayerWithDeviceCredential` application service under `runtime/internal/app/authentication`. The service validates the request before unit-of-work, generates server-issued device credential material, computes credential lookup and verifier digests, creates an active player account and active digest-only credential record in one unit of work, and returns raw credential text only after unit-of-work success. Focused tests cover ordering, digest-only storage, redaction, failure handling, no token/session issuance, and unchanged login-route account creation behavior. No protocol route, generated output, migration, repository interface change, dependency, production signup, external identity, password login, recovery, multi-device linking, release artifact, or direct compatibility was added.

  - id: M-111
    title: Next Alpha Direction Selection After Local Onboarding
    status: completed
    goal: Select the next alpha-enabling direction after local onboarding/device credential issuance implementation.
    completion_criteria:
      - The next direction is selected from the v0.1 alpha preferred queue or a maintainer redirect.
      - The selected direction preserves release publishing, protocol expansion, generated output, migration, dependency, production signup, external identity, password login, account recovery, multi-device linking, direct Nakama/Pitaya API compatibility, and broad product modules behind explicit future work unless selected.
      - The resulting next ready work item is bounded and checkable.
    non_goals:
      - implementing authenticated gameplay E2E in the direction-selection step
      - refreshing the runtime runbook in the direction-selection step
      - adding a client script, health surface, release artifact, dependency, protocol route, or direct compatibility in the direction-selection step
    completion_summary: Selected `define_authenticated_gameplay_e2e_slice` as the next alpha-enabling direction after local onboarding implementation. The next step is a bounded functional slice to prove onboarding -> login -> connection binding -> protected inventory -> presence query -> logout as one documented local flow without adding production signup, broad product modules, dependencies, release artifacts, or direct Nakama/Pitaya API compatibility in the direction-selection step.

  - id: M-112
    title: Authenticated Gameplay E2E Functional Slice
    status: completed
    goal: Define and prove the first authenticated local gameplay end-to-end path across existing onboarding, login, connection binding, protected inventory, presence query, and logout capabilities.
    completion_criteria:
      - The slice records the authenticated gameplay E2E boundary in its change spec before implementation.
      - The flow starts from local onboarding/device credential issuance and exercises the existing protocol-authenticated gameplay path.
      - The implementation, if added in the slice, remains bounded to E2E verification, local documentation, or request-loop proof over existing capabilities unless separately authorized.
      - Protocol expansion, generated output, migrations, dependencies, release publishing, production signup, external identity, password login, account recovery, multi-device linking, direct Nakama/Pitaya API compatibility, and broad product modules remain behind explicit ask-first boundaries unless the W-0184 spec scopes them.
      - The resulting state is checkable through focused tests, docs, scripts, or repository checks.
    non_goals:
      - adding new gameplay modules beyond the existing inventory proof slice
      - adding public signup or production identity behavior
      - adding new protocol routes unless the W-0184 change spec explicitly scopes them
      - adding migrations or dependencies
      - publishing v0.1 alpha
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added a focused protocol-level Go E2E proof that composes local onboarding, device credential login, first-message connection binding, protected inventory grant/read, protected presence query, logout, and post-logout protected-route rejection using existing runtime capabilities. The slice added no production runtime behavior, protocol routes, Protobuf sources, generated output, migrations, repository interfaces, dependencies, release artifacts, production signup, broad product modules, or direct Nakama/Pitaya API compatibility.

  - id: M-113
    title: Runtime Runbook Alpha Path Refresh
    status: completed
    goal: Refresh runtime runbook guidance around the now-proven local authenticated gameplay alpha path.
    completion_criteria:
      - The runbook describes the current local alpha path accurately enough for a developer to prepare configuration, understand prerequisites, and follow the proven onboarding -> login -> connection binding -> protected inventory -> presence query -> logout flow.
      - The runbook distinguishes in-memory test proof from PostgreSQL-backed runtime operation.
      - Secret, verifier key, raw credential, raw token, and DSN handling remain redacted and environment-driven.
      - The refresh does not add runtime behavior, protocol routes, generated output, migrations, dependencies, release artifacts, production signup, broad product modules, or direct Nakama/Pitaya API compatibility.
      - The resulting runbook state is covered by repository checks and verification notes.
    non_goals:
      - adding a runnable client or request-loop script
      - changing runtime startup behavior
      - adding protocol routes, Protobuf sources, generated output, migrations, or dependencies
      - publishing v0.1 alpha
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Refreshed `docs/runtime-runbook.md` and `docs/runtime-runbook.zh-CN.md` around the proven local alpha path. The runbook now distinguishes memory bootstrap from PostgreSQL alpha runtime composition, documents verifier key environment handling and redaction rules, points to the focused authenticated gameplay E2E proof, and states that local onboarding remains application-service-only rather than a public protocol route. No runtime behavior, startup configuration semantics, protocol route, Protobuf source, generated output, migration, dependency, release artifact, production signup, broad product module, or direct Nakama/Pitaya API compatibility was added.

  - id: M-114
    title: Minimal Example Client Or Request Loop
    status: completed
    goal: Add the smallest developer-facing local example or request-loop script that exercises the proven alpha path without expanding protocol, persistence, identity, release, or product scope.
    completion_criteria:
      - The example or script follows the documented local alpha path or clearly records why part of the path is still not externally runnable.
      - The example keeps raw credentials, raw tokens, verifier keys, DSNs, digests, and transport metadata redacted.
      - The example does not add production signup, new protocol routes, generated output, migrations, dependencies, broad product modules, release artifacts, or direct Nakama/Pitaya API compatibility.
      - The example is covered by repository checks or explicit verification notes.
    non_goals:
      - changing runtime behavior
      - changing startup configuration semantics
      - adding public protocol onboarding
      - adding Protobuf sources or generated output
      - adding migrations or dependencies
      - publishing v0.1 alpha
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added `examples/local-alpha-request-loop.sh` as the minimal local alpha request-loop script. The script runs the existing authenticated gameplay E2E proof, prints a redacted path summary, and does not add runtime behavior, startup semantics, public protocol onboarding, Protobuf sources, generated output, migrations, dependencies, release artifacts, production signup, broad product modules, or direct Nakama/Pitaya API compatibility.

  - id: M-115
    title: Health Readiness Version Config Surface
    status: completed
    goal: Add the smallest local health/readiness/version/config surface needed for alpha troubleshooting without expanding protocol, persistence, release, operations, or product scope.
    completion_criteria:
      - The surface helps a local developer understand whether the runtime can start and which alpha posture is active.
      - The surface avoids leaking raw credentials, raw tokens, verifier keys, DSNs, digests, transport metadata, or internal secret configuration.
      - The surface does not add broad operations/admin behavior, observability dependencies, release packaging, product modules, generated output, migrations, or direct Nakama/Pitaya API compatibility.
      - The surface is covered by repository checks or explicit verification notes.
    non_goals:
      - production observability
      - admin console
      - metrics backend integration
      - changing authentication or session behavior
      - adding Protobuf sources or generated output
      - adding migrations or dependencies
      - publishing v0.1 alpha
    completion_summary: Added minimal JSON `/healthz`, `/readyz`, `/version`, and `/configz` endpoints to the runtime HTTP mux. The surface reports process health, readiness posture, pre-alpha version, runtime store posture, WebSocket path, local request-loop script path, PostgreSQL configuration presence, authentication configuration requirement, and explicit secret-redaction posture without exposing verifier keys, raw credentials, raw tokens, DSNs, digests, headers, cookies, query strings, subprotocol values, remote addresses, or concrete transport metadata. No broad operations/admin behavior, observability dependency, authentication/session behavior change, startup configuration semantic change, Protobuf source, generated output, migration, release artifact, broad product module, or direct Nakama/Pitaya API compatibility was added.

  - id: M-116
    title: Alpha Acceptance Checklist
    status: completed
    goal: Add a concise alpha acceptance checklist or equivalent check that lets maintainers and contributors judge whether the v0.1 alpha developer flow is ready without publishing a release.
    completion_criteria:
      - The checklist covers clone, prerequisites, migrations, local configuration, local onboarding posture, login, connection binding, protected inventory, presence query, logout, status endpoints, checks, redaction, and contribution entry points.
      - The checklist distinguishes completed alpha prerequisites from release-publishing work that remains deferred.
      - The checklist avoids adding runtime behavior, protocol changes, generated output, migrations, dependencies, broad operations/admin behavior, product modules, or direct Nakama/Pitaya API compatibility.
      - The resulting checklist is covered by repository checks or explicit verification notes.
    non_goals:
      - publishing v0.1 alpha
      - release packaging
      - adding runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added `docs/alpha-acceptance-checklist.md` and `docs/alpha-acceptance-checklist.zh-CN.md` as the local v0.1 alpha acceptance checklist. The checklist covers repository intake, local prerequisites, configuration and redaction posture, migration posture, runtime surfaces, authenticated gameplay flow, verification commands, contribution entry point, and release deferrals. It does not declare or publish a release, add release packaging, change runtime behavior, change protocol routes, change Protobuf sources or generated output, add migrations, add dependencies, broaden operations/admin behavior, change authentication/session behavior, expand product modules, or select direct Nakama/Pitaya API compatibility.

  - id: M-117
    title: Package Alpha Developer Flow
    status: completed
    goal: Package the existing alpha developer flow documentation and entry points into a coherent local developer journey without publishing a release or adding runtime behavior.
    completion_criteria:
      - README, runbook, request-loop script, status endpoints, acceptance checklist, and contribution path fit together as one developer journey.
      - The packaged flow distinguishes local proof, manual PostgreSQL setup, deferred public onboarding, and release-publishing work.
      - The packaging preserves secret redaction and avoids adding runtime behavior, protocol changes, generated output, migrations, dependencies, broad operations/admin behavior, product modules, or direct Nakama/Pitaya API compatibility.
      - The resulting packaged flow is covered by repository checks or explicit verification notes.
    non_goals:
      - publishing v0.1 alpha
      - creating release tags or binaries
      - adding runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added `docs/alpha-developer-flow.md` and `docs/alpha-developer-flow.zh-CN.md` as the packaged local alpha developer journey. The flow connects README intake, v0.1 alpha goal, alpha acceptance checklist, runtime runbook, status endpoints, the redacted local request-loop script, PostgreSQL manual setup posture, verification commands, redaction rules, and the next contribution entry point. It does not declare or publish a release, add release packaging, create release artifacts, add hosted deployments, change runtime behavior, change protocol routes, change Protobuf sources or generated output, add migrations, add dependencies, broaden operations/admin behavior, change authentication/session behavior, expand product modules, or select direct Nakama/Pitaya API compatibility.

  - id: M-118
    title: Release Publishing Decision Gate
    status: completed
    goal: Decide whether and how to publish v0.1 alpha without publishing it in the gate step.
    completion_criteria:
      - The gate defines release-publishing prerequisites, release artifact boundaries, verification requirements, and stop conditions.
      - The gate distinguishes a decision to publish later from the act of publishing.
      - The gate preserves runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, and direct compatibility deferrals unless separately authorized.
      - The resulting decision gate is covered by repository checks or explicit verification notes.
    non_goals:
      - publishing v0.1 alpha in the gate slice
      - creating release tags or binaries in the gate slice
      - creating hosted deployments
      - adding runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added `docs/release-publishing-decision-gate.md` and `docs/release-publishing-decision-gate.zh-CN.md` as the release publishing decision gate, recorded `ADR-0098`, and added repository check coverage through `runtime.release_publishing_decision_gate`. The gate defines publishing prerequisites, release artifact boundaries, verification requirements, stop conditions, redaction expectations, and the next release execution preparation gate while preserving pre-alpha state, release publishing, release artifacts, hosted deployment, runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, broad product module, and direct compatibility deferrals.

  - id: M-119
    title: Release Execution Preparation Gate
    status: completed
    goal: Prepare the decision framework for a future v0.1 alpha release execution step without publishing a release or creating release artifacts.
    completion_criteria:
      - The gate defines the future release execution plan, release-note inputs, artifact plan boundaries, maintainer approval points, and rollback or stop conditions.
      - The gate distinguishes release execution preparation from creating release artifacts or publishing a release.
      - The gate preserves runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals unless separately authorized.
      - The resulting preparation gate is covered by repository checks or explicit verification notes.
    non_goals:
      - publishing v0.1 alpha in the preparation gate
      - creating release tags, binaries, archives, containers, packages, checksums, or hosted deployments in the preparation gate
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - changing authentication/session behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added `docs/release-execution-preparation-gate.md` and `docs/release-execution-preparation-gate.zh-CN.md` as the release execution preparation gate, recorded `ADR-0099`, and added repository check coverage through `runtime.release_execution_preparation_gate`. The gate defines planning inputs, release-note input boundaries, artifact plan boundaries, maintainer approval points, verification requirements, rollback notes, stop conditions, redaction expectations, and the next release execution authorization gate while preserving pre-alpha state, release publishing, release execution, release artifacts, hosted deployment, runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, broad product module, and direct compatibility deferrals.

  - id: M-120
    title: Release Execution Authorization Gate
    status: completed
    goal: Define authorization criteria for a future v0.1 alpha release execution step without publishing a release or creating release artifacts.
    completion_criteria:
      - The gate defines final go/no-go criteria, required verification state, release identifier review, artifact authorization boundaries, and maintainer approval requirements.
      - The gate distinguishes authorization from creating release artifacts or publishing a release.
      - The gate preserves runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals unless separately authorized.
      - The resulting authorization gate is covered by repository checks or explicit verification notes.
    non_goals:
      - publishing v0.1 alpha in the authorization gate
      - creating release tags, binaries, archives, containers, packages, checksums, provenance files, or hosted deployments in the authorization gate
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - changing authentication/session behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added `docs/release-execution-authorization-gate.md` and `docs/release-execution-authorization-gate.zh-CN.md` as the release execution authorization gate, recorded `ADR-0100`, and added repository check coverage through `runtime.release_execution_authorization_gate`. The gate defines final go/no-go criteria, required verification state, release identifier review, artifact authorization boundaries, maintainer approval requirements, authorization outcome, stop conditions, redaction expectations, and a blocked next `release_execution_maintainer_decision` direction while preserving pre-alpha state, release publishing, release execution, release identifier selection, release artifacts, hosted deployment, runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, broad product module, and direct compatibility deferrals.

  - id: M-121
    title: Release Execution Maintainer Decision
    status: completed
    goal: Hold release execution at an explicit maintainer go/no-go decision before any release identifier, release artifact, tag, hosted deployment, or publication command can run.
    completion_criteria:
      - The maintainer explicitly chooses go or no-go for release execution.
      - Any approved release identifier, artifact family, publication surface, and command set is recorded in a repository artifact before execution.
      - A no-go decision records the reason and the next bounded remediation or deferral.
      - Runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals remain preserved unless separately authorized.
    non_goals:
      - implicitly publishing v0.1 alpha
      - choosing release identifiers or tags without maintainer approval
      - creating release binaries, archives, containers, packages, checksums, provenance files, or hosted deployments without explicit authorization
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - changing authentication/session behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Recorded the maintainer's go decision in `docs/release-execution-maintainer-decision.md` and `docs/release-execution-maintainer-decision.zh-CN.md`, accepted `ADR-0101`, and added repository check coverage through `runtime.release_execution_maintainer_decision`. The decision authorizes continuing to a release identifier and artifact planning step only; it does not approve a final release identifier, release tag, release artifacts, GitHub release record, hosted deployment, publication surface, release execution command, runtime behavior, protocol route, generated output, migration, dependency, operations/admin behavior, authentication/session behavior, broad product module, or direct compatibility change.

  - id: M-122
    title: Release Identifier And Artifact Plan
    status: completed
    goal: Define the proposed v0.1 alpha release identifier and smallest artifact/publication plan before any release command creates public artifacts.
    completion_criteria:
      - The release identifier proposal is recorded and checked for existing tag or release conflicts without creating the tag.
      - Artifact families, publication surface, allowed future commands, verification requirements, known warnings, and stop conditions are recorded.
      - Deferred artifact families and hosted deployment choices remain explicit.
      - Runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals remain preserved unless separately authorized.
    non_goals:
      - publishing v0.1 alpha
      - creating release tags
      - creating release binaries, archives, containers, packages, checksums, provenance files, or hosted deployments
      - creating GitHub release records
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - changing authentication/session behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Recorded the proposed `v0.1.0-alpha.1` release identifier, checked local tags, remote tags, and GitHub release records for conflicts without creating anything, and defined the smallest future release surface as tag plus GitHub release record/source archive while deferring binaries, packages, containers, checksums, provenance, hosted deployments, publication commands, runtime behavior, protocol routes, generated output, migrations, dependencies, operations/admin behavior, authentication/session changes, broad product modules, and direct compatibility.

  - id: M-123
    title: Release Execution Final Authorization
    status: completed
    goal: Hold release execution at a final maintainer authorization point after the release identifier and artifact plan is defined.
    completion_criteria:
      - The maintainer explicitly approves or rejects executing the planned release commands.
      - Any approved execution command, release identifier, tag action, artifact family, GitHub release record, and publication surface is recorded before commands run.
      - Required verification results, known warnings, stop conditions, and rollback posture are reviewed.
      - Runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals remain preserved unless separately authorized.
    non_goals:
      - publishing v0.1 alpha without final maintainer authorization
      - creating or pushing release tags without final maintainer authorization
      - creating release binaries, archives, containers, packages, checksums, provenance files, hosted deployments, or GitHub release records without final maintainer authorization
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - changing authentication/session behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Recorded the maintainer's final `go` authorization for `v0.1.0-alpha.1`, accepted `ADR-0103`, authorized creating and pushing the Git tag, authorized creating the GitHub release record, limited artifacts to the GitHub source archive, refreshed README as the external alpha user entry point, added release notes, and preserved deferrals for binaries, packages, containers, checksums, signing/provenance, hosted deployments, install scripts, registry publication, announcements beyond the GitHub release record, runtime behavior, protocol routes, generated output, migrations, dependencies, operations/admin behavior, authentication/session behavior, broad product modules, and direct Nakama/Pitaya API compatibility.

  - id: M-124
    title: First Alpha User Discovery
    status: completed
    goal: Turn the source-first v0.1 alpha release and refreshed README into a bounded loop for finding early developers and converting their friction into next work items.
    completion_criteria:
      - The first target developer segments and outreach surfaces are recorded.
      - The feedback loop, success signals, and stop conditions are defined before public announcements beyond the GitHub release record.
      - Runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals remain preserved unless separately authorized.
    non_goals:
      - creating additional release artifacts
      - publishing binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, or hosted deployments
      - running broad public announcement campaigns without a bounded plan
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - changing authentication/session behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the first alpha user discovery loop in `docs/first-alpha-user-discovery-loop.md` and `docs/first-alpha-user-discovery-loop.zh-CN.md`, accepted `ADR-0104`, recorded target developer segments, outreach surfaces, feedback capture fields, review questions, success signals, and stop conditions, and preserved deferrals for public announcements beyond the GitHub release record, paid promotion, hosted deployments, additional release artifacts, runtime behavior, protocol routes, generated output, migrations, dependencies, operations/admin behavior, authentication/session behavior, broad product modules, and direct Nakama/Pitaya API compatibility.

  - id: M-125
    title: First Alpha Feedback Intake Surfaces
    status: completed
    goal: Prepare the smallest repository-owned surfaces for receiving early alpha user feedback before any broader outreach.
    completion_criteria:
      - The first feedback intake surface choice is recorded.
      - Feedback guidance, redaction expectations, labels or templates, and triage posture are prepared without executing broad announcements.
      - Runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, release artifact, public announcement, paid promotion, and direct compatibility deferrals remain preserved unless separately authorized.
    non_goals:
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - changing authentication/session behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Prepared the first repository-owned alpha feedback intake surface at `.github/ISSUE_TEMPLATE/alpha-feedback.yml`, documented feedback fields, redaction expectations, triage labels, product maturity mapping, and triage posture in `docs/first-alpha-feedback-intake-surfaces.md` and its Simplified Chinese translation, recorded product maturity milestones from source-first alpha to prototype-ready foundation, single-node production-candidate foundation, and Nakama/Pitaya-class product in `docs/product-maturity-milestones.md`, accepted `ADR-0105`, added `runtime.first_alpha_feedback_intake_surfaces` check coverage, and opened `W-0198 Define prototype-ready foundation execution plan` without executing announcements, adding hosted deployment, expanding release artifacts, or changing runtime scope.

  - id: M-126
    title: Prototype Ready Foundation Execution Plan
    status: completed
    goal: Define the smallest execution plan that moves vibit from source-first alpha toward a prototype-ready game/backend foundation.
    completion_criteria:
      - The first prototype-ready execution plan is recorded.
      - The plan chooses a bounded sequence across setup friction, example/client ergonomics, shared online services, storage, messaging/push, verification, or operations inspection.
      - The plan maps candidate work to product maturity milestones and Nakama/Pitaya capability families without claiming production readiness.
      - Runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, release artifact, public announcement, paid promotion, and direct compatibility deferrals remain preserved unless separately authorized.
    non_goals:
      - implementing prototype-ready runtime behavior in the planning slice
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding broad operations/admin behavior
      - changing authentication/session behavior
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the first prototype-ready foundation execution plan in `docs/prototype-ready-foundation-execution-plan.md` and `docs/prototype-ready-foundation-execution-plan.zh-CN.md`, accepted `ADR-0106`, selected `prototype_ready_local_development_path_gate` as the first execution slice, opened `W-0199 Define prototype-ready local development path gate`, and preserved deferrals for runtime behavior, protocol routes, Protobuf sources or generated output, migrations, dependencies, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, and direct Nakama/Pitaya API compatibility.

  - id: M-127
    title: Prototype Ready Local Development Path Gate
    status: completed
    goal: Define the gate for a repeatable local development path that can later improve setup, migrations, configuration, and example flow for prototype authors.
    completion_criteria:
      - The local development path gate is recorded.
      - The gate defines supported prerequisites, startup expectations, migration expectations, configuration and secret posture, example-flow shape, allowed future write areas, verification expectations, and stop conditions.
      - The gate preserves runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - implementing local development path behavior in the gate slice
      - changing runtime behavior
      - adding protocol routes or generated output
      - adding migrations or dependencies
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding broad product modules
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the prototype-ready local development path gate in `docs/prototype-ready-local-development-path-gate.md` and `docs/prototype-ready-local-development-path-gate.zh-CN.md`, accepted `ADR-0107`, recorded supported local prerequisites, startup expectations, migration expectations, configuration and secret posture, example-flow shape, allowed future write areas, verification expectations, and stop conditions, added `runtime.prototype_ready_local_development_path_gate` check coverage, opened `W-0200 Implement prototype-ready local development path package`, and preserved deferrals for runtime behavior, protocol routes, Protobuf sources or generated output, migrations, dependencies, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, and direct Nakama/Pitaya API compatibility.

  - id: M-128
    title: Prototype Ready Local Development Path Package
    status: completed
    goal: Package a repeatable source-first local development path for prototype authors using the gate recorded in M-127.
    completion_criteria:
      - The local development path package is recorded and linked from contributor entry points.
      - The package improves setup, startup, migration, configuration, secret redaction, example-flow, and verification ergonomics using docs, scripts, examples, static checks, or focused verification of existing behavior.
      - The package preserves runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - changing production runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - adding migrations or dependencies
      - adding automatic startup migration behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding broad product modules
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the prototype-ready local development path package in `docs/prototype-ready-local-development-path-package.md` and `docs/prototype-ready-local-development-path-package.zh-CN.md`, added example entry points and a redacted placeholder local environment template under `examples/`, added `.gitignore` guardrails for private local env files, accepted `ADR-0108`, added `runtime.prototype_ready_local_development_path_package` check coverage, opened `W-0201 Define storage objects behavior gate`, and preserved deferrals for runtime behavior, protocol routes, Protobuf sources or generated output, migrations, dependencies, automatic startup migrations, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, and direct Nakama/Pitaya API compatibility.

  - id: M-129
    title: Storage Objects Behavior Gate
    status: completed
    goal: Define the first general storage-object behavior beyond the inventory proof slice before implementation.
    completion_criteria:
      - The first storage objects behavior gate is recorded.
      - The gate defines object ownership, key/scope posture, read/write behavior, permission posture, conflict semantics, protocol/data expectations, verification expectations, and stop conditions before implementation.
      - The gate preserves runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - implementing storage objects runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - adding migrations or dependencies
      - changing repository interfaces or storage adapters
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the storage objects behavior gate in `docs/storage-objects-behavior-gate.md` and `docs/storage-objects-behavior-gate.zh-CN.md`, accepted `ADR-0109`, recorded player-owned small JSON object posture, object identity, ownership, scope/key posture, read/write semantics, route-scoped permission posture, optimistic conflict semantics, protocol/data expectations, verification expectations, and stop conditions, added `runtime.storage_objects_behavior_gate` check coverage, opened `W-0202 Define storage objects persistence schema gate`, and preserved deferrals for runtime behavior, protocol routes, Protobuf sources or generated output, migrations, dependencies, repository/storage adapters, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, and direct Nakama/Pitaya API compatibility.

  - id: M-130
    title: Storage Objects Persistence Schema Gate
    status: completed
    goal: Define the first persistence schema posture for storage objects before adding migration source, repository interfaces, adapters, protocol, or runtime behavior.
    completion_criteria:
      - The storage objects persistence schema gate is recorded.
      - The gate defines table candidate, owner identity representation, collection/key constraints, value representation, version representation, timestamps, uniqueness and indexes, redaction posture, migration source candidate, and future repository/adapter boundaries.
      - The gate preserves runtime behavior, protocol, generated output, migration, dependency, repository implementation, storage adapter implementation, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - adding SQL migration source
      - implementing storage objects runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - adding repository interfaces or storage adapters
      - adding dependencies
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the storage objects persistence schema gate in `docs/storage-objects-persistence-schema-gate.md` and `docs/storage-objects-persistence-schema-gate.zh-CN.md`, accepted `ADR-0110`, recorded PostgreSQL as the first store, `storage_objects` as the future logical table, `runtime/migrations/postgres/000006_create_storage_objects.sql` as the future migration source candidate, player owner identity, collection/key constraints, JSONB value posture, BIGINT version posture, timestamp and soft-delete posture, active uniqueness/index posture, redaction rules, future repository/adapter boundaries, added `runtime.storage_objects_persistence_schema_gate` check coverage, opened `W-0203 Add storage objects migration source`, and preserved deferrals for SQL migration source creation, runtime behavior, protocol routes, Protobuf sources or generated output, dependencies, repository interfaces, storage adapters, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, and direct Nakama/Pitaya compatibility.

  - id: M-131
    title: Storage Objects Migration Source
    status: completed
    goal: Add the first SQL migration source for storage objects after the schema gate, without adding repository interfaces, adapters, protocol, or runtime behavior.
    completion_criteria:
      - The `runtime/migrations/postgres/000006_create_storage_objects.sql` migration source exists and follows the storage objects persistence schema gate.
      - The migration source records `storage_objects` ownership, identity, collection/key constraints, JSONB value representation, BIGINT version representation, timestamps, uniqueness/index posture, and redaction-sensitive forbidden fields.
      - The slice preserves runtime behavior, protocol, generated output, dependency, repository implementation, storage adapter implementation, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - implementing storage objects runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - adding repository interfaces or storage adapters
      - adding dependencies
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Added the storage objects PostgreSQL migration source at `runtime/migrations/postgres/000006_create_storage_objects.sql`, accepted `ADR-0111`, recorded player-owned small JSON object persistence with `storage_objects`, JSONB object values, BIGINT versions, soft delete posture, active logical identity uniqueness, owner/collection lookup and updated-at indexes, added `runtime.storage_objects_migration_source` check coverage, opened `W-0204 Define storage objects repository boundary`, and preserved deferrals for storage objects runtime behavior, repository interfaces, storage adapters, protocol routes, Protobuf sources or generated output, dependencies, authentication/session changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, and direct Nakama/Pitaya API compatibility.

  - id: M-132
    title: Storage Objects Repository Boundary Gate
    status: completed
    goal: Define the storage-neutral storage objects repository boundary after the migration source, without adding repository implementation, PostgreSQL adapter behavior, protocol, or runtime behavior.
    completion_criteria:
      - The storage objects repository boundary gate is recorded.
      - The gate defines future repository ownership, candidate value types, create/read/update/delete/conflict vocabulary, version handoff, redaction posture, PostgreSQL adapter expectations, and stop conditions before implementation.
      - The gate preserves repository interface implementation, storage adapter implementation, runtime behavior, protocol, generated output, dependency, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - implementing storage objects runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - adding repository interfaces or storage adapters
      - adding dependencies
      - changing migrations
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the storage objects repository boundary in `docs/storage-objects-repository-boundary.md` and `docs/storage-objects-repository-boundary.zh-CN.md`, accepted `ADR-0112`, recorded future owner `runtime/internal/modules/storage`, future interface candidate `runtime/internal/modules/storage.Repository`, future PostgreSQL adapter owner `runtime/internal/platform/persistence/postgres`, candidate storage object value types, create/read/list/update/delete repository vocabulary, version and conflict handoff, redaction posture, PostgreSQL adapter expectations, `runtime.storage_objects_repository_boundary` check coverage, and selected `W-0205 Implement storage-neutral storage objects repository interface` as the next-ready work item without adding repository implementation, adapter behavior, runtime behavior, protocol routes, Protobuf sources or generated output, dependencies, migration changes, operations/admin behavior, authentication/session changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility.

  - id: M-133
    title: Storage Objects Repository Interface Implementation
    status: completed
    goal: Implement the storage-neutral storage objects repository interface after the repository boundary, without adding PostgreSQL adapter behavior, protocol, or runtime behavior.
    completion_criteria:
      - The storage-neutral storage objects repository interface is implemented under the authorized storage module owner.
      - The implementation defines module-owned value types, create/read/list/update/delete input and result vocabulary, validation helpers, version/conflict vocabulary, redacted errors, and focused unit tests.
      - The slice preserves PostgreSQL adapter implementation, SQL execution, runtime behavior, protocol, generated output, dependency, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - implementing storage objects runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - adding PostgreSQL storage adapters
      - adding SQL execution behavior
      - adding dependencies
      - changing migrations
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the storage-neutral storage objects repository interface under `runtime/internal/modules/storage`, added module-owned value types, create/read/list/update/delete input and result vocabulary, version and conflict helpers, redacted repository errors, normalization helpers, focused unit tests, storage module manifest and guides, accepted `ADR-0113`, added `runtime.storage_objects_repository_interface_implementation` check coverage, and selected `W-0206 Define storage objects PostgreSQL adapter gate` as the next-ready work item without adding PostgreSQL storage adapters, SQL execution behavior, runtime behavior, protocol routes, Protobuf sources or generated output, dependencies, migration changes, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct Nakama/Pitaya API compatibility.

  - id: M-134
    title: Storage Objects PostgreSQL Adapter Gate
    status: completed
    goal: Define the storage objects PostgreSQL adapter gate after the storage-neutral repository interface, without adding adapter implementation, SQL execution behavior, protocol, or runtime behavior.
    completion_criteria:
      - The storage objects PostgreSQL adapter gate is recorded.
      - The gate defines future PostgreSQL adapter ownership, constructor and unit-of-work expectations, SQL mapping posture, transaction handoff, error mapping, tests, and stop conditions before implementation.
      - The gate preserves PostgreSQL adapter implementation, SQL execution behavior, runtime behavior, protocol, generated output, dependency, migration, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - implementing storage objects runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - adding PostgreSQL storage adapters
      - adding SQL execution behavior
      - adding dependencies
      - changing migrations
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the storage objects PostgreSQL adapter gate in `docs/storage-objects-postgresql-adapter-gate.md` and `docs/storage-objects-postgresql-adapter-gate.zh-CN.md`, accepted `ADR-0114`, recorded future owner `runtime/internal/platform/persistence/postgres`, future adapter source and test candidates, constructor and unit-of-work expectations, SQL mapping posture, transaction handoff, error mapping, future tests, stop conditions, `runtime.storage_objects_postgresql_adapter_gate` check coverage, and selected `W-0207 Implement storage objects PostgreSQL adapter` as the next-ready work item without adding PostgreSQL storage adapters, SQL execution behavior, runtime behavior, protocol routes, Protobuf sources or generated output, dependencies, migration changes, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct Nakama/Pitaya API compatibility.

  - id: M-135
    title: Storage Objects PostgreSQL Adapter Implementation
    status: completed
    goal: Implement the storage objects PostgreSQL adapter after the adapter gate, without adding runtime behavior, protocol routes, generated output, migrations, or dependency changes.
    completion_criteria:
      - The storage objects PostgreSQL adapter implements the storage-neutral repository interface under the authorized platform persistence owner.
      - The implementation maps create/read/list/update/delete behavior to the existing `storage_objects` table with version-aware conflicts, redacted errors, and focused tests.
      - The slice preserves runtime behavior, protocol, generated output, dependency, migration, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - implementing storage objects runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - adding dependencies
      - changing migrations
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the storage objects PostgreSQL adapter under `runtime/internal/platform/persistence/postgres`, added `runtime/internal/platform/persistence/postgres/storage_object_repository.go` and focused fake-executor tests in `runtime/internal/platform/persistence/postgres/storage_object_repository_test.go`, added unit-of-work repository handoff through `UnitOfWork.NewStorageObjectRepository`, mapped create/read/list/update/delete to the existing `storage_objects` table with active-row filtering, version-aware conflicts, soft-delete behavior, redacted storage module errors, and accepted `ADR-0115` plus `runtime.storage_objects_postgresql_adapter_implementation` check coverage. Runtime behavior, protocol routes, Protobuf sources/generated output, dependencies, migration changes, authentication/session behavior, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, and direct compatibility remain deferred.

  - id: M-136
    title: Storage Objects Runtime Behavior Gate
    status: completed
    goal: Define the storage objects runtime behavior gate after the PostgreSQL adapter, without adding runtime handlers, protocol routes, generated output, migrations, or dependency changes.
    completion_criteria:
      - The storage objects runtime behavior gate records owner identity derivation, request identity requirements, permissions, validation, conflict semantics, route-policy expectations, service/application ownership, and stop conditions before runtime behavior implementation.
      - The gate preserves protocol routes, Protobuf source/generated output, dependency, migration, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
      - The next implementation queue remains bounded and does not treat metadata-only player identity as authenticated proof.
    non_goals:
      - implementing storage objects runtime behavior
      - adding protocol routes or generated output
      - adding Protobuf source files
      - changing storage object repository interfaces or PostgreSQL adapters
      - adding dependencies
      - changing migrations
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the storage objects runtime behavior gate in `docs/storage-objects-runtime-behavior-gate.md` and `docs/storage-objects-runtime-behavior-gate.zh-CN.md`, accepted `ADR-0116`, recorded future application owner `runtime/internal/app`, candidate package `runtime/internal/app/storage`, future service source and test candidates, validated request identity owner derivation, metadata-only `player_id` and `session_id` refusal, route-policy expectation `request_token_required`, permission, validation, conflict, unit-of-work handoff, redaction, future tests, stop conditions, and `runtime.storage_objects_runtime_behavior_gate` check coverage. Runtime behavior implementation, runtime handlers, startup wiring, protocol routes, Protobuf sources/generated output, repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, and direct compatibility remain deferred.

  - id: M-137
    title: Storage Objects Runtime Behavior Implementation
    status: completed
    goal: Implement storage objects runtime behavior after the runtime behavior gate, without adding protocol routes, generated output, migrations, dependencies, or authentication/session changes.
    completion_criteria:
      - The storage objects runtime behavior implementation derives player ownership from validated request identity and rejects metadata-only identity before repository access.
      - The implementation composes storage repository operations through application-owned service behavior with validation, conflict mapping, redaction, and unit-of-work handoff tests.
      - The slice preserves protocol routes, Protobuf source/generated output, dependency, migration, repository-interface, PostgreSQL-adapter, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - adding protocol routes or generated output
      - adding Protobuf source files
      - changing storage object repository interfaces or PostgreSQL adapters
      - adding dependencies
      - changing migrations
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Implemented application-owned storage objects runtime behavior under `runtime/internal/app/storage`, added `Service`, own-object get/list/put/delete operations, validated request identity owner derivation, metadata-only identity refusal before repository access, collection/key/value/list/expected-version validation, redacted public error mapping, unit-of-work repository handoff through `NewStorageObjectRepository`, focused fake-repository tests, accepted `ADR-0117`, and registered `runtime.storage_objects_runtime_behavior_implementation`. Protocol routes, Protobuf sources/generated output, runtime handlers, startup wiring, repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, and direct compatibility remain deferred.

  - id: M-138
    title: Storage Objects Protocol Route Gate
    status: completed
    goal: Define the storage objects protocol route gate after application runtime behavior, without adding route implementation, generated output, startup wiring, migrations, dependencies, or authentication/session changes.
    completion_criteria:
      - The gate records future storage object route names, message-shape posture, protected-route policy, protocol adapter ownership, generated-output expectations, and stop conditions before exposing runtime behavior over WebSocket/Protobuf.
      - The gate preserves route implementation, Protobuf source/generated output, dependency, migration, repository-interface, PostgreSQL-adapter, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
      - Metadata-only identity remains rejected by future protocol handlers.
    non_goals:
      - implementing storage object protocol routes
      - adding Protobuf source files or generated output
      - registering runtime handlers or startup composition
      - changing storage object service behavior
      - changing storage object repository interfaces or PostgreSQL adapters
      - adding dependencies
      - changing migrations
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined `docs/storage-objects-protocol-route-gate.md` and `docs/storage-objects-protocol-route-gate.zh-CN.md`, accepted `ADR-0118`, registered `runtime.storage_objects_protocol_route_gate`, and recorded future client-facing storage object routes, request/response message-shape posture, protected-route policy, authenticated-wrapper and validated request identity handoff, metadata-only identity refusal, protocol adapter ownership, generated-output expectations, redaction expectations, required future tests, and Nakama/Pitaya reference mapping. The gate adapts Nakama storage object capability expectations and Pitaya route/session/handler separation without adding route implementation, Protobuf sources/generated output, runtime handlers, startup wiring, repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility.

  - id: M-139
    title: Storage Objects Protocol Route Implementation
    status: completed
    goal: Implement storage objects protocol routes after the route gate, without broadening storage scope, changing authentication/session behavior, adding migrations, adding dependencies, or adopting direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The implementation exposes the W-0210 route family through explicit WebSocket/Protobuf route registration, generated output, and protocol bridge mapping.
      - The implementation preserves validated request identity handoff, metadata-only identity refusal, protected-route policy, redacted error mapping, and application-owned service behavior.
      - The slice preserves repository-interface, PostgreSQL-adapter, migration, dependency, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - adding public ACLs or cross-owner access
      - adding admin search
      - adding group/guild/party/room/match storage scopes
      - adding batch writes
      - adding JSON patch or merge semantics
      - adding TTL or script hooks
      - changing storage object service behavior beyond route mapping
      - changing storage object repository interfaces or PostgreSQL adapters
      - adding dependencies
      - changing migrations
      - changing authentication/session behavior
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the own-player storage object protocol route family authorized by `ADR-0118`, added `proto/vibit/storage/v1/storage.proto`, generated Go Protobuf output under `runtime/internal/generated/proto/vibit/storage/v1/storage.pb.go`, route keys under `runtime/internal/app/storage/routes.go`, application route handlers under `runtime/internal/app/bootstrap/storage.go`, protocol bridge mapping under `runtime/internal/platform/protocol/protobuf/storage_bridge.go`, PostgreSQL startup registration, transaction bypass for service-owned write routes, focused route/bridge/protected-route/startup tests, accepted `ADR-0119`, and registered `runtime.storage_objects_protocol_route_implementation`. The implementation exposes `storage.GetOwnStorageObject`, `storage.ListOwnStorageObjects`, `storage.PutOwnStorageObject`, and `storage.DeleteOwnStorageObject` while preserving validated request identity handoff, metadata-only identity refusal, request-token protection, redacted errors, application-owned service behavior, repository interface, PostgreSQL adapter, migration, dependency, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, large object/blob storage, S3-compatible storage, and direct Nakama/Pitaya API compatibility deferrals.

  - id: M-140
    title: Storage Objects Protocol Route Local Proof
    status: completed
    goal: Prove the completed storage objects protocol route in the local alpha request flow without broadening route semantics, changing protocol shape, changing authentication/session behavior, or adding direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The local alpha request flow can demonstrate authenticated own-player storage object put/get/list/delete behavior through the existing WebSocket/Protobuf path.
      - The proof records exact local prerequisites, commands, expected request/response shape, and redaction expectations for contributors.
      - The proof preserves protocol shape, storage service behavior, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session semantics, hosted deployment, release artifact, public announcement, paid promotion, object/blob storage, S3-compatible storage, ACL, batch, hook, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - adding new protocol messages or routes
      - changing storage object service behavior
      - changing storage object repository interfaces or PostgreSQL adapters
      - changing migrations
      - adding dependencies
      - changing authentication/session behavior
      - adding public ACLs or cross-owner access
      - adding admin search
      - adding group/guild/party/room/match storage scopes
      - adding batch writes
      - adding JSON patch or merge semantics
      - adding TTL or script hooks
      - adding broad operations/admin behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Proved the completed own-player storage object protocol route family in the local alpha WebSocket/Protobuf request flow. Added `TestStorageObjectsProtocolRouteLocalAlphaFlow`, extended the authenticated gameplay E2E fixture with test-only storage route registration and a test-only in-memory storage repository, updated `examples/local-alpha-request-loop.sh` to run both the existing authenticated gameplay proof and the storage objects proof, documented the request/response proof path and redaction expectations in `examples/README.md` and `examples/README.zh-CN.md`, accepted `ADR-0120`, and registered `runtime.storage_objects_protocol_route_local_proof`. The proof demonstrates authenticated `storage.PutOwnStorageObject`, `storage.GetOwnStorageObject`, `storage.ListOwnStorageObjects`, and `storage.DeleteOwnStorageObject` over the existing request-token protected WebSocket/Protobuf flow while preserving protocol shape, storage service behavior, repository interface, PostgreSQL adapter, migrations, dependencies, authentication/session behavior, hosted deployment, release artifact, public announcement, paid promotion, public ACL, cross-owner access, admin search, group/guild/party/room/match scopes, batch write, JSON patch, merge, TTL, script hook, large object/blob storage, S3-compatible storage, production memory storage, and direct Nakama/Pitaya API compatibility deferrals.

  - id: M-141
    title: Next Direction Confirmation After Storage Objects Local Proof
    status: completed
    goal: Confirm the next bounded alpha direction after proving storage objects in the local request flow, preserving Nakama/Pitaya alignment and avoiding broad product module expansion without explicit selection.
    completion_criteria:
      - The next direction after storage objects local proof is selected and recorded with Nakama/Pitaya reference alignment.
      - The selection identifies one bounded follow-up work item and preserves current protocol, storage, authentication/session, dependency, migration, hosted deployment, release artifact, public announcement, paid promotion, blob/S3, and direct compatibility deferrals unless separately authorized.
    non_goals:
      - implementing the selected direction
      - adding protocol messages or routes
      - changing storage object service behavior
      - changing repository interfaces or PostgreSQL adapters
      - changing migrations
      - adding dependencies
      - changing authentication/session behavior
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Selected `define_first_server_push_realtime_messaging_gate` as the next prototype-ready alpha direction after the storage objects local proof, accepted `ADR-0121`, recorded Nakama/Pitaya alignment for outbound realtime vocabulary and Pitaya-style layering, registered `runtime.next_alpha_direction_after_storage_objects_local_proof`, and opened `M-142/W-0214 Define first server push and realtime messaging gate` as the next-ready item. No runtime behavior, protocol routes, Protobuf messages, generated output, storage behavior, repository interface, PostgreSQL adapter, migration, dependency, authentication/session behavior, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, matchmaking, match runtime, distributed runtime, broad social module, or direct Nakama/Pitaya API compatibility was added.

  - id: M-142
    title: First Server Push And Realtime Messaging Gate
    status: completed
    goal: Define the first bounded server push and realtime messaging gate for prototype-ready shared online services while preserving transport/session/protocol/application/backend separation and avoiding direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The first server push and realtime messaging gate is recorded with ownership, vocabulary, allowed future files, verification expectations, and stop conditions.
      - The gate maps Nakama-style outbound realtime capability pressure and Pitaya-style push/broadcast/group/session/handler layering without implementing runtime behavior or copying public APIs.
      - Current protocol, generated output, storage, authentication/session, dependency, migration, hosted deployment, release artifact, public announcement, paid promotion, matchmaking, match runtime, distributed runtime, blob/S3, and direct compatibility deferrals remain preserved unless separately authorized.
    non_goals:
      - implementing server push, broadcast, streams, chat, notifications, or realtime messaging
      - adding protocol messages or routes
      - changing Protobuf source or generated output
      - changing storage object service behavior
      - changing repository interfaces or PostgreSQL adapters
      - changing migrations
      - adding dependencies
      - changing authentication/session behavior
      - changing route protection
      - adding hosted deployments or demos
      - creating release binaries, packages, containers, checksums, provenance, signing artifacts, install scripts, registry publications, or SDK packages
      - executing public announcements beyond the GitHub release record
      - running paid promotion
      - adding large object/blob storage or S3-compatible object storage
      - adding matchmaking or match runtime behavior
      - adding distributed runtime, frontend/backend split, RPC, or service discovery
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Defined the first server push and realtime messaging gate in `docs/first-server-push-realtime-messaging-gate.md`, accepted `ADR-0122`, registered `runtime.first_server_push_realtime_messaging_gate`, recorded Nakama alignment for notifications, streams, chat, and presence-adjacent outbound messaging, recorded Pitaya alignment for acceptor/session/handler/push/group/broadcast/backend-service separation, and opened `M-143/W-0215 Implement first server push and realtime messaging runtime slice` as next-ready. No runtime behavior, transport delivery, protocol route, Protobuf source, generated output, startup wiring, persistence, migration, dependency, authentication/session behavior, hosted deployment, release artifact, public announcement, paid promotion, matchmaking, match runtime, distributed runtime, broad social module, blob/S3 storage, or direct Nakama/Pitaya API compatibility was added.

  - id: M-143
    title: First Server Push And Realtime Messaging Runtime Slice
    status: completed
    goal: Implement the smallest first server push and realtime messaging runtime slice authorized by ADR-0122 while preserving protocol/generated-output, persistence, delivery guarantees, distributed fanout, matchmaking, match runtime, broad social, and direct compatibility deferrals unless explicitly authorized.
    completion_criteria:
      - The first runtime slice implements only the smallest application-owned outbound realtime behavior and focused verification authorized by `docs/first-server-push-realtime-messaging-gate.md`.
      - Recipient target validation, metadata-only identity refusal, redacted delivery outcomes, and Nakama/Pitaya layering alignment are verified.
      - Protocol routes, Protobuf source, generated output, persistence, migrations, dependencies, authentication/session behavior changes, hosted deployments, release artifacts, paid promotion, matchmaking, match runtime, distributed runtime, and direct compatibility remain deferred unless a later implementation ADR explicitly authorizes a narrower exception.
    non_goals:
      - implementing broad chat, channels, groups, parties, rooms, matches, matchmaking, or match runtime
      - adding Protobuf source or generated output unless separately authorized
      - adding public client publish routes
      - adding stream subscription persistence
      - adding offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, or backpressure mechanisms
      - adding distributed fanout, frontend/backend split, RPC, service discovery, or cluster groups
      - changing authentication/session semantics
      - changing storage object behavior, repository interfaces, PostgreSQL adapters, migrations, or dependencies
      - adding hosted deployments, release artifacts, public announcements beyond the GitHub release record, or paid promotion
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the first application-owned realtime runtime service under `runtime/internal/app/realtime`, accepted `ADR-0123`, registered `runtime.first_server_push_realtime_messaging_runtime_slice`, and added focused tests for server-authored intent validation, metadata-only and player sender refusal, bound-connection and player-current-connection recipient resolution, future-only stream subscribers, redacted errors, and copied delivery intents. The slice preserves Nakama-aligned outbound realtime capability pressure and Pitaya-aligned transport/session/handler/protocol/backend separation without WebSocket writes, Protobuf source, generated output, protocol routes, startup wiring, persistence, migrations, dependencies, authentication/session behavior changes, route-protection changes, delivery guarantees, distributed fanout, broad chat/social behavior, matchmaking, match runtime, hosted deployments, release artifacts, public announcements, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility.

  - id: M-144
    title: Next Direction Confirmation After Realtime Runtime Slice
    status: completed
    goal: Confirm the next bounded alpha direction after the first server push and realtime messaging runtime slice, without implementing protocol, transport delivery, persistence, broad realtime/social behavior, matchmaking, match runtime, distributed runtime, or direct compatibility.
    completion_criteria:
      - The next alpha direction after the realtime runtime slice is selected and recorded in an ADR.
      - One follow-up work item is opened as `next_ready`.
      - Nakama/Pitaya reference alignment is recorded for the selected direction.
      - Existing deferrals for protocol expansion, WebSocket outbound delivery, generated output, persistence, delivery guarantees, broad chat/social modules, matchmaking, match runtime, distributed runtime, hosted/release surfaces, and direct compatibility remain explicit.
    non_goals:
      - implementing WebSocket outbound delivery
      - adding Protobuf source or generated output
      - adding protocol routes or startup wiring
      - adding stream subscriptions, chat, groups, broadcast fanout, offline inboxes, ordering, retries, durable offsets, or backpressure
      - adding persistence, migrations, dependencies, or authentication/session behavior changes
      - adding matchmaking, match runtime, distributed runtime, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, or direct Nakama/Pitaya API compatibility
    completion_summary: Selected `define_realtime_protocol_websocket_outbound_delivery_gate` as the next prototype-ready alpha direction after the first server push and realtime messaging runtime slice, accepted `ADR-0124`, recorded Nakama/Pitaya alignment for moving from application-owned delivery intents toward bounded protocol and transport delivery planning, registered `runtime.next_alpha_direction_after_realtime_runtime_slice`, and opened `M-145/W-0217 Define realtime protocol and WebSocket outbound delivery gate` as the next-ready item. No WebSocket outbound delivery, Protobuf source, generated output, protocol bridge, application bootstrap handler, startup wiring, persistence, migration, dependency, authentication/session behavior change, route-protection change, delivery guarantee, distributed fanout, broad social module, matchmaking, match runtime, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility was added.

  - id: M-145
    title: Realtime Protocol And WebSocket Outbound Delivery Gate
    status: completed
    goal: Define the gate for realtime protocol payloads and WebSocket outbound delivery after the application-owned realtime runtime slice, while preserving protocol/generated-output, transport, startup, persistence, delivery guarantee, broad realtime/social, matchmaking, match runtime, distributed runtime, and direct compatibility deferrals.
    completion_criteria:
      - The realtime protocol and WebSocket outbound delivery gate is recorded with ownership, allowed future files, verification expectations, and stop conditions.
      - The gate maps Nakama-style realtime notifications/streams/chat/presence pressure and Pitaya-style protocol/transport/session/backend push layering without implementing protocol source, generated output, socket writes, startup wiring, persistence, or broad fanout.
      - Existing deferrals for stream subscription persistence, offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, backpressure, distributed fanout, broad social modules, matchmaking, match runtime, hosted/release surfaces, and direct compatibility remain explicit.
    non_goals:
      - implementing WebSocket outbound delivery
      - adding Protobuf source or generated output
      - adding protocol routes, protocol bridge behavior, application bootstrap handlers, or startup wiring
      - adding stream subscriptions, chat, groups, broadcast fanout, offline inboxes, ordering, retries, durable offsets, or backpressure
      - adding persistence, migrations, dependencies, or authentication/session behavior changes
      - adding matchmaking, match runtime, distributed runtime, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the realtime protocol and WebSocket outbound delivery gate in `docs/realtime-protocol-websocket-outbound-delivery-gate.md`, accepted `ADR-0125`, registered `runtime.realtime_protocol_websocket_outbound_delivery_gate`, recorded Nakama alignment for future client-visible notifications, streams, chat, and presence-adjacent outbound payloads, recorded Pitaya alignment for preserving acceptor/session/handler/protocol/backend push separation, and opened `M-146/W-0218 Implement realtime protocol and WebSocket outbound delivery slice` as next-ready. No WebSocket outbound delivery, concrete socket writes, Protobuf source, generated output, protocol bridge behavior, application bootstrap handler, startup wiring, persistence, migration, dependency, authentication/session behavior change, route-protection change, delivery guarantee, distributed fanout, broad social module, matchmaking, match runtime, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility was added.

  - id: M-146
    title: Realtime Protocol And WebSocket Outbound Delivery Implementation Slice
    status: completed
    goal: Implement the smallest realtime protocol payload and WebSocket outbound delivery slice authorized by ADR-0125 while preserving stream subscription, chat, group, broadcast fanout, persistence, delivery guarantee, distributed runtime, matchmaking, match runtime, and direct compatibility deferrals unless explicitly authorized.
    completion_criteria:
      - The implementation adds only the narrow protocol, generated-output, protocol-bridge, and transport outbound delivery behavior authorized by `docs/realtime-protocol-websocket-outbound-delivery-gate.md`.
      - Application-owned realtime policy remains separate from Protobuf mapping and WebSocket transport writes.
      - Server-observed connection id and epoch are the only connection targeting authority.
      - Stream subscription ownership, chat semantics, groups, broadcast fanout, offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, backpressure, distributed fanout, matchmaking, match runtime, hosted/release surfaces, and direct compatibility remain deferred.
    non_goals:
      - adding stream subscriptions, chat semantics, groups, broadcast fanout, offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, or backpressure
      - adding persistence, migrations, dependencies, authentication/session behavior changes, or route-protection changes
      - adding matchmaking, match runtime, distributed runtime, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the smallest realtime protocol and WebSocket outbound delivery slice authorized by `ADR-0125`, accepted `ADR-0126`, added `proto/vibit/realtime/v1/realtime.proto`, generated Go Protobuf output, realtime protocol bridge mapping, payload registry coverage, and WebSocket outbound binary frame delivery by server-observed connection id and epoch. The slice preserves application policy, protocol mapping, and transport write ownership boundaries, keeps `stream_message` future-only, and avoids startup wiring, realtime bootstrap handlers, persistence, migrations, dependencies, authentication/session changes, route-protection changes, stream subscriptions, chat rooms, groups, broadcast fanout, delivery guarantees, distributed runtime, matchmaking, match runtime, hosted/release surfaces, public announcements, paid promotion, blob/S3 storage, and direct Nakama/Pitaya API compatibility.

  - id: M-147
    title: Next Direction Confirmation After Realtime Outbound Delivery Slice
    status: completed
    goal: Hold the work queue at an explicit maintainer confirmation point after realtime protocol and WebSocket outbound delivery implementation before choosing the next prototype-ready alpha direction.
    completion_criteria:
      - The next bounded alpha direction after the realtime outbound delivery slice is selected.
      - The selected direction is recorded as a bounded milestone with at least one next_ready work item.
      - Nakama is recorded as the primary product capability reference, while Pitaya is deferred as a future distributed architecture reference.
      - AI-native development and AI-native testing are recorded as the product purpose before broadening feature work.
      - Ask-first boundaries remain preserved for startup wiring, stream subscriptions, chat, groups, broadcast fanout, persistence, delivery guarantees, distributed runtime, matchmaking, match runtime, SDKs, hosted deployments, release artifacts, public announcements, paid promotion, and direct compatibility.
    non_goals:
      - implementing startup wiring or realtime bootstrap handlers
      - adding stream subscriptions, chat semantics, groups, broadcast fanout, offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, or backpressure
      - adding persistence, migrations, dependencies, authentication/session behavior changes, or route-protection changes
      - adding matchmaking, match runtime, distributed runtime, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility
    completion_summary: Selected `define_agent_native_feature_request_test_workflow` as the next prototype-ready alpha direction after the realtime protocol and WebSocket outbound delivery slice, accepted `ADR-0127`, made Nakama the primary product capability reference, deferred Pitaya as a future distributed architecture reference, recorded AI-native development and AI-native testing as the product purpose, and opened `M-148/W-0220 Define agent-native feature request and test workflow` as the next-ready item. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, distributed runtime, broad product module, or direct Nakama/Pitaya API compatibility was added.

  - id: M-148
    title: Agent-Native Feature Request And Test Workflow
    status: completed
    goal: Define the workflow that turns user backend requirements into specs, acceptance criteria, test plans, tests, implementation boundaries, verification records, and durable project memory before broadening product modules.
    completion_criteria:
      - The workflow is documented as the default for non-trivial user-facing feature work.
      - Required artifacts for request, acceptance, test plan, implementation, verification, and durable memory are declared.
      - The workflow keeps Nakama-first product mapping and Pitaya-deferred architecture posture.
      - Repository checks can detect the workflow standard and next-ready state.
    non_goals:
      - implementing runtime behavior
      - adding protocol routes or messages
      - adding generated output
      - changing migrations or dependencies
      - adding chat, groups, matchmaking, match runtime, or distributed runtime
    completion_summary: Defined the agent-native feature request and test workflow standard, accepted ADR-0128, and recorded how user backend requirements become bounded specs, Nakama capability mapping, acceptance criteria, test plans, tests or explicit test rationale, implementation boundaries, verification records, and durable project memory. Opened M-149/W-0221 as the next-ready workflow pilot for a Nakama-aligned feature slice. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, broad module, Pitaya-style distributed architecture, hosted deployment, release artifact, public announcement, paid promotion, or direct Nakama/Pitaya API compatibility was added.

  - id: M-149
    title: Nakama-Aligned Feature Request Workflow Pilot
    status: completed
    goal: Apply the agent-native feature request and test workflow to choose and shape the next bounded Nakama-aligned prototype-ready feature slice.
    completion_criteria:
      - A concrete user-facing Nakama-style capability request is captured.
      - The request is mapped through the agent-native workflow into spec, acceptance criteria, test plan, implementation boundaries, verification plan, and durable memory expectations.
      - The pilot selects or opens one bounded follow-up slice without adding broad runtime behavior prematurely.
      - Pitaya remains deferred unless a later ADR explicitly reactivates it for distributed architecture planning.
    non_goals:
      - implementing broad runtime behavior directly in the pilot
      - adding protocol routes, Protobuf source, generated output, migrations, or dependencies without a bounded follow-up work item
      - adding chat, groups, matchmaking, match runtime, leaderboards, economy, operations, SDKs, or distributed runtime as an unbounded module expansion
      - adding direct Nakama/Pitaya API compatibility
    completion_summary: Applied the agent-native feature request and test workflow to a concrete Nakama-style presence/status capability request, accepted ADR-0129, recorded requirement/spec/acceptance criteria/test plan/implementation boundaries/verification/memory expectations, selected `presence_status_and_notifications` as the Nakama capability family, and opened `M-150/W-0222 Harden presence status local proof through close and offline cases` as the next-ready bounded follow-up. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, broad product module, Pitaya-style distributed architecture, hosted deployment, release artifact, public announcement, paid promotion, or direct Nakama/Pitaya API compatibility was added.

  - id: M-150
    title: Presence Status Local Proof Hardening
    status: completed
    goal: Harden the existing Nakama-style self-presence/status local alpha proof around close, invalidation, offline, and redaction behavior without broadening into subscriptions, chat, groups, matchmaking, match runtime, persistence, or direct compatibility.
    completion_criteria:
      - Existing presence/status behavior is proven through focused tests for online active bound connections and offline after close or invalidation.
      - The local alpha request-flow proof covers the selected presence/status behavior or records why a lower-level proof is the right boundary.
      - The proof uses existing runtime, protocol, and generated surfaces unless the bounded work item explicitly records otherwise.
      - Pitaya remains deferred and direct Nakama/Pitaya API compatibility remains out of scope.
    non_goals:
      - adding new presence protocol routes or Protobuf messages
      - adding presence subscriptions, status broadcast fanout, chat, groups, parties, matchmaking, match runtime, leaderboards, economy, SDKs, operations, or distributed runtime
      - adding persistence, migrations, dependencies, startup wiring, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility
    completion_summary: Hardened the existing Nakama-style self-presence/status local alpha proof with focused application and authenticated Protobuf request-flow tests for online active bound connections, offline after transport close, offline after close-policy invalidation, and response redaction. Accepted ADR-0130, registered `runtime.presence_status_local_proof_hardening`, and opened `M-151/W-0223 Strengthen authenticated gameplay failure-path verification` as the next-ready bounded follow-up. No production runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, subscription, broadcast fanout, broad product module, distributed runtime, or direct Nakama/Pitaya API compatibility was added.

  - id: M-151
    title: Authenticated Gameplay Failure Path Verification
    status: completed
    goal: Strengthen the authenticated local alpha gameplay loop failure-path proof around missing, malformed, invalid, expired, revoked, route-protection, and redaction behavior without adding new protocol routes, persistence, or broad product modules.
    completion_criteria:
      - Existing authenticated gameplay request-flow failure behavior is covered by focused tests for missing or malformed proof where current surfaces support it.
      - Invalid, expired, revoked, or otherwise unusable access-token paths remain fail-closed and redacted.
      - Protected gameplay routes continue to require authenticated identity handoff before application behavior executes.
      - The proof uses existing runtime, protocol, and generated surfaces unless a later bounded work item explicitly records otherwise.
    non_goals:
      - adding new authentication, session, or gameplay protocol routes
      - adding Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, logout behavior changes, token refresh, cleanup jobs, SDKs, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
      - adding chat, groups, parties, matchmaking, match runtime, leaderboards, economy, operations/admin behavior, distributed runtime, or Pitaya-style cluster architecture
    completion_summary: Strengthened the authenticated local alpha gameplay failure-path proof with `TestAuthenticatedGameplayFailurePathsLocalAlphaFlow`, covering missing wrapper, malformed wrapper, malformed token, unknown well-formed token, expired token, revoked token after logout, protected presence missing wrapper, and redaction. Accepted ADR-0131, registered `runtime.authenticated_gameplay_failure_path_verification`, and opened `M-152/W-0224 Select next Nakama prototype-ready capability after authenticated failure-path proof` as the next-ready bounded follow-up. No production runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, authentication/session behavior change, token refresh, cleanup job, broad product module, distributed runtime, or direct Nakama/Pitaya API compatibility was added.

  - id: M-152
    title: Next Nakama Prototype-Ready Capability Selection After Authenticated Failure Path Proof
    status: completed
    goal: Select the next bounded Nakama prototype-ready capability after authenticated gameplay failure-path verification without adding runtime behavior, protocol routes, persistence, broad product modules, distributed runtime, or direct compatibility.
    completion_criteria:
      - The next Nakama prototype-ready capability family is selected and recorded with rationale.
      - The selected follow-up work item is narrow, testable, and aligned with the agent-native requirement/test workflow.
      - Pitaya remains deferred as a future distributed architecture reference unless a later ADR explicitly reactivates it.
      - No runtime behavior, protocol, generated output, migration, dependency, release artifact, hosted deployment, or direct compatibility scope is added by the selection slice.
    non_goals:
      - implementing chat, friends, groups, parties, leaderboards, matchmaking, match runtime, operations/admin behavior, SDKs, or distributed runtime in the selection slice
      - adding protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, release artifacts, hosted deployments, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility
    completion_summary: Selected `client_sdks_examples_and_developer_experience` as the next bounded Nakama prototype-ready capability family, with `define_local_alpha_example_client_path_gate` as the follow-up direction. Accepted ADR-0132, registered `runtime.next_nakama_prototype_ready_capability_selection`, and opened `M-153/W-0225 Define local alpha example client path gate` as the next-ready bounded follow-up. No example client implementation, SDK publication, runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, operations/admin behavior, hosted deployment, release artifact, broad product module, distributed runtime, or direct Nakama/Pitaya API compatibility was added.

  - id: M-153
    title: Local Alpha Example Client Path Gate
    status: completed
    goal: Define the source-first local alpha example client or example app path before implementation, using existing runtime and protocol behavior while preserving redaction, no-SDK-publication, no-hosted-demo, no-new-protocol, and direct compatibility deferrals.
    completion_criteria:
      - The allowed example client or example app shape is defined with explicit ownership, file candidates, and redaction rules.
      - The example path maps to existing local alpha capabilities without adding runtime behavior, protocol routes, Protobuf messages, generated output, dependencies, migrations, persistence, startup wiring, hosted deployment, release artifacts, SDK publication, or direct compatibility.
      - The future implementation work item is narrow and testable.
      - Pitaya remains deferred as a future distributed architecture reference unless a later ADR explicitly reactivates it.
    non_goals:
      - implementing the example client or example app in the gate slice
      - publishing SDKs, generated client libraries, packages, binaries, containers, hosted demos, install scripts, or release artifacts
      - adding new protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, distributed runtime, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the source-first local alpha example client path gate in `docs/local-alpha-example-client-path-gate.md` and `ADR-0133`, registered `runtime.local_alpha_example_client_path_gate`, selected a repository-local example path posture, and opened `M-154/W-0226 Implement local alpha example client path` as the next-ready bounded follow-up. No example implementation, SDK publication, generated client library, runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, hosted deployment, release artifact, distributed runtime, or direct Nakama/Pitaya API compatibility was added.

  - id: M-154
    title: Local Alpha Example Client Path Implementation
    status: completed
    goal: Implement the first source-first local alpha example client path using existing runtime and protocol behavior while preserving redaction, internal package boundaries, no-SDK-publication, no-hosted-demo, no-new-protocol, and direct compatibility deferrals.
    completion_criteria:
      - A clear repository-local example path exists under `examples/` for developers and AI agents to follow.
      - The example path demonstrates the existing local alpha loop without adding runtime behavior, protocol routes, Protobuf messages, generated output, dependencies, migrations, persistence, startup wiring, hosted deployment, release artifacts, SDK publication, or direct compatibility.
      - The example path keeps raw credentials, raw access tokens, digests, verifier keys, DSNs, and transport metadata redacted.
      - Verification covers the example path and the repository continuation state.
    non_goals:
      - publishing SDKs, generated client libraries, packages, binaries, containers, hosted demos, install scripts, or release artifacts
      - adding public onboarding routes, live external client guarantees, new protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, distributed runtime, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the first source-first local alpha example client path under `examples/local-alpha-client/` and `examples/local-alpha-example-client.sh`, preserved `examples/local-alpha-request-loop.sh` as a compatibility wrapper, accepted `ADR-0134`, registered `runtime.local_alpha_example_client_path_implementation`, and opened `M-155/W-0227 Select next Nakama prototype-ready capability after local alpha example client path` as the next-ready follow-up. The path reuses existing local alpha E2E proofs for onboarding, login, connection binding, protected inventory, presence, storage objects, logout, post-logout rejection, and failure-path redaction without adding SDKs, generated clients, runtime behavior, protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, hosted deployment, release artifacts, distributed runtime, or direct Nakama/Pitaya API compatibility.

  - id: M-155
    title: Next Nakama Prototype-Ready Capability Selection After Example Path
    status: completed
    goal: Select the next bounded Nakama-first prototype-ready capability after the source-first local alpha example client path is implemented, while preserving AI-native development/testing workflow and keeping Pitaya deferred.
    completion_criteria:
      - The next capability family is selected from the Nakama-first roadmap.
      - The selection records why the next step should precede broad chat, groups, matchmaking, match runtime, operations/admin, SDK publication, hosted surfaces, or distributed runtime.
      - A bounded follow-up work item is opened.
      - Ask-first boundaries remain in place for runtime behavior, protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, SDK publication, hosted deployments, release artifacts, Pitaya-style distributed architecture, and direct compatibility.
    non_goals:
      - implementing the selected capability in the selection slice
      - publishing SDKs, generated client libraries, packages, binaries, containers, hosted demos, install scripts, or release artifacts
      - adding public onboarding routes, live external client guarantees, new protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, distributed runtime, or direct Nakama/Pitaya API compatibility
    completion_summary: Selected `agent_native_requirement_test_implementation_workflow` as the next bounded Nakama prototype-ready capability family and `define_agent_native_feature_request_scaffolding_gate` as the follow-up direction. The selection records that the local alpha example path makes existing capability readable, and the next highest-leverage gap is making the user requirement -> spec -> acceptance criteria -> tests -> implementation -> verification -> durable memory workflow executable before broadening into chat, social, matchmaking, match runtime, operations, SDK publication, hosted surfaces, or distributed runtime.

  - id: M-156
    title: Agent-Native Feature Request Scaffolding Gate
    status: completed
    goal: Define the gate for future feature request scaffolding so user backend requirements can be turned into bounded specs, acceptance criteria, test plans, implementation boundaries, verification records, and durable memory before coding.
    completion_criteria:
      - The accepted scaffolding posture is defined before implementation.
      - The gate records artifact ownership, template/tool candidates, redaction requirements, check coverage, test expectations, and stop conditions.
      - A bounded follow-up work item is opened only if implementation can remain inside docs/tooling/templates/checks without runtime, protocol, migration, dependency, hosted, SDK, distributed, or direct compatibility scope.
      - Nakama remains the primary product reference and Pitaya remains deferred.
    non_goals:
      - implementing scaffolding in the gate slice
      - adding runtime behavior, new protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the agent-native feature request scaffolding gate in `docs/agent-native-feature-request-scaffolding-gate.md` and its Simplified Chinese translation, accepted `ADR-0136`, registered `runtime.agent_native_feature_request_scaffolding_gate`, selected source-first change artifact scaffolding, recorded future command and template candidates, required `request.md`, `spec.yaml`, `impact.md`, `plan.md`, `checklist.md`, and `verification.md`, and opened `M-157/W-0229 Implement agent-native feature request scaffolding` as the next-ready bounded follow-up. No scaffolding implementation, templates, scaffold command, runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, hosted deployment, SDK, distributed runtime, or direct Nakama/Pitaya API compatibility was added.

  - id: M-157
    title: Agent-Native Feature Request Scaffolding Implementation
    status: completed
    goal: Implement source-first feature request scaffolding so user backend requirements can be turned into bounded change artifacts before coding, while staying inside docs, templates, tooling, and repository checks.
    completion_criteria:
      - A source-first feature request template exists or an equivalent scaffold output is implemented.
      - The scaffold creates or guides the six required artifacts: `request.md`, `spec.yaml`, `impact.md`, `plan.md`, `checklist.md`, and `verification.md`.
      - The scaffold includes Nakama capability mapping, Pitaya deferral, acceptance criteria, test planning, implementation boundaries, redaction reminders, verification commands, and durable memory expectations.
      - The implementation remains bounded to docs, templates, tooling, and checks.
      - No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, hosted, SDK, distributed runtime, or direct compatibility scope is added.
    non_goals:
      - adding runtime behavior, protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented source-first feature request scaffolding with `changes/_template/feature-request/`, the six required template artifacts, `docs/agent-native-feature-request-scaffolding.md` and its Simplified Chinese translation, a narrow `tools/vibit scaffold feature` command, `ADR-0137`, and `runtime.agent_native_feature_request_scaffolding_implementation` check coverage. The scaffold creates or guides `request.md`, `spec.yaml`, `impact.md`, `plan.md`, `checklist.md`, and `verification.md` with Nakama capability mapping, Pitaya deferral, acceptance criteria, test planning, implementation boundaries, redaction, verification, and durable memory prompts. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, hosted deployment, SDK, distributed runtime, or direct compatibility scope was added.

  - id: M-158
    title: Scaffolded Nakama Feature Request Pilot
    status: completed
    goal: Use the new source-first feature request scaffold on one bounded Nakama-style request intake before broad product module expansion.
    completion_criteria:
      - A scaffolded change directory is created with `tools/vibit scaffold feature`.
      - The scaffolded request records Nakama capability mapping, Pitaya deferral, acceptance criteria, test plan, implementation boundaries, verification, and durable memory expectations.
      - The pilot chooses a bounded follow-up that remains aligned with the Nakama-first prototype-ready roadmap.
      - No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, hosted, SDK, distributed runtime, or direct compatibility scope is added unless the scaffolded request explicitly opens a later bounded work item.
    non_goals:
      - adding runtime behavior, protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Used `tools/vibit scaffold feature` to create and fill `changes/2026-05-24-pilot-scaffolded-nakama-feature-request-intake/`, accepted `ADR-0138`, registered `runtime.scaffolded_nakama_feature_request_intake_pilot`, selected the Nakama `friends_groups_and_parties` capability family through a concrete player friendship relationship lifecycle request, kept Pitaya deferred, and opened `M-159/W-0231 Define friends relationship lifecycle gate` as the next-ready bounded follow-up. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, hosted deployment, SDK, distributed runtime, or direct compatibility scope was added.

  - id: M-159
    title: Friends Relationship Lifecycle Gate
    status: completed
    goal: Define a contract-first gate for a future player friendship relationship lifecycle before adding runtime behavior, protocol routes, persistence, generated output, or broader social features.
    completion_criteria:
      - The future friendship lifecycle semantic scope is defined: request, accept, reject, remove, block, unblock, list, and read relationship status.
      - The gate records ownership, identity, permissions, invariants, errors, events, persistence posture, protocol posture, generated output posture, redaction, test expectations, and stop conditions.
      - Nakama remains the primary product capability reference and Pitaya remains deferred.
      - A bounded follow-up is opened only if implementation can preserve current ask-first boundaries.
      - No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, hosted, SDK, distributed runtime, or direct compatibility scope is added in the gate itself.
    non_goals:
      - implementing friendship runtime behavior, protocol routes, Protobuf source, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the friends relationship lifecycle gate in `docs/friends-relationship-lifecycle-gate.md` and its Simplified Chinese translation, accepted `ADR-0139`, registered `runtime.friends_relationship_lifecycle_gate`, recorded future request, accept, reject, remove, block, unblock, list, and status-read semantics, future command/query/event/error/permission vocabulary, actor-relative relationship states, validated identity requirements, invariants, redaction, and test expectations, and opened `M-160/W-0232 Define friends relationship persistence schema gate` as the next-ready bounded follow-up. No runtime behavior, protocol route, Protobuf source, generated output, migration, repository interface, PostgreSQL adapter, dependency, startup wiring, broader social feature, distributed runtime, hosted deployment, SDK, release artifact, or direct compatibility scope was added.

  - id: M-160
    title: Friends Relationship Persistence Schema Gate
    status: completed
    goal: Define the first persistence schema gate for future friends relationship lifecycle state before adding migration source, repositories, adapters, runtime behavior, protocol routes, generated output, or broader social features.
    completion_criteria:
      - Future table candidates, pair identity posture, relationship state representation, block representation, indexes, uniqueness, timestamps, event/audit posture, and redaction expectations are defined before SQL.
      - Repository and PostgreSQL adapter boundaries are recorded without implementation.
      - Nakama remains the primary product capability reference and Pitaya remains deferred.
      - A bounded follow-up is opened only if implementation can preserve current ask-first boundaries.
      - No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, hosted, SDK, distributed runtime, or direct compatibility scope is added in the gate itself.
    non_goals:
      - adding a migration source, relationship table, repository interface, PostgreSQL adapter, runtime behavior, protocol routes, Protobuf source, generated output, dependencies, persistence execution, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the friends relationship persistence schema gate in `docs/friends-relationship-persistence-schema-gate.md` and its Simplified Chinese translation, accepted `ADR-0140`, registered `runtime.friends_relationship_persistence_schema_gate`, recorded PostgreSQL as the first store, `friend_relationships` as the future current-state logical table, `runtime/migrations/postgres/000007_create_friend_relationships.sql` as the future migration source candidate, canonical unordered player pair identity, lifecycle state representation, actor-specific block timestamps, relationship version and timestamp posture, uniqueness/index expectations, event/audit deferral, redaction, and future repository/adapter ownership candidates, and opened `M-161/W-0233 Add friends relationship migration source` as the next-ready bounded follow-up. No migration source, relationship table, repository interface, PostgreSQL adapter, runtime behavior, protocol route, Protobuf source, generated output, dependency, startup wiring, broader social feature, distributed runtime, hosted deployment, SDK, release artifact, event/audit table, or direct compatibility scope was added.

  - id: M-161
    title: Friends Relationship Migration Source
    status: completed
    goal: Add the first SQL migration source for future friends relationship current-state persistence before repository interfaces, PostgreSQL adapters, runtime behavior, protocol routes, generated output, or broader social features.
    completion_criteria:
      - The migration source `runtime/migrations/postgres/000007_create_friend_relationships.sql` exists with goose up/down markers.
      - The migration creates only the `friend_relationships` current-state table and required indexes/checks from the schema gate.
      - Forbidden secret, digest, transport, chat, group, party, match, Pitaya, or Nakama compatibility columns are absent.
      - Repository interfaces, PostgreSQL adapter behavior, runtime behavior, protocol routes, Protobuf source, generated output, dependencies, hosted, SDK, distributed runtime, event/audit tables, and direct compatibility remain deferred.
    non_goals:
      - adding repository interfaces, PostgreSQL adapter behavior, runtime handlers, protocol routes, Protobuf source, generated output, dependencies, startup wiring, automatic migration application, event/audit tables, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Added the friends relationship PostgreSQL migration source at `runtime/migrations/postgres/000007_create_friend_relationships.sql`, accepted `ADR-0141`, registered `runtime.friends_relationship_migration_source`, created only the `friend_relationships` current-state table with canonical unordered player pair identity, lifecycle state, pair-member actor columns, actor-specific block timestamps, relationship version, timestamp checks, pair uniqueness, pair-member state indexes, and updated-at index, opened `M-162/W-0234 Define friends relationship repository boundary`, and preserved deferrals for repository interfaces, PostgreSQL adapter behavior, runtime friendship behavior, protocol routes, Protobuf source, generated output, dependencies, startup wiring, event/audit tables, hosted surfaces, SDKs, distributed runtime, and direct Nakama/Pitaya API compatibility.

  - id: M-162
    title: Friends Relationship Repository Boundary
    status: completed
    goal: Define the storage-neutral repository boundary for future friends relationship lifecycle behavior before repository implementation, PostgreSQL adapter behavior, runtime behavior, protocol routes, generated output, or broader social features.
    completion_criteria:
      - Future repository owner, interface candidate, value types, command/query vocabulary, conflict classes, transaction handoff, redaction posture, PostgreSQL adapter expectations, test expectations, and stop conditions are defined before implementation.
      - Nakama remains the primary product capability reference and Pitaya remains deferred.
      - A bounded follow-up is opened only if implementation can preserve current ask-first boundaries.
      - No repository implementation, PostgreSQL adapter behavior, runtime behavior, protocol route, Protobuf source, generated output, dependency, hosted, SDK, distributed runtime, event/audit table, or direct compatibility scope is added in the boundary itself.
    non_goals:
      - adding repository interface implementation, PostgreSQL adapter behavior, runtime handlers, protocol routes, Protobuf source, generated output, dependencies, startup wiring, automatic migration application, event/audit tables, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the friends relationship repository boundary in `docs/friends-relationship-repository-boundary.md` and its Simplified Chinese translation, accepted `ADR-0142`, registered `runtime.friends_relationship_repository_boundary`, recorded future owner `runtime/internal/modules/friends`, future interface candidate `runtime/internal/modules/friends.Repository`, candidate value types, lifecycle repository capabilities, canonical pair identity and request identity handoff, typed conflict classes, private social graph redaction, PostgreSQL adapter expectations, and opened `M-163/W-0235 Implement storage-neutral friends relationship repository interface` as the next-ready bounded follow-up. No repository interface implementation, PostgreSQL adapter behavior, runtime friendship behavior, protocol route, Protobuf source, generated output, migration, dependency, startup wiring, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: M-163
    title: Friends Relationship Repository Interface Implementation
    status: completed
    goal: Implement the storage-neutral friends relationship repository interface and value vocabulary under `runtime/internal/modules/friends` before PostgreSQL adapter behavior, runtime behavior, protocol routes, generated output, or broader social features.
    completion_criteria:
      - The module-owned repository interface and value types exist under `runtime/internal/modules/friends`.
      - The implementation preserves canonical pair identity, lifecycle state vocabulary, actor handoff, version posture, conflict classes, redaction, and storage neutrality from ADR-0142.
      - Focused Go tests cover normalization, conflict vocabulary, redacted errors, copying, and forbidden dependency posture.
      - No PostgreSQL adapter behavior, runtime friendship behavior, protocol route, Protobuf source, generated output, dependency, hosted, SDK, distributed runtime, event/audit table, or direct compatibility scope is added in the interface implementation itself.
    non_goals:
      - adding PostgreSQL adapter behavior, SQL execution, runtime handlers, protocol routes, Protobuf source, generated output, dependencies, startup wiring, automatic migration application, event/audit tables, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the storage-neutral friends relationship repository interface under `runtime/internal/modules/friends`, accepted `ADR-0143`, registered `runtime.friends_relationship_repository_interface_implementation`, added module-owned value types, canonical pair and actor normalizers, lifecycle/status/conflict vocabularies, redacted repository errors, focused tests, `modules/friends` manifest and guides, and opened `M-164/W-0236 Define friends relationship PostgreSQL adapter gate` as the next-ready follow-up. No PostgreSQL adapter behavior, SQL execution, runtime friendship behavior, protocol route, Protobuf source, generated output, migration, dependency, startup wiring, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: M-164
    title: Friends Relationship PostgreSQL Adapter Gate
    status: completed
    goal: Define the PostgreSQL adapter gate for the friends relationship repository interface before adding adapter implementation, SQL execution behavior, runtime friendship behavior, protocol routes, generated output, or broader social features.
    completion_criteria:
      - The future PostgreSQL adapter owner, source/test candidates, transaction handoff, SQL mapping posture, conflict mapping, redaction posture, test expectations, and stop conditions are defined before implementation.
      - Nakama remains the primary product capability reference and Pitaya remains deferred.
      - A bounded follow-up is opened only if implementation can preserve current ask-first boundaries.
      - No PostgreSQL adapter implementation, SQL execution behavior, runtime behavior, protocol route, Protobuf source, generated output, dependency, hosted, SDK, distributed runtime, event/audit table, or direct compatibility scope is added in the gate itself.
    non_goals:
      - adding PostgreSQL adapter implementation, SQL execution behavior, runtime handlers, protocol routes, Protobuf source, generated output, dependencies, startup wiring, automatic migration application, event/audit tables, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the friends relationship PostgreSQL adapter gate in `docs/friends-relationship-postgresql-adapter-gate.md` and its Simplified Chinese translation, accepted `ADR-0144`, registered `runtime.friends_relationship_postgresql_adapter_gate`, recorded future owner `runtime/internal/platform/persistence/postgres`, future source/test candidates, constructor posture, caller-supplied unit-of-work handoff, SQL mapping posture for `friend_relationships`, conflict and redaction mapping, focused implementation test expectations, and opened `M-165/W-0237 Implement friends relationship PostgreSQL adapter` as the next-ready follow-up. No PostgreSQL adapter implementation, SQL execution behavior, runtime friendship behavior, protocol route, Protobuf source, generated output, migration, dependency, startup wiring, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: M-165
    title: Friends Relationship PostgreSQL Adapter Implementation
    status: completed
    goal: Implement the friends relationship PostgreSQL adapter under `runtime/internal/platform/persistence/postgres` using the storage-neutral friends repository interface and the existing `friend_relationships` migration source.
    completion_criteria:
      - The adapter implements `runtime/internal/modules/friends.Repository` under the PostgreSQL platform package.
      - The implementation uses caller-supplied unit-of-work executor handoff and does not own transaction control.
      - SQL mapping, row scanning, lifecycle transitions, actor-specific block columns, affected-row handling, conflict mapping, and redacted errors follow `ADR-0144`.
      - Focused adapter tests cover constructor validation, SQL capture or fake executor behavior, row scanning, conflicts, redaction, and transaction-control SQL absence.
      - No runtime friendship behavior, protocol route, Protobuf source, generated output, dependency, hosted, SDK, distributed runtime, event/audit table, or direct compatibility scope is added in the adapter implementation itself.
    non_goals:
      - adding runtime handlers, protocol routes, Protobuf source, generated output, dependencies, startup wiring beyond any already-authorized unit-of-work handoff, automatic migration application, event/audit tables, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the friends relationship PostgreSQL adapter under `runtime/internal/platform/persistence/postgres`, added `runtime/internal/platform/persistence/postgres/friend_relationship_repository.go` and focused fake-executor tests in `runtime/internal/platform/persistence/postgres/friend_relationship_repository_test.go`, added unit-of-work repository handoff through `UnitOfWork.NewFriendRelationshipRepository`, mapped create/update request, pair lookup, player-scoped list, accept, reject, remove, block, and unblock operations to the existing `friend_relationships` table with canonical pair handling, actor-specific block columns, relationship version increments, row scanning through friends normalizers, redacted friends repository errors, and accepted `ADR-0145` plus `runtime.friends_relationship_postgresql_adapter_implementation` check coverage. Runtime friendship behavior, protocol routes, Protobuf sources/generated output, dependencies, migration changes, event/audit tables, startup wiring beyond the bounded unit-of-work handoff, hosted deployments, release artifacts, public announcements, paid promotion, broader social features, distributed runtime, and direct compatibility remain deferred.

  - id: M-166
    title: Friends Relationship Runtime Behavior Gate
    status: completed
    goal: Define the runtime behavior gate for friends relationship application semantics before adding runtime service behavior, handlers, protocol routes, generated output, event/audit tables, or broader social features.
    completion_criteria:
      - The future application owner, validated request identity derivation, actor-relative behavior, permissions, route-policy expectations, repository/unit-of-work handoff, conflict mapping, redaction posture, tests, and stop conditions are defined before implementation.
      - Nakama remains the primary product capability reference and Pitaya remains deferred.
      - A bounded follow-up is opened only if implementation can preserve current ask-first boundaries.
      - No runtime behavior implementation, protocol route, Protobuf source, generated output, dependency, migration, hosted, SDK, distributed runtime, event/audit table, or direct compatibility scope is added in the gate itself.
    non_goals:
      - adding runtime friendship handlers, protocol routes, Protobuf source, generated output, dependencies, startup wiring beyond gate metadata, automatic migration application, event/audit tables, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the friends relationship runtime behavior gate in `docs/friends-relationship-runtime-behavior-gate.md` and its Simplified Chinese translation, accepted `ADR-0146`, registered `runtime.friends_relationship_runtime_behavior_gate`, recorded future owner `runtime/internal/app`, future package/source/test candidates under `runtime/internal/app/friends`, validated request identity as the actor source, metadata-only identity refusal, actor-relative public status behavior, conservative `request_token_required` route-policy posture, repository/unit-of-work handoff, conflict mapping, redaction posture, focused implementation test expectations, and opened `M-167/W-0239 Implement friends relationship runtime behavior` as the next-ready follow-up. No runtime friendship behavior implementation, runtime handler, startup wiring, protocol route, Protobuf source, generated output, repository or adapter change, migration, dependency, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: M-167
    title: Friends Relationship Runtime Behavior Implementation
    status: completed
    goal: Implement the application-owned friends relationship runtime behavior service using validated request identity and the storage-neutral friends repository through unit-of-work boundaries.
    completion_criteria:
      - The service is owned under `runtime/internal/app/friends` or an equivalent application-owned package ratified by the implementation slice.
      - The implementation derives actor identity from validated request identity and rejects metadata-only identity before repository mutation.
      - Send, accept, reject, remove, block, unblock, list, and status behavior preserve actor-relative semantics, conflict mapping, redaction, and unit-of-work handoff from `ADR-0146`.
      - Focused service tests cover validated identity, metadata-only rejection, self-targeting, actor-relative status, lifecycle conflicts, redaction, and no protocol/transport dependency.
      - No protocol route, Protobuf source, generated output, dependency, migration, startup wiring beyond application service tests, event/audit table, hosted, SDK, distributed runtime, or direct compatibility scope is added in the implementation itself.
    non_goals:
      - adding protocol routes, Protobuf source, generated output, dependencies, migration changes, startup wiring beyond application-owned service behavior, automatic migration application, event/audit tables, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the application-owned friends relationship runtime behavior service under `runtime/internal/app/friends`, accepted `ADR-0147`, registered `runtime.friends_relationship_runtime_behavior_implementation`, added focused service tests, derived actor identity from validated request identity, rejected metadata-only identity before id generation or repository access, used the storage-neutral friends repository through unit-of-work handoff, implemented send, accept, reject, remove, block, unblock, list, and status behavior, preserved actor-relative public status, conflict mapping, and redaction, and opened `M-168/W-0240 Define friends relationship protocol route gate` as the next-ready follow-up. No protocol route, Protobuf source, generated output, dependency, migration, startup wiring beyond application-owned service tests, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: M-168
    title: Friends Relationship Protocol Route Gate
    status: completed
    goal: Define the future protocol route gate for exposing friends relationship behavior without implementing routes, Protobuf source, generated output, startup wiring, event/audit tables, broad social features, distributed runtime, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The gate names the future protected friends relationship command/query route family and maps it to the application service from `ADR-0147`.
      - The gate records request identity handoff, route policy, public error mapping, Protobuf/generation expectations, and compatibility posture.
      - The gate preserves protocol route implementation, Protobuf source, generated output, startup wiring, dependencies, migrations, event/audit tables, chat, groups, parties, matchmaking, match runtime, hosted, SDK, distributed runtime, and direct compatibility deferrals.
      - Repository checks, manifests, module docs, README/alpha docs, and ADR/conversation records identify the next bounded implementation slice only after the gate is accepted.
    non_goals:
      - adding protocol route implementation, Protobuf source, generated output, dependencies, migration changes, startup wiring, automatic migration application, event/audit tables, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined `docs/friends-relationship-protocol-route-gate.md` and `docs/friends-relationship-protocol-route-gate.zh-CN.md`, accepted `ADR-0148`, registered `runtime.friends_relationship_protocol_route_gate`, and recorded future protected routes `friends.SendFriendRequest`, `friends.AcceptFriendRequest`, `friends.RejectFriendRequest`, `friends.RemoveFriend`, `friends.BlockPlayer`, `friends.UnblockPlayer`, `friends.ListFriendRelationships`, and `friends.GetFriendRelationshipStatus`, candidate `vibit.friends.v1` Protobuf message shapes, protected-route policy `request_token_required`, authenticated-wrapper and validated request identity handoff, metadata-only `player_id` and `session_id` refusal, no client-supplied actor id proof, public `FRIENDSHIP_*` error mapping, protocol adapter/application handler/startup ownership, generated-output expectations, redaction rules, required future tests, and Nakama/Pitaya reference mapping. No route implementation, Protobuf source/generated output, startup wiring, runtime handlers, repository interface changes, PostgreSQL adapter changes, dependencies, migrations, authentication/session behavior changes, event/audit tables, stream subscriptions, chat, groups, parties, matchmaking, match runtime, hosted deployments, release artifacts, public announcements, paid promotion, SDKs, distributed runtime, or direct compatibility were added.

  - id: M-169
    title: Friends Relationship Protocol Route Implementation
    status: completed
    goal: Implement the protected friends relationship protocol route family authorized by `ADR-0148` without expanding repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session semantics, event/audit tables, broad social features, distributed runtime, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The implementation adds the route family authorized by `ADR-0148` and maps it to the application service from `ADR-0147`.
      - The implementation preserves request-token protection, authenticated wrapper handoff, validated request identity as actor proof, metadata-only identity refusal, public error mapping, and generated-output traceability.
      - Focused tests cover route registration, payload mapping, actor identity handoff, public status mapping, public error mapping, redaction, and no transport/persistence ownership leakage.
      - No repository interface, PostgreSQL adapter, migration, dependency, authentication/session behavior, event/audit table, broad social feature, distributed runtime, hosted, SDK, or direct compatibility scope is added unless a later bounded work item explicitly authorizes it.
    non_goals:
      - adding repository interface changes, PostgreSQL adapter changes, migration changes, dependencies, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, Pitaya-style distributed architecture, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented the protected friends relationship protocol route family authorized by `ADR-0148`, added `proto/vibit/friends/v1/friends.proto`, generated Go Protobuf output under `runtime/internal/generated/proto/vibit/friends/v1/friends.pb.go`, route keys under `runtime/internal/app/friends/routes.go`, application route handlers under `runtime/internal/app/bootstrap/friends.go`, protocol bridge mapping under `runtime/internal/platform/protocol/protobuf/friends_bridge.go`, PostgreSQL startup registration, transaction bypass for service-owned friends command routes, focused route/bridge/startup tests, accepted `ADR-0149`, and registered `runtime.friends_relationship_protocol_route_implementation`. The implementation exposes `friends.SendFriendRequest`, `friends.AcceptFriendRequest`, `friends.RejectFriendRequest`, `friends.RemoveFriend`, `friends.BlockPlayer`, `friends.UnblockPlayer`, `friends.ListFriendRelationships`, and `friends.GetFriendRelationshipStatus` while preserving validated request identity handoff, metadata-only identity refusal, request-token protection, redacted errors, application-owned service behavior, repository interface, PostgreSQL adapter, migration, dependency, authentication/session, event/audit, broad social, distributed runtime, hosted/SDK, and direct Nakama/Pitaya API compatibility deferrals.

  - id: M-170
    title: Friends Relationship Protocol Route Local Proof
    status: completed
    goal: Prove the completed friends relationship protocol route family through a focused local alpha request flow without changing protocol shape, service behavior, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session semantics, broad social scope, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The proof exercises the completed friends relationship route implementation through the existing WebSocket/Protobuf protected-route flow.
      - The proof preserves generated output, application service behavior, route protection, and persistence boundaries.
      - Any test-only fixtures remain clearly local proof support and do not become production memory social graph behavior.
    non_goals:
      - adding new protocol messages or routes, generated output changes, friends service behavior changes, repository interface changes, PostgreSQL adapter changes, migration changes, dependencies, authentication/session behavior changes, groups, parties, chat, stream subscriptions, event/audit tables, matchmaking, match runtime, SDK publication, hosted deployments, release artifacts, public announcements, paid promotion, distributed runtime, or direct Nakama/Pitaya API compatibility
    completion_summary: Proved the completed friends relationship protocol route family through the authenticated local alpha WebSocket/Protobuf request flow with `TestFriendsRelationshipProtocolRouteLocalAlphaFlow`, accepted `ADR-0150`, registered `runtime.friends_relationship_protocol_route_local_proof`, updated local alpha example scripts and docs, and opened `M-171/W-0243 Select next Nakama prototype-ready capability after friends relationship route proof` as the next-ready bounded follow-up. Protocol shape, generated output, application service behavior, repository interfaces, PostgreSQL adapter behavior, migrations, dependencies, authentication/session behavior, event/audit tables, broad social features, hosted/SDK/release scope, distributed runtime, and direct compatibility remained deferred.

  - id: M-171
    title: Next Nakama Prototype-Ready Capability Selection After Friends Route Proof
    status: completed
    goal: Select the next bounded Nakama/Pitaya-aligned prototype-ready capability direction after proving the protected friends relationship route family locally, without implementing runtime behavior, protocol shape, generated output, persistence, dependencies, broad social scope, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility in the selection slice.
    completion_criteria:
      - The selected next direction is recorded in `.arch/work-items.yaml` and supporting architecture artifacts.
      - The selection considers current alpha gaps after friends relationship route proof.
      - The selection preserves all implementation, protocol, persistence, dependency, hosted, SDK, distributed-runtime, and direct-compatibility deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding new runtime behavior, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, groups, parties, chat, stream subscriptions, event/audit tables, matchmaking, match runtime, SDK publication, hosted deployments, release artifacts, public announcements, paid promotion, distributed runtime, or direct Nakama/Pitaya API compatibility
    completion_summary: Selected `define_minimum_operations_inspection_surface_gate` as the next Nakama-first prototype-ready direction after friends relationship route proof, accepted `ADR-0151`, registered `runtime.next_nakama_prototype_ready_capability_after_friends_route_proof`, completed `W-0243`, and opened `M-172/W-0244 Define minimum operations inspection surface gate` as next-ready. Runtime behavior, operations/admin endpoint implementation, protocol routes, generated output, persistence, dependencies, broad social scope, hosted surfaces, SDKs, distributed runtime, and direct compatibility remained deferred.

  - id: M-172
    title: Minimum Operations Inspection Surface Gate
    status: completed
    goal: Define the minimum source-first operations/admin inspection surface needed before broader prototype use, without implementing runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, distributed runtime, or direct Nakama/Pitaya API compatibility in the gate slice.
    completion_criteria:
      - The minimum inspectable state categories and redaction posture are documented before implementation.
      - The gate defines ownership, test expectations, and stop conditions for any future operations inspection implementation.
      - The gate preserves all runtime, protocol, persistence, dependency, hosted, SDK, distributed-runtime, and direct-compatibility deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding operations/admin endpoint implementation, metrics endpoints, observability pipelines, dashboards, new runtime behavior, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, groups, parties, chat, stream subscriptions, event/audit tables, matchmaking, match runtime, SDK publication, hosted deployments, release artifacts, public announcements, paid promotion, distributed runtime, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the minimum source-first operations inspection surface gate, accepted `ADR-0152`, registered `runtime.minimum_operations_inspection_surface_gate`, completed `W-0244`, and opened `M-173/W-0245 Implement minimum operations inspection source-first surface` as next-ready. Preserved operations/admin endpoint implementation, metrics, observability, dashboards, runtime behavior, protocol routes, generated output, repository/adapter/migration/dependency/authentication/session/event/direct-compatibility deferrals, hosted/SDK/release scope, and distributed runtime deferrals.

  - id: M-173
    title: Minimum Operations Inspection Source-First Surface Implementation
    status: completed
    goal: Implement the minimum source-first operations inspection surface authorized by ADR-0152, preferably through tools/vibit repository inspection and documentation updates, without adding runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, distributed runtime, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A source-first operations inspection surface summarizes accepted local alpha runtime/source categories.
      - Redaction tests or repository checks prove raw credentials, tokens, digests, verifier keys, DSNs, transport metadata, sensitive identifiers, and database payloads are not exposed.
      - The implementation remains within the ADR-0152 gate and preserves all runtime, protocol, persistence, dependency, hosted, SDK, distributed-runtime, and direct-compatibility deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding operations/admin endpoint implementation, metrics endpoints, observability pipelines, dashboards, new runtime behavior, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, groups, parties, chat, stream subscriptions, event/audit tables, matchmaking, match runtime, SDK publication, hosted deployments, release artifacts, public announcements, paid promotion, distributed runtime, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented `node tools/vibit inspect operations --json` as the source-first local operations inspection surface, accepted `ADR-0153`, registered `runtime.minimum_operations_inspection_source_first_surface_implementation`, completed `W-0245`, and opened `M-174/W-0246 Define Pitaya-aligned distributed runtime vocabulary reactivation gate` as next-ready. The inspection output summarizes existing local alpha status endpoints, source surfaces, route families, local alpha flow steps, persistence/migration posture, generated-output posture, repository verification posture, redaction flags, and a Pitaya deferred architecture map without adding runtime endpoint behavior, metrics, observability, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, distributed runtime implementation, or direct compatibility.

  - id: M-174
    title: Pitaya-Aligned Distributed Runtime Vocabulary Reactivation Gate
    status: completed
    goal: Define a narrow Pitaya-aligned distributed runtime vocabulary reactivation gate after the source-first operations inspection surface, without implementing frontend/backend roles, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A gate records which Pitaya architecture vocabulary may be reactivated for future planning.
      - The gate maps existing single-process acceptor, session binding, and route handler concepts to future frontend/backend, RPC, service discovery, and distributed group vocabulary.
      - The gate preserves implementation deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding distributed runtime implementation, frontend/backend server role implementation, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the Pitaya-aligned distributed runtime vocabulary reactivation gate, accepted `ADR-0154`, registered `runtime.pitaya_aligned_distributed_runtime_vocabulary_reactivation_gate`, completed `W-0246`, and opened `M-175/W-0247 Implement Pitaya-aligned distributed runtime vocabulary source-first map` as next-ready. The gate reactivates Pitaya vocabulary for future architecture planning while preserving distributed runtime implementation, frontend/backend roles, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-175
    title: Pitaya-Aligned Distributed Runtime Vocabulary Source-First Map
    status: completed
    goal: Implement a source-first repository map for the Pitaya-aligned distributed runtime vocabulary defined by ADR-0154, without implementing frontend/backend roles, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A source-first repository inspection surface summarizes the Pitaya-aligned vocabulary and current single-process mapping.
      - The map preserves vocabulary-only posture and does not add runtime behavior.
      - Repository checks verify the map and deferrals.
    non_goals:
      - adding distributed runtime implementation, frontend/backend server role implementation, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented `node tools/vibit inspect pitaya-vocabulary --json` as a source-first Pitaya-aligned distributed runtime vocabulary map, accepted `ADR-0155`, registered `runtime.pitaya_aligned_distributed_runtime_vocabulary_source_first_map`, completed `W-0247`, and opened `M-176/W-0248 Define Pitaya-aligned frontend/backend role boundary gate` as next-ready. The map summarizes allowed ADR-0154 vocabulary, current single-process mapping, redaction posture, source surfaces, and future distributed deferrals without adding distributed runtime implementation, frontend/backend role implementation, server-to-server RPC, remote calls, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct compatibility.

  - id: M-176
    title: Pitaya-Aligned Frontend Backend Role Boundary Gate
    status: completed
    goal: Define a narrow Pitaya-aligned frontend/backend role boundary gate after the source-first vocabulary map, without implementing frontend/backend roles, distributed runtime behavior, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A gate records how `frontend_server` and `backend_server` vocabulary may be used for future planning.
      - The gate maps current single-process runtime responsibilities to deferred frontend/backend role vocabulary.
      - The gate preserves implementation deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding frontend/backend server role implementation, distributed runtime implementation, server-to-server RPC, remote call behavior, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined the Pitaya-aligned frontend/backend role boundary gate, accepted `ADR-0156`, registered `runtime.pitaya_aligned_frontend_backend_role_boundary_gate`, completed `W-0248`, and opened `M-177/W-0249 Implement Pitaya-aligned frontend/backend role source-first map` as next-ready. The gate defines `frontend_server` and `backend_server` as future architecture vocabulary only, maps current single-process acceptor, session binding, protocol bridge, application dispatch, and module handler responsibilities to deferred role vocabulary, and preserves frontend/backend role implementation, distributed runtime implementation, RPC, service discovery, distributed groups, broadcast fanout, cluster-safe session routing, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-177
    title: Pitaya-Aligned Frontend Backend Role Source-First Map
    status: completed
    goal: Implement a source-first repository map for the Pitaya-aligned frontend/backend role vocabulary defined by ADR-0156, without implementing frontend/backend roles, distributed runtime behavior, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A source-first repository inspection surface summarizes the frontend/backend role vocabulary and current single-process mapping.
      - The map preserves role-vocabulary-only posture and does not add runtime behavior.
      - Repository checks verify the map and deferrals.
    non_goals:
      - adding frontend/backend server role implementation, distributed runtime implementation, server-to-server RPC, remote call behavior, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented `node tools/vibit inspect pitaya-roles --json` as a source-first repository inspection map for Pitaya-aligned frontend/backend role vocabulary, current single-process role mapping, source surfaces, deferrals, and redaction flags. Accepted `ADR-0157`, registered `runtime.pitaya_aligned_frontend_backend_role_source_first_map`, completed `W-0249`, and opened `M-178/W-0250 Define Pitaya-aligned server-to-server RPC boundary gate` as next-ready. Preserved frontend/backend role implementation, distributed runtime implementation, server-to-server RPC, remote calls, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-178
    title: Pitaya-Aligned Server To Server RPC Boundary Gate
    status: completed
    goal: Define a narrow Pitaya-aligned server-to-server RPC boundary gate after the frontend/backend role source-first map, without implementing RPC, remote calls, service discovery, distributed runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A gate records server-to-server RPC and remote call vocabulary for future planning.
      - The gate maps current vibit route/application dispatch boundaries to deferred RPC vocabulary.
      - The gate preserves implementation deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding server-to-server RPC implementation, remote call behavior, service discovery, frontend/backend server roles, distributed runtime implementation, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined a gate-only Pitaya-aligned server-to-server RPC boundary for `server_to_server_rpc` and `remote_call`, mapped current single-process application dispatch and module handler responsibilities to deferred RPC vocabulary, accepted `ADR-0158`, registered `runtime.pitaya_aligned_server_to_server_rpc_boundary_gate`, completed `W-0250`, and opened `M-179/W-0251 Implement Pitaya-aligned server-to-server RPC source-first map` as next-ready. Preserved server-to-server RPC implementation, remote calls, service discovery, frontend/backend role implementation, distributed runtime implementation, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-179
    title: Pitaya-Aligned Server To Server RPC Source-First Map
    status: completed
    goal: Implement a source-first repository map for the Pitaya-aligned server-to-server RPC vocabulary defined by ADR-0158, without implementing RPC, remote calls, service discovery, frontend/backend roles, distributed runtime behavior, distributed groups, cluster-safe session routing, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A source-first repository inspection surface summarizes the server-to-server RPC and remote-call vocabulary and current single-process mapping.
      - The map preserves RPC-vocabulary-only posture and does not add runtime behavior.
      - Repository checks verify the map and deferrals.
    non_goals:
      - adding server-to-server RPC implementation, remote call behavior, service discovery, frontend/backend server role implementation, distributed runtime implementation, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented `node tools/vibit inspect pitaya-rpc --json` as a source-first repository inspection map for Pitaya-aligned server-to-server RPC and remote-call vocabulary, current single-process RPC mapping, source surfaces, deferrals, and redaction flags. Accepted `ADR-0159`, registered `runtime.pitaya_aligned_server_to_server_rpc_source_first_map`, completed `W-0251`, and opened `M-180/W-0252 Define Pitaya-aligned service discovery boundary gate` as next-ready. Preserved server-to-server RPC implementation, remote calls, service discovery, frontend/backend role implementation, distributed runtime implementation, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-180
    title: Pitaya-Aligned Service Discovery Boundary Gate
    status: completed
    goal: Define a narrow Pitaya-aligned service discovery boundary gate after the server-to-server RPC source-first map, without implementing service discovery, RPC, remote calls, frontend/backend roles, distributed runtime behavior, distributed groups, cluster-safe session routing, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A gate records service discovery vocabulary for future planning.
      - The gate maps current static single-process composition to deferred service discovery vocabulary.
      - The gate preserves implementation deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding service discovery implementation, server-to-server RPC implementation, remote call behavior, frontend/backend server role implementation, distributed runtime implementation, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined a gate-only Pitaya-aligned service discovery boundary for `service_discovery`, `service_registry`, `service_instance`, and `service_selector`, mapped current static single-process startup composition and direct handler dispatch to deferred discovery vocabulary, accepted `ADR-0160`, registered `runtime.pitaya_aligned_service_discovery_boundary_gate`, completed `W-0252`, and opened `M-181/W-0253 Implement Pitaya-aligned service discovery source-first map` as next-ready. Preserved service discovery implementation, registries, selectors, node identity, RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-181
    title: Pitaya-Aligned Service Discovery Source-First Map
    status: completed
    goal: Implement a source-first repository map for the Pitaya-aligned service discovery vocabulary defined by ADR-0160, without implementing service discovery, registries, selectors, node identity, RPC, remote calls, frontend/backend roles, distributed runtime behavior, distributed groups, cluster-safe session routing, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A source-first repository inspection surface summarizes service discovery vocabulary and current static single-process composition.
      - The map preserves service-discovery-vocabulary-only posture and does not add runtime behavior.
      - Repository checks verify the map and deferrals.
    non_goals:
      - adding service discovery implementation, service registry behavior, service selector behavior, node identity, server-to-server RPC implementation, remote call behavior, frontend/backend server role implementation, distributed runtime implementation, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented `node tools/vibit inspect pitaya-discovery --json` as a source-first repository inspection map for Pitaya-aligned service discovery vocabulary, current static single-process composition, direct dispatch, route-handler and module-handler mapping, registry/selector deferrals, source surfaces, deferrals, and redaction flags. Accepted `ADR-0161`, registered `runtime.pitaya_aligned_service_discovery_source_first_map`, completed `W-0253`, and opened `M-182/W-0254 Define Pitaya-aligned distributed group and broadcast boundary gate` as next-ready. Preserved service discovery implementation, registries, selectors, node identity, server-to-server RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-182
    title: Pitaya-Aligned Distributed Group And Broadcast Boundary Gate
    status: completed
    goal: Define a narrow Pitaya-aligned distributed group and room broadcast boundary gate after the service discovery source-first map, without implementing distributed groups, room broadcast fanout, service discovery, RPC, remote calls, frontend/backend roles, distributed runtime behavior, cluster-safe session routing, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A gate records distributed group and room broadcast vocabulary for future planning.
      - The gate maps current single-process target-scope and server-push intent surfaces to deferred distributed group and broadcast vocabulary.
      - The gate preserves implementation deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding distributed group implementation, room broadcast fanout, delivery guarantees, stream subscriptions, groups, parties, chat, service discovery implementation, service registries, service selectors, node identity, server-to-server RPC implementation, remote call behavior, frontend/backend server role implementation, distributed runtime implementation, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined a gate-only Pitaya-aligned distributed group and broadcast boundary for `distributed_group`, `room_broadcast`, `broadcast_target`, `group_membership`, and `broadcast_fanout`, mapped current target-scope metadata and single-process server-push intent to deferred group and broadcast vocabulary, accepted `ADR-0162`, registered `runtime.pitaya_aligned_distributed_group_broadcast_boundary_gate`, completed `W-0254`, and opened `M-183/W-0255 Implement Pitaya-aligned distributed group and broadcast source-first map` as next-ready. Preserved distributed group implementation, room broadcast fanout, delivery guarantees, stream subscriptions, service discovery implementation, RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-183
    title: Pitaya-Aligned Distributed Group And Broadcast Source-First Map
    status: completed
    goal: Implement a source-first repository map for the Pitaya-aligned distributed group and broadcast vocabulary defined by ADR-0162, without implementing distributed groups, room broadcast fanout, delivery guarantees, stream subscriptions, groups, parties, chat, service discovery, RPC, remote calls, frontend/backend roles, distributed runtime behavior, cluster-safe session routing, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A source-first repository inspection surface summarizes distributed group and broadcast vocabulary and current target-scope/server-push intent mapping.
      - The map preserves group-and-broadcast-vocabulary-only posture and does not add runtime behavior.
      - Repository checks verify the map and deferrals.
    non_goals:
      - adding distributed group implementation, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, groups, parties, chat rooms, matchmaking, match runtime, service discovery implementation, service registries, service selectors, node identity, server-to-server RPC implementation, remote call behavior, frontend/backend server role implementation, distributed runtime implementation, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented `node tools/vibit inspect pitaya-groups --json` as a source-first repository inspection map for Pitaya-aligned distributed group and broadcast vocabulary, current target-scope metadata, server-push intent, single-process delivery, absent distributed group state, absent group membership registry, absent room broadcast fanout, source surfaces, deferrals, and redaction flags. Accepted `ADR-0163`, registered `runtime.pitaya_aligned_distributed_group_broadcast_source_first_map`, completed `W-0255`, and opened `M-184/W-0256 Define Pitaya-aligned cluster-safe session routing boundary gate` as next-ready. Preserved distributed group implementation, group membership registries, room broadcast fanout, delivery guarantees, stream subscriptions, service discovery implementation, RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-184
    title: Pitaya-Aligned Cluster-Safe Session Routing Boundary Gate
    status: completed
    goal: Define a narrow Pitaya-aligned cluster-safe session routing boundary gate after the distributed group and broadcast source-first map, without implementing cluster-safe session routing, distributed runtime behavior, distributed groups, room broadcast fanout, service discovery, RPC, remote calls, frontend/backend roles, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A gate records cluster-safe session routing vocabulary for future planning.
      - The gate maps current single-process connection/session binding to deferred cluster-safe routing vocabulary.
      - The gate preserves implementation deferrals unless a later bounded work item authorizes them.
    non_goals:
      - adding cluster-safe session routing, distributed runtime implementation, distributed group implementation, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, service discovery implementation, service registries, service selectors, node identity, server-to-server RPC implementation, remote call behavior, frontend/backend server role implementation, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Defined a gate-only Pitaya-aligned cluster-safe session routing boundary for `cluster_safe_session_routing`, `session_location`, `connection_owner_node`, `routing_epoch`, `session_route_target`, `connection_handoff`, and `reconnect_route`, mapped current single-process connection binding, active connection registry, runtime session validation, and request identity surfaces to deferred session routing vocabulary, accepted `ADR-0164`, registered `runtime.pitaya_aligned_cluster_safe_session_routing_boundary_gate`, completed `W-0256`, and opened `M-185/W-0257 Implement Pitaya-aligned cluster-safe session routing source-first map` as next-ready. Preserved cluster-safe session routing behavior, session location registries, connection owner node registries, routing epoch behavior, remote handoff, reconnect routing, distributed runtime implementation, service discovery implementation, RPC implementation, remote calls, frontend/backend role implementation, distributed groups, room broadcast fanout, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-185
    title: Pitaya-Aligned Cluster-Safe Session Routing Source-First Map
    status: completed
    goal: Implement a source-first repository map for the Pitaya-aligned cluster-safe session routing vocabulary defined by ADR-0164, without implementing cluster-safe session routing, session location registries, connection owner node registries, routing epoch behavior, remote handoff, reconnect routing, distributed runtime behavior, service discovery, RPC, remote calls, frontend/backend roles, distributed groups, room broadcast fanout, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - A source-first repository inspection surface summarizes cluster-safe session routing vocabulary and current connection/session mapping.
      - The map preserves session-routing-vocabulary-only posture and does not add runtime behavior.
      - Repository checks verify the map and deferrals.
    non_goals:
      - adding cluster-safe session routing behavior, session location registries, connection owner node registries, routing epoch behavior, remote connection handoff, reconnect routing, distributed session routing, distributed runtime implementation, distributed group implementation, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, service discovery implementation, service registries, service selectors, node identity, server-to-server RPC implementation, remote call behavior, frontend/backend server role implementation, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility
    completion_summary: Implemented `node tools/vibit inspect pitaya-sessions --json` as a source-first repository inspection map for Pitaya-aligned cluster-safe session routing vocabulary, current single-process connection id, connection epoch, first-message binding, active connection registry, runtime session, bound identity, request token identity, session validation, absent remote handoff, absent reconnect routing, source surfaces, deferrals, and redaction flags. Accepted `ADR-0165`, registered `runtime.pitaya_aligned_cluster_safe_session_routing_source_first_map`, completed `W-0257`, and opened `M-186/W-0258 Select next Pitaya-aligned direction after cluster-safe session routing map` as next-ready. Preserved cluster-safe session routing behavior, session location registries, connection owner node registries, routing epoch behavior, remote handoff, reconnect routing, distributed session routing, distributed runtime implementation, service discovery implementation, RPC implementation, remote calls, frontend/backend role implementation, distributed groups, room broadcast fanout, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: M-186
    title: Select Next Pitaya-Aligned Direction After Cluster-Safe Session Routing Map
    status: active
    goal: Select the next bounded Pitaya-aligned direction after the cluster-safe session routing source-first map, without implementing cluster-safe session routing, session location registries, connection owner node registries, routing epoch behavior, remote handoff, reconnect routing, distributed session routing, distributed runtime behavior, service discovery, RPC, remote calls, frontend/backend roles, distributed groups, room broadcast fanout, runtime behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, or direct Nakama/Pitaya API compatibility.
    completion_criteria:
      - The next Pitaya-aligned direction is selected as a bounded follow-up.
      - Candidate directions and deferrals are recorded before implementation.
      - Repository checks verify the selection and next-ready state.
    non_goals:
      - adding cluster-safe session routing behavior, session location registries, connection owner node registries, routing epoch behavior, remote connection handoff, reconnect routing, distributed session routing, distributed runtime implementation, distributed group implementation, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, service discovery implementation, service registries, service selectors, node identity, server-to-server RPC implementation, remote call behavior, frontend/backend server role implementation, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility

work_items:
  - id: W-0001
    milestone: M-001
    title: Define runtime protocol adapter boundary
    status: completed
    change: changes/2026-05-13-define-runtime-protocol-adapter-boundary
    commits:
      - fa92f94
    related_decisions:
      - ADR-0018
    completion:
      - Runtime handoff between transport, protocol, app, domain, and generated output is documented.

  - id: W-0002
    milestone: M-001
    title: Add application dispatch skeleton
    status: completed
    change: changes/2026-05-13-add-application-dispatch-skeleton
    commits:
      - e119e02
    related_decisions:
      - ADR-0014
      - ADR-0018
    completion:
      - Application dispatch routes command and query requests through vibit-owned interfaces.

  - id: W-0003
    milestone: M-001
    title: Add inventory runtime boundary
    status: completed
    change: changes/2026-05-13-add-inventory-runtime-boundary
    commits:
      - b5fe5b2
    related_decisions:
      - ADR-0014
    completion:
      - Inventory runtime has handwritten repository, policy, command, query, event, and invariant behavior.

  - id: W-0004
    milestone: M-001
    title: Add inventory Protobuf domain bridge
    status: completed
    change: changes/2026-05-13-add-inventory-protobuf-domain-bridge
    commits:
      - 3034f11
    related_decisions:
      - ADR-0016
      - ADR-0018
    completion:
      - Inventory wire payloads are mapped to and from handwritten domain runtime payloads outside domain modules.

  - id: W-0005
    milestone: M-001
    title: Add application error envelope mapping
    status: completed
    change: changes/2026-05-13-add-application-error-envelope-mapping
    commits:
      - d64513d
    related_decisions:
      - ADR-0016
      - ADR-0018
    completion:
      - Application errors can be encoded as Protobuf error envelopes without exposing internal errors.

  - id: W-0006
    milestone: M-001
    title: Add first WebSocket transport adapter
    status: completed
    change: changes/2026-05-13-add-first-websocket-transport-adapter
    commits:
      - 63556cb
    related_decisions:
      - ADR-0013
      - ADR-0018
    completion:
      - WebSocket transport accepts binary frames, rejects text frames, copies payload bytes, and delegates opaque frames to an injected handler.

  - id: W-0007
    milestone: M-001
    title: Add WebSocket Protobuf application composition adapter
    status: completed
    change: changes/2026-05-13-add-websocket-protobuf-application-composition
    commits:
      - 450a7a5
    depends_on:
      - W-0002
      - W-0004
      - W-0005
      - W-0006
    related_decisions:
      - ADR-0018
    completion:
      - Decode binary WebSocket frame payloads as Protobuf envelopes outside the WebSocket transport package.
      - Convert command and query envelopes to application route requests.
      - Dispatch through app.Dispatcher.
      - Encode application payload results and application errors back to Protobuf envelopes.
      - Preserve transport, protocol, application, domain, and generated output boundaries.
      - Cover the composition with Go tests.
    ask_first:
      - Changing the protocol envelope shape.
      - Moving generated Protobuf imports into application or domain packages.
      - Adding authentication or session validation.

  - id: W-0008
    milestone: M-001
    title: Add minimal /v1/ws process wiring
    status: completed
    change: changes/2026-05-13-add-minimal-websocket-process-wiring
    commits:
      - a308009
    depends_on:
      - W-0007
    related_decisions:
      - ADR-0014
      - ADR-0015
      - ADR-0018
    completion:
      - Mount the composed WebSocket handler at /v1/ws from runtime process startup.
      - Keep business logic out of cmd/vibit-server.
      - Add a narrow integration test or documented manual run path.

  - id: W-0009
    milestone: M-001
    title: Add request loop test fixture
    status: completed
    change: changes/2026-05-13-add-request-loop-test-fixture
    commits:
      - 77374fe
    depends_on:
      - W-0007
    completion:
      - Provide a repeatable test fixture for command/query request loop tests without opening unrelated runtime dependencies.

  - id: W-0010
    milestone: M-002
    title: Define PostgreSQL repository and migration boundary
    status: completed
    change: changes/2026-05-13-define-postgresql-repository-and-migration-boundary
    commits:
      - cc07894
    depends_on:
      - W-0008
    related_decisions:
      - ADR-0011
      - ADR-0013
      - ADR-0014
      - ADR-0020
    completion:
      - Define repository, transaction, migration, and ownership rules before adding persistent inventory behavior.

  - id: W-0011
    milestone: M-002
    title: Add command-safe inventory repository mutation lock boundary
    status: completed
    change: changes/2026-05-13-add-inventory-repository-mutation-lock-boundary
    commits:
      - 5dcb5ba
    depends_on:
      - W-0010
    related_decisions:
      - ADR-0020
    completion:
      - Extend the inventory repository boundary so persistent GrantItem flows can lock the player inventory aggregate before capacity-sensitive mutation.
    ask_first:
      - Replacing the inventory account row lock with advisory locks or another concurrency model.
      - Moving transaction ownership into inventory repositories.

  - id: W-0012
    milestone: M-002
    title: Add application transaction boundary skeleton
    status: completed
    change: changes/2026-05-13-add-application-transaction-boundary-skeleton
    commits:
      - 9006e4a
    depends_on:
      - W-0011
    related_decisions:
      - ADR-0014
      - ADR-0020
    completion:
      - Add vibit-owned unit-of-work interfaces and application orchestration points without binding domain modules to pgx.
    ask_first:
      - Changing the transaction boundary owner away from runtime/internal/platform/tx and runtime/internal/app.
      - Introducing an outbox or durable event delivery standard.

  - id: W-0013
    milestone: M-002
    title: Add first inventory PostgreSQL migration
    status: completed
    change: changes/2026-05-13-add-first-inventory-postgresql-migration
    commits:
      - fc77c44
    depends_on:
      - W-0012
    related_decisions:
      - ADR-0011
      - ADR-0013
      - ADR-0020
    completion:
      - Add SQL-first goose migration sources for inventory accounts, inventory items, and inventory item grant records.
    ask_first:
      - Adding Go migrations.
      - Adding object storage or MinIO.
      - Changing durable inventory data ownership.

  - id: W-0014
    milestone: M-002
    title: Add migration verification checks
    status: completed
    change: changes/2026-05-13-add-migration-verification-checks
    commits:
      - 2dba135
    depends_on:
      - W-0013
    related_decisions:
      - ADR-0013
      - ADR-0020
    completion:
      - Extend repository checks to validate PostgreSQL migration naming, goose markers, and persistence boundary references.

  - id: W-0015
    milestone: M-002
    title: Add PostgreSQL inventory repository adapter
    status: completed
    change: changes/2026-05-13-add-postgresql-inventory-repository-adapter
    commits:
      - b17bc23
    depends_on:
      - W-0014
    related_decisions:
      - ADR-0011
      - ADR-0013
      - ADR-0020
    completion:
      - Implement the inventory repository interface behind the PostgreSQL platform adapter with focused tests and recorded integration verification status.
    ask_first:
      - Skipping transaction-bound repository behavior.
      - Making PostgreSQL integration tests mandatory without a local test environment standard.

  - id: W-0016
    milestone: M-002
    title: Plan durable inventory runtime work queue
    status: completed
    change: changes/2026-05-13-plan-durable-inventory-runtime-work-queue
    depends_on:
      - W-0015
    related_decisions:
      - ADR-0011
      - ADR-0014
      - ADR-0018
      - ADR-0020
    completion:
      - Restore deterministic continuation by planning the remaining M-002 work items.
      - Mark the next conservative durable inventory work item as next_ready.
      - Keep persistence wiring, migration execution, integration environment, and end-to-end verification as separate bounded steps.

  - id: W-0017
    milestone: M-002
    title: Add PostgreSQL configuration and transaction runner
    status: completed
    change: changes/2026-05-13-add-postgresql-configuration-and-transaction-runner
    commits:
      - 5083825
    depends_on:
      - W-0016
    related_decisions:
      - ADR-0011
      - ADR-0013
      - ADR-0014
      - ADR-0020
    completion:
      - Add explicit PostgreSQL runtime configuration for connection input without storing secrets in tracked files.
      - Add a pgx-backed transaction runner adapter that implements the existing vibit-owned tx.Runner boundary.
      - Keep pgx imports inside the PostgreSQL platform owner package.
      - Keep transaction interfaces driver-neutral for application and domain packages.
      - Cover configuration parsing and transaction runner behavior with focused tests that do not require a live PostgreSQL server unless an explicit integration environment is present.
    ask_first:
      - Exposing pgx types outside the PostgreSQL platform owner package.
      - Changing transaction ownership away from runtime/internal/platform/tx and runtime/internal/app.
      - Introducing transaction retry or isolation policy beyond a documented default.
      - Making a live PostgreSQL server mandatory for normal unit tests.
      - Adding a new configuration framework dependency.

  - id: W-0018
    milestone: M-002
    title: Add migration apply and status tooling
    status: completed
    change: changes/2026-05-13-add-postgresql-migration-apply-and-status-tooling
    commits:
      - 1dd729b
    depends_on:
      - W-0017
    related_decisions:
      - ADR-0011
      - ADR-0013
      - ADR-0020
    completion:
      - Add explicit migration apply and status tooling through the migration platform owner package.
      - Keep goose ownership under runtime/internal/platform/migrations.
      - Provide CLI or runtime-invoked commands that can report migration state and apply SQL-first sources.
      - Record what can be verified without a live PostgreSQL server and what requires the disposable verification environment.
    ask_first:
      - Applying migrations automatically during normal server startup.
      - Replacing SQL-first migrations with Go migrations.
      - Adding a second migration tool.
      - Weakening migration source validation.

  - id: W-0019
    milestone: M-002
    title: Define disposable PostgreSQL verification environment
    status: completed
    change: changes/2026-05-13-define-disposable-postgresql-verification-environment
    commits:
      - 422afad
    depends_on:
      - W-0018
    related_decisions:
      - ADR-0011
      - ADR-0013
      - ADR-0020
    completion:
      - Define how agents run PostgreSQL integration checks without relying on maintainer memory.
      - Document required environment variables, cleanup expectations, and what checks may be skipped when the environment is unavailable.
      - Keep integration checks opt-in until the environment standard is available in repository tooling.
      - Add repository guidance so agents do not confuse source validation with live database verification.
    ask_first:
      - Making Docker, Podman, or any external service manager a required project dependency.
      - Making live PostgreSQL integration tests mandatory in every default check.
      - Adding cloud-hosted PostgreSQL assumptions.

  - id: W-0020
    milestone: M-002
    title: Wire persistent inventory runtime composition
    status: completed
    change: changes/2026-05-13-wire-persistent-inventory-runtime-composition
    commits:
      - ef20ec2
    depends_on:
      - W-0019
    related_decisions:
      - ADR-0011
      - ADR-0014
      - ADR-0018
      - ADR-0020
    completion:
      - Add runtime composition that binds inventory handlers to PostgreSQL repositories through the application-owned transaction boundary.
      - Keep business logic out of process startup, transport, protocol, and persistence adapters.
      - Keep the in-memory inventory path available for tests or explicitly documented local development until a replacement is confirmed.
      - Update the runtime runbook and agent guides with the persistent process path.
    ask_first:
      - Making PostgreSQL mandatory for local process startup.
      - Removing the in-memory bootstrap path.
      - Introducing authentication, session validation, or player account ownership.
      - Replacing the inventory account row-lock model.

  - id: W-0021
    milestone: M-002
    title: Verify durable inventory runtime end to end
    status: completed
    change: changes/2026-05-13-verify-durable-inventory-runtime-end-to-end
    commits:
      - 1104690
    depends_on:
      - W-0020
    related_decisions:
      - ADR-0011
      - ADR-0015
      - ADR-0018
      - ADR-0020
    completion:
      - Verify the WebSocket Protobuf GrantItem and GetInventory request loop against the persistent inventory runtime.
      - Verify migration apply/status behavior against the defined disposable PostgreSQL environment or record explicit unavailability.
      - Verify that domain, application, protocol, transport, persistence, and migration boundaries still pass repository checks.
      - Update M-002 completion status when durable inventory runtime criteria are satisfied.
    completion_summary: Added an opt-in live PostgreSQL request-loop verification test. The live test later passed on local Termux PostgreSQL 18.2 with VIBIT_POSTGRES_TEST_DSN set, destructive cleanup allowed, and default drop_schema cleanup. M-002 remains active until milestone closure and follow-on planning are handled explicitly.
    ask_first:
      - Closing M-002 with unavailable live PostgreSQL verification.
      - Expanding scope into outbox delivery, auth/session, player accounts, item catalog, currency, rewards, or match sessions.

  - id: W-0022
    milestone: M-003
    title: Define player identity and session boundary
    status: completed
    change: changes/2026-05-14-define-player-identity-and-session-boundary
    depends_on:
      - W-0021
    related_decisions:
      - ADR-0015
      - ADR-0019
      - ADR-0021
    completion:
      - Define what owns player identity, player accounts, authentication, WebSocket sessions, and request identity context.
      - Record how inventory should consume authenticated player context without owning player accounts.
      - Align the next implementation sequence with Nakama account/session coverage and Pitaya session/route vocabulary while preserving vibit's agent-native boundaries.
      - Keep protocol envelope changes, token format, credential storage, and migrations out of this boundary step unless separately ratified.
    completion_summary: Added docs/player-identity-session-boundary.md, the paired Simplified Chinese translation, ADR-0021, architecture manifest references, and inventory guidance that keeps player identity, account lifecycle, authentication, runtime sessions, transport connection metadata, and request identity context separate without implementing authentication.
    ask_first:
      - Choosing a concrete authentication scheme, token format, password model, or external identity provider.
      - Changing the Protobuf envelope or WebSocket handshake contract.
      - Adding player account database migrations.
      - Introducing cryptography, JWT, OAuth, OIDC, or password-hashing dependencies.
      - Moving inventory ownership into a player module.

  - id: W-0023
    milestone: M-003
    title: Add player module manifest boundary
    status: completed
    change: changes/2026-05-14-add-player-module-manifest-boundary
    depends_on:
      - W-0022
    related_decisions:
      - ADR-0019
      - ADR-0021
    completion:
      - Create modules/player/module.yaml and module-level AGENTS guides as boundary artifacts only.
      - Declare player identity and player account lifecycle ownership without adding runtime code, migrations, credential storage, or authentication providers.
      - Register the player module in .arch/modules.yaml after its manifest and guides exist.
      - Define initial public vocabulary placeholders for player identity and account lifecycle without ratifying concrete authentication commands.
      - Keep inventory state ownership unchanged and dependency-free unless a later change explicitly allows a dependency.
    completion_summary: Added modules/player/module.yaml and bilingual module agent guides, registered player as a first-class module, recorded player as boundary-only in the contract registry, and taught repository checks not to force public contracts or Protobuf sources for a boundary-only module.
    ask_first:
      - Choosing a concrete authentication scheme, token format, password model, guest login, device login, social login, or external identity provider.
      - Adding player account database migrations or persistent schema.
      - Introducing cryptography, JWT, OAuth, OIDC, password-hashing, or credential storage dependencies.
      - Changing the Protobuf envelope or WebSocket handshake contract.
      - Moving inventory ownership or inventory data under the player module.

  - id: W-0024
    milestone: M-003
    title: Define application request identity handoff boundary
    status: completed
    change: changes/2026-05-14-define-application-request-identity-handoff-boundary
    depends_on:
      - W-0023
    related_decisions:
      - ADR-0018
      - ADR-0021
    completion:
      - Define application-owned request identity and session-validation handoff types in runtime/internal/app without implementing authentication.
      - Preserve existing Protobuf envelope fields and WebSocket handshake behavior.
      - Keep client-supplied player_id and session_id as metadata until validation exists.
      - Document how future session validation will normalize identity before domain handlers receive requests.
      - Add focused Go tests for normalization and zero-value behavior only if runtime types change.
    completion_summary: Added application-owned RequestIdentity and SessionValidationResult handoff types, propagated metadata-only request identity through Protobuf envelope decoding, frame metadata refresh, application dispatch, application results, bootstrap, and inventory results, documented that validation flags remain false before real session validation, and verified the change with Go and repository checks.
    ask_first:
      - Choosing a concrete authentication scheme, token format, password model, guest login, device login, social login, or external identity provider.
      - Adding player account database migrations or persistent schema.
      - Introducing cryptography, JWT, OAuth, OIDC, password-hashing, or credential storage dependencies.
      - Changing the Protobuf envelope or WebSocket handshake contract.
      - Making inventory depend directly on the player module.

  - id: W-0025
    milestone: M-003
    title: Plan player/session follow-up work queue
    status: completed
    change: changes/2026-05-14-plan-player-session-follow-up-work-queue
    depends_on:
      - W-0024
    related_decisions:
      - ADR-0019
      - ADR-0021
    completion:
      - Plan the remaining M-003 work items after the request identity handoff exists.
      - Keep inventory identity-aware permission work, session validation vocabulary, optional no-op validation boundaries, and M-003 closure as separate bounded steps.
      - Preserve all ask-first boundaries for authentication schemes, token formats, credential storage, player account persistence, Protobuf envelope changes, and WebSocket handshake changes.
      - Avoid treating metadata-only request identity as authenticated proof in any planned implementation step.
    completion_summary: Planned the remaining M-003 player/session boundary queue as bounded work items for a session validator hook, inventory request identity permission handoff, metadata-only identity checks, and milestone closure, while preserving ask-first boundaries for authentication, token, credential, persistence, protocol, handshake, and direct player dependency decisions.
    ask_first:
      - Choosing a concrete authentication scheme, token format, password model, guest login, device login, social login, or external identity provider.
      - Adding player account database migrations or persistent schema.
      - Introducing cryptography, JWT, OAuth, OIDC, password-hashing, credential storage, or session-store dependencies.
      - Changing the Protobuf envelope or WebSocket handshake contract.
      - Making inventory depend directly on the player module.

  - id: W-0026
    milestone: M-003
    title: Add session validator hook boundary
    status: completed
    change: changes/2026-05-14-add-session-validator-hook-boundary
    depends_on:
      - W-0025
    related_decisions:
      - ADR-0018
      - ADR-0021
    completion:
      - Add an application-owned session validator interface or wrapper that can normalize request identity before route handlers run.
      - Provide a metadata-only validator/default path that preserves current behavior without authenticating clients.
      - Ensure real authentication, token parsing, credential lookup, session persistence, and player account lookup remain unimplemented.
      - Keep Protobuf envelope fields and WebSocket handshake behavior unchanged.
      - Add focused Go tests proving the hook can pass through metadata-only identity and replace identity only when an injected validator says it is valid.
    completion_summary: Added an application-owned SessionValidator hook, metadata-only default validator, SessionValidatingDispatcher wrapper, process composition for the default validator, and focused tests proving metadata-only pass-through, injected validated identity replacement, invalid-session stopping, and missing/errored validator behavior without implementing authentication, tokens, credential lookup, session persistence, player account lookup, Protobuf envelope changes, or WebSocket handshake changes.
    ask_first:
      - Choosing a concrete authentication scheme, token format, password model, guest login, device login, social login, or external identity provider.
      - Adding player account database migrations or persistent schema.
      - Introducing cryptography, JWT, OAuth, OIDC, password-hashing, credential storage, or session-store dependencies.
      - Changing the Protobuf envelope or WebSocket handshake contract.
      - Making inventory depend directly on the player module.

  - id: W-0027
    milestone: M-003
    title: Add inventory request identity permission handoff boundary
    status: completed
    change: changes/2026-05-14-add-inventory-request-identity-permission-handoff-boundary
    depends_on:
      - W-0026
    related_decisions:
      - ADR-0014
      - ADR-0021
    completion:
      - Update inventory permission handoff so policies can receive application request identity context without inventory owning authentication or player accounts.
      - Preserve current static bootstrap permission behavior as an explicit bootstrap policy until real authenticated policies are ratified.
      - Ensure metadata-only request identity is not treated as authenticated proof for privileged grants.
      - Keep inventory independent from the player module.
      - Add focused Go tests for permission handoff behavior and metadata-only denial/neutral semantics where applicable.
    completion_summary: Added inventory PermissionContext carrying requested actor text, target player_id, and app.RequestIdentity into permission decisions; preserved StaticPermissionPolicy as explicit bootstrap behavior; added MetadataOnlyDenyPermissionPolicy as a guard that refuses metadata-only privileged grants and only permits optional validated self-read; verified route-level identity handoff through bootstrap dispatcher tests without changing Protobuf, WebSocket, authentication, token, credential, session store, player account persistence, or inventory-to-player dependencies.
    ask_first:
      - Making metadata-only player_id sufficient for production permission grants.
      - Choosing service/admin authorization semantics beyond explicit placeholder vocabulary.
      - Adding player module dependencies to inventory.
      - Adding authentication, token, credential, or player account persistence dependencies.
      - Changing inventory public contracts or Protobuf messages.

  - id: W-0028
    milestone: M-003
    title: Add metadata-only identity repository checks
    status: completed
    change: changes/2026-05-14-add-metadata-only-identity-repository-checks
    depends_on:
      - W-0027
    related_decisions:
      - ADR-0018
      - ADR-0021
    completion:
      - Add repository checks that protect the current metadata-only identity boundary from common regression paths.
      - Check that WebSocket transport does not import player, inventory, authentication, generated Protobuf, or domain module packages.
      - Check that domain modules do not import WebSocket, generated Protobuf, authentication provider, token, or credential dependencies.
      - Check that player remains boundary-only until public contracts are ratified.
      - Record any intentionally deferred checks that are too brittle for the current repository shape.
    completion_summary: Added the runtime.identity_boundary repository check to prevent WebSocket transport, domain modules, dependency declarations, player runtime code, player Protobuf roots, and player/account migrations from crossing the current metadata-only player/session boundary before public contracts, authentication, credentials, tokens, session persistence, player account schemas, Protobuf envelope changes, or WebSocket handshake changes are ratified.
    ask_first:
      - Adding broad static analysis dependencies.
      - Making optional live services mandatory for default repository checks.
      - Weakening existing runtime, protocol, contract, generated, or work checks.
      - Changing generated file conventions.

  - id: W-0029
    milestone: M-003
    title: Close player identity and session boundary milestone
    status: completed
    change: changes/2026-05-14-close-player-identity-and-session-boundary-milestone
    depends_on:
      - W-0028
    related_decisions:
      - ADR-0019
      - ADR-0021
    completion:
      - Review M-003 completion criteria against implemented standards, manifests, handoff code, permission handoff, and checks.
      - Mark M-003 completed only if the boundary is durable and no authentication, token, credential, player account persistence, Protobuf envelope, or WebSocket handshake decision was crossed.
      - Record remaining deferred decisions and the recommended next milestone without making a major architectural decision implicitly.
      - Leave exactly one next_ready work item only if the next milestone is conservative and does not require maintainer confirmation.
    completion_summary: Reviewed M-003 against its completion criteria and closed it without crossing authentication, token, credential, player account persistence, Protobuf envelope, WebSocket handshake, or production permission boundaries. No next_ready work item was created because the next meaningful milestone requires maintainer confirmation.
    ask_first:
      - Starting production authentication implementation.
      - Choosing token, credential, player account schema, session persistence, or WebSocket handshake authentication design.
      - Introducing a new major milestone that commits to a major external framework dependency or architectural pattern.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0030
    milestone: M-004
    title: Confirm next milestone direction
    status: completed
    change: changes/2026-05-14-confirm-next-milestone-direction
    depends_on:
      - W-0029
    related_decisions:
      - ADR-0019
      - ADR-0021
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for authentication, token, credential, player account schema, session persistence, Protobuf envelope, WebSocket handshake, major dependency, and production permission decisions.
    completion_summary: The maintainer selected ratify_player_account_and_session_contracts and explicitly asked agents to keep referencing Nakama and Pitaya. The choice created M-005 as a contract-first milestone and W-0031 as the next ready work item without choosing authentication, token, credential, player account schema, session persistence, Protobuf envelope, WebSocket handshake, major dependency, or production permission behavior.
    ask_first:
      - Starting production authentication implementation.
      - Choosing token, credential, player account schema, session persistence, or WebSocket handshake authentication design.
      - Introducing a new major milestone that commits to a major external framework dependency or architectural pattern.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0031
    milestone: M-005
    title: Define player account and session contract standard
    status: completed
    change: changes/2026-05-14-define-player-account-session-contract-standard
    depends_on:
      - W-0030
    related_decisions:
      - ADR-0019
      - ADR-0021
    completion:
      - Define the contract standard for player account lifecycle and runtime session validation before implementation.
      - Map Nakama account, user, authentication, and session capability concepts into adopted, adapted, deferred, or rejected vibit terms.
      - Map Pitaya session binding, route handler, frontend/backend, and realtime server vocabulary into adopted, adapted, deferred, or rejected vibit terms.
      - Declare which player account commands, queries, events, errors, permissions, invariants, and generated outputs may be ratified next.
      - Keep authentication scheme, token format, credential storage, player account persistence, session persistence, Protobuf envelope changes, and WebSocket handshake changes out of this standard unless separately confirmed.
    completion_summary: Added docs/player-account-session-contracts.md and the Simplified Chinese translation, mapped Nakama and Pitaya reference patterns into adopted, adapted, deferred, and rejected vibit positions, and defined the conservative ratification order for player account and runtime session contracts without implementing or choosing authentication, token, credential, persistence, Protobuf envelope, WebSocket handshake, or external API compatibility behavior.
    ask_first:
      - Choosing concrete login methods such as guest, device, email/password, social login, custom ID, or external identity providers.
      - Choosing JWT, opaque token, refresh-token, signing, expiration, or revocation behavior.
      - Adding credential storage, password hashing, cryptography, OAuth, OIDC, or external auth dependencies.
      - Adding player account database schema, migrations, or session persistence.
      - Changing the Protobuf envelope or WebSocket handshake.
      - Making metadata-only player_id or session_id sufficient for production permission grants.
      - Copying Nakama or Pitaya public API shape instead of defining vibit-native contracts.

  - id: W-0032
    milestone: M-005
    title: Ratify minimal player account semantic contracts
    status: completed
    change: changes/2026-05-14-ratify-minimal-player-account-semantic-contracts
    depends_on:
      - W-0031
    related_decisions:
      - ADR-0019
      - ADR-0021
    completion:
      - Add minimal player account semantic contracts for account creation, account lookup, account-created event, account errors, and account permissions.
      - Update modules/player/module.yaml and .arch/contracts.yaml so the contracts are registered before implementation.
      - Keep the contracts semantic and storage-independent; do not add player account migrations or database schema.
      - Keep authentication schemes, token formats, credential storage, and login methods out of the player account lifecycle contracts.
      - Preserve Nakama/Pitaya reference alignment without copying external API shape.
    completion_summary: Added semantic player account contracts for CreatePlayerAccount, GetPlayerAccount, PlayerAccountCreated, player_account_errors, and player_account_permissions; registered them in .arch/contracts.yaml and modules/player/module.yaml; and kept runtime implementation, generated output, Protobuf messages, WebSocket routes, authentication, token behavior, credential storage, session persistence, and player account migrations deferred.
    ask_first:
      - Choosing concrete login methods such as guest, device, email/password, social login, custom ID, or external identity providers.
      - Choosing token format, signing, refresh, expiration, revocation, or credential behavior.
      - Adding player account database schema, migrations, indexes, or PostgreSQL repository implementation.
      - Adding runtime session persistence.
      - Changing Protobuf envelope or WebSocket handshake behavior.
      - Adding runtime player module Go code before semantic contracts are verified.

  - id: W-0033
    milestone: M-005
    title: Ratify player account Protobuf and generated shape boundary
    status: completed
    change: changes/2026-05-14-ratify-player-account-protobuf-generated-shape-boundary
    depends_on:
      - W-0032
    related_decisions:
      - ADR-0016
      - ADR-0017
      - ADR-0021
    completion:
      - Decide whether the ratified player account semantic contracts should receive Protobuf wire messages and generated dispatch shapes now.
      - If approved, define the Protobuf source and generated output boundary without adding handwritten runtime handlers.
      - Preserve existing envelope and WebSocket handshake behavior unless separately confirmed.
      - Keep authentication, token behavior, credential storage, player account persistence, and session persistence deferred.
    completion_summary: Added proto/vibit/player/v1/player.proto and generated runtime/internal/generated/proto/vibit/player/v1/player.pb.go for CreatePlayerAccount, GetPlayerAccount, and PlayerAccountCreated; updated manifests and runtime identity-boundary checks so ratified player wire shape is allowed while runtime player handlers, WebSocket routes, authentication, token behavior, credential storage, persistence, session storage, envelope changes, and handshake changes remain deferred.
    ask_first:
      - Changing generated file conventions.
      - Changing the Protobuf envelope.
      - Adding WebSocket routes for player account contracts.
      - Adding runtime player module Go code.
      - Choosing authentication, token, credential, persistence, or session storage behavior.

  - id: W-0034
    milestone: M-005
    title: Ratify runtime session validation semantic contracts
    status: completed
    change: changes/2026-05-14-ratify-runtime-session-validation-semantic-contracts
    depends_on:
      - W-0033
    related_decisions:
      - ADR-0018
      - ADR-0021
    completion:
      - Ratify minimal runtime session validation semantic contracts and catalogs without implementing authentication, token validation, credential storage, session persistence, or WebSocket handshake authentication.
      - Define runtime-owned contract source location and registration for ValidateSession, SessionValidated, session_errors, and session_permissions or a narrower justified set.
      - Preserve the existing metadata-only validation behavior until a real session validator is separately ratified and implemented.
      - Keep Nakama session and Pitaya session-binding concepts as references without copying public API shape.
    completion_summary: Added runtime-owned semantic contracts for ValidateSession, SessionValidated, session_errors, and session_permissions under contracts/runtime/session; registered them in .arch/contracts.yaml; updated runtime and account/session standards; and taught contract checks to validate runtime session contracts without treating them as player or inventory module contracts. The existing metadata-only validator remains pass-through behavior only, and no authentication, token behavior, credential storage, session persistence, player account persistence, Protobuf envelope change, WebSocket handshake change, or player runtime handler was added.
    ask_first:
      - Choosing token format, signing, refresh, expiration, revocation, or credential behavior.
      - Choosing concrete login methods such as guest, device, email/password, social login, custom ID, or external identity providers.
      - Adding session persistence, player account persistence, database schema, migrations, or indexes.
      - Changing Protobuf envelope or WebSocket handshake behavior.
      - Adding runtime authentication or player account handler Go code.

  - id: W-0035
    milestone: M-005
    title: Add runtime contract inspection support
    status: completed
    change: changes/2026-05-14-add-runtime-contract-inspection
    depends_on:
      - W-0034
    related_decisions:
      - ADR-0018
      - ADR-0021
    completion:
      - Extend `node tools/vibit inspect contract` so agents can inspect runtime-owned contract families.
      - Preserve existing domain module contract inspection behavior.
      - Keep the inspect output schema stable while reporting runtime family registry, source, owner, and consistency details.
    completion_summary: Added runtime contract inspection support for runtime-owned contract families, starting with `runtime:ValidateSession`, while preserving player and inventory contract inspection behavior. This closes an agent tooling gap after runtime session semantic contracts were ratified.
    ask_first:
      - Changing inspect output schema compatibility.
      - Replacing the contract registry structure.
      - Introducing a new YAML parser or schema validation dependency.

  - id: W-0036
    milestone: M-005
    title: Close player account and session contracts milestone
    status: completed
    change: changes/2026-05-14-close-player-account-session-contracts-milestone
    depends_on:
      - W-0035
    related_decisions:
      - ADR-0019
      - ADR-0021
    completion:
      - Review M-005 completion criteria against ratified player account contracts, Protobuf wire shape, runtime session validation contracts, tooling support, standards, manifests, and repository checks.
      - Mark M-005 completed only if no authentication, token, credential, player account persistence, session persistence, Protobuf envelope, WebSocket handshake, runtime player handler, WebSocket route, direct external API compatibility, or major dependency decision was crossed.
      - Record remaining deferred decisions and next milestone candidates without choosing one implicitly.
      - Create a blocked confirmation gate for the next major direction.
    completion_summary: Reviewed M-005 against its completion criteria and closed it as a contract-first milestone. Player account and runtime session validation contracts are ratified and inspectable, while authentication, token, credential, persistence, Protobuf envelope, WebSocket handshake, runtime player handler, WebSocket route, direct external API compatibility, and major dependency decisions remain deferred behind M-006.
    ask_first:
      - Starting production authentication implementation.
      - Choosing login methods, token format, credential storage, player account schema, session persistence, or WebSocket handshake authentication design.
      - Adding runtime player account handlers or WebSocket routes.
      - Introducing a new major milestone that commits to a major external framework dependency or architectural pattern.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0037
    milestone: M-006
    title: Confirm next milestone direction after account and session contracts
    status: completed
    change: changes/2026-05-14-confirm-generator-contract-tooling-direction
    depends_on:
      - W-0036
    related_decisions:
      - ADR-0019
      - ADR-0021
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for authentication, token, credential, player account schema, session persistence, Protobuf envelope, WebSocket handshake, major dependency, runtime player handler, and production permission decisions.
    completion_summary: The maintainer accepted the recommended direction to improve generators, contract tooling, and architecture checks before adding more runtime features. M-007 was created with W-0038 as the first tooling work item, preserving all deferred authentication, token, credential, persistence, protocol handshake, runtime handler, and production permission decisions.
    selected_direction: improve_generators_and_contract_tooling_before_more_runtime_features
    candidate_directions:
      - ratify_authentication_and_token_session_validation_design
      - ratify_player_account_postgresql_persistence_schema
      - implement_runtime_player_account_handlers_from_ratified_contracts
      - expand_core_game_domain_modules_after_nakama_pitaya_review
      - improve_generators_and_contract_tooling_before_more_runtime_features
    ask_first:
      - Starting production authentication implementation.
      - Choosing login methods, token format, credential storage, player account schema, session persistence, or WebSocket handshake authentication design.
      - Adding runtime player account handlers or WebSocket routes.
      - Introducing a new major milestone that commits to a major external framework dependency or architectural pattern.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0038
    milestone: M-007
    title: Add agent tooling inspection standard and commands
    status: completed
    change: changes/2026-05-14-add-agent-tooling-inspection-standard
    depends_on:
      - W-0037
    related_decisions:
      - ADR-0017
      - ADR-0019
      - ADR-0021
    completion:
      - Add an English and Simplified Chinese standard for agent-facing tooling.
      - Add JSON inspection commands for next work, contract index, generated output, and reference planning context.
      - Add `check agent-tooling` and include it in `check all`.
      - Update AGENTS guidance, conventions, runtime, reference, generated-output docs, and rule catalog.
      - Do not add runtime behavior, authentication, token, persistence, protocol envelope, WebSocket handshake, or new game module decisions.
    completion_summary: Added docs/agent-tooling.md and the Simplified Chinese translation; added `inspect next`, `inspect contracts`, `inspect generated`, `inspect reference`, and `check agent-tooling`; updated AGENTS guidance, conventions, runtime/reference manifests, generated-output standards, and the rule catalog; and kept the work scoped to agent tooling.
    ask_first:
      - Changing generated file conventions beyond contract-shape trace and drift checks.
      - Replacing the contract registry structure.
      - Introducing a YAML parser or other major CLI dependency.
      - Starting authentication, token, credential, persistence, runtime handler, protocol envelope, or WebSocket handshake implementation.

  - id: W-0039
    milestone: M-007
    title: Generate reproducible Go contract shapes
    status: completed
    change: changes/2026-05-14-generate-reproducible-go-contract-shapes
    depends_on:
      - W-0038
    related_decisions:
      - ADR-0017
    completion:
      - Add `node tools/vibit generate contract-shapes <module|all>`.
      - Generate Go contract shape files from registered non-runtime semantic contracts.
      - Add generated marker, source trace, generator trace, and contract trace to each generated file.
      - Extend `check generated` to require generated contract shapes and detect drift.
      - Keep generated shapes as inspectable metadata only, not runtime handlers or business logic.
    completion_summary: Added the contract-shapes generator, generated inventory and player contract shape files under `runtime/internal/generated/contracts/`, and extended `check generated` to verify source traces and generator drift for registered non-runtime semantic contracts.
    ask_first:
      - Making generated contract shapes runtime dispatch code.
      - Adding generated WebSocket route registration.
      - Adding generated persistence, authentication, token, credential, or session behavior.
      - Changing generated output roots or package conventions beyond this traceable shape.

  - id: W-0040
    milestone: M-007
    title: Verify agent tooling and generated contract shapes
    status: completed
    change: changes/2026-05-14-verify-agent-tooling-generated-contract-shapes
    depends_on:
      - W-0039
    related_decisions:
      - ADR-0017
      - ADR-0019
    completion:
      - Run focused inspections for next work, contracts, generated output, and reference context.
      - Run focused checks for agent tooling and generated output.
      - Run repository and Go runtime verification.
      - Record verification results in the relevant change specs.
      - Leave the next work item ready only after all checks pass.
    completion_summary: Verified the new agent tooling intake commands, generated contract shape output, work queue consistency, schema consistency, memory consistency, and Go runtime tests. The generated contract shape drift check passes for inventory and player semantic contracts.
    ask_first:
      - Weakening generated drift checks.
      - Marking generated output verified without running generator and checks.

  - id: W-0041
    milestone: M-007
    title: Add generated inspection module and type filters
    status: completed
    change: changes/2026-05-14-add-generated-inspection-filters
    depends_on:
      - W-0040
    related_decisions:
      - ADR-0017
    completion:
      - Extend `node tools/vibit inspect generated` with optional `--module <module>` and `--type <contract-type>` filters for generated contract shapes.
      - Keep the output schema backward-compatible for unfiltered inspection.
      - Update the agent tooling standard and Simplified Chinese translation.
      - Add focused verification for filtered inventory and player generated inspections.
    completion_summary: Added optional `--module <module>` and `--type <contract-type>` filters to generated output inspection, documented the filters in English and Simplified Chinese, and verified filtered generated inspections without changing generated roots or treating generated shapes as runtime handlers.
    ask_first:
      - Changing generated output roots.
      - Replacing the generated inspection output schema instead of adding compatible filter metadata.
      - Treating generated contract shapes as runtime handlers.

  - id: W-0042
    milestone: M-007
    title: Add contract inspection type and status filters
    status: completed
    change: changes/2026-05-14-add-contract-inspection-filters
    depends_on:
      - W-0041
    related_decisions:
      - ADR-0017
      - ADR-0021
    completion:
      - Extend `node tools/vibit inspect contracts` with optional `--type <contract-type>` and `--status <status>` filters.
      - Preserve existing unfiltered and module-filtered contract inspection behavior.
      - Document the narrower contract inspection commands in English and Simplified Chinese.
      - Keep the change limited to agent tooling; do not alter contract semantics or runtime behavior.
    completion_summary: Added optional `--type <contract-type>` and `--status <status>` filters to contract index inspection, kept unfiltered and module-filtered behavior compatible, documented the commands in English and Simplified Chinese, and verified the focused tooling checks.
    ask_first:
      - Replacing the contract registry structure.
      - Changing contract source format.
      - Treating inspection filters as permission to cross authentication, persistence, protocol, or runtime handler boundaries.

  - id: W-0043
    milestone: M-007
    title: Add contract source field summaries to inspection
    status: completed
    change: changes/2026-05-14-add-contract-source-field-summaries
    depends_on:
      - W-0042
    related_decisions:
      - ADR-0017
    completion:
      - Extend contract index inspection output with machine-readable summaries of input, output, and event payload fields when present.
      - Include required field names and declared property names without replacing full contract source files.
      - Preserve existing contract inspection filters and output compatibility.
      - Keep the change limited to inspection; do not alter contract semantics, Protobuf schemas, generated output, or runtime behavior.
    completion_summary: Added derived `fields` summaries to contract index inspection for command/query input and output fields plus event payload fields, preserving existing filters and keeping contract manifests as the source of truth.
    ask_first:
      - Changing contract source format.
      - Making field summaries authoritative over source contract manifests.
      - Changing Protobuf schemas or generated output conventions.

  - id: W-0044
    milestone: M-007
    title: Add stale generated contract shape detection
    status: completed
    change: changes/2026-05-14-add-stale-generated-contract-shape-detection
    depends_on:
      - W-0043
    related_decisions:
      - ADR-0017
    completion:
      - Extend `node tools/vibit check generated` so committed generated contract shape files must correspond to registered non-runtime semantic contracts.
      - Report stale generated contract shape files as check failures instead of only validating generated markers.
      - Preserve existing source trace and drift checks.
      - Keep generated contract shape files as metadata only, not runtime handlers.
    completion_summary: Extended `check generated` so generated Go contract shape files must correspond to registered non-runtime semantic contracts, preserving source trace and reproducibility checks while failing stale generated outputs.
    ask_first:
      - Changing generated output roots.
      - Deleting generated files without proving they are stale.
      - Changing generated file conventions beyond stale-file validation.

  - id: W-0045
    milestone: M-007
    title: Add generated inspection stale file summaries
    status: completed
    change: changes/2026-05-14-add-generated-inspection-stale-file-summaries
    depends_on:
      - W-0044
    related_decisions:
      - ADR-0017
    completion:
      - Extend `node tools/vibit inspect generated` to report stale generated contract shape files derived from the same expected-path model as `check generated`.
      - Include stale file counts and file paths in JSON inspection output.
      - Preserve existing module/type filters and summary counts.
      - Keep the change inspection-only; do not delete files automatically.
    completion_summary: Added `stale_contract_shape_files` and stale counts to generated output inspection, using the same expected contract shape model as `check generated` while preserving module/type filters and avoiding automatic deletion.
    ask_first:
      - Automatically deleting stale generated files.
      - Changing generated output roots.
      - Replacing generated inspection output schema instead of adding compatible fields.

  - id: W-0046
    milestone: M-007
    title: Add generated inspection drift status summary
    status: completed
    change: changes/2026-05-14-add-generated-inspection-drift-status-summary
    depends_on:
      - W-0045
    related_decisions:
      - ADR-0017
    completion:
      - Extend generated output inspection with a compact status derived from missing and stale generated contract shape counts.
      - Report whether generated contract shapes are complete for the requested filter.
      - Preserve existing generated inspection fields and filters.
      - Keep detailed drift validation in `check generated`.
    completion_summary: Added `missing_contract_shapes`, `contract_shape_status`, and matching summary fields to generated output inspection so agents can quickly determine whether filtered generated contract shape output is complete while leaving detailed drift validation in `check generated`.
    ask_first:
      - Weakening `check generated` drift validation.
      - Replacing existing generated inspection output fields.
      - Changing generated output roots or generated file conventions.

  - id: W-0047
    milestone: M-007
    title: Run M-007 focused repository verification
    status: completed
    change: changes/2026-05-14-verify-m007-tooling-hardening-state
    depends_on:
      - W-0046
    related_decisions:
      - ADR-0017
      - ADR-0019
      - ADR-0021
    completion:
      - Run repository-level checks after the M-007 inspection and generator hardening changes.
      - Run Go runtime tests and vet checks without adding runtime behavior.
      - Run whitespace diff validation.
      - Record failures or unavailable checks explicitly before planning further M-007 work.
    completion_summary: Verified the accumulated M-007 tooling hardening state with next/contracts/generated/reference inspections, agent-tooling/generated/work/schema/change/all checks, Go tests, Go vet, and diff whitespace validation. The only issue found was the new verification change spec initially using an unsupported `type: verification`; it was corrected to the existing `tooling` type and full checks passed.
    ask_first:
      - Weakening repository checks to make verification pass.
      - Ignoring runtime test failures caused by current tooling changes.
      - Starting authentication, token, credential, persistence, protocol envelope, WebSocket handshake, runtime player handler, or new game-domain implementation work.

  - id: W-0048
    milestone: M-007
    title: Close M-007 tooling hardening milestone
    status: completed
    change: changes/2026-05-14-close-m007-tooling-hardening-milestone
    depends_on:
      - W-0047
    related_decisions:
      - ADR-0017
      - ADR-0019
      - ADR-0021
    completion:
      - Review M-007 completion criteria against completed work.
      - Mark M-007 completed if the criteria are met.
      - Create the next direction confirmation gate without choosing authentication, persistence, protocol, runtime player handler, or new game-domain implementation.
      - Leave no runtime behavior change in this milestone closeout.
    completion_summary: Reviewed M-007 against its completion criteria and closed it as a tooling hardening milestone. The next major direction now sits behind M-008 as an explicit maintainer confirmation gate, with authentication, token, credential, persistence, protocol, runtime player handler, generated file convention, and new game-domain decisions still deferred.
    ask_first:
      - Choosing the next major implementation direction instead of creating a confirmation gate.
      - Starting authentication, token, credential, player account persistence, session persistence, Protobuf envelope, WebSocket handshake, runtime player handler, or new game-domain implementation work.

  - id: W-0049
    milestone: M-008
    title: Confirm next milestone direction after tooling hardening
    status: completed
    change: changes/2026-05-14-confirm-next-direction-after-tooling-hardening
    depends_on:
      - W-0048
    related_decisions:
      - ADR-0017
      - ADR-0019
      - ADR-0021
    candidate_directions:
      - ratify_authentication_and_token_session_validation_design
      - ratify_player_account_postgresql_persistence_schema
      - implement_runtime_player_account_handlers_from_ratified_contracts
      - expand_core_game_domain_modules_after_nakama_pitaya_review
      - expand_generator_runtime_dispatch_and_test_fixture_generation
      - strengthen_operations_observability_and_admin_tooling
    selected_direction: ratify_player_account_postgresql_persistence_schema
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for authentication, token, credential, player account schema, session persistence, Protobuf envelope, WebSocket handshake, major dependency, runtime player handler, generated file conventions, production permission, and major game-domain expansion decisions.
    completion_summary: The maintainer authorized the professional recommendation to proceed with player account PostgreSQL persistence schema ratification. M-009 was created with W-0050 as the first bounded work item, preserving all deferred authentication, token, credential, session persistence, protocol, runtime handler, generated file convention, production permission, and game-domain expansion decisions.
    ask_first:
      - Starting production authentication implementation.
      - Choosing login methods, token format, credential storage, player account schema, session persistence, or WebSocket handshake authentication design.
      - Adding runtime player account handlers or WebSocket routes.
      - Changing generated file conventions or making generated contract shapes runtime dispatch code.
      - Introducing a new major milestone that commits to a major external framework dependency or architectural pattern.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0050
    milestone: M-009
    title: Define player account PostgreSQL persistence schema boundary
    status: completed
    change: changes/2026-05-14-define-player-account-postgresql-persistence-schema-boundary
    depends_on:
      - W-0049
    related_decisions:
      - ADR-0019
      - ADR-0020
      - ADR-0021
      - ADR-0022
    completion:
      - Ratify the player account PostgreSQL schema boundary without adding migration source.
      - Define player-owned tables, columns, indexes, constraints, event/audit rows, and repository boundaries.
      - Update manifests, module guides, persistence standards, and repository checks so future migrations can be validated against the ratified boundary.
      - Keep authentication, token behavior, credentials, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime handlers, and WebSocket routes deferred.
    completion_summary: Ratified the player account PostgreSQL schema boundary for player_accounts and player_account_events; documented that the schema stores account lifecycle only and excludes credentials, tokens, sessions, external identities, WebSocket state, and inventory ownership; updated manifests, guides, and checks so player account migration source can be added only after schema ratification; and preserved runtime implementation as a later work item.
    ask_first:
      - Changing the ratified player account table shape after migration source is added.
      - Adding authentication, token, credential, password, OAuth, OIDC, or external identity-provider behavior.
      - Adding session persistence or WebSocket handshake authentication.
      - Adding runtime player handlers or WebSocket routes.
      - Adding direct Nakama or Pitaya public API compatibility.
      - Making metadata-only identity sufficient for production permission grants.

  - id: W-0051
    milestone: M-009
    title: Add first player account PostgreSQL migration source
    status: completed
    change: changes/2026-05-14-add-first-player-account-postgresql-migration
    depends_on:
      - W-0050
    related_decisions:
      - ADR-0020
      - ADR-0021
      - ADR-0022
    completion:
      - Add SQL-first migration source for the ratified player account schema under runtime/migrations/postgres.
      - Include goose Up and Down sections, owning module trace, deterministic sequence number, constraints, and indexes.
      - Update module and runtime manifests with the migration source path.
      - Verify migration source checks without adding repository adapter, runtime handlers, authentication, tokens, credentials, session persistence, Protobuf envelope changes, or WebSocket handshake changes.
    completion_summary: Added runtime/migrations/postgres/000002_create_player_account_state.sql as the first SQL-first player account migration source; it creates only player_accounts and player_account_events lifecycle state, records PlayerAccountCreated support, includes explicit constraints and indexes, and keeps repository adapters, runtime handlers, authentication, tokens, credentials, session persistence, Protobuf envelope changes, and WebSocket handshake changes deferred.
    ask_first:
      - Changing the ratified table names, credential/session exclusions, or cross-module reference policy.
      - Adding runtime player repository implementation in the migration step.
      - Adding authentication, tokens, credentials, session persistence, runtime handlers, WebSocket routes, or protocol handshake behavior.

  - id: W-0052
    milestone: M-009
    title: Define player account repository interface boundary
    status: completed
    change: changes/2026-05-14-define-player-account-repository-interface-boundary
    depends_on:
      - W-0051
    related_decisions:
      - ADR-0020
      - ADR-0021
      - ADR-0022
    completion:
      - Define the module-owned player account repository interface and storage-neutral mutation/query shapes after the migration source exists.
      - Update runtime identity checks to allow only this repository boundary without permitting handlers, adapters, authentication, tokens, credentials, session persistence, Protobuf envelope changes, or WebSocket handshake changes.
      - Update player/runtime manifests and guides so future PostgreSQL adapter work has a declared interface to implement.
      - Verify repository boundary checks without adding PostgreSQL adapter implementation or runtime player routes.
    completion_summary: Added the storage-neutral player account repository interface under runtime/internal/modules/player with account lifecycle structs, CreatePlayerAccountMutation metadata, Repository.CreatePlayerAccount, Repository.GetPlayerAccount, and focused Go tests; updated manifests, guides, standards, and runtime identity checks so only this player runtime repository boundary is allowed while PostgreSQL adapters, runtime handlers, authentication, tokens, credentials, session persistence, Protobuf envelope changes, and WebSocket handshake changes remain deferred.
    ask_first:
      - Adding PostgreSQL player account adapter implementation instead of only the interface boundary.
      - Adding runtime player account command/query handlers or WebSocket routes.
      - Adding authentication, tokens, credentials, session persistence, Protobuf envelope changes, or WebSocket handshake behavior.
      - Changing the ratified player account migration schema or table ownership.

  - id: W-0053
    milestone: M-009
    title: Define player account PostgreSQL adapter boundary
    status: completed
    change: changes/2026-05-14-define-player-account-postgresql-adapter-boundary
    depends_on:
      - W-0052
    related_decisions:
      - ADR-0020
      - ADR-0021
      - ADR-0022
    completion:
      - Define the PostgreSQL adapter implementation boundary for the player account repository after the module-owned repository interface exists.
      - Specify executor ownership, transaction expectations, SQL operation scope, error mapping expectations, and tests needed before adapter implementation.
      - Update runtime identity and persistence checks so future player account adapter work is allowed only under runtime/internal/platform/persistence/postgres and still forbidden from handlers, authentication, tokens, credentials, session persistence, Protobuf envelope changes, or WebSocket handshake changes.
      - Verify the adapter boundary without implementing runtime player account command/query handlers or WebSocket routes.
    completion_summary: Defined the player account PostgreSQL adapter boundary without implementing the adapter. The boundary reserves runtime/internal/platform/persistence/postgres/player_account_repository.go and its focused test file, specifies NewPlayerAccountRepositoryForUnitOfWork, caller-owned executor and no transaction-control rules, first SQL operation scope, error mapping expectations, and required tests; runtime identity checks now require the adapter boundary while preserving handler, route, authentication, token, credential, session, Protobuf envelope, and WebSocket handshake deferrals.
    ask_first:
      - Implementing runtime player account command/query handlers or WebSocket routes.
      - Adding authentication, tokens, credentials, session persistence, Protobuf envelope changes, or WebSocket handshake behavior.
      - Changing the ratified migration schema, table ownership, repository interface shape, or transaction ownership model.
      - Making live PostgreSQL verification mandatory in default repository checks.

  - id: W-0054
    milestone: M-009
    title: Implement player account PostgreSQL adapter
    status: completed
    change: changes/2026-05-14-implement-player-account-postgresql-adapter
    depends_on:
      - W-0053
    related_decisions:
      - ADR-0020
      - ADR-0021
      - ADR-0022
    completion:
      - Implement the player account PostgreSQL adapter only under runtime/internal/platform/persistence/postgres/player_account_repository.go.
      - Add focused fake-executor tests under runtime/internal/platform/persistence/postgres/player_account_repository_test.go for SQL shape, no transaction-control SQL, mutation normalization, row mapping, missing-row behavior, duplicate/constraint error mapping, and default no-live-PostgreSQL behavior.
      - Add the PostgreSQL UnitOfWork.NewPlayerAccountRepository helper if needed without exposing pgx to application or domain packages.
      - Preserve existing repository interface shape, migration schema, transaction ownership model, runtime handler deferral, WebSocket route deferral, authentication/token/credential/session deferral, Protobuf envelope behavior, and WebSocket handshake behavior.
    completion_summary: Implemented the player account PostgreSQL adapter under runtime/internal/platform/persistence/postgres with fake-executor tests for account/event insert SQL, account lookup SQL, no transaction-control SQL, mutation normalization, UTC and nullable timestamp mapping, missing-row behavior, duplicate/constraint error mapping, and default no-live-PostgreSQL behavior; added UnitOfWork.NewPlayerAccountRepository without exposing pgx outside the PostgreSQL platform package; and kept runtime handlers, WebSocket routes, authentication, tokens, credentials, external identities, session persistence, Protobuf envelope behavior, and WebSocket handshake behavior deferred.
    ask_first:
      - Implementing runtime player account command/query handlers or WebSocket routes.
      - Adding authentication, tokens, credentials, external identity linking, session persistence, Protobuf envelope changes, or WebSocket handshake behavior.
      - Changing the ratified player account migration schema, repository interface shape, or transaction ownership model.
      - Making live PostgreSQL verification mandatory in default repository checks.

  - id: W-0055
    milestone: M-009
    title: Close player account PostgreSQL persistence milestone
    status: completed
    change: changes/2026-05-14-close-player-account-postgresql-persistence-milestone
    depends_on:
      - W-0054
    related_decisions:
      - ADR-0020
      - ADR-0021
      - ADR-0022
    completion:
      - Review M-009 completion criteria against the ratified schema, migration source, repository interface, PostgreSQL adapter implementation, manifests, standards, and repository checks.
      - Mark M-009 completed only if runtime handlers, WebSocket routes, authentication, token behavior, credential storage, external identity linking, session persistence, Protobuf envelope behavior, and WebSocket handshake behavior remain deferred.
      - Record remaining deferred decisions and next milestone candidates without choosing one implicitly.
      - Create or leave a confirmation gate for the next major direction instead of starting runtime player handlers or authentication implicitly.
    completion_summary: Reviewed M-009 against its completion criteria and closed it as a player account PostgreSQL persistence milestone. Schema, migration source, repository interface, adapter boundary, adapter implementation, manifests, standards, fake-executor tests, and repository checks are in place. Runtime handlers, WebSocket routes, authentication, tokens, credentials, external identity links, session persistence, Protobuf envelope behavior, and WebSocket handshake behavior remain deferred behind M-010.
    ask_first:
      - Choosing the next major implementation direction instead of creating or preserving a confirmation gate.
      - Starting production authentication implementation.
      - Adding runtime player account command/query handlers or WebSocket routes.
      - Choosing login methods, token format, credential storage, external identity linking, session persistence, Protobuf envelope changes, or WebSocket handshake authentication behavior.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0056
    milestone: M-010
    title: Confirm next milestone direction after player account PostgreSQL persistence
    status: completed
    change: changes/2026-05-14-confirm-next-direction-after-player-account-postgresql-persistence
    depends_on:
      - W-0055
    related_decisions:
      - ADR-0019
      - ADR-0020
      - ADR-0021
      - ADR-0022
    block_reason: The next major direction requires maintainer confirmation because each candidate affects long-lived architecture.
    candidate_directions:
      - ratify_authentication_and_token_session_validation_design
      - implement_runtime_player_account_handlers_from_ratified_contracts
      - ratify_external_identity_linking_and_credential_storage
      - ratify_session_persistence_and_websocket_handshake_authentication
      - expand_core_game_domain_modules_after_nakama_pitaya_review
      - strengthen_operations_observability_and_admin_tooling
    selected_direction: ratify_authentication_and_token_session_validation_design
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for authentication, token, credential, external identity, session persistence, Protobuf envelope, WebSocket handshake, major dependency, runtime player handler, generated file conventions, operations posture, production permission, and major game-domain expansion decisions.
    completion_summary: The maintainer selected option 1, ratify_authentication_and_token_session_validation_design. M-011 was created with W-0057 as the first bounded design-standard work item, preserving ask-first boundaries for concrete login methods, token format, credential storage, external identity linking, session persistence, Protobuf envelope behavior, WebSocket handshake behavior, runtime player handlers, WebSocket routes, major dependencies, generated conventions, operations posture, production permissions, and broader game-domain expansion.
    ask_first:
      - Starting production authentication implementation.
      - Choosing login methods, token format, credential storage, external identity linking, session persistence, Protobuf envelope changes, or WebSocket handshake authentication behavior.
      - Adding runtime player account command/query handlers or WebSocket routes.
      - Changing generated file conventions or making generated contract shapes runtime dispatch code.
      - Introducing a new major milestone that commits to a major external framework dependency or architectural pattern.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0057
    milestone: M-011
    title: Define authentication and token session validation design standard
    status: completed
    change: changes/2026-05-14-define-authentication-token-session-validation-design-standard
    depends_on:
      - W-0056
    related_decisions:
      - ADR-0019
      - ADR-0021
      - ADR-0022
      - ADR-0023
    completion:
      - Define the design standard for authentication, token behavior, session validation, credential storage boundaries, external identity linking boundaries, session persistence boundaries, request identity trust, Protobuf envelope interaction, and WebSocket handshake interaction before implementation.
      - Map Nakama account, authentication, session token, refresh token, and realtime socket concepts into adopted, adapted, deferred, or rejected vibit terms.
      - Map Pitaya session binding, route handler, frontend/backend, and realtime session vocabulary into adopted, adapted, deferred, or rejected vibit terms.
      - Preserve current metadata-only validator behavior as non-authenticated until a real validator is separately ratified and implemented.
      - Plan the remaining M-011 work queue without adding runtime authentication code, token parsing, credential storage, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime player handlers, or WebSocket routes.
    completion_summary: Defined the authentication, token, and session validation design standard and ADR-0023; separated authentication proof, login methods, tokens, credentials, external identity links, runtime sessions, request identity, WebSocket handshake authentication, player account lifecycle, transport metadata, and Protobuf envelope metadata; mapped Nakama and Pitaya reference concepts into vibit adopted/adapted/deferred/rejected positions; preserved metadata-only identity as non-authenticated; and planned the remaining M-011 queue without adding runtime authentication, token parsing, credential storage, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime player handlers, or WebSocket routes.
    ask_first:
      - Choosing a concrete login method such as guest, device, email/password, custom ID, social login, OAuth, OIDC, or external identity provider.
      - Choosing JWT, opaque token, refresh token, signing, expiration, revocation, or token storage behavior.
      - Adding credential storage, password hashing, cryptography, OAuth, OIDC, external identity, or session-store dependencies.
      - Adding authentication, token parsing, credential lookup, external identity linking, or session persistence runtime code.
      - Changing Protobuf envelope behavior or WebSocket handshake authentication behavior.
      - Adding runtime player account handlers or WebSocket routes.
      - Declaring metadata-only player_id or session_id sufficient for production permissions.

  - id: W-0058
    milestone: M-011
    title: Add authentication token session boundary architecture checks
    status: completed
    change: changes/2026-05-14-add-authentication-token-session-boundary-checks
    depends_on:
      - W-0057
    related_decisions:
      - ADR-0021
      - ADR-0022
      - ADR-0023
    completion:
      - Add repository checks that enforce the authentication/token/session design standard in architecture, runtime, player module, PostgreSQL migration, and generated-output boundaries where statically checkable.
      - Ensure checks keep metadata-only player_id and session_id non-authenticated and prevent credentials, tokens, external identity links, runtime sessions, WebSocket state, and request validation results from being added to player account lifecycle persistence by convenience.
      - Ensure checks keep WebSocket transport, Protobuf adapters, domain modules, and player repositories from silently owning authentication, token parsing, credential lookup, or session persistence before ratification.
      - Update rule catalog, manifests, guidance, and change spec verification without adding runtime authentication code, token parsing, credential storage, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime player handlers, or WebSocket routes.
    completion_summary: Added the runtime.authentication_token_session_boundary repository check and rule catalog entry. The check verifies authentication/token/session standard and ADR references, manifest and guide markers, metadata-only validator markers, Protobuf source and generated-output boundaries, WebSocket transport boundaries, runtime module and protocol adapter boundaries, player repository boundaries, and player account lifecycle migration boundaries without adding runtime authentication, token parsing, credential storage, external identity linking, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime player handlers, WebSocket routes, or live external-service requirements.
    ask_first:
      - Weakening existing identity/authentication/session boundary checks.
      - Adding runtime authentication, token, credential, external identity, or session persistence implementation while adding checks.
      - Choosing concrete login methods, token format, credential storage, external identity linking, session persistence, Protobuf envelope behavior, or WebSocket handshake authentication behavior.
      - Making repository checks require live external services by default.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0059
    milestone: M-011
    title: Ratify authentication proof and token session validation contract dimensions
    status: completed
    change: changes/2026-05-14-ratify-authentication-proof-token-session-contract-dimensions
    depends_on:
      - W-0058
    related_decisions:
      - ADR-0021
      - ADR-0023
    completion:
      - Ratify semantic contract dimensions for authentication proof and token/session validation without choosing a concrete login method or token format.
      - Define required command/query/event/error/permission vocabulary dimensions, actor kinds, validation statuses, failure classes, retryability expectations, and request identity handoff semantics.
      - Register only design-level or semantic contract artifacts that do not implement token parsing, credential lookup, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime player handlers, or WebSocket routes.
    completion_summary: Ratified the authentication proof and token/session contract dimensions standard with English and Simplified Chinese documentation; defined actor kinds, validation statuses, proof statuses, failure classes, retryability rules, command/query/event/error/permission dimensions, request identity handoff semantics, Nakama/Pitaya reference mapping, and non-goals; updated runtime session semantic contract sources and manifests to reference the dimensions without adding runtime authentication, token parsing, credential lookup, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime player handlers, or WebSocket routes.
    ask_first:
      - Choosing JWT, opaque tokens, refresh tokens, signing, expiration, revocation, rotation, token storage, or token carrier behavior.
      - Choosing guest, device, email/password, custom ID, social login, OAuth, OIDC, or external identity provider login.
      - Adding runtime authentication code or token parsing.
      - Changing the Protobuf envelope or WebSocket handshake behavior.

  - id: W-0060
    milestone: M-011
    title: Define credential storage and external identity linking boundaries
    status: completed
    change: changes/2026-05-14-define-credential-storage-external-identity-linking-boundaries
    depends_on:
      - W-0059
    related_decisions:
      - ADR-0022
      - ADR-0023
    completion:
      - Define credential storage and external identity linking boundaries without adding schema, dependencies, credential records, provider subjects, password hashing, OAuth, OIDC, or runtime lookup code.
      - Preserve player account lifecycle tables as account lifecycle storage only until credential and external identity schemas are separately ratified.
      - Map Nakama authentication method coverage into deferred vibit login-method families and keep direct API compatibility rejected unless a future ADR changes it.
    completion_summary: Defined the credential storage and external identity linking boundary standard with English and Simplified Chinese documentation; preserved `player_accounts` and `player_account_events` as lifecycle-only tables; mapped Nakama login-method coverage into deferred vibit capability families; mapped Pitaya session binding vocabulary without adopting direct API compatibility; updated manifests, guides, and player module metadata to keep credential storage, external identity linking, login method selection, provider dependencies, runtime lookup, account linking, recovery, merge behavior, and player lifecycle table changes deferred.
    ask_first:
      - Choosing a supported login method.
      - Adding credential tables, external identity tables, password hashing, cryptography, OAuth, OIDC, or provider dependencies.
      - Adding runtime credential lookup, account linking handlers, recovery flows, or merge behavior.
      - Changing player account lifecycle table shape.

  - id: W-0061
    milestone: M-011
    title: Define session persistence and WebSocket handshake decision gates
    status: completed
    change: changes/2026-05-14-define-session-persistence-websocket-handshake-decision-gates
    depends_on:
      - W-0060
    related_decisions:
      - ADR-0015
      - ADR-0018
      - ADR-0021
      - ADR-0023
    completion:
      - Define decision gates for session persistence and WebSocket handshake authentication without implementing either path.
      - Separate request-level validation, first-message validation, handshake validation, every-request validation, and hybrid validation as future choices.
      - Define what future artifacts are required for session store, expiration, refresh, revocation, reconnect, connection epoch, Protobuf envelope interaction, and WebSocket handshake behavior.
    completion_summary: Defined the session persistence and WebSocket handshake decision-gates standard with English and Simplified Chinese documentation; separated request-level, first-message, handshake-level, every-request, and hybrid validation as future choices without selecting a production model; defined future artifact gates for session store, expiration, refresh, revocation, reconnect, connection epoch, Protobuf envelope interaction, and WebSocket handshake behavior; updated manifests and agent guides while preserving metadata-only identity, credential-neutral WebSocket transport, unchanged Protobuf envelope behavior, no session tables, no session store, no handshake authentication, and no route-level authentication implementation.
    ask_first:
      - Choosing request-level, first-message, handshake-level, every-request, or hybrid validation as the production model.
      - Choosing a session persistence store or adding session tables.
      - Changing Protobuf envelope fields or adding handshake/system messages.
      - Adding WebSocket handshake authentication behavior or route-level auth implementation.

  - id: W-0062
    milestone: M-011
    title: Close authentication and token session validation design milestone
    status: completed
    change: changes/2026-05-14-close-authentication-token-session-validation-design-milestone
    depends_on:
      - W-0061
    related_decisions:
      - ADR-0019
      - ADR-0021
      - ADR-0022
      - ADR-0023
    completion:
      - Review M-011 completion criteria against the design standard, checks, semantic dimensions, credential/external identity boundaries, session persistence gates, WebSocket handshake gates, manifests, guides, and repository checks.
      - Mark M-011 completed only if runtime authentication, token parsing, credential storage, external identity linking, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime player handlers, and WebSocket routes remain deferred unless separately ratified.
      - Create a next-direction confirmation gate instead of starting implementation implicitly.
    completion_summary: Reviewed M-011 against its completion criteria and closed it as a design-only authentication/token/session validation milestone. The design standard, boundary checks, semantic dimensions, credential/external identity boundaries, session persistence and WebSocket handshake gates, manifests, module guidance, runtime guidance, and repository checks are in place. Runtime authentication, token parsing, credential storage, external identity linking, session persistence, Protobuf envelope changes, WebSocket handshake authentication, runtime player handlers, WebSocket routes, and production permission reliance on metadata-only identity remain deferred. M-012 and W-0063 now hold the queue at an explicit next-direction confirmation gate.
    ask_first:
      - Choosing the next implementation direction instead of creating a confirmation gate.
      - Starting production authentication implementation.
      - Adding runtime player account command/query handlers or WebSocket routes.
      - Choosing login methods, token format, credential storage, external identity linking, session persistence, Protobuf envelope changes, or WebSocket handshake authentication behavior.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0063
    milestone: M-012
    title: Confirm next milestone direction after authentication design
    status: completed
    change: changes/2026-05-14-confirm-next-direction-after-authentication-design
    depends_on:
      - W-0062
    related_decisions:
      - ADR-0019
      - ADR-0021
      - ADR-0022
      - ADR-0023
    block_reason: The next major direction requires maintainer confirmation because each candidate affects long-lived architecture, public behavior, runtime authority, or game-server capability scope.
    candidate_directions:
      - login_method_and_token_format_ratification
      - runtime_player_account_handlers_and_websocket_routes
      - session_persistence_and_validation_model_selection
      - websocket_handshake_authentication_design
      - credential_storage_external_identity_linking_implementation_design
      - additional_game_backend_modules_after_nakama_pitaya_review
      - operations_observability_admin_tooling
      - multiplayer_presence_matchmaking_planning
    selected_direction: login_method_and_token_format_ratification
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for login methods, token format, credential storage, external identity linking, session persistence, Protobuf envelope behavior, WebSocket handshake authentication, runtime player handlers, WebSocket routes, major dependencies, production permission, operations posture, multiplayer/presence/matchmaking scope, and major game-domain expansion decisions.
    completion_summary: The maintainer authorized the agent's professional recommendation to select `login_method_and_token_format_ratification`. M-013 was created with W-0064 as the first bounded design-standard work item. Authentication implementation, credential storage, external identity linking, session persistence, Protobuf envelope changes, WebSocket handshake authentication, runtime player handlers, WebSocket routes, major dependencies, production permission reliance on metadata-only identity, and game-domain expansion remain deferred.
    ask_first:
      - Starting production authentication implementation.
      - Choosing login methods, token format, credential storage, external identity linking, session persistence, Protobuf envelope behavior, or WebSocket handshake authentication behavior.
      - Adding runtime player account command/query handlers or WebSocket routes.
      - Adding a new game-domain module or multiplayer/presence/matchmaking milestone.
      - Introducing a new major dependency or architectural pattern.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0064
    milestone: M-013
    title: Define login method and token format ratification standard
    status: completed
    change: changes/2026-05-14-define-login-method-token-format-ratification-standard
    depends_on:
      - W-0063
    related_decisions:
      - ADR-0019
      - ADR-0021
      - ADR-0022
      - ADR-0023
      - ADR-0024
    completion:
      - Define the ratification standard for first login methods, token model, proof carrier boundaries, lifecycle semantics, storage implications, and implementation gates before code.
      - Compare Nakama authentication/session token capability coverage and Pitaya session binding vocabulary against vibit-native choices.
      - Plan the remaining M-013 work queue without adding runtime authentication, token parsing, credential storage, external identity linking, session persistence, Protobuf envelope changes, WebSocket handshake changes, runtime player handlers, or WebSocket routes.
    completion_summary: Defined the login method and token format ratification standard with English and Simplified Chinese documentation; created ADR-0024; mapped Nakama authentication/session-token capability coverage and Pitaya session-binding/handler vocabulary into vibit-native ratification inputs; registered the standard in manifests and agent guides; and planned the remaining M-013 queue without adding runtime authentication, token parsing, credential storage, external identity linking, session persistence, Protobuf envelope changes, WebSocket handshake authentication, runtime player handlers, or WebSocket routes.
    ask_first:
      - Implementing production authentication while defining the standard.
      - Adding credential tables, external identity tables, token tables, session tables, or migrations.
      - Adding password hashing, JWT, OAuth, OIDC, provider SDK, cryptography, key-management, Redis-like, or other major dependencies.
      - Changing Protobuf envelope behavior or WebSocket handshake authentication behavior.
      - Adding runtime player account handlers or WebSocket routes.
      - Declaring metadata-only identity sufficient for production permissions.

  - id: W-0065
    milestone: M-013
    title: Compare first login method candidates
    status: completed
    change: changes/2026-05-14-compare-first-login-method-candidates
    depends_on:
      - W-0064
    related_decisions:
      - ADR-0019
      - ADR-0023
      - ADR-0024
    completion:
      - Compare device credential, guest/anonymous, custom ID, email/password, external provider, and service-auth login families against vibit production safety, game-server ergonomics, Nakama capability coverage, and Pitaya session vocabulary.
      - Record benefits, risks, required artifacts, security concerns, abuse concerns, contract impact, storage impact, dependency impact, and implementation complexity for each candidate.
      - Recommend a first login-method set for W-0066 without implementing authentication, adding credential storage, adding dependencies, changing Protobuf/WebSocket behavior, or adding runtime player routes.
    completion_summary: Compared device credential, guest/anonymous, custom ID, email/password, external provider, and service-auth login families against production safety, game-server ergonomics, agent readability, Nakama capability coverage, and Pitaya session vocabulary. Recommended `device_credential_login` as the first login-method set for W-0066, with the credential treated as high-entropy secret proof material rather than a raw OS device ID, and deferred all other candidate families. No authentication implementation, credential storage, dependency, Protobuf/WebSocket behavior change, runtime player handler, or WebSocket route was added.
    ask_first:
      - Implementing any login method.
      - Adding credential tables, external identity tables, token tables, session tables, or migrations.
      - Adding password hashing, JWT, OAuth, OIDC, provider SDK, cryptography, key-management, Redis-like, or other major dependencies.
      - Changing Protobuf envelope behavior or WebSocket handshake authentication behavior.
      - Adding runtime player account handlers or WebSocket routes.

  - id: W-0066
    milestone: M-013
    title: Ratify first login method set
    status: completed
    change: changes/2026-05-14-ratify-first-login-method-set
    depends_on:
      - W-0065
    related_decisions:
      - ADR-0019
      - ADR-0023
      - ADR-0024
      - ADR-0025
    completion:
      - Ratify the first login-method set with public rationale, rejected alternatives, decision weights, confidence, known gaps, and implementation gates.
      - State whether each selected method is production-capable, bootstrap-only, or local-development-only.
      - State whether each selected method creates player accounts, links existing accounts, or authenticates existing accounts only.
      - Preserve implementation deferral until contracts, schemas, dependencies, tests, checks, and runtime ownership are planned.
    completion_summary: Ratified `device_credential_login` as the only first login-method set, defined it as proof of possession of a high-entropy installation credential rather than a raw device ID or metadata-only value, recorded rejected alternatives, decision weights, known gaps, account creation/authentication posture, and implementation gates, and added ADR-0025 plus bilingual documentation. No authentication implementation, credential storage, dependency, Protobuf/WebSocket behavior change, runtime player handler, or WebSocket route was added.
    ask_first:
      - Selecting a login method that requires a major dependency before a dependency adoption record exists.
      - Treating metadata-only identity as production proof.
      - Implementing login handlers while ratifying the set.
      - Adding runtime player routes or WebSocket routes.

  - id: W-0067
    milestone: M-013
    title: Compare token format and carrier options
    status: completed
    change: changes/2026-05-14-compare-token-format-and-carrier-options
    depends_on:
      - W-0066
    related_decisions:
      - ADR-0015
      - ADR-0018
      - ADR-0023
      - ADR-0024
    completion:
      - Compare opaque high-entropy tokens, signed structured tokens, external provider tokens, and plain session-ID-as-secret posture.
      - Compare login command payload, per-request payload, Protobuf envelope extension, current Session metadata fields, WebSocket handshake carriers, and first system-message carriers.
      - Recommend first token format and carrier posture for W-0068 without changing Protobuf, WebSocket handshake, or runtime code.
    completion_summary: Compared opaque high-entropy tokens, signed structured tokens, external provider tokens, and plain session-ID-as-secret posture, plus login command response, explicit request proof payload, Protobuf envelope extension, current Session metadata, WebSocket handshake, and first system-message carriers. Recommended `opaque_high_entropy_token` with `login_command_response_token` issuance and `explicit_request_proof_payload` request proof for W-0068, rejected current Session metadata as proof, and preserved implementation, Protobuf, and WebSocket handshake deferral.
    ask_first:
      - Adding JWT, signing, cryptography, OAuth, OIDC, provider SDK, key-management, or session-store dependencies.
      - Changing Protobuf envelope fields or using metadata-only session fields as proof.
      - Changing WebSocket handshake authentication behavior.
      - Implementing token parsing or validation.

  - id: W-0068
    milestone: M-013
    title: Ratify first token format and proof carrier posture
    status: completed
    change: changes/2026-05-14-ratify-first-token-format-and-proof-carrier-posture
    depends_on:
      - W-0067
    related_decisions:
      - ADR-0015
      - ADR-0018
      - ADR-0023
      - ADR-0024
      - ADR-0026
    completion:
      - Ratify the first token format and proof carrier posture with issuer, verifier, subject, audience, expiration, refresh, revocation, rotation, replay, redaction, and storage implications.
      - State whether Protobuf envelope behavior, current Session metadata fields, WebSocket handshake behavior, or first-message/system-message behavior remain unchanged or require a future decision.
      - Preserve implementation deferral until lifecycle, storage, contracts, schemas, checks, and tests are planned.
    completion_summary: Ratified `opaque_high_entropy_token` as the first access-token format, with future application-owned issuer and verifier boundaries, player account subject, vibit gameplay runtime audience, finite-expiration requirement, redaction requirement, and non-plaintext verifier storage requirement. Ratified `login_command_response_token` as the issuance carrier and `explicit_request_proof_payload` as the first request proof carrier. Refresh tokens, session token vocabulary, exact lifecycle, token storage schema, Protobuf envelope changes, WebSocket handshake authentication, first system-message binding, and runtime implementation remain deferred.
    ask_first:
      - Choosing a signed token model that needs key management without a dependency/adoption record.
      - Repurposing existing metadata-only Session fields as authenticated proof.
      - Changing Protobuf envelope or WebSocket handshake behavior in this step.
      - Implementing token parsing, signing, validation, refresh, or revocation.

  - id: W-0069
    milestone: M-013
    title: Define token lifecycle and storage implications
    status: completed
    change: changes/2026-05-14-define-token-lifecycle-and-storage-implications
    depends_on:
      - W-0068
    related_decisions:
      - ADR-0022
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
    completion:
      - Define token issuance, expiration, refresh, revocation, rotation, replay, logout, cleanup, redaction, and audit implications for the ratified token posture.
      - Define whether credential, token, external identity, or session storage is required later and what schema gates must exist first.
      - Preserve player account lifecycle tables as credential-free, token-free, external-identity-free, and session-free.
    completion_summary: Defined the first opaque access-token lifecycle posture with at least 256 bits of entropy, URL-safe unpadded Base64 or equivalent encoding, one-hour TTL, renewal by reauthenticating with the selected login method, refresh tokens excluded from the first implementation, revocation, rotation, replay controls, logout, cleanup, redaction, and audit required before implementation. Defined that credential storage and token verifier storage are required behind future schema gates, PostgreSQL is the default future durable token-verifier target, external identity and session storage are not required for the first posture, and player account lifecycle tables remain credential-free, token-free, external-identity-free, and session-free. No schema, migration, dependency, or runtime token behavior was added.
    ask_first:
      - Adding credential, token, external identity, or session tables.
      - Adding migrations.
      - Adding password hashing, token signing, key-management, Redis-like, OAuth, OIDC, or provider dependencies.
      - Implementing token lifecycle behavior.

  - id: W-0070
    milestone: M-013
    title: Define authentication contract error permission surfaces
    status: completed
    change: changes/2026-05-14-define-authentication-contract-error-permission-surfaces
    depends_on:
      - W-0069
    related_decisions:
      - ADR-0021
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
      - ADR-0028
    completion:
      - Define planned semantic contract surfaces for selected login methods, token issuance, token refresh, logout, token validation, and any account-linking behavior in scope.
      - Define required error and permission catalog surfaces before implementation.
      - Preserve current runtime code and Protobuf behavior unless a later implementation work item separately ratifies changes.
    completion_summary: Defined the runtime-owned authentication semantic contract family with command surfaces for device credential login, access-token validation, presented-token logout, and deferred refresh; audit-oriented event surfaces for authentication success/failure, token issuance, token validation failure, token revocation, and logout request; and authentication error and permission catalogs. Extended contract inspection/check tooling to recognize the runtime authentication family. Preserved implementation, Protobuf, WebSocket, generated output, schema, migration, handler, route, and dependency deferral.
    ask_first:
      - Adding runtime handlers or WebSocket routes.
      - Adding Protobuf wire messages or generated output before the contract/protocol boundary is selected.
      - Weakening existing session validation or permission boundaries.

  - id: W-0071
    milestone: M-013
    title: Define credential token session schema gates
    status: completed
    change: changes/2026-05-14-define-credential-token-session-schema-gates
    depends_on:
      - W-0070
    related_decisions:
      - ADR-0020
      - ADR-0022
      - ADR-0023
      - ADR-0024
      - ADR-0029
    completion:
      - Define the future schema gates for credential records, external identity links, token records, and session records required by the selected posture.
      - Separate schema ratification, migration sources, repository interfaces, adapters, live verification, and runtime wiring into future bounded work.
      - Preserve player account lifecycle schema without adding credential, provider subject, token, session, or WebSocket state columns.
    completion_summary: Defined schema gates for credential records, token verifier records, external identity links, runtime session records, audit persistence, and player account lifecycle preservation. Required future credential and token verifier schema ratification for the selected device credential and opaque access-token posture while deferring external identity storage, runtime session persistence, refresh-token storage, session-token vocabulary, WebSocket binding, migrations, repositories, adapters, runtime lookup, handlers, routes, Protobuf changes, and authentication implementation.
    ask_first:
      - Adding migration sources.
      - Changing existing player account lifecycle tables.
      - Choosing a new durable store or Redis-like dependency.
      - Implementing repositories or runtime lookup code.

  - id: W-0072
    milestone: M-013
    title: Add repository checks for selected login token boundaries
    status: completed
    change: changes/2026-05-14-add-selected-login-token-boundary-checks
    depends_on:
      - W-0071
    related_decisions:
      - ADR-0023
      - ADR-0024
      - ADR-0030
    completion:
      - Add or update repository checks that enforce selected login/token boundaries and prevent shortcut implementations where statically checkable.
      - Keep checks machine-readable and narrow enough for agents to act on.
      - Preserve existing runtime, generated, migration, protocol, and identity checks without requiring live external services by default.
    completion_summary: Added the runtime.selected_login_token_boundary repository check and rule catalog entry. The check verifies selected device credential login and opaque access-token posture markers, semantic-contract-only blocked-until markers, refresh-token deferral, generated authentication contract shape deferral, authentication Protobuf source deferral, WebSocket proof carrier deferral, credential/token/session/external-identity/audit migration deferral, player account lifecycle storage separation, forbidden authentication dependency additions, and stable machine-readable rule metadata without adding runtime authentication, token behavior, credential storage, token storage, session persistence, external identity linking, generated output, Protobuf messages, WebSocket routes, WebSocket handshake authentication, repositories, adapters, or live external-service requirements.
    ask_first:
      - Weakening existing architecture checks.
      - Making live PostgreSQL or external provider services mandatory for default checks.
      - Adding broad static-analysis dependencies.
      - Implementing authentication behavior while adding checks.

  - id: W-0073
    milestone: M-013
    title: Close login method token format ratification milestone
    status: completed
    change: changes/2026-05-14-close-login-method-token-format-ratification-milestone
    depends_on:
      - W-0072
    related_decisions:
      - ADR-0019
      - ADR-0023
      - ADR-0024
      - ADR-0031
    completion:
      - Review M-013 completion criteria against the standard, comparison artifacts, ratified login-method set, ratified token posture, lifecycle/storage implications, contract/error/permission surfaces, schema gates, checks, manifests, and guides.
      - Mark M-013 completed only if implementation boundaries remain deferred or explicitly planned for the next milestone.
      - Create the next implementation or confirmation gate rather than starting runtime authentication implicitly.
    completion_summary: Reviewed M-013 against its exit criteria, marked the login method and token format ratification milestone completed, and opened M-014 as the next credential/token schema ratification gate. Preserved implementation deferral for runtime authentication, handlers, routes, Protobuf changes, WebSocket changes, migrations, repositories, adapters, generated authentication shapes, and major authentication dependencies.
    ask_first:
      - Starting production authentication implementation while closing the milestone.
      - Adding runtime player handlers or WebSocket routes.
      - Changing Protobuf envelope or WebSocket handshake behavior.
      - Adding new major dependencies or data migrations.

  - id: W-0074
    milestone: M-014
    title: Define credential record schema boundary
    status: completed
    change: changes/2026-05-14-define-credential-record-schema-boundary
    depends_on:
      - W-0073
    related_decisions:
      - ADR-0025
      - ADR-0029
      - ADR-0031
    completion:
      - Ratify credential record ownership, lifecycle states, verifier semantics, uniqueness rules, player account relationship, disabled/revoked credential behavior, rotation/replacement semantics, redaction rules, and required future migration/repository/adapter gates.
      - Preserve PostgreSQL as the default durable target without adding migration source, repository interfaces, adapters, runtime lookup, handlers, routes, generated output, Protobuf messages, WebSocket behavior, or authentication dependencies.
      - Keep player account lifecycle tables credential-free and provider-subject-free.
    completion_summary: Ratified the credential record schema boundary for the selected device_credential_login posture. Defined runtime.authentication ownership, future logical table authentication_device_credentials, log-safe credential_record_id, non-plaintext lookup and verifier semantics, active/disabled/revoked/replaced lifecycle states, immutable player relationship, at most one active device credential per player for the first posture, rotation/replacement lineage, redaction rules, and future migration/repository/adapter gates. No migration source, table, repository interface, PostgreSQL adapter, runtime lookup, handler, route, generated authentication output, Protobuf message, WebSocket carrier, authentication dependency, or authentication behavior was added.
    ask_first:
      - Adding credential migration sources or tables.
      - Adding credential repository interfaces or PostgreSQL adapters.
      - Implementing credential lookup or login behavior.
      - Adding password hashing, OAuth, OIDC, provider SDK, key-management, or Redis-like dependencies.

  - id: W-0075
    milestone: M-014
    title: Define token verifier record schema boundary
    status: completed
    change: changes/2026-05-14-define-token-verifier-record-schema-boundary
    depends_on:
      - W-0074
    related_decisions:
      - ADR-0026
      - ADR-0027
      - ADR-0029
      - ADR-0032
    completion:
      - Ratify token verifier record ownership, non-plaintext verifier storage, statuses, expiration, revocation, credential-token linkage, retention, cleanup, replay-sensitive failure classes, redaction rules, and required future migration/repository/adapter gates.
      - Preserve PostgreSQL as the default durable target without adding migration source, repository interfaces, adapters, runtime validation, handlers, routes, generated output, Protobuf messages, WebSocket behavior, or authentication dependencies.
      - Keep refresh-token storage, runtime session persistence, WebSocket connection binding, and Redis-like token/session stores deferred for the first posture.
    completion_summary: Ratified the token verifier record schema boundary for the selected opaque access-token posture. Defined runtime.authentication ownership, future logical table authentication_access_tokens, log-safe token_record_id, non-plaintext lookup and verifier semantics, active/expired/revoked lifecycle states, player actor and credential linkage, immutable player and credential relationships, access-token TTL and expiration semantics, presented-token logout and revocation semantics, rotation support for previous active tokens on the same credential, seven-day expired/revoked retention recommendation, cleanup expectations, replay-sensitive failure classes, redaction rules, and future migration/repository/adapter gates. No migration source, table, repository interface, PostgreSQL adapter, runtime token validation, token issuance, logout, refresh, cleanup job, handler, route, generated authentication output, Protobuf message, WebSocket carrier, authentication dependency, or authentication behavior was added.
    ask_first:
      - Adding token verifier migration sources or tables.
      - Adding token repository interfaces or PostgreSQL adapters.
      - Implementing token issuance, validation, logout, refresh, or cleanup behavior.
      - Adding JWT, signing, key-management, Redis-like, OAuth, OIDC, or provider dependencies.

  - id: W-0076
    milestone: M-014
    title: Plan authentication schema migration queue
    status: completed
    change: changes/2026-05-14-plan-authentication-schema-migration-queue
    depends_on:
      - W-0075
    related_decisions:
      - ADR-0029
      - ADR-0032
      - ADR-0033
    completion:
      - Plan the credential and token verifier migration sequence, repository-interface gates, PostgreSQL adapter gates, redaction checks, live PostgreSQL verification expectations, and future work-item ordering now that both schema boundaries are ratified.
      - Preserve PostgreSQL as the default durable target without adding migration source, repository interfaces, adapters, runtime validation, handlers, routes, generated output, Protobuf messages, WebSocket behavior, or authentication dependencies.
      - Keep player account lifecycle tables credential-free, token-free, external-identity-free, session-free, WebSocket-state-free, and request-validation-free.
    completion_summary: Planned the remaining M-014 authentication schema migration queue. Established credential migration before token verifier migration, followed by authentication migration static checks, authentication repository interface boundary, authentication PostgreSQL adapter boundary, and M-014 closeout. Recorded docs/authentication-schema-migration-queue.md and ADR-0034. No migration source, table, repository interface, PostgreSQL adapter, runtime credential lookup, token issuance, token validation, logout, refresh, cleanup job, handler, route, generated authentication output, Protobuf message, WebSocket carrier, authentication dependency, or authentication behavior was added.
    ask_first:
      - Adding authentication migration sources or tables.
      - Adding authentication repository interfaces or PostgreSQL adapters.
      - Implementing credential lookup, token issuance, token validation, logout, refresh, or cleanup behavior.
      - Adding JWT, signing, key-management, Redis-like, OAuth, OIDC, password-hashing, or provider dependencies.
      - Changing Protobuf envelope, WebSocket proof carrier, or WebSocket handshake authentication behavior.

  - id: W-0077
    milestone: M-014
    title: Add credential PostgreSQL migration source
    status: completed
    change: changes/2026-05-14-add-credential-postgresql-migration-source
    depends_on:
      - W-0076
    related_decisions:
      - ADR-0029
      - ADR-0032
      - ADR-0034
    completion:
      - Add SQL-first migration source for the ratified authentication_device_credentials schema under runtime/migrations/postgres.
      - Include goose Up and Down sections, owning module trace, deterministic sequence number, constraints, indexes, redaction-safe column names, and player account lifecycle separation.
      - Preserve PostgreSQL as the default durable target without adding token verifier tables, repository interfaces, adapters, runtime lookup, handlers, routes, generated output, Protobuf messages, WebSocket behavior, or authentication dependencies.
    completion_summary: Added runtime/migrations/postgres/000003_create_authentication_device_credentials.sql as the SQL-first credential verifier migration source for the selected device_credential_login posture. It creates only authentication_device_credentials, records runtime.authentication ownership, stores non-plaintext credential lookup and verifier digests, enforces ratified lifecycle and at-most-one-active-per-player constraints, references player_accounts without modifying player lifecycle tables, and keeps token verifier tables, repository interfaces, PostgreSQL adapters, runtime credential lookup, login, token issuance, token validation, logout, refresh, cleanup, handlers, routes, generated authentication output, Protobuf messages, WebSocket behavior, authentication dependencies, and production authentication behavior deferred.
    ask_first:
      - Adding token verifier migration source in the credential migration step.
      - Adding authentication repository interfaces or PostgreSQL adapters.
      - Implementing credential lookup, login, token issuance, token validation, logout, refresh, or cleanup behavior.
      - Adding password hashing, JWT, signing, key-management, Redis-like, OAuth, OIDC, or provider dependencies.
      - Adding generic metadata columns or storing raw credential material.

  - id: W-0078
    milestone: M-014
    title: Add token verifier PostgreSQL migration source
    status: completed
    change: changes/2026-05-14-add-token-verifier-postgresql-migration-source
    depends_on:
      - W-0077
    related_decisions:
      - ADR-0029
      - ADR-0033
      - ADR-0034
    completion:
      - Add SQL-first migration source for the ratified authentication_access_tokens schema under runtime/migrations/postgres.
      - Include goose Up and Down sections, owning module trace, deterministic sequence number, constraints, indexes, redaction-safe column names, credential-token linkage, expiration, revocation, retention, and cleanup eligibility semantics.
      - Preserve PostgreSQL as the default durable target without adding repository interfaces, adapters, runtime validation, handlers, routes, generated output, Protobuf messages, WebSocket behavior, or authentication dependencies.
    completion_summary: Added runtime/migrations/postgres/000004_create_authentication_access_tokens.sql as the SQL-first opaque access-token verifier migration source for the selected token posture. It creates only authentication_access_tokens, records runtime.authentication ownership, stores non-plaintext token lookup and verifier digests, links tokens to player_accounts and authentication_device_credentials without modifying those tables, preserves expiration, revocation, rotation lineage, retention, and cleanup eligibility semantics, and keeps repository interfaces, PostgreSQL adapters, runtime credential lookup, token issuance, token validation, logout, refresh, cleanup, handlers, routes, generated authentication output, Protobuf messages, WebSocket behavior, authentication dependencies, and production authentication behavior deferred.
    ask_first:
      - Changing the ratified credential migration schema.
      - Adding authentication repository interfaces or PostgreSQL adapters.
      - Implementing token issuance, validation, logout, refresh, cleanup, or credential lookup behavior.
      - Adding JWT, signing, key-management, Redis-like, OAuth, OIDC, password-hashing, or provider dependencies.
      - Adding generic metadata columns or storing raw token material.

  - id: W-0079
    milestone: M-014
    title: Add authentication migration static checks
    status: completed
    change: changes/2026-05-14-add-authentication-migration-static-checks
    depends_on:
      - W-0078
    related_decisions:
      - ADR-0029
      - ADR-0032
      - ADR-0033
      - ADR-0034
    completion:
      - Add static repository checks for authentication migration sequence, owning module trace, goose sections, table names, forbidden raw secret columns, forbidden generic metadata columns, and player account lifecycle table separation.
      - Preserve JSON check output with forward-slash repository-relative paths on every platform.
      - Keep checks local by default without requiring live PostgreSQL, runtime authentication, repository interfaces, adapters, generated output, Protobuf messages, WebSocket behavior, or authentication dependencies.
    completion_summary: Hardened local migration checks for the ratified authentication migration sources. The check now validates the credential and token verifier migration sequence, owning module traces, goose sections, table references, required columns, required indexes, raw secret exclusions, generic metadata exclusions, and player account lifecycle table separation while keeping live PostgreSQL, repositories, adapters, runtime authentication, generated output, Protobuf, WebSocket behavior, and authentication dependencies deferred.
    ask_first:
      - Weakening existing migration, runtime, or selected login/token checks.
      - Making live PostgreSQL mandatory for default checks.
      - Adding broad static-analysis dependencies.
      - Implementing authentication behavior while adding checks.

  - id: W-0080
    milestone: M-014
    title: Define authentication repository interface boundary
    status: completed
    change: changes/2026-05-14-define-authentication-repository-interface-boundary
    depends_on:
      - W-0079
    related_decisions:
      - ADR-0029
      - ADR-0032
      - ADR-0033
      - ADR-0034
    completion:
      - Define storage-neutral authentication repository interface boundaries for credential and token verifier records after migrations and static checks exist.
      - Define mutation/query shapes, unit-of-work expectations, redaction rules, and future test expectations without adding PostgreSQL adapters or runtime authentication behavior.
      - Preserve selected login/token implementation deferral for handlers, routes, generated output, Protobuf messages, WebSocket behavior, and authentication dependencies.
    completion_summary: Added the storage-neutral authentication repository interface boundary under runtime/internal/modules/authentication with credential and token verifier record shapes, mutation/query shapes, repository methods, normalization helpers, focused tests, and module guides. PostgreSQL adapters, runtime credential lookup, token issuance, token validation, logout, refresh, cleanup jobs, handlers, routes, generated authentication output, Protobuf messages, WebSocket behavior, authentication dependencies, and production authentication behavior remain deferred.
    ask_first:
      - Implementing PostgreSQL authentication adapters in the repository-interface step.
      - Implementing credential lookup, token issuance, token validation, logout, refresh, or cleanup behavior.
      - Changing ratified migration schemas or table ownership.
      - Adding authentication dependencies or WebSocket/Protobuf behavior.

  - id: W-0081
    milestone: M-014
    title: Define authentication PostgreSQL adapter boundary
    status: completed
    change: changes/2026-05-14-define-authentication-postgresql-adapter-boundary
    depends_on:
      - W-0080
    related_decisions:
      - ADR-0029
      - ADR-0032
      - ADR-0033
      - ADR-0034
    completion:
      - Define PostgreSQL adapter source/test paths, constructors, executor ownership, transaction expectations, SQL operation scope, error mapping expectations, fake-executor tests, and optional live PostgreSQL verification expectations for authentication repositories.
      - Preserve runtime authentication implementation deferral and keep adapter implementation separate unless a later work item explicitly authorizes it.
    completion_summary: Defined the authentication PostgreSQL adapter boundary without implementing the adapter. The boundary reserves runtime/internal/platform/persistence/postgres/authentication_repository.go and its focused test file, specifies NewAuthenticationRepositoryForUnitOfWork, caller-owned executor and no transaction-control rules, first SQL operation scope for credential and token verifier persistence, error mapping expectations, and required tests while preserving runtime authentication, token issuance, token validation, logout, refresh, cleanup, handlers, routes, generated authentication output, Protobuf, WebSocket, dependency, and production behavior deferrals.
    ask_first:
      - Implementing adapter behavior in the boundary step.
      - Implementing runtime credential lookup, token issuance, token validation, logout, refresh, or cleanup behavior.
      - Changing ratified repository interface shape, migration schemas, table ownership, or transaction ownership model.
      - Making live PostgreSQL mandatory in default repository checks.

  - id: W-0082
    milestone: M-014
    title: Close credential and token verifier schema ratification milestone
    status: completed
    change: changes/2026-05-14-close-credential-token-verifier-schema-ratification-milestone
    depends_on:
      - W-0081
    related_decisions:
      - ADR-0029
      - ADR-0032
      - ADR-0033
      - ADR-0034
    completion:
      - Review M-014 completion criteria against credential schema, token verifier schema, migration queue, migration sources, static checks, repository boundary, adapter boundary, manifests, standards, and repository checks.
      - Mark M-014 completed only if runtime authentication, handlers, routes, generated output, Protobuf changes, WebSocket changes, production token validation, credential lookup, logout, refresh, cleanup, and major authentication dependencies remain deferred or are explicitly planned for a later milestone.
      - Create the next confirmation or implementation gate rather than starting runtime authentication implicitly.
    completion_summary: Reviewed M-014 against credential schema, token verifier schema, migration queue, migration sources, static checks, repository interface boundary, PostgreSQL adapter boundary, manifests, standards, and repository checks. Marked M-014 completed and opened M-015 as the bounded authentication PostgreSQL adapter implementation milestone. Runtime authentication, token issuance, token validation, logout execution, refresh, cleanup jobs, handlers, routes, generated authentication shapes, Protobuf messages, WebSocket proof carriers, WebSocket handshake authentication, major authentication dependencies, external identity storage, runtime session persistence, and production authentication behavior remain deferred.
    ask_first:
      - Starting production authentication implementation while closing the milestone.
      - Adding runtime player handlers or WebSocket routes.
      - Changing Protobuf envelope or WebSocket handshake behavior.
      - Adding new major dependencies or data migrations.

  - id: W-0083
    milestone: M-015
    title: Refine authentication adapter implementation checks
    status: completed
    change: changes/2026-05-14-refine-authentication-adapter-implementation-checks
    depends_on:
      - W-0082
    related_decisions:
      - ADR-0035
    completion:
      - Update repository checks so authentication PostgreSQL adapter files may use ratified credential and token verifier persistence vocabulary only inside the declared PostgreSQL platform boundary.
      - Preserve checks that forbid runtime authentication, token generation, token validation, verifier comparison, bearer parsing, WebSocket behavior, Protobuf behavior, generated authentication shapes, transaction ownership, and major authentication dependencies.
      - Keep JSON check output machine-readable and path-normalized for agents across platforms.
    completion_summary: Refined selected login/token boundary checks so declared authentication PostgreSQL adapter files can use ratified credential and token verifier persistence vocabulary inside the PostgreSQL platform package. Added adapter-only static checks that still forbid token generation, token validation, verifier comparison, bearer parsing, WebSocket behavior, Protobuf behavior, generated authentication shapes, transaction ownership, and major authentication dependencies. Runtime authentication and adapter implementation remain deferred.
    ask_first:
      - Weakening selected login/token boundary checks for runtime, protocol, transport, generated output, or domain-module code.
      - Authorizing token generation, verifier comparison, login, token validation, logout execution, refresh, cleanup jobs, WebSocket routes, Protobuf messages, or authentication dependencies while refining checks.

  - id: W-0084
    milestone: M-015
    title: Implement authentication PostgreSQL adapter
    status: completed
    change: changes/2026-05-14-implement-authentication-postgresql-adapter
    depends_on:
      - W-0083
    related_decisions:
      - ADR-0035
    completion:
      - Add runtime/internal/platform/persistence/postgres/authentication_repository.go implementing authentication.Repository with a caller-supplied executor.
      - Add focused fake-executor tests at runtime/internal/platform/persistence/postgres/authentication_repository_test.go.
      - Add UnitOfWork.NewAuthenticationRepository without exposing pgx to domain modules.
      - Preserve runtime authentication, token generation, token validation, logout execution, refresh, cleanup jobs, protocol, WebSocket, generated output, and dependency deferrals.
    completion_summary: Implemented the authentication PostgreSQL adapter under runtime/internal/platform/persistence/postgres with fake-executor tests for credential storage/lookup, token storage/lookup, credential and token revocation, cleanup eligibility, error mapping, UTC timestamp mapping, no transaction-control SQL, and default no-live-PostgreSQL behavior; added UnitOfWork.NewAuthenticationRepository without exposing pgx outside the PostgreSQL platform package; and kept runtime authentication, token generation, token validation, login, logout execution, refresh, cleanup jobs, protocol, WebSocket, generated authentication output, and authentication dependencies deferred.
    ask_first:
      - Implementing token generation, credential generation, verifier comparison, token validation, login, logout execution, refresh, or cleanup jobs.
      - Changing the authentication.Repository interface while implementing the adapter.
      - Changing migration schemas or transaction ownership.
      - Making live PostgreSQL mandatory in default checks.

  - id: W-0085
    milestone: M-015
    title: Close authentication PostgreSQL adapter implementation milestone
    status: completed
    change: changes/2026-05-14-close-authentication-postgresql-adapter-implementation-milestone
    depends_on:
      - W-0084
    related_decisions:
      - ADR-0035
    completion:
      - Review M-015 against adapter implementation, tests, manifests, standards, and repository checks.
      - Mark M-015 completed only if runtime authentication, protocol, WebSocket, generated output, login, token generation, token validation, logout execution, refresh, cleanup jobs, and major dependencies remain deferred or are explicitly planned for a later milestone.
      - Open the next bounded planning or implementation gate.
    completion_summary: Reviewed M-015 against the implemented authentication PostgreSQL adapter, focused tests, module manifest, persistence boundary standard, selected login/token boundary checks, runtime checks, work queue, and repository verification. Marked M-015 completed and opened M-016 as the runtime authentication implementation boundary planning milestone. Runtime authentication, login execution, token generation, token validation, verifier comparison, logout execution, refresh, cleanup jobs, Protobuf messages, WebSocket proof carriers, generated authentication shapes, major authentication dependencies, external identity storage, runtime session persistence, audit persistence, and production authentication behavior remain deferred.
    ask_first:
      - Starting runtime authentication implementation while closing the adapter milestone.
      - Adding Protobuf messages, WebSocket routes, WebSocket proof carriers, generated authentication shapes, token generation, token validation, or authentication dependencies.

  - id: W-0086
    milestone: M-016
    title: Define runtime authentication implementation boundary
    status: completed
    change: changes/2026-05-14-define-runtime-authentication-implementation-boundary
    depends_on:
      - W-0085
    related_decisions:
      - ADR-0023
      - ADR-0024
      - ADR-0030
      - ADR-0035
      - ADR-0036
    completion:
      - Define application service ownership, repository usage, token material lifecycle placeholders, verifier comparison boundaries, request identity handoff, error mapping, and test expectations for future runtime authentication.
      - Preserve separate gates for token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, generated authentication shapes, and dependency adoption.
      - Update manifests, standards, and repository checks only as planning artifacts.
    completion_summary: Defined the runtime authentication implementation boundary with English and Simplified Chinese standards, ADR-0036, manifest and guide references, application-owned future authentication orchestration, storage-neutral repository usage, persistence-only PostgreSQL adapter rules, request identity handoff, error/permission/audit mapping, Nakama/Pitaya alignment, and a separately gated future implementation queue. Runtime authentication, token generation, token validation, verifier comparison, login execution, logout execution, refresh, cleanup jobs, Protobuf messages, WebSocket proof carriers, generated authentication shapes, authentication dependencies, authentication repository changes, and migration schema changes remain deferred.
    ask_first:
      - Implementing runtime authentication behavior rather than defining the boundary.
      - Adding token generation, verifier comparison, token validation, login, logout execution, refresh, cleanup jobs, WebSocket routes, Protobuf messages, generated authentication shapes, or authentication dependencies.
      - Changing the authentication.Repository interface or ratified migration schemas.

  - id: W-0087
    milestone: M-016
    title: Close runtime authentication implementation boundary planning milestone
    status: completed
    change: changes/2026-05-14-close-runtime-authentication-implementation-boundary-planning-milestone
    depends_on:
      - W-0086
    related_decisions:
      - ADR-0036
      - ADR-0037
    completion:
      - Review M-016 against the runtime authentication implementation boundary standard, manifests, guides, checks, and work queue.
      - Mark M-016 completed only if runtime authentication, token generation, token validation, verifier comparison, login execution, logout execution, refresh, cleanup jobs, Protobuf messages, WebSocket proof carriers, generated authentication shapes, authentication dependencies, authentication repository changes, and migration schema changes remain deferred or are explicitly planned for later gated work.
      - Open the next bounded implementation-preparation or confirmation milestone without starting runtime authentication behavior implicitly.
    completion_summary: Reviewed M-016 against the runtime authentication implementation boundary standard, ADR-0036, manifests, guides, runtime boundary checks, and work queue. Marked M-016 and W-0087 completed, opened M-017 Authentication Generated Contract Shape Gate, and made W-0088 Decide authentication generated contract shape timing the next ready work item. Runtime authentication behavior, token generation, token validation, verifier comparison, login execution, logout execution, refresh, cleanup jobs, Protobuf authentication messages, WebSocket proof carriers, generated authentication shapes, authentication dependencies, repository interface changes, and migration schema changes remain deferred.
    ask_first:
      - Starting runtime authentication implementation while closing the boundary planning milestone.
      - Adding token generation, verifier comparison, token validation, login, logout execution, refresh, cleanup jobs, WebSocket routes, Protobuf messages, generated authentication shapes, authentication dependencies, repository interface changes, or migration schema changes.

  - id: W-0088
    milestone: M-017
    title: Decide authentication generated contract shape timing
    status: completed
    change: changes/2026-05-14-decide-authentication-generated-contract-shape-timing
    depends_on:
      - W-0087
    related_decisions:
      - ADR-0017
      - ADR-0028
      - ADR-0036
      - ADR-0037
      - ADR-0038
    completion:
      - Decide whether generated Go authentication contract shapes should be introduced before application service interfaces and runtime authentication behavior.
      - Define the allowed generated shape source, output path, immutable status, check requirements, and relationship to semantic authentication contracts if generation is recommended.
      - Preserve deferral for actual generated authentication shape files unless a later work item explicitly authorizes generation.
    completion_summary: Decided that generated Go authentication contract shapes should be introduced after the runtime authentication implementation boundary and before application authentication service interfaces or runtime authentication behavior. Recorded the source family `contracts/runtime/authentication`, planned family-aware output root `runtime/internal/generated/contracts/runtime/authentication`, immutability rules, check requirements, Nakama/Pitaya reference alignment, and next gated generation path in ADR-0038 and the paired timing standard. No generated authentication shape files, runtime authentication behavior, Protobuf messages, WebSocket carriers, authentication dependencies, repository changes, or migration schema changes were added.
    ask_first:
      - Generating authentication contract shape files rather than deciding timing and boundary.
      - Adding application authentication service code, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, or migration schema changes.

  - id: W-0089
    milestone: M-017
    title: Add runtime authentication contract shape generator support and output
    status: completed
    change: changes/2026-05-14-add-runtime-authentication-contract-shape-generator-support-and-output
    depends_on:
      - W-0088
    related_decisions:
      - ADR-0017
      - ADR-0028
      - ADR-0036
      - ADR-0038
    completion:
      - Extend generated contract shape generation, inspection, and generated-output checks for the runtime authentication family-aware output path defined by ADR-0038.
      - Generate metadata-only Go authentication contract shape files from `contracts/runtime/authentication/**` under `runtime/internal/generated/contracts/runtime/authentication/`.
      - Update runtime authentication boundary checks so only source-traced metadata-only generated authentication shapes are allowed while runtime behavior remains deferred.
      - Verify that no application authentication service code, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, or migration schema changes are added.
    completion_summary: Extended `tools/vibit` contract-shape generation, inspection, and generated-output drift checks for the runtime authentication family-aware path. Generated 12 metadata-only Go authentication contract shape files under `runtime/internal/generated/contracts/runtime/authentication/`, each source-traced to `contracts/runtime/authentication/**` with `Family: authentication`. Updated runtime selected login/token boundary checks to allow only generated metadata shapes from the authorized source family while preserving deferral for runtime authentication behavior, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, and migration schema changes.
    ask_first:
      - Changing the ADR-0038 source family or planned output root.
      - Adding application authentication service code, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior.

  - id: W-0090
    milestone: M-018
    title: Define application authentication service interface boundary
    status: completed
    change: changes/2026-05-15-define-application-authentication-service-interface-boundary
    depends_on:
      - W-0089
    related_decisions:
      - ADR-0028
      - ADR-0036
      - ADR-0038
      - ADR-0039
    completion:
      - Define the future application-owned authentication service interface boundary under `runtime/internal/app` without implementing behavior.
      - Map generated authentication contract shapes to service-level request/result vocabulary, redaction expectations, and error/permission/audit handoff rules.
      - Declare how the service boundary may use the application unit-of-work and `authentication.Repository` without moving authentication behavior into the repository or PostgreSQL adapter.
      - Preserve deferral for token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, and migration schema changes.
    completion_summary: Defined the application-owned authentication service interface boundary in ADR-0039 and the paired boundary standard. Mapped generated authentication contract shapes to future service request/result vocabulary, redaction expectations, unit-of-work usage, authentication.Repository usage, error/permission/audit handoff, and RequestIdentity handoff. Added a runtime check rule for the boundary and updated manifests and guides. No Go service code, runtime authentication behavior, token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, or migration schema changes were added.
    ask_first:
      - Implementing application authentication service code rather than defining its boundary.
      - Adding token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior.

  - id: W-0091
    milestone: M-019
    title: Define token and credential verifier algorithm redaction boundary
    status: completed
    change: changes/2026-05-15-define-token-credential-verifier-algorithm-redaction-boundary
    depends_on:
      - W-0090
    related_decisions:
      - ADR-0027
      - ADR-0036
      - ADR-0039
    completion:
      - Define verifier algorithm posture for device credential lookup/verifier material and opaque access-token lookup/verifier material without implementing generation or comparison behavior.
      - Define minimum entropy, token encoding, digest classification, key identifier, constant-time comparison, and redaction test expectations for later application service code.
      - Declare whether the first planned algorithms can use the Go standard library or whether an external dependency adoption record is required before implementation.
      - Preserve deferral for token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, and migration schema changes.
    completion_summary: Defined `vibit_hmac_sha256_v1` as the first planned high-entropy verifier algorithm family in ADR-0040 and the paired verifier algorithm/redaction boundary standard. Classified credential and token lookup digests as secret-adjacent index material, credential and token verifier digests as secret verifier material, and required purpose-label domain separation, minimum 256-bit raw credential/token entropy, URL-safe token encoding, constant-time verifier digest comparison, Go standard library dependency posture, and future redaction tests. No Go code, token generation, credential generation, verifier comparison, application authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket carriers, authentication dependencies, repository changes, migration changes, secret loading, or production authentication behavior were added.
    ask_first:
      - Selecting a major external cryptography, password-hashing, JWT, OAuth, OIDC, KMS, or provider dependency.
      - Implementing token generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, application authentication service code, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior.

  - id: W-0092
    milestone: M-020
    title: Define secret configuration and verifier key loading boundary
    status: completed
    change: changes/2026-05-15-define-secret-configuration-verifier-key-loading-boundary
    depends_on:
      - W-0091
    related_decisions:
      - ADR-0036
      - ADR-0039
      - ADR-0040
    completion:
      - Define secret configuration ownership, key loading posture, verifier key separation, key identifier handling, rotation expectations, and redaction requirements without implementing secret loading.
      - Declare whether the first local implementation may use process environment configuration or whether an external KMS/secret-manager dependency adoption record is required before implementation.
      - Define development/test key posture without committing production-like secrets or raw verifier values.
      - Preserve deferral for token generation, credential generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, and production authentication behavior.
    completion_summary: Defined the secret configuration and verifier key loading boundary in ADR-0041 and the paired boundary standard. Future key loading is application-owned under runtime/internal/app, the first local implementation may use process environment configuration or explicit runtime secret input after a later code gate, external KMS/secret-manager integration remains deferred behind dependency and operations gates, four distinct logical verifier keys are required, verifier_key_id is internal and not log-safe by default, production behavior must fail closed for invalid key configuration, and development/test keys must remain explicit without committed production-like secrets. No secret loading, environment parsing, token generation, credential generation, verifier comparison, application authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket carriers, authentication dependencies, repository changes, migration changes, KMS integration, secret-manager integration, or production authentication behavior were added.
    ask_first:
      - Selecting a major external KMS, cloud secret-manager, cryptography, password-hashing, JWT, OAuth, OIDC, provider, or operations dependency.
      - Implementing secret loading, token generation, credential generation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, application authentication service code, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior.

  - id: W-0093
    milestone: M-021
    title: Define token and credential material generation boundary
    status: completed
    change: changes/2026-05-15-define-token-credential-material-generation-boundary
    depends_on:
      - W-0092
    related_decisions:
      - ADR-0040
      - ADR-0041
    completion:
      - Define future application-owned token and credential material generation boundaries without implementing generation helpers.
      - Define raw material entropy enforcement, URL-safe text encoding, one-time presentation, storage prohibition, redaction, test fixture posture, and repository interaction expectations.
      - Declare whether future first-posture generation helpers can use Go standard library `crypto/rand` and `encoding/base64` without an external dependency adoption record.
      - Preserve deferral for secret loading, verifier digest computation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, and production authentication behavior.
    completion_summary: Defined the token and credential material generation boundary in ADR-0042 and the paired boundary standard. Future raw credential and access-token material generation is application-owned under runtime/internal/app; the first device credential and access token are server-issued and application-generated; raw material must be 32 cryptographically random bytes with at least 256 bits of entropy; text presentation is URL-safe unpadded Base64 or equivalent; raw material is one-time client-visible and must not be stored; repository handoff remains digest-only; future first-posture generation helpers may use Go standard library `crypto/rand` and `encoding/base64` after a later code gate. No token generation, credential generation, secret loading, verifier digest computation, verifier comparison, application authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket carriers, authentication dependencies, repository changes, migration changes, or production authentication behavior were added.
    ask_first:
      - Selecting a major external randomness, cryptography, password-hashing, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, or operations dependency.
      - Implementing token generation, credential generation, secret loading, verifier digest computation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, application authentication service code, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior.

  - id: W-0094
    milestone: M-022
    title: Define verifier digest computation and comparison boundary
    status: completed
    change: changes/2026-05-15-define-verifier-digest-computation-comparison-boundary
    depends_on:
      - W-0093
    related_decisions:
      - ADR-0040
      - ADR-0041
      - ADR-0042
    completion:
      - Define future application-owned verifier digest computation and constant-time comparison boundaries without implementing digest helpers.
      - Define canonical byte input construction, purpose-label use, logical key use, key-id selection, lookup digest handoff, verifier digest comparison, and failure redaction expectations.
      - Declare whether future first-posture digest helpers can use Go standard library `crypto/hmac`, `crypto/sha256`, and `crypto/subtle` without an external dependency adoption record.
      - Preserve deferral for token generation, credential generation, secret loading, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, and production authentication behavior.
    completion_summary: Defined the verifier digest computation and comparison boundary in ADR-0043 and the paired boundary standard. Future digest computation and verifier comparison is application-owned under runtime/internal/app; canonical HMAC input is versioned and length-prefixed; lookup and verifier digest classes remain separated across credential and token proof; validation may compute lookup candidates across active and accepted previous key sets while using stored verifier_key_id for verifier digest computation; lookup digest equality is record selection only; verifier digest comparison must be constant-time; invalid lookup, mismatch, unknown key id, unsupported algorithm, malformed proof, and expired or revoked proof collapse to the same public invalid-proof class; future first-posture helpers may use Go standard library `crypto/hmac`, `crypto/sha256`, and `crypto/subtle` after a later code gate. No verifier digest computation, verifier comparison, token generation, credential generation, secret loading, application authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket carriers, authentication dependencies, repository changes, migration changes, or production authentication behavior were added.
    ask_first:
      - Selecting a major external cryptography, password-hashing, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, or operations dependency.
      - Implementing verifier digest computation, verifier comparison, token generation, credential generation, secret loading, login execution, token validation, logout execution, cleanup jobs, application authentication service code, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, or production authentication behavior.

  - id: W-0095
    milestone: M-023
    title: Define authentication service implementation readiness gate
    status: completed
    change: changes/2026-05-15-define-authentication-service-implementation-readiness-gate
    depends_on:
      - W-0094
    related_decisions:
      - ADR-0036
      - ADR-0039
      - ADR-0040
      - ADR-0041
      - ADR-0042
      - ADR-0043
    completion:
      - Consolidate the first authentication service implementation entry criteria across service ownership, secret configuration, material generation, digest computation, verifier comparison, repository usage, redaction, tests, and deferrals.
      - Define the smallest safe first implementation queue without adding code.
      - Map Nakama and Pitaya authentication/session references into the readiness checklist.
      - Preserve deferral for authentication service code, secret loading, token generation, credential generation, verifier digest computation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, and production authentication behavior.
    completion_summary: Defined the authentication service implementation readiness gate in ADR-0044 and the paired readiness standard. The gate consolidates required prior boundaries, application-owned package candidates, first-slice write boundaries, forbidden write areas, recommended implementation queue, local verifier key configuration loading as the next recommended gate, service behavior entry criteria, required test classes, redaction and observability constraints, and Nakama/Pitaya capability mapping. No authentication service code, secret loading, token generation, credential generation, verifier digest computation, verifier comparison, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket carriers, authentication dependencies, repository changes, migration changes, or production authentication behavior were added.
    ask_first:
      - Starting implementation code while defining readiness.
      - Selecting a major external dependency.
      - Changing repository interfaces, migration schemas, Protobuf messages, WebSocket proof carriers, or runtime authentication behavior.

  - id: W-0096
    milestone: M-024
    title: Define local verifier key configuration loading gate
    status: completed
    change: changes/2026-05-15-define-local-verifier-key-configuration-loading-gate
    depends_on:
      - W-0095
    related_decisions:
      - ADR-0041
      - ADR-0043
      - ADR-0044
    completion:
      - Define the first local verifier key configuration loading code gate without implementing secret loading.
      - Choose whether the first implementation gate accepts explicit in-memory configuration, process environment configuration, or a sequenced combination.
      - Define validation, redaction, tests, package ownership, file boundaries, and failure behavior for the future code slice.
      - Preserve deferral for token generation, credential generation, verifier digest computation, verifier comparison, authentication service code, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, authentication dependencies, repository interface changes, migration schema changes, external secret-manager integrations, and production authentication behavior.
    completion_summary: Defined the local verifier key configuration loading gate in ADR-0045 and the paired gate standard. The first future implementation slice is explicit in-memory verifier key set validation with already-decoded key bytes; process environment parsing and Base64 text decoding are deferred to a follow-up adapter gate. The future owner is runtime/internal/app/authentication; expected future files are verifier_key_config.go and verifier_key_config_test.go. The gate defines required logical keys, key length, duplicate-key rejection, all-zero rejection, repeated-byte rejection, input copying, immutable accessor expectations, redacted errors, required tests, dependency posture, and Nakama/Pitaya mapping. No Go code, environment parsing, secret loading, token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket carriers, authentication dependencies, repository changes, migration changes, KMS integration, secret-manager integration, or production authentication behavior were added.
    ask_first:
      - Implementing secret loading while defining the gate.
      - Selecting a major external KMS, cloud secret-manager, operations, cryptography, password-hashing, JWT, OAuth, OIDC, or provider dependency.
      - Changing repository interfaces, migration schemas, Protobuf messages, WebSocket proof carriers, or runtime authentication behavior.

  - id: W-0097
    milestone: M-025
    title: Implement explicit in-memory verifier key set validator
    status: completed
    change: changes/2026-05-15-implement-explicit-in-memory-verifier-key-set-validator
    depends_on:
      - W-0096
    related_decisions:
      - ADR-0041
      - ADR-0043
      - ADR-0044
      - ADR-0045
    completion:
      - Implement the explicit in-memory verifier key set validator under runtime/internal/app/authentication without adding environment parsing or runtime authentication behavior.
      - Add tests for accepted complete distinct key sets, input copying, immutable accessors, missing key set id, missing keys, short keys, duplicate logical keys, all-zero keys, repeated single-byte keys, and redacted errors.
      - Adjust runtime checks only as needed to permit the ADR-0045-authorized validator while preserving deferrals for environment parsing, token generation, credential generation, digest helpers, verifier comparison, service behavior, protocol carriers, repository changes, migrations, external secret managers, and production behavior.
    completion_summary: Implemented runtime/internal/app/authentication/verifier_key_config.go and verifier_key_config_test.go. The new VerifierKeySetConfig and VerifierKeySet validate explicit in-memory, already-decoded key bytes; require a non-empty key set id; require four logical keys; enforce a 32-byte minimum; reject missing, short, duplicate, all-zero, and repeated single-byte keys; copy input key material; return copies from accessors; and expose redacted validation errors. Tests cover accepted complete distinct key sets, key set id trimming, input copying, immutable accessors, missing key set id, missing keys, short keys, duplicate keys, all-zero keys, repeated-byte keys, and redaction. Runtime checks now allow only the ADR-0045-authorized validator file while preserving deferrals for environment parsing, Base64 text decoding, startup wiring, token generation, credential generation, digest helpers, verifier comparison, service behavior, protocol carriers, repository changes, migrations, external secret managers, dependencies, and production behavior.
    ask_first:
      - Parsing process environment variables or reading local secret files.
      - Selecting a major external KMS, cloud secret-manager, operations, cryptography, password-hashing, JWT, OAuth, OIDC, provider, or dotenv dependency.
      - Implementing token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, repository changes, migration changes, or production authentication behavior.

  - id: W-0098
    milestone: M-026
    title: Define environment verifier key loader gate
    status: completed
    change: changes/2026-05-15-define-environment-verifier-key-loader-gate
    depends_on:
      - W-0097
    related_decisions:
      - ADR-0041
      - ADR-0045
    completion:
      - Define the process environment verifier key loader gate without implementing environment parsing or Base64 text decoding.
      - Declare the environment variable names, decoding posture, redaction rules, test cases, package ownership, file boundaries, and failure behavior for the future loader.
      - Require the future loader to call the explicit in-memory validator instead of duplicating key-set validation logic.
      - Preserve deferral for startup wiring, KMS, cloud secret-manager integration, token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, protocol carriers, repository changes, migrations, authentication dependencies, and production authentication behavior.
    completion_summary: Defined the environment verifier key loader gate in docs/environment-verifier-key-loader-gate.md, docs/environment-verifier-key-loader-gate.zh-CN.md, and ADR-0046. The gate declares the future process environment variables VIBIT_AUTH_VERIFIER_KEY_SET_ID, VIBIT_AUTH_CREDENTIAL_LOOKUP_KEY, VIBIT_AUTH_CREDENTIAL_VERIFIER_KEY, VIBIT_AUTH_TOKEN_LOOKUP_KEY, and VIBIT_AUTH_TOKEN_VERIFIER_KEY; sets Base64URL unpadded as preferred encoding with standard padded Base64 compatibility; requires future handoff to NewVerifierKeySet; defines future files verifier_key_env.go and verifier_key_env_test.go; requires redacted loader errors and focused tests; and preserves deferrals for startup wiring, local secret files, dotenv behavior, CLI secret input, KMS, cloud secret-manager integration, token generation, credential generation, digest helpers, verifier comparison, authentication service behavior, protocol carriers, repository changes, migrations, dependencies, and production behavior. No environment parsing, Base64 decoding, Go loader code, startup wiring, external dependency, or runtime authentication behavior was added.
    ask_first:
      - Implementing process environment parsing while defining the gate.
      - Reading local secret files or adding dotenv behavior.
      - Selecting a major external KMS, cloud secret-manager, operations, cryptography, password-hashing, JWT, OAuth, OIDC, provider, or dotenv dependency.
      - Implementing startup wiring, token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, repository changes, migration changes, or production authentication behavior.

  - id: W-0099
    milestone: M-027
    title: Implement environment verifier key loader
    status: completed
    change: changes/2026-05-15-implement-environment-verifier-key-loader
    depends_on:
      - W-0098
    related_decisions:
      - ADR-0045
      - ADR-0046
    completion:
      - Implement the environment verifier key loader under runtime/internal/app/authentication without startup wiring or runtime authentication behavior.
      - Add a testable lookup-function loader and, if still small, a tiny process environment adapter.
      - Decode supported Base64 key text and call NewVerifierKeySet instead of duplicating key-set validation logic.
      - Add focused tests for complete key sets, missing variables, invalid encoded keys, validator handoff failures, redaction, and unwired process adapter behavior.
      - Preserve deferral for startup wiring, local secret files, dotenv behavior, CLI secret input, KMS, cloud secret-manager integration, token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, protocol carriers, repository changes, migrations, authentication dependencies, and production authentication behavior.
    completion_summary: Implemented runtime/internal/app/authentication/verifier_key_env.go and verifier_key_env_test.go. The loader defines the required VIBIT_AUTH environment variable names, accepts an EnvLookup for tests, exposes LoadVerifierKeySetFromEnvironment and LoadVerifierKeySetFromProcessEnvironment, decodes Base64URL unpadded and standard padded Base64 key text, calls NewVerifierKeySet instead of duplicating key validation, wraps missing variable, invalid encoding, and invalid key set failures in redacted typed errors, and keeps the process environment adapter unwired from startup. Tests cover valid Base64URL and standard Base64 key sets, nil lookup, missing variables, invalid encoded key text, short decoded key, duplicate decoded keys, all-zero key, repeated-byte key, empty key set id, error redaction, and the process environment adapter.
    ask_first:
      - Wiring the loader into process startup.
      - Reading local secret files, adding dotenv behavior, or accepting CLI secret input.
      - Selecting a major external KMS, cloud secret-manager, operations, cryptography, password-hashing, JWT, OAuth, OIDC, provider, or dotenv dependency.
      - Implementing token generation, credential generation, verifier digest computation, verifier comparison, authentication service behavior, login execution, token validation, logout execution, cleanup jobs, Protobuf messages, WebSocket proof carriers, repository changes, migration changes, or production authentication behavior.

  - id: W-0100
    milestone: M-028
    title: Define token and credential material generation implementation gate
    status: completed
    change: changes/2026-05-15-define-token-credential-material-generation-implementation-gate
    depends_on:
      - W-0099
    related_decisions:
      - ADR-0042
      - ADR-0044
    completion:
      - Define the implementation gate for token and credential material generation helpers without adding generation code.
      - Name future helper files, function boundaries, entropy source, URL-safe encoding posture, redaction rules, and test requirements.
      - Preserve deferral for verifier digest computation, verifier comparison, authentication service behavior, protocol carriers, repository changes, migrations, startup wiring, authentication dependencies, and production authentication behavior.
    completion_summary: Defined the token and credential material generation implementation gate in docs/token-credential-material-generation-implementation-gate.md, docs/token-credential-material-generation-implementation-gate.zh-CN.md, and ADR-0047. The gate names future helper files runtime/internal/app/authentication/material_generation.go and material_generation_test.go, requires explicit io.Reader entropy-source handoff, crypto/rand.Reader production posture, io.ReadFull, 32-byte raw material, URL-safe unpadded Base64 presentation, 43-character encoded text, copied raw bytes, redacted errors, all-zero and repeated-single-byte rejection, and focused tests. It preserves deferrals for digest computation, verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior. No generation code was added.
    ask_first:
      - Implementing token or credential generation code instead of only defining the gate.
      - Changing public contracts, repositories, migrations, protocol carriers, or startup wiring.
      - Adding a major external randomness, cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, or dotenv dependency.

  - id: W-0101
    milestone: M-029
    title: Implement token and credential material generation helpers
    status: completed
    change: changes/2026-05-15-implement-token-credential-material-generation-helpers
    depends_on:
      - W-0100
    related_decisions:
      - ADR-0042
      - ADR-0047
    completion:
      - Implement helper-only token and credential material generation under runtime/internal/app/authentication/material_generation.go.
      - Add focused tests under runtime/internal/app/authentication/material_generation_test.go.
      - Use explicit io.Reader handoff, 32-byte raw material, URL-safe unpadded Base64 text, copied raw bytes, redacted errors, and weak generated material rejection.
      - Preserve deferral for digest computation, verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
    completion_summary: Implemented runtime/internal/app/authentication/material_generation.go and runtime/internal/app/authentication/material_generation_test.go. The helper-only slice adds MaterialKind, GeneratedSecretMaterial, GenerateDeviceCredentialMaterial, GenerateAccessTokenMaterial, and redacted MaterialGenerationError classes. Generation uses an explicit io.Reader, io.ReadFull, 32 raw bytes, URL-safe unpadded Base64 text, copied raw bytes, and weak repeated-byte rejection. Tests cover byte length, encoding, round-trip, 43-character presentation, kind preservation, copy safety, nil/error/short-read failure, all-zero and repeated-byte failure, progressing-source non-constant output, redaction, and no digest/comparison behavior. No digest computation, verifier comparison, authentication service behavior, protocol carriers, repository changes, migrations, startup wiring, dependencies, or production authentication behavior was added.
    ask_first:
      - Computing lookup or verifier digests.
      - Comparing verifier material.
      - Implementing authentication service behavior, login execution, token validation, logout, refresh, cleanup, or production authentication behavior.
      - Changing public contracts, repositories, migrations, protocol carriers, or startup wiring.
      - Adding a major external randomness, cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, or password-hashing dependency.

  - id: W-0102
    milestone: M-030
    title: Define verifier digest helper implementation gate
    status: completed
    change: changes/2026-05-15-define-verifier-digest-helper-implementation-gate
    depends_on:
      - W-0101
    related_decisions:
      - ADR-0040
      - ADR-0041
      - ADR-0043
      - ADR-0047
      - ADR-0048
    completion:
      - Define the implementation gate for lookup and verifier digest helpers without adding digest code.
      - Name future helper files, canonical input construction boundaries, verifier key handoff, redaction rules, and focused tests.
      - Preserve deferral for verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
    completion_summary: Defined the verifier digest helper implementation gate in docs/verifier-digest-helper-implementation-gate.md, docs/verifier-digest-helper-implementation-gate.zh-CN.md, and ADR-0048. The gate names future helper files runtime/internal/app/authentication/verifier_digest.go and verifier_digest_test.go, defines the canonical input construction with versioned ASCII header, null separator, length-prefixed purpose label and raw material, requires HMAC-SHA-256 with the matching logical key from an already-validated VerifierKeySet, defines four digest classes with registered purpose labels and key mappings, requires copied 32-byte digest output, redacted errors, and focused tests. It preserves deferrals for verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior. No digest computation code was added.
    ask_first:
      - Implementing verifier digest computation code instead of only defining the gate.
      - Comparing verifier material.
      - Implementing authentication service behavior, login execution, token validation, logout, refresh, cleanup, or production authentication behavior.
      - Changing public contracts, repositories, migrations, protocol carriers, or startup wiring.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, or password-hashing dependency.

  - id: W-0103
    milestone: M-031
    title: Implement verifier digest computation helpers
    status: completed
    change: changes/2026-05-15-implement-verifier-digest-computation-helpers
    depends_on:
      - W-0102
    related_decisions:
      - ADR-0040
      - ADR-0043
      - ADR-0048
    completion:
      - Implement helper-only lookup and verifier digest computation under runtime/internal/app/authentication/verifier_digest.go.
      - Add focused tests under runtime/internal/app/authentication/verifier_digest_test.go.
      - Use deterministic canonical input with versioned header, null separator, length-prefixed purpose label and raw material.
      - Compute HMAC-SHA-256 with the matching logical key from an already-validated VerifierKeySet.
      - Return copied 32-byte digest output with redacted errors.
      - Preserve deferral for verifier comparison, authentication service behavior, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
    completion_summary: Implemented runtime/internal/app/authentication/verifier_digest.go and runtime/internal/app/authentication/verifier_digest_test.go. The helpers expose ComputedDigest, DigestClass, VerifierDigestError, ComputeCredentialLookupDigest, ComputeCredentialVerifierDigest, ComputeTokenLookupDigest, and ComputeTokenVerifierDigest. Canonical input uses the literal vibit.auth.verifier.input.v1 header, null separator, uint16 big-endian purpose-label length, purpose-label bytes, uint16 big-endian raw-material length, and raw material bytes before HMAC-SHA-256. Each helper uses the matching VerifierKeySet accessor, returns copied 32-byte digest bytes, rejects missing key sets and empty raw material with redacted typed errors, and does not compare verifier digests or implement authentication service behavior. Tests cover canonical input, purpose separation, key mapping, output length, sensitivity to key/raw changes, copy safety, fail-closed behavior, redaction, and no comparison behavior.
    ask_first:
      - Comparing verifier digests.
      - Implementing authentication service behavior, login execution, token validation, logout, refresh, cleanup, or production authentication behavior.
      - Changing public contracts, repositories, migrations, protocol carriers, or startup wiring.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, or password-hashing dependency.

  - id: W-0104
    milestone: M-032
    title: Define verifier digest comparison helper gate
    status: completed
    change: changes/2026-05-15-define-verifier-digest-comparison-helper-gate
    depends_on:
      - W-0103
    related_decisions:
      - ADR-0040
      - ADR-0043
      - ADR-0048
      - ADR-0049
    completion:
      - Define the implementation gate for constant-time verifier digest comparison helpers without adding comparison code.
      - Name future helper ownership, file boundaries, allowed constant-time primitive, redaction rules, failure posture, and focused tests.
      - Preserve deferral for authentication service behavior, login execution, token validation, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
    completion_summary: Defined the verifier digest comparison helper gate in docs/verifier-digest-comparison-helper-gate.md, docs/verifier-digest-comparison-helper-gate.zh-CN.md, and ADR-0049. The gate names future helper files runtime/internal/app/authentication/verifier_comparison.go and verifier_comparison_test.go, keeps verifier_digest.go computation-only, requires crypto/hmac.Equal as the preferred constant-time primitive, allows crypto/subtle.ConstantTimeCompare as an equivalent alternative, forbids raw material, lookup digest, key id, database-only, and protocol metadata comparison, defines redacted failure posture, and preserves all service, protocol, repository, migration, startup, dependency, and production behavior deferrals. No verifier comparison code was added.
    ask_first:
      - Implementing verifier comparison code instead of only defining the gate.
      - Implementing authentication service behavior, login execution, token validation, logout, refresh, cleanup, or production authentication behavior.
      - Changing public contracts, repositories, migrations, protocol carriers, or startup wiring.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, or password-hashing dependency.

  - id: W-0105
    milestone: M-033
    title: Implement verifier digest comparison helpers
    status: completed
    change: changes/2026-05-15-implement-verifier-digest-comparison-helpers
    depends_on:
      - W-0104
    related_decisions:
      - ADR-0040
      - ADR-0043
      - ADR-0048
      - ADR-0049
    completion:
      - Implement helper-only verifier digest comparison under runtime/internal/app/authentication/verifier_comparison.go.
      - Add focused tests under runtime/internal/app/authentication/verifier_comparison_test.go.
      - Compare only computed credential/token verifier digests against stored verifier digest bytes with crypto/hmac.Equal or an equivalent constant-time primitive.
      - Reject lookup digest classes, wrong verifier classes, missing input, malformed stored digest bytes, and invalid comparison input with redacted errors.
      - Preserve deferral for authentication service behavior, login execution, token validation, protocol carriers, repositories, migrations, startup wiring, authentication dependencies, and production authentication behavior.
    completion_summary: Implemented runtime/internal/app/authentication/verifier_comparison.go and verifier_comparison_test.go. The helpers expose VerifierComparisonResult, VerifierComparisonError, CompareCredentialVerifierDigest, and CompareTokenVerifierDigest. They use crypto/hmac.Equal to compare ComputedDigest verifier bytes with stored verifier digest bytes, reject lookup digest classes, wrong verifier classes, missing computed input, malformed computed input, missing stored input, and malformed stored input with redacted typed errors, and return mismatch as a redacted non-match result. Tests cover credential and token matches and mismatches, hmac.Equal usage, lookup and wrong-class rejection, missing and malformed input, raw-material API exclusion, redaction, and absence of authentication service behavior. Authentication service behavior, login execution, token validation, protocol carriers, repositories, migrations, startup wiring, dependencies, and production authentication behavior remain deferred.
    ask_first:
      - Implementing authentication service behavior, login execution, token validation, logout, refresh, cleanup, or production authentication behavior.
      - Changing public contracts, repositories, migrations, protocol carriers, or startup wiring.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, or password-hashing dependency.

  - id: W-0106
    milestone: M-034
    title: Define authentication service behavior implementation gate
    status: completed
    change: changes/2026-05-15-define-authentication-service-behavior-implementation-gate
    depends_on:
      - W-0105
    related_decisions:
      - ADR-0036
      - ADR-0039
      - ADR-0040
      - ADR-0041
      - ADR-0042
      - ADR-0043
      - ADR-0044
      - ADR-0049
      - ADR-0050
    completion:
      - Define the implementation gate for future authentication service behavior without adding service code.
      - Name future ownership, file boundaries, repository handoff, helper composition flow, public error collapse, redaction rules, and focused tests.
      - Preserve deferral for login execution, token validation, logout, refresh, cleanup, protocol carriers, repository changes, migrations, startup wiring, dependencies, and production authentication behavior unless a later work item explicitly authorizes each slice.
    completion_summary: Defined the authentication service behavior implementation gate in docs/authentication-service-behavior-implementation-gate.md, docs/authentication-service-behavior-implementation-gate.zh-CN.md, and ADR-0050. The gate names future service files runtime/internal/app/authentication/service.go and service_test.go, keeps service behavior application-owned, requires repository usage through the application unit-of-work boundary, maps existing verifier key, material generation, digest computation, and verifier comparison helpers into future login and access-token validation flows, defines public proof failure collapse, redaction requirements, request identity handoff, and focused tests, and preserves deferrals for login execution, token validation execution, logout, refresh, cleanup, protocol carriers, repository changes, migrations, startup wiring, dependencies, and production authentication behavior. No service code was added.
    ask_first:
      - Implementing authentication service behavior instead of only defining the gate.
      - Changing public contracts, repositories, migrations, protocol carriers, or startup wiring.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, password-hashing, Redis-like, queue, or session-store dependency.

  - id: W-0107
    milestone: M-035
    title: Implement authentication service behavior skeleton
    status: completed
    change: changes/2026-05-16-implement-authentication-service-behavior-skeleton
    depends_on:
      - W-0106
    related_decisions:
      - ADR-0036
      - ADR-0039
      - ADR-0040
      - ADR-0041
      - ADR-0042
      - ADR-0043
      - ADR-0044
      - ADR-0050
    completion:
      - Add skeleton-only service shape under runtime/internal/app/authentication/service.go.
      - Add focused tests under runtime/internal/app/authentication/service_test.go.
      - Define typed dependencies, request/result vocabulary, internal redacted failure classes, and fail-closed not-implemented behavior.
      - Preserve deferral for real login execution, access-token validation execution, token issuance, repository mutation/query behavior, logout, refresh, cleanup, protocol carriers, repository changes, migrations, startup wiring, dependencies, and production authentication behavior.
    completion_summary: Implemented runtime/internal/app/authentication/service.go and service_test.go. The skeleton exposes Service, ServiceDependencies, UnitOfWorkRunner, ServiceError, reserved operation names, request/result vocabulary, redacted internal failure classes, and public error codes. AuthenticateWithDeviceCredential, ValidateAccessToken, and LogoutAccessToken fail closed with AUTHENTICATION_NOT_IMPLEMENTED; RefreshAccessToken fails closed with AUTHENTICATION_REFRESH_NOT_SUPPORTED. Tests prove nil dependency rejection, no unit-of-work calls, no proof leakage, no token issuance, no validated identity output, no revocation, no refresh output, and reserved vocabulary presence. Repository orchestration, login execution, access-token validation, token issuance, logout execution, cleanup, protocol carriers, startup wiring, dependencies, migrations, and production authentication behavior remain deferred.
    ask_first:
      - Implementing real device credential login instead of skeleton-only not-implemented behavior.
      - Implementing real access-token validation instead of skeleton-only not-implemented behavior.
      - Calling repositories for real behavior, changing repository interfaces, changing PostgreSQL adapters, or changing migrations.
      - Exposing Protobuf authentication messages, WebSocket proof carriers, or startup wiring.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, password-hashing, Redis-like, queue, or session-store dependency.

  - id: W-0108
    milestone: M-036
    title: Define device credential login service behavior gate
    status: completed
    change: changes/2026-05-16-define-device-credential-login-service-behavior-gate
    depends_on:
      - W-0107
    related_decisions:
      - ADR-0036
      - ADR-0039
      - ADR-0040
      - ADR-0041
      - ADR-0042
      - ADR-0043
      - ADR-0044
      - ADR-0050
      - ADR-0051
    completion:
      - Define the future device credential login service behavior gate without adding login execution code.
      - Name the helper composition sequence, repository handoff sequence, token issuance posture, public error collapse posture, redaction requirements, and focused tests required before login execution may be implemented.
      - Preserve deferral for access-token validation execution, logout, refresh, cleanup, protocol carriers, repository interface changes, migrations, startup wiring, dependencies, and production authentication behavior.
    completion_summary: Defined the future device credential login service behavior gate without adding login execution code. The gate establishes server-issued Base64URL unpadded 32-byte proof input, proof pre-validation before unit-of-work, credential lookup digest computation, authentication and player repository handoff through unit-of-work capabilities, verifier digest comparison before token generation, active credential and active player account requirements, opaque access-token generation and digest-only storage, one-time token return after unit-of-work success, public error collapse, redaction rules, and required tests. Access-token validation, logout, refresh, cleanup, protocol carriers, repository interface changes, migrations, startup wiring, dependencies, and production behavior remain deferred.
    ask_first:
      - Implementing real device credential login instead of only defining the gate.
      - Changing service method signatures, public contracts, repository interfaces, PostgreSQL adapters, migrations, Protobuf messages, WebSocket carriers, or startup wiring.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, password-hashing, Redis-like, queue, or session-store dependency.

  - id: W-0109
    milestone: M-037
    title: Implement device credential login service behavior
    status: completed
    change: changes/2026-05-16-implement-device-credential-login-service-behavior
    depends_on:
      - W-0108
    related_decisions:
      - ADR-0036
      - ADR-0039
      - ADR-0040
      - ADR-0041
      - ADR-0042
      - ADR-0043
      - ADR-0044
      - ADR-0050
      - ADR-0051
    completion:
      - Implement device credential login inside runtime/internal/app/authentication/service.go using the ADR-0051 sequence.
      - Add focused tests under runtime/internal/app/authentication/service_test.go for proof rejection, helper ordering, unit-of-work repository handoff, public error collapse, active player account requirement, digest-only token storage, redaction, and token return only after successful storage and commit.
      - Preserve deferral for access-token validation, logout, refresh, cleanup, protocol carriers, repository interface changes, migrations, startup wiring, dependencies, generated files, and broader production authentication behavior.
    completion_summary: Implemented the ADR-0051 device credential login sequence inside runtime/internal/app/authentication/service.go and added focused tests in runtime/internal/app/authentication/service_test.go. The service now rejects missing or malformed proof before unit-of-work, computes credential lookup digest before repository lookup, uses authentication and player repositories from a local unit-of-work capability, collapses lookup miss and invalid credential states to public invalid-credential errors, compares credential verifier digest before token generation, requires active player account state, generates opaque access-token material only after proof and player acceptance, stores token lookup and verifier digests only, and returns raw token text only after successful token storage and unit-of-work commit. Access-token validation, logout, refresh, cleanup, protocol carriers, startup wiring, repository interface changes, migrations, generated files, dependencies, and broader production behavior remain deferred.
    ask_first:
      - Changing service method signatures, public contracts, repository interfaces, PostgreSQL adapters, migrations, Protobuf messages, WebSocket carriers, startup wiring, or generated files.
      - Choosing a production token lifetime, public token failure disclosure policy, token rotation policy, refresh-token model, session persistence model, or protocol proof carrier beyond the service-local behavior slice.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, password-hashing, Redis-like, queue, session-store, UUID, ULID, or KSUID dependency.

  - id: W-0110
    milestone: M-038
    title: Define access-token validation service behavior gate
    status: completed
    change: changes/2026-05-16-define-access-token-validation-service-behavior-gate
    depends_on:
      - W-0109
    related_decisions:
      - ADR-0036
      - ADR-0039
      - ADR-0040
      - ADR-0041
      - ADR-0043
      - ADR-0049
      - ADR-0050
      - ADR-0051
      - ADR-0052
    completion:
      - Define the future access-token validation service behavior gate without adding token validation execution code.
      - Name the proof parsing boundary, helper composition sequence, repository handoff sequence, token status and expiration checks, RequestIdentity handoff, public error collapse posture, redaction requirements, and focused tests required before validation execution may be implemented.
      - Preserve deferral for WebSocket proof carriers, handshake authentication, route protection, session persistence, logout, refresh, cleanup, repository interface changes, migrations, startup wiring, dependencies, generated files, and broader production authentication behavior.
    completion_summary: Defined the future access-token validation service behavior gate without adding validation execution code. The gate establishes Base64URL unpadded 32-byte opaque access-token proof input, proof pre-validation before unit-of-work, token lookup digest computation, authentication and player repository handoff through unit-of-work capabilities, token lifecycle and audience checks, verifier digest comparison before request identity, active player account requirement, request identity handoff with SessionValidated kept false until session persistence is ratified, public error collapse, redaction rules, and required tests. Protocol carriers, WebSocket handshake authentication, route protection, session persistence, logout, refresh, cleanup, repository interface changes, migrations, startup wiring, dependencies, generated files, and production behavior remain deferred.
    ask_first:
      - Implementing real access-token validation instead of only defining the gate.
      - Choosing or changing WebSocket proof carrier, handshake authentication, route protection, session persistence, token rotation, refresh-token behavior, repository interfaces, migrations, startup wiring, or generated files.

  - id: W-0111
    milestone: M-039
    title: Implement access-token validation service behavior
    status: completed
    change: changes/2026-05-16-implement-access-token-validation-service-behavior
    depends_on:
      - W-0110
    related_decisions:
      - ADR-0036
      - ADR-0039
      - ADR-0040
      - ADR-0041
      - ADR-0043
      - ADR-0049
      - ADR-0050
      - ADR-0051
      - ADR-0052
    completion:
      - Implement access-token validation inside runtime/internal/app/authentication/service.go using the ADR-0052 sequence.
      - Add focused tests under runtime/internal/app/authentication/service_test.go for proof rejection, helper ordering, unit-of-work repository handoff, public error collapse, token lifecycle and audience checks, active player account requirement, request identity handoff, redaction, and no validated identity return on failure.
      - Preserve deferral for protocol carriers, WebSocket handshake authentication, route protection, session persistence, logout, refresh, cleanup, repository interface changes, migrations, startup wiring, dependencies, generated files, and broader production authentication behavior.
    completion_summary: Implemented the ADR-0052 access-token validation sequence inside runtime/internal/app/authentication/service.go and added focused tests in runtime/internal/app/authentication/service_test.go. The service now rejects missing or malformed proof before unit-of-work, computes token lookup digest before repository lookup, uses authentication and player repositories from a local unit-of-work capability, collapses lookup miss and invalid token/player states to public invalid-token errors, checks token kind/status/verifier posture/audience/issue time/expiration, compares token verifier digest before player lookup and request identity, requires active player account state, returns validated player identity only after unit-of-work success, and keeps SessionValidated false. Protocol carriers, WebSocket handshake authentication, route protection, session persistence, logout, refresh, cleanup, startup wiring, repository interface changes, migrations, generated files, dependencies, and broader production behavior remain deferred.
    ask_first:
      - Changing service method signatures, public contracts, repository interfaces, PostgreSQL adapters, migrations, Protobuf messages, WebSocket carriers, route protection, startup wiring, session persistence, or generated files.
      - Choosing a public expired/revoked/account-disabled disclosure policy different from ADR-0052, token rotation policy, refresh-token model, session persistence model, or protocol proof carrier beyond the service-local behavior slice.
      - Adding a major external cryptography, JWT, OAuth, OIDC, provider, KMS, cloud secret-manager, operations, password-hashing, Redis-like, queue, session-store, UUID, ULID, or KSUID dependency.

  - id: W-0112
    milestone: M-040
    title: Confirm next milestone direction after service-local authentication
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-service-local-authentication
    depends_on:
      - W-0111
    related_decisions:
      - ADR-0019
      - ADR-0021
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
      - ADR-0036
      - ADR-0039
      - ADR-0051
      - ADR-0052
    block_reason: The next major direction requires maintainer confirmation because each candidate crosses a long-lived architecture or product capability boundary.
    candidate_directions:
      - expose_access_token_protocol_carrier_and_route_protection_gate
      - ratify_session_persistence_and_websocket_handshake_authentication
      - implement_logout_refresh_cleanup_behavior_gates
      - wire_runtime_authentication_startup_composition
      - expand_core_game_backend_modules_after_nakama_pitaya_review
      - strengthen_operations_observability_and_admin_tooling
      - review_self_bootstrapping_agent_native_process_before_more_runtime_scope
    selected_direction: expose_access_token_protocol_carrier_and_route_protection_gate
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for protocol carriers, route protection, session persistence, WebSocket handshake authentication, startup wiring, logout, refresh, cleanup, runtime composition, generated files, repository changes, migrations, major dependencies, and core game backend module expansion.
    completion_summary: The maintainer authorized continued progress for five hours. The selected next direction is `expose_access_token_protocol_carrier_and_route_protection_gate`. M-041 and W-0113 were created as a gate-only milestone and work item, preserving ask-first boundaries for actual protocol sources, generated output, protocol adapter behavior, route protection implementation, WebSocket handshake authentication, session persistence, startup wiring, repository changes, migrations, dependencies, logout, refresh, cleanup, and core game backend expansion.
    ask_first:
      - Choosing or implementing protocol proof carriers, route protection, session persistence, WebSocket handshake authentication, startup wiring, logout, refresh, cleanup, token rotation, repository changes, migrations, generated files, major dependencies, or core game backend module expansion.

  - id: W-0113
    milestone: M-041
    title: Define access-token protocol carrier and route-protection gate
    status: completed
    change: changes/2026-05-17-define-access-token-protocol-carrier-route-protection-gate
    depends_on:
      - W-0112
    related_decisions:
      - ADR-0015
      - ADR-0018
      - ADR-0019
      - ADR-0021
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
      - ADR-0036
      - ADR-0039
      - ADR-0051
      - ADR-0052
      - ADR-0053
    completion:
      - Define the future access-token protocol carrier and route-protection gate without implementation.
      - Select the request-level validation posture and first proof-carrier posture.
      - Preserve deferrals for `.proto` sources, generated output, existing envelope changes, protocol adapter behavior, route-protection implementation, WebSocket handshake authentication, session persistence, startup wiring, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior.
    completion_summary: Defined the gate-only standard and ADR-0053. The gate selects request-level validation with a future Protobuf payload wrapper candidate, keeps the existing envelope unchanged, keeps WebSocket transport credential-neutral, requires application-owned route policy for protected routes, and records required tests, redaction, error mapping, and implementation deferrals.
    ask_first:
      - Adding `.proto` sources or generated Protobuf output.
      - Changing the existing Protobuf envelope.
      - Implementing protocol adapter wrapper handling, application route policy, authentication validation adapter, startup wiring, WebSocket handshake authentication, session persistence, repository changes, migrations, dependencies, logout, refresh, cleanup, or token rotation.

  - id: W-0114
    milestone: M-042
    title: Implement access-token payload wrapper and route-protection slice
    status: completed
    change: changes/2026-05-17-implement-access-token-payload-wrapper-route-protection
    depends_on:
      - W-0113
    related_decisions:
      - ADR-0015
      - ADR-0018
      - ADR-0021
      - ADR-0023
      - ADR-0036
      - ADR-0039
      - ADR-0052
      - ADR-0053
    completion:
      - Add the gate-authorized authenticated request payload wrapper protocol source and generated output through the normal generator path.
      - Add protocol adapter and application route policy behavior that validates access-token proof before protected domain dispatch.
      - Preserve deferrals for WebSocket handshake authentication, session persistence, startup wiring, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior.
    completion_summary: Added `proto/vibit/authentication/v1/authenticated_request.proto` and generated `runtime/internal/generated/proto/vibit/authentication/v1/authenticated_request.pb.go` through Buf. Added route authentication policy and validator handoff in `runtime/internal/app`, integrated wrapper handling into the Protobuf frame handler without changing the envelope, and added focused app/protocol/WebSocket tests. Protected routes now require an authenticated wrapper and validated player identity before dispatch; public device-credential login remains explicit. WebSocket transport still ignores Authorization, Bearer, cookie, query, and subprotocol credential carriers. Startup wiring, session persistence, WebSocket handshake authentication, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior remain deferred.
    ask_first:
      - Changing the existing Protobuf envelope.
      - Parsing HTTP Authorization headers, Bearer values, cookies, query strings, or WebSocket subprotocols.
      - Wiring process startup or requiring live PostgreSQL for default tests.

  - id: W-0115
    milestone: M-043
    title: Confirm next milestone direction after request-level route protection
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-request-level-route-protection
    depends_on:
      - W-0114
    block_reason: The next major direction crosses ask-first boundaries such as session persistence, WebSocket handshake authentication, startup composition, logout/refresh/cleanup behavior, token rotation, repository changes, migrations, dependencies, operations posture, or broader game backend scope.
    candidate_directions:
      - ratify_session_persistence_and_websocket_handshake_authentication
      - wire_runtime_authentication_startup_composition
      - implement_logout_refresh_cleanup_behavior_gates
      - ratify_token_rotation_and_reauthentication_lifecycle
      - add_protocol_authentication_command_messages_after_wrapper_slice
      - expand_core_game_backend_modules_after_nakama_pitaya_review
      - strengthen_operations_observability_and_admin_tooling
    selected_direction: wire_runtime_authentication_startup_composition
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for session persistence, WebSocket handshake authentication, startup wiring, logout, refresh, cleanup, token rotation, repository changes, migrations, major dependencies, runtime composition, operations posture, generated authentication messages beyond the wrapper, and core game backend module expansion.
    completion_summary: The maintainer explicitly selected `wire_runtime_authentication_startup_composition` and asked to continue for ten steps while strongly referencing Nakama and Pitaya. The selected direction creates M-044/W-0116 as a gate-only startup composition step before implementation and keeps session persistence, WebSocket handshake authentication, logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, generated authentication messages beyond the existing wrapper, operations posture, and core game backend expansion deferred.
    ask_first:
      - Selecting or implementing session persistence, WebSocket handshake authentication, startup wiring, logout, refresh, cleanup, token rotation, repository changes, migrations, major dependencies, generated authentication messages beyond the wrapper, operations posture, or core game backend module expansion.

  - id: W-0116
    milestone: M-044
    title: Define runtime authentication startup composition gate
    status: completed
    change: changes/2026-05-17-define-runtime-authentication-startup-composition-gate
    depends_on:
      - W-0115
    related_decisions:
      - ADR-0018
      - ADR-0019
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
      - ADR-0036
      - ADR-0039
      - ADR-0041
      - ADR-0046
      - ADR-0050
      - ADR-0051
      - ADR-0052
      - ADR-0053
      - ADR-0054
    completion:
      - Define the future runtime authentication startup composition gate without implementation.
      - Name the startup owner, PostgreSQL-only first composition path, environment verifier key loading posture, authentication service dependency defaults, route-protector handoff, tests, redaction requirements, and Nakama/Pitaya reference mapping.
      - Preserve deferrals for session persistence, WebSocket handshake authentication, authentication command routes, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior.
    completion_summary: Defined the gate-only startup composition standard and ADR-0054. The gate authorizes a later implementation slice to wire existing authentication service validation into request-level route protection in `runtime/cmd/vibit-server` only for `VIBIT_RUNTIME_STORE=postgres`, using existing environment verifier key loading, PostgreSQL unit-of-work, standard-library entropy, a startup-owned clock and token record id generator, a 1h/default or configured token lifetime, and a `vibit_gameplay_runtime_requests`/default or configured audience. It preserves memory startup as a bootstrap path and keeps WebSocket transport credential-neutral, the existing Protobuf envelope unchanged, session persistence deferred, and authentication lifecycle expansions deferred.
    ask_first:
      - Implementing startup composition instead of only defining the gate.
      - Changing WebSocket handshake authentication, session persistence, repository interfaces, PostgreSQL adapters, migrations, generated files, dependencies, logout, refresh, cleanup, or token rotation.
      - Adding runtime authentication command routes or direct Nakama/Pitaya API compatibility.

  - id: W-0117
    milestone: M-045
    title: Implement runtime authentication startup composition
    status: completed
    change: changes/2026-05-17-implement-runtime-authentication-startup-composition
    depends_on:
      - W-0116
    related_decisions:
      - ADR-0018
      - ADR-0019
      - ADR-0023
      - ADR-0026
      - ADR-0027
      - ADR-0036
      - ADR-0039
      - ADR-0046
      - ADR-0051
      - ADR-0052
      - ADR-0053
      - ADR-0054
    completion:
      - Wire existing authentication service validation into the PostgreSQL runtime startup path.
      - Load verifier keys from explicit environment lookup and fail closed for missing or invalid authentication configuration.
      - Pass application route protection into the Protobuf frame handler while keeping WebSocket transport credential-neutral.
      - Preserve memory startup as an unauthenticated bootstrap path and preserve deferrals for session persistence, WebSocket handshake authentication, repository changes, migrations, generated files, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior.
    completion_summary: Implemented runtime authentication startup composition in `runtime/cmd/vibit-server`. The PostgreSQL runtime path now composes `authentication.Service` with existing verifier key loading, PostgreSQL unit-of-work, `crypto/rand.Reader`, startup-owned system clock, startup-owned token record id generator, token lifetime, and token audience, and injects `app.NewRouteProtector(authentication.NewRouteAccessTokenValidator(service))` into the Protobuf frame handler. Tests cover memory bootstrap route-protector absence, explicit route-protector injection, missing verifier-key startup failure, invalid lifetime failure, configured auth dependency parsing, and token record id generation shape. WebSocket transport remains credential-neutral, the existing envelope is unchanged, and deferred authentication lifecycle work remains deferred.
    ask_first:
      - Changing WebSocket handshake authentication, session persistence, repository interfaces, PostgreSQL adapters, migrations, generated files, dependencies, logout, refresh, cleanup, token rotation, token validation audit mutation, or runtime authentication command routes.

  - id: W-0118
    milestone: M-046
    title: Confirm next milestone direction after startup composition
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-runtime-authentication-startup-composition
    depends_on:
      - W-0117
    block_reason: The next major direction crosses ask-first boundaries such as session persistence, WebSocket handshake authentication, authentication command routes, logout/refresh/cleanup behavior, token rotation, repository changes, migrations, dependencies, operations posture, or broader game backend scope.
    candidate_directions:
      - ratify_session_persistence_and_websocket_handshake_authentication
      - add_authentication_command_protocol_messages_and_login_route_registration
      - implement_logout_refresh_cleanup_behavior_gates
      - ratify_token_rotation_and_reauthentication_lifecycle
      - expand_core_game_backend_modules_after_nakama_pitaya_review
      - strengthen_operations_observability_and_admin_tooling
    selected_direction: add_authentication_command_protocol_messages_and_login_route_registration
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for session persistence, WebSocket handshake authentication, authentication command routes, logout, refresh, cleanup, token rotation, repository changes, migrations, major dependencies, operations posture, and core game backend module expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to that recommendation. The selected direction is `add_authentication_command_protocol_messages_and_login_route_registration`, creating M-047/W-0119 as a gate-only step for public device credential login protocol messages and route registration. The selection keeps session persistence, WebSocket handshake authentication, logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, operations posture, and broader game backend expansion deferred.
    ask_first:
      - Selecting or implementing session persistence, WebSocket handshake authentication, authentication command routes, logout, refresh, cleanup, token rotation, repository changes, migrations, major dependencies, operations posture, or core game backend module expansion.

  - id: W-0119
    milestone: M-047
    title: Define authentication command protocol and login route gate
    status: completed
    change: changes/2026-05-17-define-authentication-command-protocol-login-route-gate
    depends_on:
      - W-0118
    related_decisions:
      - ADR-0018
      - ADR-0019
      - ADR-0023
      - ADR-0024
      - ADR-0028
      - ADR-0036
      - ADR-0039
      - ADR-0051
      - ADR-0052
      - ADR-0053
      - ADR-0054
      - ADR-0055
    completion:
      - Define the future authentication command protocol and login route gate without implementation.
      - Name the public login route, planned Protobuf source, generated output, protocol bridge, route registration, startup composition, transaction wrapper bypass, tests, redaction rules, and Nakama/Pitaya reference mapping.
      - Preserve deferrals for session persistence, WebSocket handshake authentication, non-Protobuf credential carriers, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior.
    completion_summary: Defined the gate-only authentication command protocol and login route standard and ADR-0055. The gate authorizes a later implementation slice to expose the existing `AuthenticateWithDeviceCredential` service method through the public `runtime.authentication.AuthenticateWithDeviceCredential` command route using Protobuf request/response messages, generated output, protocol bridge behavior, application route registration, PostgreSQL startup composition, and focused tests. It keeps the existing envelope unchanged, keeps WebSocket transport credential-neutral, keeps memory durable login unavailable, and preserves session persistence, WebSocket handshake authentication, logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, and direct Nakama/Pitaya API compatibility deferrals.
    ask_first:
      - Implementing protocol messages, generated output, route registration, startup composition, transaction wrapper bypass, or protocol bridge behavior instead of only defining the gate.
      - Changing WebSocket handshake authentication, session persistence, repository interfaces, PostgreSQL adapters, migrations, dependencies, logout, refresh, cleanup, token rotation, memory durable authentication behavior, or direct Nakama/Pitaya API compatibility.

  - id: W-0120
    milestone: M-048
    title: Implement authentication command protocol and login route
    status: completed
    change: changes/2026-05-17-implement-authentication-command-protocol-login-route
    depends_on:
      - W-0119
    related_decisions:
      - ADR-0018
      - ADR-0023
      - ADR-0036
      - ADR-0039
      - ADR-0051
      - ADR-0053
      - ADR-0054
      - ADR-0055
    completion:
      - Add the gate-authorized authentication command Protobuf source and generated output through the normal Buf path.
      - Add protocol bridge behavior and application route registration for `AuthenticateWithDeviceCredential`.
      - Wire the route only through PostgreSQL startup composition where the authentication service exists.
      - Ensure the public authentication route bypasses the outer transactional dispatcher because the authentication service owns its own unit-of-work.
      - Preserve deferrals for session persistence, WebSocket handshake authentication, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, and broader production behavior.
    completion_summary: Added the gate-authorized public device credential login command route. `proto/vibit/authentication/v1/authentication.proto` defines the login request/response messages and generated Go output was produced through Buf. The Protobuf adapter maps request and response payloads, application bootstrap registers the explicit public route, PostgreSQL startup composition wires the route when the authentication service exists, and `TransactionalDispatcher` bypasses the outer transaction for the route. Focused tests cover route registration, bridge mapping, public route behavior, transaction bypass, startup composition, and unchanged protected-route behavior. Session persistence, WebSocket handshake authentication, repository changes, migrations, dependencies, logout, refresh, cleanup, token rotation, token validation audit mutation, memory durable login, and direct Nakama/Pitaya API compatibility remain deferred.
    ask_first:
      - Changing repository interfaces, PostgreSQL adapters, migrations, WebSocket handshake authentication, session persistence, non-Protobuf credential carriers, logout, refresh, cleanup, token rotation, dependencies, memory durable authentication behavior, or direct Nakama/Pitaya API compatibility.

  - id: W-0121
    milestone: M-049
    title: Confirm next milestone direction after public login route
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-authentication-login-route
    depends_on:
      - W-0120
    block_reason: The next major direction crosses ask-first boundaries such as session persistence, WebSocket handshake authentication, logout/refresh/cleanup behavior, token rotation, repository changes, migrations, dependencies, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - ratify_session_persistence_and_websocket_handshake_authentication
      - implement_logout_refresh_cleanup_behavior_gates
      - ratify_token_rotation_and_reauthentication_lifecycle
      - add_memory_durable_authentication_support
      - expand_core_game_backend_modules_after_nakama_pitaya_review
      - strengthen_operations_observability_and_admin_tooling
    selected_direction: ratify_session_persistence_and_websocket_handshake_authentication
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for first-message connection binding, session persistence, WebSocket handshake authentication, logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and core game backend module expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to that recommendation. The selected direction is `ratify_session_persistence_and_websocket_handshake_authentication`, creating M-050/W-0122 as a gate-only ratification step. Nakama guides the login-session-realtime socket capability sequence; Pitaya guides the acceptor-session-route-handler separation. The selection keeps implementation of session persistence, WebSocket handshake authentication, first-message connection binding, session schema, repositories, migrations, dependencies, logout, refresh, cleanup, token rotation, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend expansion deferred.
    ask_first:
      - Selecting or implementing session persistence, WebSocket handshake authentication, logout, refresh, cleanup, token rotation, repository changes, migrations, dependencies, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or core game backend module expansion.

  - id: W-0122
    milestone: M-050
    title: Ratify session persistence and WebSocket handshake posture
    status: completed
    change: changes/2026-05-17-ratify-session-persistence-websocket-handshake-authentication
    depends_on:
      - W-0121
    related_decisions:
      - ADR-0015
      - ADR-0018
      - ADR-0019
      - ADR-0021
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
      - ADR-0036
      - ADR-0052
      - ADR-0053
      - ADR-0054
      - ADR-0055
      - ADR-0056
    completion:
      - Define the first ratified session and handshake posture after public login route exposure.
      - Keep the current production-sensitive path as request-level opaque access-token validation through `vibit.authentication.v1.AuthenticatedRequest`.
      - Keep WebSocket transport credential-neutral and the existing Protobuf envelope unchanged.
      - Prefer a future protocol/application first-message binding gate for connection-bound identity.
      - Prefer a future PostgreSQL-first schema/repository/migration gate for durable session persistence.
      - Preserve deferrals for implementation of session persistence, WebSocket handshake authentication, session schema, repositories, migrations, dependencies, logout, refresh, cleanup, token rotation, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior.
    completion_summary: Ratified the session persistence and WebSocket handshake posture in `docs/session-persistence-websocket-handshake-ratification.md`, `docs/session-persistence-websocket-handshake-ratification.zh-CN.md`, and ADR-0056. The gate records request-level access-token validation as the current path, keeps WebSocket transport credential-neutral, keeps the existing Protobuf envelope unchanged, prefers future first-message protocol/application binding for connection-level identity, and defers durable session persistence to a later PostgreSQL-first schema/repository/migration gate. No Go runtime behavior, Protobuf source, generated output, migration, dependency, logout, refresh, cleanup, token rotation, reconnect, memory durable authentication behavior, or direct Nakama/Pitaya API compatibility was added.
    ask_first:
      - Implementing first-message connection binding, session persistence, WebSocket handshake authentication, transport credential carriers, existing envelope changes, session schema, repository interfaces, PostgreSQL adapters, migrations, dependencies, logout, refresh, cleanup, token rotation, reconnect/epoch behavior, active-connection invalidation, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0123
    milestone: M-051
    title: Confirm next milestone direction after session and handshake ratification
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-session-handshake-ratification
    depends_on:
      - W-0122
    block_reason: The next major direction crosses ask-first boundaries such as first-message connection binding, session persistence schema, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_first_message_connection_binding_gate
      - define_postgres_session_persistence_schema_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - implement_logout_refresh_cleanup_behavior_gates
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: define_first_message_connection_binding_gate
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for first-message connection binding implementation, session persistence schema, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and core game backend expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it. The selected direction is `define_first_message_connection_binding_gate`, creating M-052/W-0124 as a gate-only step. Nakama guides the authenticate-then-realtime socket lifecycle and Pitaya guides acceptor-session-route-handler separation. The selection keeps connection binding implementation, Protobuf messages, generated output, connection registries, route-policy bound identity, session persistence, WebSocket handshake authentication, logout/revocation, reconnect/epoch, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend expansion deferred.
    ask_first:
      - Selecting or implementing first-message connection binding, session persistence schema, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or core game backend module expansion.

  - id: W-0124
    milestone: M-052
    title: Define first-message connection binding gate
    status: completed
    change: changes/2026-05-17-define-first-message-connection-binding-gate
    depends_on:
      - W-0123
    related_decisions:
      - ADR-0015
      - ADR-0018
      - ADR-0019
      - ADR-0021
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
      - ADR-0036
      - ADR-0052
      - ADR-0053
      - ADR-0054
      - ADR-0055
      - ADR-0056
      - ADR-0057
    completion:
      - Define the future first-message connection binding gate without implementation.
      - Select the future `runtime.authentication.BindConnection` system route and binding payload candidates.
      - Keep WebSocket transport credential-neutral and keep the existing Protobuf envelope unchanged.
      - Preserve request-level access-token validation as the current protected-route path.
      - Preserve deferrals for Protobuf messages, generated output, connection binding registries, route-policy bound identity, session persistence, repositories, migrations, dependencies, logout/revocation, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior.
    completion_summary: Defined the first-message connection binding gate in `docs/first-message-connection-binding-gate.md`, `docs/first-message-connection-binding-gate.zh-CN.md`, and ADR-0057. The gate selects the future `runtime.authentication.BindConnection` system route with `vibit.authentication.v1.BindConnectionRequest` and `vibit.authentication.v1.BindConnectionResponse` payload candidates. It keeps WebSocket transport credential-neutral, keeps the existing envelope unchanged, preserves request-level access-token validation as the current path, and records Nakama/Pitaya reference mapping without direct API compatibility. No Go runtime behavior, Protobuf source, generated output, migration, dependency, connection registry, route-policy bound identity, session persistence, logout/revocation, reconnect/epoch behavior, memory durable authentication behavior, or broader game backend behavior was added.
    ask_first:
      - Implementing first-message connection binding, adding Protobuf messages, generated output, protocol adapter behavior, application binding registries, route-policy bound identity, startup composition, session persistence, WebSocket handshake authentication, transport credential carriers, repository interfaces, PostgreSQL adapters, migrations, dependencies, logout/revocation, reconnect/epoch behavior, active-connection invalidation, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0125
    milestone: M-053
    title: Confirm next milestone direction after first-message connection binding gate
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-first-message-connection-binding-gate
    depends_on:
      - W-0124
    block_reason: The next major direction crosses ask-first boundaries such as first-message connection binding implementation, PostgreSQL session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_first_message_connection_binding_implementation_gate
      - define_postgres_session_persistence_schema_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - implement_logout_refresh_cleanup_behavior_gates
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: define_first_message_connection_binding_implementation_gate
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for implementing first-message connection binding, session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and core game backend expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it. The selected direction is `define_first_message_connection_binding_implementation_gate`, creating M-054/W-0126 as a gate-only step. Nakama guides authenticated realtime socket lifecycle and Pitaya guides acceptor-session-handler separation. The selection keeps actual connection binding implementation, Protobuf messages, generated output, connection registries, route-policy bound identity, session persistence, WebSocket handshake authentication, logout/revocation, reconnect/epoch, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend expansion deferred.
    ask_first:
      - Selecting or implementing first-message connection binding implementation, session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or core game backend module expansion.

  - id: W-0126
    milestone: M-054
    title: Define first-message connection binding implementation gate
    status: completed
    change: changes/2026-05-17-define-first-message-connection-binding-implementation-gate
    depends_on:
      - W-0125
    related_decisions:
      - ADR-0015
      - ADR-0016
      - ADR-0017
      - ADR-0018
      - ADR-0019
      - ADR-0021
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
      - ADR-0036
      - ADR-0052
      - ADR-0053
      - ADR-0054
      - ADR-0055
      - ADR-0056
      - ADR-0057
      - ADR-0058
    completion:
      - Define the future first-message connection binding implementation gate without implementation.
      - Name future BindConnection protocol messages, generated output, protocol adapter ownership, application binding ownership, startup composition, error mapping, tests, and redaction rules.
      - Keep WebSocket transport credential-neutral and keep the existing Protobuf envelope unchanged.
      - Preserve request-level access-token validation as the current protected-route path.
      - Preserve deferrals for implementation, session persistence, route-policy bound identity unless explicitly changed by the future implementation slice, repositories, migrations, dependencies, logout/revocation, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior.
    completion_summary: Defined the first-message connection binding implementation gate in `docs/first-message-connection-binding-implementation-gate.md`, `docs/first-message-connection-binding-implementation-gate.zh-CN.md`, and ADR-0058. The gate names the future `runtime.authentication.BindConnection` system route implementation artifacts, including `BindConnectionRequest`, `BindConnectionResponse`, `ConnectionBindingStatus`, generated Go output through Buf, protocol adapter bridge, application binding boundary, PostgreSQL startup composition, required tests, and public error mapping. It keeps WebSocket transport credential-neutral, keeps the existing envelope unchanged, preserves request-level access-token validation as the current path, and records Nakama/Pitaya reference mapping without direct API compatibility. No Go runtime behavior, Protobuf source, generated output, migration, dependency, connection registry, route-policy bound identity, session persistence, logout/revocation, reconnect/epoch behavior, memory durable authentication behavior, or broader game backend behavior was added.
    ask_first:
      - Implementing first-message connection binding, adding Protobuf messages, generated output, protocol adapter behavior, application binding registries, route-policy bound identity, startup composition, session persistence, WebSocket handshake authentication, transport credential carriers, repository interfaces, PostgreSQL adapters, migrations, dependencies, logout/revocation, reconnect/epoch behavior, active-connection invalidation, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0127
    milestone: M-055
    title: Confirm next milestone direction after first-message connection binding implementation gate
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-first-message-connection-binding-implementation-gate
    depends_on:
      - W-0126
    block_reason: The next major direction crosses ask-first boundaries such as implementing first-message connection binding, PostgreSQL session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_first_message_connection_binding
      - define_postgres_session_persistence_schema_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: implement_first_message_connection_binding
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and core game backend expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it. The selected direction is `implement_first_message_connection_binding`, creating M-056/W-0128 as a bounded implementation slice. Nakama guides authenticated session material before realtime socket use, and Pitaya guides acceptor/session/handler separation. The selection keeps session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, and broader game backend expansion deferred.
    ask_first:
      - Selecting or implementing first-message connection binding, session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or core game backend module expansion.

  - id: W-0128
    milestone: M-056
    title: Implement first-message connection binding
    status: completed
    change: changes/2026-05-17-implement-first-message-connection-binding
    depends_on:
      - W-0127
    related_decisions:
      - ADR-0015
      - ADR-0016
      - ADR-0017
      - ADR-0018
      - ADR-0019
      - ADR-0021
      - ADR-0023
      - ADR-0024
      - ADR-0026
      - ADR-0027
      - ADR-0036
      - ADR-0052
      - ADR-0053
      - ADR-0054
      - ADR-0055
      - ADR-0056
      - ADR-0057
      - ADR-0058
    completion:
      - Extend authentication Protobuf source with `BindConnectionRequest`, `BindConnectionResponse`, and `ConnectionBindingStatus`.
      - Regenerate Go Protobuf output through Buf.
      - Add application-owned connection binding logic under `runtime/internal/app`.
      - Add Protobuf adapter handling for the `runtime.authentication.BindConnection` system route.
      - Wire the binder into PostgreSQL startup composition only when the authentication service is composed.
      - Preserve WebSocket transport credential neutrality and unchanged envelope shape.
      - Add focused tests for success, failure mapping, redaction, server-observed connection metadata, startup injection, and request-level route-protection compatibility.
    completion_summary: Implemented first-message connection binding through the `runtime.authentication.BindConnection` system route. The slice added gate-authorized authentication Protobuf messages and Buf-generated Go output, application-owned `ConnectionBinder`, Protobuf adapter handling before normal dispatch, WebSocket connection epoch metadata handoff, and PostgreSQL startup injection. It keeps the existing envelope unchanged, preserves request-level `AuthenticatedRequest` as the current protected-route path, keeps `SessionValidated` false, and avoids durable session persistence, route-policy bound identity for protected routes, migrations, repository changes, dependencies, logout/revocation, reconnect/epoch policy, memory durable authentication behavior, and direct Nakama/Pitaya API compatibility.
    ask_first:
      - Adding durable session persistence, session tables, repositories, migrations, cleanup jobs, WebSocket handshake authentication, transport credential carriers, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, route-policy use of bound identity for ordinary protected routes, new dependencies, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0129
    milestone: M-057
    title: Confirm next milestone direction after first-message connection binding implementation
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-first-message-connection-binding-implementation
    depends_on:
      - W-0128
    block_reason: The next major direction crosses ask-first boundaries such as PostgreSQL session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_postgres_session_persistence_schema_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_bound_identity_route_policy_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: define_postgres_session_persistence_schema_gate
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for migration source, repositories, adapters, runtime session validation, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and core game backend expansion.
    completion_summary: The maintainer authorized continuing according to the agent's recommendation. The selected direction is `define_postgres_session_persistence_schema_gate`, creating M-058/W-0130 as a gate-only milestone. Nakama guides first-class session lifecycle and realtime socket identity expectations, and Pitaya guides session-like context separation from acceptors and handlers. The selection keeps SQL migration source, session repositories, PostgreSQL adapters, runtime session validation, route-policy bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    ask_first:
      - Selecting session persistence schema, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable authentication behavior, direct Nakama/Pitaya API compatibility, or core game backend module expansion.

  - id: W-0130
    milestone: M-058
    title: Define PostgreSQL session persistence schema gate
    status: completed
    change: changes/2026-05-17-define-postgres-session-persistence-schema-gate
    depends_on:
      - W-0129
    related_decisions:
      - ADR-0019
      - ADR-0020
      - ADR-0021
      - ADR-0023
      - ADR-0056
      - ADR-0057
      - ADR-0058
      - ADR-0059
    completion:
      - Define the English PostgreSQL session persistence schema gate standard.
      - Add the paired Simplified Chinese translation.
      - Record ADR-0059.
      - Record `runtime_sessions` as the future logical table candidate.
      - Record `runtime/migrations/postgres/000005_create_runtime_sessions.sql` as the future migration source candidate without creating it.
      - Update manifests, AGENTS guides, rules, checks, change specs, and conversation memory.
      - Preserve deferrals for migration source, repositories, PostgreSQL adapters, runtime session validation, WebSocket handshake authentication, route-policy bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior.
    completion_summary: Defined the PostgreSQL session persistence schema gate in `docs/postgres-session-persistence-schema-gate.md`, `docs/postgres-session-persistence-schema-gate.zh-CN.md`, and ADR-0059. The gate records PostgreSQL as the first durable runtime session target and `runtime_sessions` as the future table candidate, but adds no SQL migration, repository, PostgreSQL adapter, runtime session validation behavior, transport credential carrier, Protobuf change, route-policy bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependency, memory durable session behavior, or direct Nakama/Pitaya API compatibility.
    ask_first:
      - Adding session migration source, session tables, repository interfaces, PostgreSQL adapters, runtime session creation/validation/revocation/cleanup behavior, WebSocket handshake authentication, transport credential carriers, Protobuf messages or envelope changes, route-policy use of session or bound connection identity, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0131
    milestone: M-059
    title: Confirm next milestone direction after PostgreSQL session persistence schema gate
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-postgres-session-persistence-schema-gate
    depends_on:
      - W-0130
    block_reason: The next major direction crosses ask-first boundaries such as adding session migration source, defining session repository boundary, implementing session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_runtime_sessions_migration_source
      - define_session_repository_boundary
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_bound_identity_route_policy_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: implement_runtime_sessions_migration_source
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for session repositories, PostgreSQL adapters, runtime session validation, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and core game backend expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it. The selected direction is `implement_runtime_sessions_migration_source`, creating M-060/W-0132 as a migration-source-only implementation slice. Nakama guides the first-class session lifecycle table, and Pitaya guides session context separation from acceptors and transport. The selection keeps repositories, PostgreSQL adapters, runtime session validation behavior, WebSocket handshake authentication, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    ask_first:
      - Selecting session migration source, session repository boundary, session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.

  - id: W-0132
    milestone: M-060
    title: Implement runtime sessions migration source
    status: completed
    change: changes/2026-05-17-implement-runtime-sessions-migration-source
    depends_on:
      - W-0131
    related_decisions:
      - ADR-0019
      - ADR-0020
      - ADR-0021
      - ADR-0023
      - ADR-0056
      - ADR-0057
      - ADR-0058
      - ADR-0059
      - ADR-0060
    completion:
      - Add `runtime/migrations/postgres/000005_create_runtime_sessions.sql`.
      - Keep the migration source SQL-first with goose Up and Down markers and a runtime.session ownership trace.
      - Create `runtime_sessions` only; do not create `runtime_session_connections`.
      - Include required lifecycle columns and optional revocation/access-token record linkage columns.
      - Add constraints and indexes for player/status, status/expires_at, access-token linkage, and last_seen_at inspection.
      - Forbid raw token, raw credential, token digest, credential digest, WebSocket state, and connection registry columns.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
      - Preserve deferrals for repositories, PostgreSQL adapters, runtime session validation, WebSocket handshake authentication, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior.
    completion_summary: Added the runtime sessions PostgreSQL migration source only. The migration creates `runtime_sessions` with lifecycle fields and constraints inspired by Nakama's session lifecycle model while preserving Pitaya-style separation between session context and transport. It links to `player_accounts(player_id)` and optionally to `authentication_access_tokens(token_record_id)`, but it stores no raw token, raw credential, verifier digest, lookup digest, WebSocket connection state, or connection registry rows. No runtime behavior, repository interface, adapter, route policy, handshake auth, logout/revocation active-connection handling, reconnect/epoch behavior, dependency, memory durable session behavior, or direct Nakama/Pitaya API compatibility was added.
    ask_first:
      - Adding session repository interfaces, PostgreSQL session adapters, runtime session creation/validation/revocation/cleanup behavior, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, route-policy use of persisted session or bound connection identity, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0133
    milestone: M-061
    title: Confirm next milestone direction after runtime sessions migration source
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-runtime-sessions-migration-source
    depends_on:
      - W-0132
    block_reason: The next major direction crosses ask-first boundaries such as defining session repository boundary, implementing session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_session_repository_boundary
      - define_session_postgresql_adapter_gate
      - define_runtime_session_validation_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_bound_identity_route_policy_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: define_session_repository_boundary
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for session repository implementation, PostgreSQL adapters, runtime session validation, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and core game backend expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it. The selected direction is `define_session_repository_boundary`, creating M-062/W-0134 as a gate-only boundary slice. Nakama guides the lifecycle repository capability pressure, and Pitaya guides keeping session context separate from acceptors, routing, and transport. Go repository implementation, PostgreSQL adapter behavior, runtime session validation, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.
    ask_first:
      - Selecting session repository boundary, session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.

  - id: W-0134
    milestone: M-062
    title: Define session repository boundary
    status: completed
    change: changes/2026-05-17-define-session-repository-boundary
    depends_on:
      - W-0133
    related_decisions:
      - ADR-0019
      - ADR-0020
      - ADR-0021
      - ADR-0023
      - ADR-0056
      - ADR-0057
      - ADR-0058
      - ADR-0059
      - ADR-0060
      - ADR-0061
    completion:
      - Add `docs/session-repository-boundary.md` and `docs/session-repository-boundary.zh-CN.md`.
      - Record `ADR-0061`.
      - Record `runtime/internal/app/session` as the future storage-neutral repository owner candidate.
      - Record `runtime/internal/platform/persistence/postgres` as the future PostgreSQL adapter owner.
      - Define candidate repository capability vocabulary without adding Go code.
      - Keep token validation, request identity handoff, WebSocket transport, Protobuf carriers, route policy, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Defined the storage-neutral session repository boundary only. The standard records future capability vocabulary for session lifecycle creation, lookup, active lookup, last-seen update, expiration, revocation, and bounded player-session listing, while preserving token validation ownership under authentication, request identity ownership under runtime application dispatch, WebSocket transport credential neutrality, unchanged Protobuf envelope behavior, and all runtime session validation, adapter, route-policy, logout/revocation, reconnect, dependency, memory durable session, direct Nakama/Pitaya compatibility, and broader game backend deferrals.
    ask_first:
      - Adding Go session repository interfaces, PostgreSQL session adapters, runtime session creation/validation/revocation/cleanup behavior, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, route-policy use of persisted session or bound connection identity, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0135
    milestone: M-063
    title: Confirm next milestone direction after session repository boundary
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-session-repository-boundary
    depends_on:
      - W-0134
    block_reason: The next major direction crosses ask-first boundaries such as implementing session repository interface, defining or implementing session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_session_postgresql_adapter_gate
      - implement_session_repository_interface
      - define_runtime_session_validation_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_bound_identity_route_policy_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: implement_session_repository_interface
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for PostgreSQL adapters, runtime session validation, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and core game backend expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it. The selected direction is `implement_session_repository_interface`, creating M-064/W-0136 as a storage-neutral interface-only implementation slice. Nakama guides the first-class session lifecycle vocabulary for lookup, expiration, revocation, logout, and management pressure, and Pitaya guides keeping handler session context separate from acceptors and transport. PostgreSQL adapter behavior, runtime session creation or validation, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.
    ask_first:
      - Selecting session repository implementation, session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.

  - id: W-0136
    milestone: M-064
    title: Implement storage-neutral session repository interface
    status: completed
    change: changes/2026-05-17-implement-session-repository-interface
    depends_on:
      - W-0135
    related_decisions:
      - ADR-0019
      - ADR-0020
      - ADR-0021
      - ADR-0023
      - ADR-0056
      - ADR-0059
      - ADR-0060
      - ADR-0061
      - ADR-0062
    completion:
      - Add `runtime/internal/app/session/repository.go`.
      - Add `runtime/internal/app/session/repository_test.go`.
      - Define storage-neutral runtime session value types, status vocabulary, query and mutation types, and `Repository`.
      - Add focused tests for status validity, normalization, UTC timestamps, lifecycle mutation/query validation, listing bounds, and absence of secret material fields.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
      - Preserve deferrals for PostgreSQL adapters, unit-of-work factory wiring, runtime session creation/validation/revocation/cleanup behavior, WebSocket handshake authentication, transport credential carriers, Protobuf session messages, existing envelope changes, generated output, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior.
    completion_summary: Implemented the storage-neutral runtime session repository interface under `runtime/internal/app/session`. The interface and value types cover lifecycle creation, lookup, active lookup, last-seen mutation, expiration, revocation, and bounded active-session listing for player sessions, with normalization helpers and focused unit tests. The slice keeps PostgreSQL adapter behavior, SQL query execution, runtime session validation, session creation at login or BindConnection, route-policy use of persisted session or bound identity, WebSocket handshake authentication, transport credential carriers, Protobuf session messages, logout/revocation active-connection invalidation, reconnect/epoch behavior, dependencies, memory durable session behavior, and direct Nakama/Pitaya API compatibility deferred.
    ask_first:
      - Adding PostgreSQL session adapters, unit-of-work factory wiring, runtime session creation/validation/revocation/cleanup behavior, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, route-policy use of persisted session or bound connection identity, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0137
    milestone: M-065
    title: Confirm next milestone direction after session repository interface implementation
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-session-repository-interface
    depends_on:
      - W-0136
    block_reason: The next major direction crosses ask-first boundaries such as defining or implementing session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_session_postgresql_adapter_gate
      - implement_session_postgresql_adapter
      - define_runtime_session_validation_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_bound_identity_route_policy_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: define_session_postgresql_adapter_gate
    completion:
      - Choose the next major milestone direction with the maintainer.
      - Record the chosen direction as a bounded milestone.
      - Create exactly one next_ready work item for the chosen milestone.
      - Preserve ask-first boundaries for session PostgreSQL adapter implementation, runtime session validation, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and core game backend expansion.
    completion_summary: The maintainer asked for the agent's recommendation and authorized continuing according to it. The selected direction is `define_session_postgresql_adapter_gate`, creating M-066/W-0138 as a gate-only adapter boundary slice. Nakama guides durable lifecycle lookup, expiration, revocation, logout, and management-ready listing pressure, and Pitaya guides keeping durable session persistence separate from acceptors, routing, and transport. PostgreSQL adapter implementation, runtime session creation or validation, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior remain deferred.
    ask_first:
      - Selecting session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.

  - id: W-0138
    milestone: M-066
    title: Define session PostgreSQL adapter gate
    status: completed
    change: changes/2026-05-17-define-session-postgresql-adapter-gate
    depends_on:
      - W-0137
    related_decisions:
      - ADR-0019
      - ADR-0020
      - ADR-0021
      - ADR-0023
      - ADR-0056
      - ADR-0059
      - ADR-0060
      - ADR-0061
      - ADR-0062
      - ADR-0063
    completion:
      - Add `docs/session-postgresql-adapter-gate.md`.
      - Add `docs/session-postgresql-adapter-gate.zh-CN.md`.
      - Record `ADR-0063`.
      - Record `runtime/internal/platform/persistence/postgres` as the future PostgreSQL adapter owner for `runtime/internal/app/session.Repository`.
      - Define future adapter SQL shape, transaction handoff, error mapping, redaction, and test requirements without adding Go adapter code.
      - Keep token validation, request identity handoff, WebSocket transport, Protobuf carriers, route policy, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Defined the gate-only PostgreSQL adapter boundary for future runtime session persistence. The standard records adapter ownership, future file candidates, repository method mapping, SQL shape, error classes, redaction, transaction neutrality, and focused test requirements, while preserving separation from token validation, request identity, route policy, WebSocket transport, Protobuf protocol, logout/revocation active-connection behavior, reconnect behavior, dependencies, memory durable session behavior, and direct Nakama/Pitaya API compatibility.
    ask_first:
      - Adding PostgreSQL session adapter Go files, unit-of-work factory wiring, runtime session creation/validation/revocation/cleanup behavior, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, route-policy use of persisted session or bound connection identity, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0139
    milestone: M-067
    title: Confirm next milestone direction after session PostgreSQL adapter gate
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-session-postgresql-adapter-gate
    depends_on:
      - W-0138
    block_reason: The next major direction crosses ask-first boundaries such as implementing session PostgreSQL adapter, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_session_postgresql_adapter
      - define_runtime_session_validation_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_bound_identity_route_policy_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting session PostgreSQL adapter implementation, runtime session validation behavior, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: implement_session_postgresql_adapter
    next_milestone: M-068
    next_ready_work_item: W-0140
    completion:
      - Select `implement_session_postgresql_adapter`.
      - Create M-068/W-0140 as a bounded PostgreSQL adapter implementation slice.
      - Keep runtime session validation, session creation composition, route-policy session identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_session_postgresql_adapter`, because `runtime_sessions`, `runtime/internal/app/session.Repository`, and the adapter gate already exist. Nakama informs durable session lifecycle storage pressure; Pitaya informs separation of persistence from acceptors, routing, and transport.

  - id: W-0140
    milestone: M-068
    title: Implement session PostgreSQL adapter
    status: completed
    change: changes/2026-05-17-implement-session-postgresql-adapter
    depends_on:
      - W-0139
    related_decisions:
      - ADR-0064
    completion:
      - Add `runtime/internal/platform/persistence/postgres/session_repository.go`.
      - Add `runtime/internal/platform/persistence/postgres/session_repository_test.go`.
      - Add `UnitOfWork.NewSessionRepository()`.
      - Implement only the existing `runtime/internal/app/session.Repository` methods.
      - Map SQL to `runtime_sessions` only.
      - Normalize repository inputs and returned records through `runtime/internal/app/session`.
      - Map PostgreSQL errors to redacted typed session adapter errors.
      - Keep transaction control in the existing unit-of-work runner.
      - Keep runtime session creation, validation, route policy, WebSocket handshake authentication, protocol carriers, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added the bounded PostgreSQL session adapter and unit-of-work factory. The adapter supports create, get, active lookup, last-seen update, expiration, revocation, and bounded player active-session listing against `runtime_sessions`, with fake-executor tests and redacted error mapping. It remains persistence-only and does not validate tokens, create request identity, modify WebSocket or Protobuf behavior, invalidate active connections, or define reconnect semantics.
    ask_first:
      - Adding runtime session creation at login or BindConnection, runtime session validation, `RequestIdentity.SessionValidated = true`, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, route-policy use of persisted session or bound connection identity, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0141
    milestone: M-069
    title: Confirm next milestone direction after session PostgreSQL adapter implementation
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-session-postgresql-adapter-implementation
    depends_on:
      - W-0140
    block_reason: The next major direction crosses ask-first boundaries such as runtime session validation behavior, session creation composition, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_runtime_session_validation_gate
      - define_session_creation_composition_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_bound_identity_route_policy_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting runtime session validation behavior, session creation composition, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: define_runtime_session_validation_gate
    next_milestone: M-070
    next_ready_work_item: W-0142
    completion:
      - Select `define_runtime_session_validation_gate`.
      - Create M-070/W-0142 as a bounded runtime session validation gate slice.
      - Keep runtime session validation implementation, session creation composition, route-policy session identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_runtime_session_validation_gate`, because durable session storage and the PostgreSQL adapter now exist but validation semantics must be ratified before any code treats a persisted session row as authenticated request identity. Nakama informs session lifetime, expiration, revocation, logout, and active socket separation; Pitaya informs separation of acceptors, session context, handlers, and durable validation.

  - id: W-0142
    milestone: M-070
    title: Define runtime session validation gate
    status: completed
    change: changes/2026-05-17-define-runtime-session-validation-gate
    depends_on:
      - W-0141
    related_decisions:
      - ADR-0065
    completion:
      - Add `docs/runtime-session-validation-gate.md`.
      - Add `docs/runtime-session-validation-gate.zh-CN.md`.
      - Add `ADR-0065`.
      - Define future validation ownership under `runtime/internal/app`.
      - Define future validation order, request identity handoff, public error collapse, redaction, future tests, and Nakama/Pitaya reference mapping.
      - Keep runtime validation implementation, session creation, route policy, WebSocket handshake authentication, protocol carriers, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added a gate-only runtime session validation standard. The gate requires already-validated actor identity before trusting persisted session rows, defines active/expired/revoked and actor-mismatch public failure collapse, preserves application ownership of request identity, and keeps WebSocket transport, Protobuf adapters, persistence adapters, route policy, logout/revocation, reconnect, dependencies, and direct external API compatibility separate.
    ask_first:
      - Adding runtime session validation implementation, `RequestIdentity.SessionValidated = true`, session creation at login or BindConnection, route-policy use of session or bound connection identity, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0143
    milestone: M-071
    title: Confirm next milestone direction after runtime session validation gate
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-runtime-session-validation-gate
    depends_on:
      - W-0142
    block_reason: The next major direction crosses ask-first boundaries such as runtime session validation implementation, session creation composition, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_runtime_session_validation
      - define_session_creation_composition_gate
      - define_bound_identity_route_policy_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting runtime session validation implementation, session creation composition, logout/revocation active-connection behavior, reconnect/epoch behavior, route-policy use of session or bound identity, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: implement_runtime_session_validation
    next_milestone: M-072
    next_ready_work_item: W-0144
    completion:
      - Select `implement_runtime_session_validation`.
      - Create M-072/W-0144 as a bounded persistent runtime session validator implementation slice.
      - Keep session creation composition, route-policy session identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_runtime_session_validation`, because the runtime session validation gate, durable runtime session repository, and PostgreSQL adapter already exist. Nakama informs lifecycle validity, expiration, refresh, logout, and active socket separation; Pitaya informs separation of acceptors, session context, handlers, and durable validation.

  - id: W-0144
    milestone: M-072
    title: Implement runtime session validation
    status: completed
    change: changes/2026-05-17-implement-runtime-session-validation
    depends_on:
      - W-0143
    related_decisions:
      - ADR-0066
    completion:
      - Add `runtime/internal/app/runtime_session_validator.go`.
      - Add `runtime/internal/app/runtime_session_validator_test.go`.
      - Implement an application-owned persistent session validator using `runtime/internal/app/session.Repository`.
      - Use `FindActiveSessionByID` for active persisted runtime session lookup.
      - Require already validated player identity before trusting a persisted session row.
      - Set `RequestIdentity.SessionValidated = true` only after durable active-session validation succeeds.
      - Collapse lookup, expiration, revocation, malformed row, actor mismatch, player mismatch, nil repository, and repository errors to a stable redacted invalid-session reason.
      - Keep session creation, last-seen mutation, route policy, WebSocket handshake authentication, transport credential carriers, Protobuf session messages, existing envelope changes, generated output, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added `PersistentSessionValidator` under `runtime/internal/app` with focused tests. The validator is lookup-only, application-owned, repository-interface-based, redacts public invalid-session output, and does not wire into route policy, startup, WebSocket, Protobuf, logout, reconnect, or session creation behavior.
    ask_first:
      - Adding session creation at login or BindConnection, route-policy use of session-validated or bound connection identity, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0145
    milestone: M-073
    title: Confirm next milestone direction after runtime session validation implementation
    status: completed
    change: changes/2026-05-17-confirm-next-direction-after-runtime-session-validation-implementation
    depends_on:
      - W-0144
    block_reason: The next major direction crosses ask-first boundaries such as session creation composition, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_session_creation_composition_gate
      - define_bound_identity_route_policy_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: define_session_creation_composition_gate
    next_milestone: M-074
    next_ready_work_item: W-0146
    completion:
      - Select `define_session_creation_composition_gate`.
      - Create M-074/W-0146 as a bounded gate-only session creation composition slice.
      - Keep session creation implementation, route-policy session identity, WebSocket handshake authentication, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_session_creation_composition_gate`, because runtime session validation now exists but no durable sessions are created after login or connection binding. Nakama informs login/session lifecycle, expiration, refresh, logout, and session management pressure; Pitaya informs separation of acceptors, session context, handlers, and durable session composition.
    ask_first:
      - Selecting session creation composition, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.

  - id: W-0146
    milestone: M-074
    title: Define session creation composition gate
    status: completed
    change: changes/2026-05-17-define-session-creation-composition-gate
    depends_on:
      - W-0145
    related_decisions:
      - ADR-0067
    completion:
      - Add `docs/session-creation-composition-gate.md`.
      - Add `docs/session-creation-composition-gate.zh-CN.md`.
      - Add `ADR-0067`.
      - Define future application-owned session creation composition under `runtime/internal/app`.
      - Define future login-time composition candidate using `AuthenticateWithDeviceCredential`.
      - Define future `session.Repository.CreateRuntimeSession` use through unit-of-work capabilities.
      - Define session id, lifetime, token linkage, redaction, future tests, and Nakama/Pitaya reference mapping.
      - Keep session creation implementation, authentication service code changes, session id generation code, route policy, WebSocket handshake authentication, protocol carriers, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added a gate-only session creation composition standard. The gate makes future durable runtime session creation application-owned and transactionally composed with token storage only after a later implementation gate. It records private token-record linkage, session id and lifetime posture, redaction rules, and future test requirements without changing authentication service behavior, runtime validation, route policy, WebSocket transport, Protobuf protocol, logout/revocation behavior, reconnect behavior, dependencies, or direct external API compatibility.
    ask_first:
      - Adding session creation implementation at login or BindConnection, session id generation code, route-policy use of session-validated or bound connection identity, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0147
    milestone: M-075
    title: Confirm next milestone direction after session creation composition gate
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-session-creation-composition-gate
    depends_on:
      - W-0146
    block_reason: The next major direction crosses ask-first boundaries such as session creation implementation, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_session_creation_composition
      - define_bound_identity_route_policy_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: implement_session_creation_composition
    next_milestone: M-076
    next_ready_work_item: W-0148
    completion:
      - Select `implement_session_creation_composition`.
      - Create M-076/W-0148 as a bounded implementation slice.
      - Keep route-policy session identity, WebSocket handshake authentication, Protobuf session carriers, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_session_creation_composition`, because runtime sessions can now be persisted and validated but successful login still needed to create durable session lifecycle rows. Nakama informs authentication-created session lifecycle pressure; Pitaya informs keeping durable session creation in application composition instead of transport or protocol code.
    ask_first:
      - Selecting session creation implementation, route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.

  - id: W-0148
    milestone: M-076
    title: Implement session creation composition
    status: completed
    change: changes/2026-05-18-implement-session-creation-composition
    depends_on:
      - W-0147
    related_decisions:
      - ADR-0068
    completion:
      - Add `SessionIDGenerator` as an authentication service dependency.
      - Extend the login unit-of-work capability with `NewSessionRepository`.
      - Create a durable active runtime session in `AuthenticateWithDeviceCredential` after token storage and before commit.
      - Link the runtime session to the stored `access_token_record_id` as private server metadata.
      - Align the first session expiration with the access-token expiration and initialize `last_seen_at` to `issued_at`.
      - Return application-owned session metadata only after unit-of-work success.
      - Add startup random runtime session id generation.
      - Add focused tests for success, session repository failure, session id failure, session creation failure, commit failure, and unchanged access-token validation.
      - Keep route policy, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, generated output, logout/revocation active-connection behavior, reconnect/epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Implemented login-time durable runtime session creation in application authentication service composition. The access-token record and active runtime session are stored in the same unit of work; failure or commit failure returns a rejected result without token/session success material. The implementation keeps protocol, transport, route policy, logout, reconnect, generated output, dependencies, and direct Nakama/Pitaya API compatibility unchanged.
    ask_first:
      - Adding route-policy use of session-validated or bound connection identity, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0149
    milestone: M-077
    title: Confirm next milestone direction after session creation composition implementation
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-session-creation-composition-implementation
    depends_on:
      - W-0148
    block_reason: The next major direction crosses ask-first boundaries such as route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_bound_identity_route_policy_gate
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: define_bound_identity_route_policy_gate
    next_milestone: M-078
    next_ready_work_item: W-0150
    completion:
      - Select `define_bound_identity_route_policy_gate`.
      - Create M-078/W-0150 as a bounded gate-only route policy slice.
      - Keep route-policy implementation, logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_bound_identity_route_policy_gate`, because access-token proof, first-message connection binding, durable session validation, and login-created sessions now need an explicit application-owned policy boundary before ordinary routes can use bound or session-validated identity. Nakama informs authenticated session material as gameplay access pressure; Pitaya informs keeping acceptor, session context, and handlers separated.
    ask_first:
      - Selecting route-policy use of session or bound identity, logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.

  - id: W-0150
    milestone: M-078
    title: Define bound identity route policy gate
    status: completed
    change: changes/2026-05-18-define-bound-identity-route-policy-gate
    depends_on:
      - W-0149
    related_decisions:
      - ADR-0069
    completion:
      - Add `docs/bound-identity-route-policy-gate.md`.
      - Add `docs/bound-identity-route-policy-gate.zh-CN.md`.
      - Add `ADR-0069`.
      - Define future application-owned route policy under `runtime/internal/app`.
      - Define candidate route policy families for public, request-token, bound-connection, session-validated, and bound-session routes.
      - Recommend keeping ordinary protected domain routes on request-level access-token proof for the first implementation posture.
      - Define identity-source agreement, fail-closed behavior, redaction expectations, future tests, and Nakama/Pitaya reference mapping.
      - Keep route-policy implementation, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, generated output, logout/revocation active-connection behavior, reconnect/epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added a gate-only bound identity route policy standard. The gate makes future route-policy use of request-token, bound-connection, session-validated, and bound-session identity explicit and route-scoped. It does not change production route behavior, WebSocket transport, Protobuf protocol, logout/revocation behavior, reconnect behavior, dependencies, or direct external API compatibility.
    ask_first:
      - Adding route-policy use of session-validated or bound connection identity, removing per-request token proof from ordinary protected routes, WebSocket handshake authentication, transport credential carriers, Protobuf session messages or envelope changes, logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0151
    milestone: M-079
    title: Confirm next milestone direction after bound identity route policy gate
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-bound-identity-route-policy-gate
    depends_on:
      - W-0150
    block_reason: The next major direction crosses ask-first boundaries such as route-policy implementation, logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_bound_identity_route_policy
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    selected_direction: implement_bound_identity_route_policy
    next_milestone: M-080
    next_ready_work_item: W-0152
    completion:
      - Select `implement_bound_identity_route_policy`.
      - Create M-080/W-0152 as a bounded route policy implementation slice.
      - Keep ordinary protected domain routes on request-level access-token proof by default.
      - Keep WebSocket handshake authentication, protocol session carriers, logout/revocation active-connection behavior, reconnect/epoch behavior, dependencies, memory durable session behavior, direct Nakama/Pitaya compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_bound_identity_route_policy`, because the gate already established route-scoped policy families and the safest next step is to implement application-owned vocabulary, classification, and fail-closed identity agreement while preserving ordinary protected route behavior. Nakama informs session-backed gameplay access pressure; Pitaya informs keeping connection acceptors, session context, route policy, and handlers separated.
    ask_first:
      - Selecting logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, route-policy production reclassification for ordinary domain routes, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.

  - id: W-0152
    milestone: M-080
    title: Implement bound identity route policy
    status: completed
    change: changes/2026-05-18-implement-bound-identity-route-policy
    depends_on:
      - W-0151
    related_decisions:
      - ADR-0070
    completion:
      - Extend `RouteProtectionPolicy` with explicit route-scoped protection requirements.
      - Add `RouteProtectionPublic`, `RouteProtectionRequestTokenRequired`, `RouteProtectionBoundConnectionRequired`, `RouteProtectionSessionValidatedRequired`, and `RouteProtectionBoundSessionRequired` vocabulary.
      - Keep `AuthenticateWithDeviceCredential` public and ordinary protected routes `request_token_required` by default.
      - Keep request-token protected route behavior unchanged, including proof-carrier requirement, token validation, metadata-only rejection, and `SessionValidated=false` handoff.
      - Add fail-closed explicit bound-connection, session-validated, and bound-session policy checks for routes explicitly classified into those families.
      - Require bound/session identity source agreement for bound-session routes.
      - Add focused tests in `runtime/internal/app/route_authentication_test.go`.
      - Keep WebSocket, Protobuf, startup route registration, connection registries, session validator wiring, logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, dependencies, generated output, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Implemented the conservative bound identity route-policy slice. The application route protector can classify routes into public, request-token, bound-connection, session-validated, and bound-session policies; only explicitly classified routes can use bound/session identities; default protected routes still require per-request access-token proof. Tests cover classification, fail-closed metadata-only rejection, bound/session success and failure, identity mismatch, redaction, and unchanged token behavior.
    ask_first:
      - Adding production route reclassification away from request-token proof for ordinary protected domain routes, wiring a connection binding registry into frame handling, wiring persistent session validation into route policy, changing WebSocket handshake authentication, adding transport credential carriers, changing Protobuf session carriers or envelope shape, exposing session ids to clients, adding logout/revocation active-connection invalidation, reconnect/resume/duplicate replacement behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0153
    milestone: M-081
    title: Confirm next milestone direction after bound identity route policy implementation
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-bound-identity-route-policy-implementation
    depends_on:
      - W-0152
    block_reason: The next major direction crosses ask-first boundaries such as logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, route-policy production reclassification, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_logout_revocation_active_connection_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting logout/revocation active-connection behavior, reconnect/epoch behavior, protocol session carriers, route-policy production reclassification, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: define_logout_revocation_active_connection_gate
    next_milestone: M-082
    next_ready_work_item: W-0154
    completion:
      - Select `define_logout_revocation_active_connection_gate`.
      - Create M-082/W-0154 as a bounded gate-only logout/revocation active-connection slice.
      - Keep logout execution, token revocation execution, runtime session revocation execution, active WebSocket connection invalidation, connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_logout_revocation_active_connection_gate`, because route policy can now distinguish identity sources but future logout and revocation behavior must define whether and how active WebSocket connections are invalidated. Nakama informs logout/session lifecycle pressure; Pitaya informs keeping acceptors, session context, handlers, and connection management separate.

  - id: W-0154
    milestone: M-082
    title: Define logout revocation active connection gate
    status: completed
    change: changes/2026-05-18-define-logout-revocation-active-connection-gate
    depends_on:
      - W-0153
    related_decisions:
      - ADR-0071
    completion:
      - Add `docs/logout-revocation-active-connection-gate.md`.
      - Add `docs/logout-revocation-active-connection-gate.zh-CN.md`.
      - Add `ADR-0071`.
      - Define future application-owned logout/revocation active-connection policy under `runtime/internal/app`.
      - Define token revocation, runtime session revocation, and active-connection invalidation as separate future state transitions.
      - Recommend presented-token logout as the first future scope.
      - Require an explicit connection registry before active WebSocket sockets can be targeted.
      - Define redaction expectations, future tests, and Nakama/Pitaya reference mapping.
      - Keep logout execution, token revocation execution, runtime session revocation execution, WebSocket close policy, Protobuf logout routes, protocol session carriers, reconnect/epoch behavior, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added a gate-only logout/revocation active-connection standard. The gate records future ownership and policy questions before any code can revoke tokens, revoke runtime sessions, close active sockets, or add a connection registry. It adapts Nakama session lifecycle pressure and Pitaya acceptor/session/handler separation without changing runtime behavior.
    ask_first:
      - Adding logout execution, token revocation execution, runtime session revocation execution, active connection registries, WebSocket close behavior, kick/disconnect behavior, reconnect/resume/duplicate replacement behavior, Protobuf logout routes, protocol session carriers, WebSocket handshake authentication, transport credential carriers, cleanup jobs, operations dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0155
    milestone: M-083
    title: Confirm next milestone direction after logout revocation active connection gate
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-logout-revocation-active-connection-gate
    depends_on:
      - W-0154
    block_reason: The next major direction crosses ask-first boundaries such as logout execution, active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_logout_access_token_behavior_gate
      - define_active_connection_registry_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_logout_route_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting logout execution, active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: define_logout_access_token_behavior_gate
    next_milestone: M-084
    next_ready_work_item: W-0156
    completion:
      - Select `define_logout_access_token_behavior_gate`.
      - Create M-084/W-0156 as a bounded gate-only logout access-token behavior slice.
      - Keep `LogoutAccessToken` implementation, token revocation execution, runtime session revocation, active WebSocket connection invalidation, connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_logout_access_token_behavior_gate`, because the previous gate recommended presented-token logout as the first future scope and the service needs explicit proof validation, verifier comparison, transaction ordering, public error collapse, and session/socket/protocol deferrals before revocation code is added. Nakama informs authentication/session lifecycle pressure; Pitaya informs keeping session/connection infrastructure separate from handlers.

  - id: W-0156
    milestone: M-084
    title: Define logout access token behavior gate
    status: completed
    change: changes/2026-05-18-define-logout-access-token-behavior-gate
    depends_on:
      - W-0155
    related_decisions:
      - ADR-0072
    completion:
      - Add `docs/logout-access-token-behavior-gate.md`.
      - Add `docs/logout-access-token-behavior-gate.zh-CN.md`.
      - Add `ADR-0072`.
      - Define future application-owned `LogoutAccessToken` behavior under `runtime/internal/app/authentication`.
      - Define the first logout scope as `presented_access_token_only`.
      - Require missing/malformed token proof rejection before unit of work.
      - Require token lookup digest and verifier digest comparison before revocation.
      - Require token kind, active status, non-expiry, expected audience, supported verifier metadata, and constant-time verifier comparison before revocation.
      - Require success only after unit-of-work commit.
      - Collapse public failures to missing, malformed, invalid, or unavailable token errors.
      - Keep logout implementation, token revocation execution, runtime session revocation, active connection invalidation, connection registry, WebSocket close policy, Protobuf logout routes, protocol session carriers, refresh, logout-all, admin revocation, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added a gate-only logout access-token behavior standard. The gate makes future `LogoutAccessToken` behavior precise without changing code: revoke only the verified presented opaque access-token record, compare verifier digest before revocation, return success only after commit, and keep sessions, sockets, protocol routes, reconnect, refresh, logout-all, admin revocation, dependencies, and direct Nakama/Pitaya compatibility deferred.
    ask_first:
      - Adding `LogoutAccessToken` implementation, token revocation execution, runtime session revocation, active connection registries, WebSocket close behavior, kick/disconnect behavior, reconnect/resume/duplicate replacement behavior, Protobuf logout routes, protocol session carriers, WebSocket handshake authentication, transport credential carriers, cleanup jobs, operations dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0157
    milestone: M-085
    title: Confirm next milestone direction after logout access token behavior gate
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-logout-access-token-behavior-gate
    depends_on:
      - W-0156
    block_reason: The next major direction crosses ask-first boundaries such as logout implementation, active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_logout_access_token_behavior
      - define_active_connection_registry_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_logout_route_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting logout implementation, active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: implement_logout_access_token_behavior
    next_milestone: M-086
    next_ready_work_item: W-0158
    completion:
      - Select `implement_logout_access_token_behavior`.
      - Create M-086/W-0158 as a bounded service implementation slice.
      - Keep runtime session revocation, active WebSocket connection invalidation, connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_logout_access_token_behavior`, because the previous gate defined a precise presented-token-only service behavior and no longer required another gate before code. Nakama informs token/session lifecycle pressure; Pitaya informs keeping connection/session infrastructure separate from handler credential parsing.

  - id: W-0158
    milestone: M-086
    title: Implement logout access token behavior
    status: completed
    change: changes/2026-05-18-implement-logout-access-token-behavior
    depends_on:
      - W-0157
    related_decisions:
      - ADR-0073
    completion:
      - Replace fail-closed `LogoutAccessToken` behavior with presented opaque access-token revocation.
      - Reject missing and malformed access-token proof before unit of work.
      - Compute token lookup digest and use `FindTokenByLookupDigest`.
      - Require active access-token posture and compare verifier digest before revocation.
      - Call `RevokeToken` once with `logout_presented_access_token`.
      - Return `LogoutStatusRevoked` only after unit-of-work commit.
      - Collapse lookup miss, inactive/revoked/expired token, wrong kind, wrong audience, unsupported verifier metadata, unknown key id, and verifier mismatch to public invalid-token behavior.
      - Map repository and commit failures to token-store-unavailable behavior without claiming success.
      - Add focused tests in `runtime/internal/app/authentication/service_test.go`.
      - Keep runtime session revocation, active connection invalidation, connection registry, WebSocket close policy, Protobuf logout routes, protocol session carriers, refresh, logout-all, admin revocation, cleanup jobs, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Implemented the first logout access-token behavior slice. `LogoutAccessToken` now validates the presented opaque token, finds the matching token record by lookup digest, verifies token posture and verifier digest, revokes the token record with `logout_presented_access_token`, and reports success only after commit. Tests cover success, redacted rejection, invalid token posture, verifier mismatch, unavailable dependencies, commit failure, and absence of player/session side effects.
    ask_first:
      - Adding runtime session revocation, active connection registries, WebSocket close behavior, kick/disconnect behavior, reconnect/resume/duplicate replacement behavior, Protobuf logout routes, protocol session carriers, WebSocket handshake authentication, transport credential carriers, refresh, logout-all, admin revocation, cleanup jobs, operations dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0159
    milestone: M-087
    title: Confirm next milestone direction after logout access token behavior implementation
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-logout-access-token-behavior-implementation
    depends_on:
      - W-0158
    block_reason: The next major direction crosses ask-first boundaries such as active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_active_connection_registry_gate
      - define_protocol_logout_route_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting active connection registry behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: define_active_connection_registry_gate
    next_milestone: M-088
    next_ready_work_item: W-0160
    completion:
      - Select `define_active_connection_registry_gate`.
      - Create M-088/W-0160 as a bounded gate-only active connection registry slice.
      - Keep active connection registry implementation, WebSocket close policy, kick/disconnect behavior, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_active_connection_registry_gate`, because logout can now revoke the presented token record but future active socket targeting still needs an application-owned registry boundary. Nakama informs realtime socket lifecycle pressure; Pitaya informs keeping acceptors, sessions, handlers, and connection management separate.

  - id: W-0160
    milestone: M-088
    title: Define active connection registry gate
    status: completed
    change: changes/2026-05-18-define-active-connection-registry-gate
    depends_on:
      - W-0159
    related_decisions:
      - ADR-0074
    completion:
      - Add `docs/active-connection-registry-gate.md`.
      - Add `docs/active-connection-registry-gate.zh-CN.md`.
      - Add `ADR-0074`.
      - Define future application-owned active connection registry behavior under `runtime/internal/app/connection`.
      - Select a single-process, in-memory, non-durable first registry posture.
      - Define server-observed connection record vocabulary, validated identity linkage rules, redaction rules, and future capabilities.
      - Keep logout behavior, runtime session revocation, active socket invalidation, WebSocket close policy, kick/disconnect behavior, reconnect/epoch behavior, Protobuf logout routes, protocol session carriers, durable/distributed registry storage, dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added a gate-only active connection registry standard. The gate records future ownership and first posture before any code can target active sockets by player, runtime session, token record, connection id, or epoch. It adapts Nakama realtime lifecycle pressure and Pitaya acceptor/session/handler separation without changing runtime behavior.
    ask_first:
      - Adding active connection registry implementation, WebSocket close behavior, kick/disconnect behavior, runtime session revocation, reconnect/resume/duplicate replacement behavior, Protobuf logout routes, protocol session carriers, WebSocket handshake authentication, transport credential carriers, durable/distributed registry storage, cleanup jobs, operations dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0161
    milestone: M-089
    title: Confirm next milestone direction after active connection registry gate
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-active-connection-registry-gate
    depends_on:
      - W-0160
    block_reason: The next major direction crosses ask-first boundaries such as active connection registry implementation, WebSocket close policy, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_active_connection_registry_single_process
      - define_websocket_close_policy_gate
      - define_protocol_logout_route_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting active connection registry implementation, WebSocket close policy, reconnect/epoch behavior, protocol logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: implement_active_connection_registry_single_process
    next_milestone: M-090
    next_ready_work_item: W-0162
    completion:
      - Select `implement_active_connection_registry_single_process`.
      - Create M-090/W-0162 as a bounded application-owned active connection registry implementation slice.
      - Keep WebSocket close policy, kick/disconnect behavior, runtime session revocation, reconnect/epoch behavior, Protobuf logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_active_connection_registry_single_process`, because logout and future realtime lifecycle policy now need safe server-owned targeting state. Nakama informs coordinated session/socket lifecycle state; Pitaya informs keeping transport acceptors, sessions, route handlers, and connection management separated.

  - id: W-0162
    milestone: M-090
    title: Implement active connection registry single process
    status: completed
    change: changes/2026-05-18-implement-active-connection-registry-single-process
    depends_on:
      - W-0161
    related_decisions:
      - ADR-0075
    completion:
      - Add `runtime/internal/app/connection/registry.go`.
      - Add `runtime/internal/app/connection/registry_test.go`.
      - Implement `RegisterOpenConnection`, `BindConnectionIdentity`, `MarkConnectionClosed`, `FindConnectionByID`, `ListConnectionsByPlayerID`, `ListConnectionsByRuntimeSessionID`, `ListConnectionsByAccessTokenRecordID`, and `MarkConnectionInvalidated`.
      - Require server-observed connection id and epoch for registration and targeting.
      - Require validated player identity linkage before binding player, runtime session, or access-token record ids.
      - Keep raw token material, credential material, lookup digests, verifier digests, verifier key ids, headers, cookies, subprotocol values, query strings, remote addresses, and inner payload bytes out of registry records.
      - Keep WebSocket close policy, concrete socket closing, kick/disconnect behavior, runtime session revocation, duplicate replacement, reconnect/epoch behavior, protocol carriers, durable/distributed registry storage, dependencies, memory durable session behavior, broader game backend behavior, and direct Nakama/Pitaya API compatibility deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added the first application-owned in-memory active connection registry. The registry is single-process and non-durable, exposes policy-neutral lifecycle and lookup primitives, and is verified with focused tests for active records, validated identity binding, terminal-state exclusion, copy semantics, redacted record shape, and dependency boundaries.
    ask_first:
      - Adding WebSocket close policy, concrete socket close handoff, kick/disconnect behavior, runtime session revocation, duplicate replacement behavior, reconnect/resume/epoch behavior, Protobuf logout routes, protocol session carriers, WebSocket handshake authentication, transport credential carriers, durable/distributed registry storage, cleanup jobs, operations dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0163
    milestone: M-091
    title: Confirm next milestone direction after active connection registry implementation
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-active-connection-registry-implementation
    depends_on:
      - W-0162
    block_reason: The next major direction crosses ask-first boundaries such as WebSocket close policy, protocol logout routes, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_websocket_close_policy_gate
      - define_protocol_logout_route_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting WebSocket close policy, protocol logout routes, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: define_websocket_close_policy_gate
    next_milestone: M-092
    next_ready_work_item: W-0164
    completion:
      - Select `define_websocket_close_policy_gate`.
      - Create M-092/W-0164 as a bounded gate-only WebSocket close policy slice.
      - Keep WebSocket close implementation, kick/disconnect behavior, runtime session revocation, reconnect/epoch behavior, Protobuf logout routes, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `define_websocket_close_policy_gate`, because the registry can now target active connections but no policy yet defines whether invalidation closes sockets, which close classes are allowed, or how close failures affect application outcomes. Nakama informs explicit realtime socket lifecycle policy; Pitaya informs keeping close policy separate from acceptors, sessions, handlers, and domain modules.

  - id: W-0164
    milestone: M-092
    title: Define WebSocket close policy gate
    status: completed
    change: changes/2026-05-18-define-websocket-close-policy-gate
    depends_on:
      - W-0163
    related_decisions:
      - ADR-0076
    completion:
      - Add `docs/websocket-close-policy-gate.md`.
      - Add `docs/websocket-close-policy-gate.zh-CN.md`.
      - Add `ADR-0076`.
      - Define future application-owned WebSocket close policy behavior under `runtime/internal/app`.
      - Define future narrow transport close handoff ownership under `runtime/internal/platform/transport/ws`.
      - Define close intent, close reason class, redaction, retryability, and failure semantics as future vocabulary.
      - Keep WebSocket close implementation, kick/disconnect behavior, logout behavior changes, runtime session revocation, reconnect/epoch behavior, Protobuf logout routes, protocol session carriers, generated output, dependencies, durable/distributed storage, memory durable session behavior, direct Nakama/Pitaya compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added a gate-only WebSocket close policy standard. The gate records future application-owned close decision vocabulary before any code can close active sockets by player, runtime session, token record, connection id, or epoch. It adapts Nakama realtime lifecycle policy pressure and Pitaya acceptor/session/handler separation without changing runtime behavior.
    ask_first:
      - Adding WebSocket close implementation, transport close handoff code, close codes, close reasons, kick/disconnect behavior, logout-triggered socket close, runtime session revocation, reconnect/resume/duplicate replacement behavior, Protobuf logout routes, protocol session carriers, WebSocket handshake authentication, transport credential carriers, durable/distributed registry storage, cleanup jobs, operations dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0165
    milestone: M-093
    title: Confirm next milestone direction after WebSocket close policy gate
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-websocket-close-policy-gate
    depends_on:
      - W-0164
    block_reason: The next major direction crosses ask-first boundaries such as WebSocket close policy implementation, protocol logout routes, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - implement_websocket_close_policy_single_process
      - define_protocol_logout_route_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting WebSocket close policy implementation, protocol logout routes, reconnect/epoch behavior, protocol session carriers, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: implement_websocket_close_policy_single_process
    next_milestone: M-094
    next_ready_work_item: W-0166
    completion:
      - Select `implement_websocket_close_policy_single_process`.
      - Create M-094/W-0166 as a bounded application-owned close policy primitive.
      - Keep concrete WebSocket close handoff, close codes, close reason text, logout protocol routes, reconnect/epoch behavior, protocol session carriers, durable/distributed registry storage, operations/admin disconnect, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
    completion_summary: The maintainer authorized the recommended direction. The selected direction is `implement_websocket_close_policy_single_process`, because the close policy gate defined the vocabulary and the active registry can now be invalidated by application-owned policy without letting transport, authentication, or protocol handlers own socket lifecycle decisions. Nakama informs explicit realtime socket lifecycle semantics; Pitaya informs keeping connection management separate from acceptors, sessions, and route handlers.

  - id: W-0166
    milestone: M-094
    title: Implement WebSocket close policy single process
    status: completed
    change: changes/2026-05-18-implement-websocket-close-policy-single-process
    depends_on:
      - W-0165
    related_decisions:
      - ADR-0077
    completion:
      - Add `runtime/internal/app/connection/close_policy.go`.
      - Add `runtime/internal/app/connection/close_policy_test.go`.
      - Implement application-owned close policy targeting by connection id and epoch, player id, runtime session id, and access-token record id through active bound registry records only.
      - Emit redacted `CloseIntent` values with internal reason class, target kind, retryability, public visibility, and `mark_invalidated_only` transport action.
      - Mark matched registry records invalidated through `MarkConnectionInvalidated` without closing concrete WebSocket sockets.
      - Keep unbound, missing, closed, or otherwise inactive targets from producing synthetic close intents.
      - Keep raw token material, credential material, lookup digests, verifier digests, key ids, headers, cookies, subprotocol values, query strings, remote addresses, close codes, close reason text, and payload bytes out of close intents.
      - Keep authentication service behavior, WebSocket transport handoff, close codes, close reason text, kick/disconnect public API behavior, logout routes, reconnect/epoch behavior, generated output, dependencies, durable/distributed registry storage, operations/admin disconnect, direct Nakama/Pitaya API compatibility, and broader game backend behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change specs, ADR, and conversation memory.
    completion_summary: Added the first application-owned, single-process WebSocket close policy primitive. It resolves only active bound registry records, invalidates them with redacted internal reason classes, returns `CloseIntent` values with `mark_invalidated_only` transport action, and does not perform concrete socket close handoff. The slice adapts Nakama's explicit realtime lifecycle separation and Pitaya's session/connection-management layering without copying their public APIs.
    ask_first:
      - Adding concrete WebSocket transport close handoff, close codes, close reason text, protocol close messages, logout-triggered route behavior, runtime session revocation close behavior, duplicate replacement, reconnect/resume/epoch behavior, protocol session carriers, WebSocket handshake authentication, transport credential carriers, durable/distributed registry storage, cleanup jobs, operations dependencies, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend behavior.

  - id: W-0167
    milestone: M-095
    title: Confirm next milestone direction after WebSocket close policy implementation
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-websocket-close-policy-implementation
    depends_on:
      - W-0166
    block_reason: The next major direction crosses ask-first boundaries such as protocol logout routes, reconnect/epoch behavior, protocol session carriers, concrete transport close handoff, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend scope.
    candidate_directions:
      - define_protocol_logout_route_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - define_transport_close_handoff_gate
      - strengthen_operations_observability_and_admin_tooling
      - expand_core_game_backend_modules_after_nakama_pitaya_review
    ask_first:
      - Selecting protocol logout routes, reconnect/epoch behavior, protocol session carriers, concrete transport close handoff, operations posture, memory durable session behavior, direct Nakama/Pitaya API compatibility, or broader game backend module expansion.
    selected_direction: expand_core_game_backend_modules_after_nakama_pitaya_review
    next_milestone: M-096
    next_ready_work_item: W-0168
    completion:
      - Select `expand_core_game_backend_modules_after_nakama_pitaya_review` in response to the maintainer's clarified Nakama/Pitaya-class product target.
      - Implement the direction as `ratify_nakama_pitaya_product_parity_roadmap` before adding feature modules.
      - Preserve protocol logout routes, reconnect/epoch behavior, protocol session carriers, concrete transport close handoff, operations behavior, direct Nakama/Pitaya API compatibility, and broader game backend implementation behind later gates.
    completion_summary: The maintainer clarified that vibit must become a Nakama/Pitaya-class product covering common capability families. The selected direction is `expand_core_game_backend_modules_after_nakama_pitaya_review`, scoped first to a checkable roadmap ratification so future work can align product parity with vibit's agent-native development method.

  - id: W-0168
    milestone: M-096
    title: Ratify Nakama/Pitaya product parity roadmap
    status: completed
    change: changes/2026-05-18-ratify-nakama-pitaya-product-parity-roadmap
    depends_on:
      - W-0167
    related_decisions:
      - ADR-0078
    completion:
      - Add `docs/nakama-pitaya-product-parity-roadmap.md`.
      - Add `docs/nakama-pitaya-product-parity-roadmap.zh-CN.md`.
      - Add `ADR-0078`.
      - Define Nakama/Pitaya-class product parity as common capability coverage, not direct API compatibility.
      - Define parity capability families for identity/auth/session, connection lifecycle, storage, presence/status/notifications, chat/realtime messaging, social, competitive systems, economy/progression, matchmaking, match runtime, server runtime hooks/RPC, operations, SDK/developer experience, and distributed runtime.
      - Record lifecycle closure as the near-term phase before presence, chat, social, matchmaking, and match runtime expansion.
      - Add `runtime.reference_product_parity_roadmap` repository check coverage.
      - Keep runtime behavior, protocol route changes, generated output, dependencies, data migrations, concrete WebSocket close, reconnect behavior, protocol session carriers, direct Nakama/Pitaya API compatibility, and broad product modules deferred.
    completion_summary: Ratified a checkable Nakama/Pitaya product parity roadmap. The roadmap sets a same-class common capability coverage target, preserves vibit's agent-native constraints, and recommends `define_protocol_logout_route_gate` as the next concrete lifecycle-closure step.
    ask_first:
      - Adding direct Nakama/Pitaya API compatibility, generated output, protocol routes, concrete WebSocket close handoff, reconnect/epoch behavior, protocol session carriers, presence, chat, friends, groups, parties, leaderboards, tournaments, matchmaking, match runtime, server runtime hooks/RPC, client SDKs, admin console, distributed runtime, new dependencies, or broad game backend behavior.

  - id: W-0169
    milestone: M-097
    title: Define protocol logout route gate
    status: completed
    change: changes/2026-05-18-define-protocol-logout-route-gate
    depends_on:
      - W-0168
    related_decisions:
      - ADR-0079
      - ADR-0078
    completion:
      - Add `docs/protocol-logout-route-gate.md`.
      - Add `docs/protocol-logout-route-gate.zh-CN.md`.
      - Add `ADR-0079`.
      - Define a gate-only standard for future client-facing `runtime.authentication.LogoutAccessToken` protocol route behavior.
      - Map the gate to the Nakama/Pitaya product parity lifecycle-closure phase.
      - Specify ownership between authentication service, application bootstrap/dispatch, Protobuf protocol adapter, and WebSocket transport.
      - Specify request proof carrier posture, response semantics, public error mapping, idempotency posture, and interaction with close policy.
      - Keep concrete WebSocket close handoff, close codes, close reason text, reconnect/epoch behavior, protocol session carriers, presence, chat, social modules, match runtime, admin disconnect, generated output, dependencies, direct Nakama/Pitaya API compatibility, and implementation behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change spec, ADR, and conversation memory.
    completion_summary: Added the gate-only protocol logout route standard. The gate chooses `runtime.authentication.LogoutAccessToken` as the future explicit command route, selects `access_token_in_logout_request_payload` and `explicit_service_validated_token_lifecycle_route`, preserves the existing envelope and WebSocket transport neutrality, and creates M-098/W-0170 as the next implementation slice.
    ask_first:
      - Adding Protobuf logout source messages, generated output, runtime route registration, concrete socket close, close code mapping, close reason text, reconnect/epoch behavior, protocol session carriers, presence, chat, social modules, matchmaking, match runtime, operations/admin disconnect, dependencies, or direct Nakama/Pitaya API compatibility.

  - id: W-0170
    milestone: M-098
    title: Implement protocol logout route
    status: completed
    change: changes/2026-05-18-implement-protocol-logout-route
    depends_on:
      - W-0169
    expected_change_id: implement-protocol-logout-route
    related_decisions:
      - ADR-0079
      - ADR-0073
    completion:
      - Add the ADR-0079-authorized logout request/response messages to `proto/vibit/authentication/v1/authentication.proto`.
      - Regenerate Go Protobuf output through the accepted Buf path.
      - Add protocol bridge mapping for logout request and result payloads.
      - Add application bootstrap handler and route registration for `runtime.authentication.LogoutAccessToken`.
      - Wire the route through PostgreSQL startup composition when the authentication service exists.
      - Add transaction bypass for the logout route because authentication service owns its own unit-of-work.
      - Add focused tests for mapping, redaction, route registration, transaction bypass, unchanged envelope, transport neutrality, and no socket/session/reconnect side effects.
      - Keep concrete WebSocket close handoff, runtime session revocation, active connection invalidation, reconnect/epoch behavior, protocol session carriers, dependencies, direct Nakama/Pitaya API compatibility, and broader product behavior deferred.
    completion_summary: Exposed the existing `LogoutAccessToken` service behavior as the explicit `runtime.authentication.LogoutAccessToken` command route for the PostgreSQL runtime path. The implementation added Protobuf logout request/response messages, regenerated Go Protobuf output through Buf, mapped request/result payloads in the protocol bridge, registered the application bootstrap handler, added transaction bypass, rejected wrapped logout payloads, and covered the route with focused app/protocol/startup/dispatcher tests. It did not close sockets, revoke runtime sessions, invalidate active connection records, add reconnect behavior, add protocol session carriers, add dependencies, or add direct Nakama/Pitaya API compatibility.
    ask_first:
      - Adding concrete socket close, close code mapping, close reason text, runtime session revocation, active connection invalidation, reconnect/epoch behavior, protocol session carriers, presence, chat, social modules, matchmaking, match runtime, operations/admin disconnect, dependencies, memory durable logout behavior, or direct Nakama/Pitaya API compatibility.

  - id: W-0171
    milestone: M-099
    title: Confirm next direction after protocol logout route implementation
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-protocol-logout-route-implementation
    depends_on:
      - W-0170
    expected_change_id: confirm-next-direction-after-protocol-logout-route-implementation
    candidate_directions:
      - define_transport_close_handoff_gate
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - define_presence_lifecycle_gate
      - strengthen_operations_observability_and_admin_tooling
    recommended_direction: define_transport_close_handoff_gate
    selected_direction: define_transport_close_handoff_gate
    next_milestone: M-100
    next_ready_work_item: W-0172
    completion:
      - Select the next lifecycle-closure direction after protocol logout route implementation.
      - Prefer `define_transport_close_handoff_gate` because the close policy can mark registry records invalidated but cannot yet hand a concrete close action to WebSocket transport.
      - Preserve runtime session revocation, reconnect/epoch behavior, protocol session carriers, presence, chat, social modules, matchmaking, match runtime, operations/admin disconnect, dependencies, memory durable logout behavior, direct Nakama/Pitaya API compatibility, and broad product behavior behind later gates unless explicitly selected.
    completion_summary: Selected `define_transport_close_handoff_gate` after W-0170. The next gate must define a narrow application-to-WebSocket handoff for concrete socket close mechanics while keeping authentication, logout, session revocation, reconnect, protocol carriers, close codes, close reason text, operations/admin disconnect, dependencies, direct Nakama/Pitaya compatibility, and broad product modules deferred.
    ask_first:
      - Selecting concrete socket close handoff implementation, close code mapping, close reason text, runtime session revocation, active connection invalidation beyond the existing registry marker, reconnect/epoch behavior, protocol session carriers, presence, chat, social modules, matchmaking, match runtime, operations/admin disconnect, dependencies, memory durable behavior, direct Nakama/Pitaya API compatibility, or broader product module expansion.

  - id: W-0172
    milestone: M-100
    title: Define transport close handoff gate
    status: completed
    change: changes/2026-05-18-define-transport-close-handoff-gate
    depends_on:
      - W-0171
    expected_change_id: define-transport-close-handoff-gate
    related_decisions:
      - ADR-0077
      - ADR-0078
      - ADR-0079
      - ADR-0080
    completion:
      - Add `docs/transport-close-handoff-gate.md`.
      - Add `docs/transport-close-handoff-gate.zh-CN.md`.
      - Add a new ADR for the transport close handoff gate.
      - Define future narrow ownership between application close policy and WebSocket transport concrete close mechanics.
      - Specify whether handoff targets connection id and epoch only, and how it avoids client-supplied metadata as authority.
      - Specify redaction rules for close intent handoff and failure handling.
      - Keep concrete socket close implementation, close codes, close reason text, logout-triggered close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, and broad product behavior deferred.
      - Update manifests, AGENTS guides, rules, checks, change spec, ADR, and conversation memory.
    completion_summary: Added `docs/transport-close-handoff-gate.md`, its Simplified Chinese translation, and ADR-0080. The gate defines a future narrow application-to-WebSocket close handoff, selects server-observed `connection_id + epoch` as the first target, keeps transport credential-neutral and policy-neutral, and does not implement concrete socket close, close codes, close reason text, logout-triggered close, runtime session revocation, reconnect, protocol carriers, operations/admin disconnect, dependencies, direct compatibility, or broad product behavior.
    ask_first:
      - Adding concrete WebSocket socket close implementation, choosing close codes or close reason text, changing logout to close sockets, revoking runtime sessions, changing registry invalidation semantics, adding reconnect/epoch behavior, adding protocol session carriers, adding operations/admin disconnect behavior, adding dependencies, adding direct Nakama/Pitaya API compatibility, or expanding into presence/chat/social/matchmaking/match runtime before this gate is complete.

  - id: W-0173
    milestone: M-101
    title: Confirm next direction after transport close handoff gate
    status: completed
    change: changes/2026-05-18-confirm-next-direction-after-transport-close-handoff-gate
    depends_on:
      - W-0172
    expected_change_id: confirm-next-direction-after-transport-close-handoff-gate
    candidate_directions:
      - implement_transport_close_handoff_single_process
      - define_reconnect_connection_epoch_gate
      - define_protocol_session_carrier_gate
      - define_presence_lifecycle_gate
      - strengthen_operations_observability_and_admin_tooling
    recommended_direction: implement_transport_close_handoff_single_process
    selected_direction: implement_transport_close_handoff_single_process
    next_milestone: M-102
    next_ready_work_item: W-0174
    completion:
      - Select the next lifecycle-closure direction after transport close handoff gate.
      - Prefer `implement_transport_close_handoff_single_process` because the gate has defined server-observed connection id and epoch as the future concrete close target.
      - Preserve close code mapping, close reason text, logout-triggered close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, and broad product behavior behind later gates unless explicitly selected.
    completion_summary: Selected `implement_transport_close_handoff_single_process` after W-0172. The next implementation must remain a narrow in-process WebSocket transport handoff keyed by server-observed connection id and epoch while preserving application policy ownership, transport credential neutrality, close code/reason deferrals, logout/session/reconnect deferrals, and direct Nakama/Pitaya compatibility deferrals.
    ask_first:
      - Selecting concrete socket close implementation, close code mapping, close reason text, logout-triggered close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, or broad product module expansion.

  - id: W-0174
    milestone: M-102
    title: Implement transport close handoff single process
    status: completed
    change: changes/2026-05-19-implement-transport-close-handoff-single-process
    depends_on:
      - W-0173
    expected_change_id: implement-transport-close-handoff-single-process
    related_decisions:
      - ADR-0080
      - ADR-0077
      - ADR-0081
    completion:
      - Add the first in-process WebSocket transport close handoff owned by `runtime/internal/platform/transport/ws`.
      - Target sockets only by server-observed `connection_id` and `connection_epoch`.
      - Keep application close decisions and registry invalidation policy owned by `runtime/internal/app/connection`.
      - Return redacted outcomes for close requested, socket not found, epoch mismatch, already closed, and close failed.
      - Add focused tests for stale epoch protection, missing socket redaction, transport credential neutrality, application policy ownership, and unchanged Protobuf envelope/logout/session behavior.
      - Keep close code mapping, close reason text, logout-triggered close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, and broad product behavior deferred.
    completion_summary: Added `runtime/internal/platform/transport/ws/close_handoff.go` and focused tests. `Server` now keeps a single-process in-memory socket table for accepted sockets and exposes `RequestClose` by server-observed connection id and epoch. Outcomes are redacted as close requested, socket not found, epoch mismatch, already closed, or close failed. The implementation keeps application close decisions in `runtime/internal/app/connection`, does not parse credential carriers, does not change Protobuf envelope or logout/session behavior, does not choose close codes or reason text, and does not add reconnect, protocol session carriers, dependencies, operations/admin disconnect, or direct Nakama/Pitaya API compatibility.
    ask_first:
      - Selecting close code mapping, close reason text, logout-triggered socket close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, durable/distributed close handoff, or broad product module expansion.

  - id: W-0175
    milestone: M-103
    title: Define reconnect connection epoch functional slice
    status: completed
    change: changes/2026-05-20-define-reconnect-connection-epoch-functional-slice
    depends_on:
      - W-0174
    expected_change_id: define-reconnect-connection-epoch-functional-slice
    tier: tier_2_functional
    selected_direction: define_reconnect_connection_epoch_functional_slice
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0082
      - ADR-0083
    ask_first:
      - Selecting close code mapping, close reason text, logout-triggered socket close, runtime session revocation, protocol session carriers, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, durable/distributed close handoff, or broad product module expansion.
    completion:
      - Create a change spec that embeds the reconnect/connection epoch boundary instead of creating a separate confirmation gate.
      - Define the smallest checkable reconnect/epoch behavior for the next functional slice.
      - Preserve close code mapping, close reason text, logout-triggered socket close, runtime session revocation, protocol session carriers, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, and broad product behavior behind ask-first boundaries.
    completion_summary: Added the first application-owned server-observed connection epoch progression behavior. `RegisterOpenConnection` now accepts newer epochs for an existing connection id while superseding older active records, rejects stale or repeated epochs after a newer epoch exists with `connection_epoch_stale`, preserves superseded lifecycle records for inspection, and keeps active target lists limited to active bound records. The slice followed ADR-0082 Tier 2 gate density by embedding boundaries in the change spec and did not add protocol carriers, reconnect tokens, resume behavior, socket close coupling, session revocation, dependencies, direct compatibility, or broad product modules.

  - id: W-0176
    milestone: M-104
    title: Define protocol session carrier functional slice
    status: completed
    change: changes/2026-05-20-define-protocol-session-carrier-functional-slice
    depends_on:
      - W-0175
    expected_change_id: define-protocol-session-carrier-functional-slice
    tier: tier_2_functional
    selected_direction: define_protocol_session_carrier_functional_slice
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0082
      - ADR-0084
    ask_first:
      - Selecting reconnect tokens, resume tokens, close code mapping, close reason text, logout-triggered socket close, runtime session revocation, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, durable/distributed session routing, or broad product module expansion.
    completion:
      - Create a change spec that embeds the protocol session carrier boundary instead of creating a separate confirmation gate.
      - Define the smallest checkable protocol session carrier behavior for lifecycle closure.
      - Preserve reconnect tokens, resume behavior, close code mapping, close reason text, logout-triggered socket close, runtime session revocation, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, and broad product behavior behind ask-first boundaries.
    completion_summary: Added the first bounded protocol-visible runtime session carrier. Successful login responses now reuse existing `Envelope.Session` metadata to carry the server-created runtime session id and authenticated player id, and response envelopes can derive session metadata from already validated application identity. No Protobuf source, generated output, reconnect/resume token, handshake authentication, session id proof behavior, logout-triggered close, runtime session revocation, dependency, direct compatibility, or broad product behavior was added.

  - id: W-0177
    milestone: M-105
    title: Define presence lifecycle functional slice
    status: completed
    change: changes/2026-05-20-define-presence-lifecycle-functional-slice
    depends_on:
      - W-0176
    expected_change_id: define-presence-lifecycle-functional-slice
    tier: tier_2_functional
    selected_direction: define_presence_lifecycle_functional_slice
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0082
      - ADR-0085
    ask_first:
      - Selecting chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, dependencies, direct Nakama/Pitaya API compatibility, reconnect tokens, resume tokens, logout-triggered socket close, runtime session revocation, durable/distributed presence, or broad product module expansion.
    completion:
      - Create a change spec that embeds the presence lifecycle boundary instead of creating a separate confirmation gate.
      - Define the smallest checkable presence/status behavior after protocol session carrier support.
      - Preserve chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, dependencies, direct Nakama/Pitaya API compatibility, reconnect/resume tokens, logout-triggered socket close, runtime session revocation, durable/distributed presence, and broad product behavior behind ask-first boundaries.
    completion_summary: Added the first server-owned player presence lifecycle primitive. WebSocket transport reports credential-neutral open/closed lifecycle events to startup composition, successful first-message binding records validated player identity in the registry, and `PresenceForPlayer` derives online/offline snapshots from active bound registry records. No protocol presence query, subscription, broadcast, durable/distributed presence, social, matchmaking, match runtime, dependency, direct compatibility, logout-triggered close, or runtime session revocation was added.

  - id: W-0178
    milestone: M-106
    title: Define presence protocol query functional slice
    status: completed
    change: changes/2026-05-20-define-presence-protocol-query-functional-slice
    depends_on:
      - W-0177
    expected_change_id: define-presence-protocol-query-functional-slice
    tier: tier_2_functional
    selected_direction: define_presence_protocol_query_functional_slice
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0082
      - ADR-0085
      - ADR-0086
      - ADR-0087
    ask_first:
      - Selecting presence subscriptions, broadcasts, chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, dependencies, direct Nakama/Pitaya API compatibility, reconnect tokens, resume tokens, logout-triggered socket close, runtime session revocation, durable/distributed presence, or broad product module expansion.
    completion:
      - Create a change spec that embeds the presence protocol query boundary instead of creating a separate confirmation gate.
      - Define and implement the smallest protected presence query over the existing server-owned registry-backed player presence snapshot.
      - Preserve subscriptions, broadcasts, chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, dependencies, direct Nakama/Pitaya API compatibility, reconnect/resume tokens, logout-triggered socket close, runtime session revocation, durable/distributed presence, and broad product behavior behind ask-first boundaries.
    completion_summary: Added the protected self-player `runtime.presence.GetPlayerPresence` query over server-owned active connection registry state. The slice added `vibit.presence.v1` Protobuf messages, generated Go output through Buf, application-owned presence query handlers, protocol bridge mapping, PostgreSQL startup registration, and focused tests. It preserved deferrals for subscriptions, broadcasts, cross-player presence visibility, durable/distributed presence, chat, friends, groups, parties, matchmaking, match runtime, cluster, SDK, operations/admin behavior, reconnect/resume tokens, logout-triggered socket close, runtime session revocation, dependencies, broad product behavior, and direct Nakama/Pitaya API compatibility.

  - id: W-0179
    milestone: M-107
    title: Define v0.1 alpha goal and long-term product target
    status: completed
    change: changes/2026-05-20-define-v0-1-alpha-goal
    depends_on:
      - W-0177
    expected_change_id: define-v0-1-alpha-goal
    tier: tier_3_lightweight
    selected_direction: define_v0_1_alpha_goal_and_long_term_product_target
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0078
      - ADR-0082
      - ADR-0086
    ask_first:
      - Publishing a release, changing runtime behavior, adding protocol messages, adding generated output, adding migrations, adding dependencies, selecting direct Nakama/Pitaya API compatibility, or replacing the W-0178 continuation queue.
    completion:
      - Add v0.1 alpha goal docs in English and Simplified Chinese.
      - Add ADR and conversation memory for the maintainer's refined goal.
      - Update README, AGENTS, runtime runbook wording, and manifests with target pointers.
      - Keep W-0178 as the only next_ready work item.
    completion_summary: Documented `v0.1 alpha` as the short-term developer-usable release target and AI-era Nakama/Pitaya-class server framework as the long-term target. No runtime behavior, protocol shape, generated output, migration, dependency, direct API compatibility, or release artifact was added.

  - id: W-0180
    milestone: M-108
    title: Select local onboarding device credential issuance gate
    status: completed
    change: changes/2026-05-21-select-local-onboarding-device-credential-issuance-gate
    depends_on:
      - W-0178
      - W-0179
    expected_change_id: select-local-onboarding-device-credential-issuance-gate
    tier: tier_1_security_critical
    selected_direction: define_local_onboarding_device_credential_issuance_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0082
      - ADR-0086
      - ADR-0087
      - ADR-0088
    candidate_directions:
      - define_local_onboarding_device_credential_issuance_gate
      - define_authenticated_gameplay_e2e_slice
      - refresh_runtime_runbook_for_alpha
      - add_minimal_example_client_or_request_loop
      - define_health_readiness_version_config_surface
      - define_alpha_acceptance_checklist
    next_milestone: M-109
    next_ready_work_item: W-0181
    ask_first:
      - Implementing local onboarding, generating or displaying raw credentials, creating player accounts through a new flow, writing credential records through a new flow, adding protocol routes, generated output, migrations, dependencies, release publishing, production signup, external identity providers, password login, account recovery, multi-device linking, direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Select the next alpha-enabling direction after W-0178.
      - Prefer first local onboarding/device credential issuance because it is the next blocking alpha capability after presence query.
      - Create a gate-only work item because credential issuance is security-sensitive.
      - Preserve implementation, protocol, generated output, migration, dependency, release, and direct compatibility deferrals.
    completion_summary: Selected `define_local_onboarding_device_credential_issuance_gate` and created `M-109/W-0181` as the next ready gate-only work item. No runtime behavior, protocol messages, generated output, migration, dependency, release artifact, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0181
    milestone: M-109
    title: Define local onboarding device credential issuance gate
    status: completed
    change: changes/2026-05-21-define-local-onboarding-device-credential-issuance-gate
    depends_on:
      - W-0180
    expected_change_id: define-local-onboarding-device-credential-issuance-gate
    tier: tier_1_security_critical
    selected_direction: define_local_onboarding_device_credential_issuance_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0082
      - ADR-0088
      - ADR-0086
      - ADR-0051
      - ADR-0047
      - ADR-0043
      - ADR-0042
      - ADR-0032
      - ADR-0030
      - ADR-0023
    ask_first:
      - Implementing onboarding or credential issuance behavior, choosing a production signup model, exposing a public protocol route, adding generated output, adding migrations, changing credential schema, adding dependencies, adding external identity providers, adding password login, adding account recovery, adding multi-device linking, publishing a release, direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define the first local onboarding/device credential issuance boundary before implementation.
      - Specify future ownership, allowed files, repository handoff, material generation, digest computation, one-time raw credential presentation, redaction, and fail-closed behavior.
      - Define future tests and repository checks for the implementation slice.
      - Preserve production signup, external identity, password login, account recovery, multi-device linking, protocol route, generated output, migration, dependency, release, and direct compatibility deferrals.
    completion_summary: Added `docs/local-onboarding-device-credential-issuance-gate.md`, its Simplified Chinese translation, `ADR-0089`, and repository check coverage for `runtime.local_onboarding_device_credential_issuance_gate`. The gate defines a future local-only application service method candidate, unit-of-work ordering, use of existing material/digest helpers and repository methods, one-time raw credential presentation after commit, digest-only storage, redaction, tests, and explicit deferrals. No onboarding behavior, credential generation/display, player account creation flow, credential record write flow, protocol route, generated output, migration, dependency, release artifact, production signup, external identity, password login, account recovery, multi-device linking, or direct compatibility was added.

  - id: W-0182
    milestone: M-110
    title: Implement local onboarding device credential issuance
    status: completed
    change: changes/2026-05-21-implement-local-onboarding-device-credential-issuance
    depends_on:
      - W-0181
    expected_change_id: implement-local-onboarding-device-credential-issuance
    tier: tier_1_security_critical
    selected_direction: implement_local_onboarding_device_credential_issuance
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0090
      - ADR-0089
      - ADR-0088
      - ADR-0086
      - ADR-0051
      - ADR-0049
      - ADR-0048
      - ADR-0047
      - ADR-0043
      - ADR-0042
      - ADR-0032
      - ADR-0030
      - ADR-0023
    ask_first:
      - Exposing onboarding through a public protocol route, changing Protobuf sources or generated output, adding migrations, changing repository interfaces, adding dependencies, choosing a production signup model, making the existing login route create accounts, issuing access tokens or runtime sessions from onboarding, adding external identity providers, adding password login, adding account recovery, adding account merge, adding multi-device linking, publishing a release, direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Implement only the local application service behavior authorized by `docs/local-onboarding-device-credential-issuance-gate.md`.
      - Create active player account and active device credential record in one application-owned unit of work.
      - Generate device credential material with explicit entropy reader, compute credential lookup and verifier digests, store only digest material, and return raw credential text only after commit.
      - Add focused tests for ordering, digest-only storage, one-time return, fail-closed behavior, redaction, and unchanged login-route account creation behavior.
      - Preserve protocol route, generated output, migration, dependency, production signup, external identity, password login, recovery, multi-device, release, and direct compatibility deferrals.
    completion_summary: Added `OnboardLocalPlayerWithDeviceCredential`, local onboarding request/result vocabulary, explicit device credential entropy and id generator dependencies, startup dependency composition, and focused tests. The service uses `GenerateDeviceCredentialMaterial`, credential lookup/verifier digest helpers, `CreatePlayerAccount`, and `StoreCredential` in the same unit of work. It stores only digests, returns raw credential text only after commit, does not issue access tokens or runtime sessions, and leaves the existing login route non-creating.

  - id: W-0183
    milestone: M-111
    title: Select next alpha direction after local onboarding
    status: completed
    change: changes/2026-05-21-select-next-alpha-direction-after-local-onboarding
    depends_on:
      - W-0182
    expected_change_id: select-next-alpha-direction-after-local-onboarding
    tier: tier_2_direction_selection
    candidate_directions:
      - define_authenticated_gameplay_e2e_slice
      - refresh_runtime_runbook_for_alpha
      - add_minimal_example_client_or_request_loop
      - define_health_readiness_version_config_surface
      - define_alpha_acceptance_checklist
    recommended_direction: define_authenticated_gameplay_e2e_slice
    selected_direction: define_authenticated_gameplay_e2e_slice
    next_milestone: M-112
    next_ready_work_item: W-0184
    related_decisions:
      - ADR-0091
      - ADR-0090
      - ADR-0089
      - ADR-0088
      - ADR-0087
      - ADR-0086
      - ADR-0084
      - ADR-0082
    ask_first:
      - Implementing authenticated gameplay E2E, adding protocol routes, adding generated output, changing migrations, adding dependencies, publishing a release, selecting direct Nakama/Pitaya API compatibility, production signup, external identity providers, password login, account recovery, multi-device linking, broad product module expansion, or skipping required verification in the direction-selection step.
    completion:
      - Select the next alpha-enabling direction after local onboarding implementation.
      - Prefer authenticated gameplay E2E because onboarding, login, connection binding, protected inventory, presence query, and logout now exist as separate pieces but are not yet proven as one documented flow.
      - Preserve implementation, protocol, generated output, migration, dependency, release, production identity, recovery, multi-device, and direct compatibility deferrals unless the selected next item explicitly scopes them.
    completion_summary: Selected `define_authenticated_gameplay_e2e_slice` as the next alpha-enabling direction and created `M-112/W-0184` as the next ready bounded functional slice. No runtime behavior, protocol route, generated output, migration, dependency, release artifact, production signup, external identity provider, password login, account recovery, multi-device linking, broad product module, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0184
    milestone: M-112
    title: Define authenticated gameplay E2E slice
    status: completed
    change: changes/2026-05-21-define-authenticated-gameplay-e2e-slice
    depends_on:
      - W-0183
    expected_change_id: define-authenticated-gameplay-e2e-slice
    tier: tier_2_functional
    selected_direction: define_authenticated_gameplay_e2e_slice
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0092
      - ADR-0091
      - ADR-0090
      - ADR-0087
      - ADR-0086
      - ADR-0084
      - ADR-0082
    ask_first:
      - Adding new protocol routes outside the existing alpha flow, changing Protobuf sources or generated output, adding migrations, changing repository interfaces, adding dependencies, publishing a release, choosing production signup, adding external identity providers, password login, account recovery, account merge, multi-device linking, adding chat/social/matchmaking/match runtime modules, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define and, if scoped by the change spec, prove the first authenticated gameplay E2E path across local onboarding, login, connection binding, protected inventory, presence query, and logout.
      - Prefer using existing runtime capabilities and focused request-loop or test coverage before adding any new protocol surface.
      - Document local prerequisites, proof material handling, redaction expectations, and known deferrals.
      - Preserve protocol expansion, generated output, migration, dependency, release, production identity, recovery, multi-device, broad product module, and direct compatibility deferrals unless explicitly scoped by the W-0184 change spec.
    completion_summary: Defined the authenticated gameplay E2E boundary in the change spec and added `runtime/internal/platform/protocol/protobuf/authenticated_gameplay_e2e_test.go` as the focused proof. The test starts with local onboarding, logs in through the existing protocol route, binds the connection, exercises protected inventory grant/read and protected presence query through `AuthenticatedRequest`, logs out through the existing logout route, and verifies the revoked token is rejected on a later protected request. No production runtime behavior, protocol route, Protobuf source, generated output, migration, repository interface, dependency, release artifact, production signup, broad product module, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0185
    milestone: M-113
    title: Refresh runtime runbook for alpha path
    status: completed
    change: changes/2026-05-21-refresh-runtime-runbook-for-alpha
    depends_on:
      - W-0184
    expected_change_id: refresh-runtime-runbook-for-alpha
    tier: tier_2_documentation
    selected_direction: refresh_runtime_runbook_for_alpha
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0093
      - ADR-0092
      - ADR-0091
      - ADR-0090
      - ADR-0087
      - ADR-0086
      - ADR-0084
      - ADR-0082
    ask_first:
      - Adding runtime behavior, changing startup configuration semantics, adding protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, publishing a release, choosing production signup, adding external identity providers, password login, account recovery, account merge, multi-device linking, adding chat/social/matchmaking/match runtime modules, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Refresh `docs/runtime-runbook.md` and its Simplified Chinese translation around the now-proven local authenticated gameplay alpha path.
      - Document prerequisites, environment configuration, verifier key handling, PostgreSQL operation posture, in-memory test proof posture, and redaction expectations.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, release, production identity, recovery, multi-device, broad product module, and direct compatibility deferrals.
    completion_summary: Refreshed the runtime runbook and Simplified Chinese translation around the proven local alpha path. The runbook now documents memory vs PostgreSQL runtime posture, verifier key requirements, the focused authenticated gameplay E2E proof, application-service-only local onboarding, protected route proof carriers, logout behavior, post-logout rejection, and redaction rules.

  - id: W-0186
    milestone: M-114
    title: Add minimal example client or request-loop script
    status: completed
    change: changes/2026-05-21-add-minimal-example-client-or-request-loop-script
    depends_on:
      - W-0185
    expected_change_id: add-minimal-example-client-or-request-loop-script
    tier: tier_2_developer_experience
    selected_direction: add_minimal_example_client_or_request_loop
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0093
      - ADR-0092
      - ADR-0091
      - ADR-0090
      - ADR-0087
      - ADR-0086
      - ADR-0084
      - ADR-0082
    ask_first:
      - Adding runtime behavior, changing startup configuration semantics, adding public protocol onboarding, changing Protobuf sources or generated output, adding migrations, adding dependencies, publishing a release, choosing production signup, adding external identity providers, password login, account recovery, account merge, multi-device linking, adding chat/social/matchmaking/match runtime modules, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Add the smallest local example client or request-loop script that helps a developer exercise the alpha path described in the runbook.
      - Preserve verifier key, raw credential, raw token, digest, DSN, and transport metadata redaction expectations.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, release, production identity, recovery, multi-device, broad product module, and direct compatibility deferrals.
    completion_summary: Added `examples/local-alpha-request-loop.sh` as the minimal local alpha request-loop script. The script wraps the existing focused authenticated gameplay E2E proof, prints only a redacted path summary and test status, and leaves public onboarding, live PostgreSQL client behavior, runtime behavior changes, startup semantics, Protobuf/generated output, migrations, dependencies, release publishing, production signup, broad product modules, and direct Nakama/Pitaya API compatibility deferred.

  - id: W-0187
    milestone: M-115
    title: Add health/readiness/version/config surface
    status: completed
    change: changes/2026-05-21-add-health-readiness-version-config-surface
    depends_on:
      - W-0186
    expected_change_id: add-health-readiness-version-config-surface
    tier: tier_2_developer_experience
    selected_direction: health_readiness_version_config_surface
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Adding broad operations/admin behavior, adding observability dependencies, exposing secrets or DSNs, changing authentication/session behavior, changing startup configuration semantics, adding Protobuf sources or generated output, adding migrations, publishing a release, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Add the smallest health/readiness/version/config surface that helps a developer troubleshoot local alpha startup.
      - Preserve redaction of verifier keys, raw credentials, raw tokens, digests, DSNs, transport metadata, and secret configuration.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, release, production operations, broad product module, and direct compatibility deferrals unless explicitly scoped.
    completion_summary: Added minimal JSON `/healthz`, `/readyz`, `/version`, and `/configz` endpoints to `runtime/cmd/vibit-server`. The endpoints use the existing HTTP mux and standard library JSON encoding, expose only redacted local alpha troubleshooting posture, and are covered by focused tests plus the `runtime.health_readiness_version_config_surface` repository check. The slice did not add broad operations/admin behavior, observability dependencies, authentication/session behavior changes, startup configuration semantic changes, Protobuf sources, generated output, migrations, dependencies, release artifacts, broad product modules, or direct Nakama/Pitaya API compatibility.

  - id: W-0188
    milestone: M-116
    title: Add alpha acceptance checklist
    status: completed
    change: changes/2026-05-21-add-alpha-acceptance-checklist
    depends_on:
      - W-0187
    expected_change_id: add-alpha-acceptance-checklist
    tier: tier_2_developer_experience
    selected_direction: alpha_acceptance_checklist
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0096
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Publishing v0.1 alpha, adding release packaging, adding runtime behavior, changing protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, broadening operations/admin behavior, changing authentication/session behavior, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Add a concise alpha acceptance checklist or equivalent check for the developer-usable v0.1 alpha flow.
      - Cover prerequisites, local configuration, migration posture, local onboarding status, login, connection binding, protected inventory, presence query, logout, status endpoints, redaction, verification commands, and contribution entry points.
      - Preserve release publishing, release packaging, runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, and direct compatibility deferrals.
    completion_summary: Added the alpha acceptance checklist and Simplified Chinese translation, recorded `ADR-0096`, and added repository check coverage through `runtime.alpha_acceptance_checklist`. The checklist classifies ready, manual, deferred, and blocked acceptance states for the local developer flow and preserves release publishing, release packaging, runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, and direct compatibility deferrals.

  - id: W-0189
    milestone: M-117
    title: Package alpha developer flow
    status: completed
    change: changes/2026-05-21-package-alpha-developer-flow
    depends_on:
      - W-0188
    expected_change_id: package-alpha-developer-flow
    tier: tier_2_developer_experience
    selected_direction: package_alpha_developer_flow
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0097
      - ADR-0096
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Publishing v0.1 alpha, creating release tags or binaries, adding hosted deployments, adding runtime behavior, changing protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, broadening operations/admin behavior, changing authentication/session behavior, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Package the existing alpha developer flow documentation and entry points into a coherent local developer journey.
      - Align README, runbook, request-loop script, status endpoints, acceptance checklist, and contribution path.
      - Preserve release publishing, runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, and direct compatibility deferrals.
    completion_summary: Added the packaged alpha developer flow and Simplified Chinese translation, recorded `ADR-0097`, and added repository check coverage through `runtime.package_alpha_developer_flow`. The package connects README, goal, runbook, request-loop script, status endpoints, acceptance checklist, redaction rules, PostgreSQL manual setup posture, verification commands, and contribution continuation into one local developer journey while preserving release publishing, release packaging, hosted deployment, runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, and direct compatibility deferrals.

  - id: W-0190
    milestone: M-118
    title: Define release publishing decision gate
    status: completed
    change: changes/2026-05-21-define-release-publishing-decision-gate
    commits:
      - e64cb2f
    depends_on:
      - W-0189
    expected_change_id: define-release-publishing-decision-gate
    tier: tier_2_developer_experience
    selected_direction: release_publishing_decision_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0098
      - ADR-0097
      - ADR-0096
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Publishing v0.1 alpha, creating release tags or binaries, adding hosted deployments, adding runtime behavior, changing protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, broadening operations/admin behavior, changing authentication/session behavior, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define the release publishing decision gate without publishing a release.
      - Specify release-publishing prerequisites, release artifact boundaries, verification requirements, and stop conditions.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, and direct compatibility deferrals.
    completion_summary: Added the release publishing decision gate and Simplified Chinese translation, recorded `ADR-0098`, and added repository check coverage through `runtime.release_publishing_decision_gate`. The gate defines release-publishing prerequisites, artifact boundaries, verification requirements, stop conditions, redaction expectations, and a next `release_execution_preparation_gate` direction while preserving pre-alpha state, release publishing, release artifact, hosted deployment, runtime, protocol, generated output, migration, dependency, operations/admin, authentication/session, broad product module, and direct compatibility deferrals.

  - id: W-0191
    milestone: M-119
    title: Define release execution preparation gate
    status: completed
    change: changes/2026-05-21-define-release-execution-preparation-gate
    commits:
      - aeb43e6
    depends_on:
      - W-0190
    expected_change_id: define-release-execution-preparation-gate
    tier: tier_2_developer_experience
    selected_direction: release_execution_preparation_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0099
      - ADR-0098
      - ADR-0097
      - ADR-0096
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Publishing v0.1 alpha, creating release tags, binaries, archives, containers, packages, checksums, or hosted deployments, adding runtime behavior, changing protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, broadening operations/admin behavior, changing authentication/session behavior, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define the release execution preparation gate without publishing a release or creating release artifacts.
      - Specify release execution planning inputs, release-note boundaries, artifact plan boundaries, maintainer approval points, verification requirements, rollback notes, and stop conditions.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals.
    completion_summary: Added the release execution preparation gate and Simplified Chinese translation, recorded `ADR-0099`, and added repository check coverage through `runtime.release_execution_preparation_gate`. The gate defines release execution planning inputs, release-note input boundaries, artifact plan boundaries, maintainer approval points, verification requirements, rollback notes, stop conditions, redaction expectations, and a next `release_execution_authorization_gate` direction while preserving pre-alpha state, release publishing, release execution, release artifact, hosted deployment, runtime, protocol, generated output, migration, dependency, operations/admin, authentication/session, broad product module, and direct compatibility deferrals.

  - id: W-0192
    milestone: M-120
    title: Define release execution authorization gate
    status: completed
    change: changes/2026-05-21-define-release-execution-authorization-gate
    commits:
      - 13c4e81
    depends_on:
      - W-0191
    expected_change_id: define-release-execution-authorization-gate
    tier: tier_2_developer_experience
    selected_direction: release_execution_authorization_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0100
      - ADR-0099
      - ADR-0098
      - ADR-0097
      - ADR-0096
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Publishing v0.1 alpha, choosing or creating release identifiers or tags, creating release binaries, archives, containers, packages, checksums, provenance files, or hosted deployments, adding runtime behavior, changing protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, broadening operations/admin behavior, changing authentication/session behavior, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define the release execution authorization gate without publishing a release or creating release artifacts.
      - Specify final go/no-go criteria, release identifier review, artifact authorization boundaries, maintainer approval requirements, verification requirements, and stop conditions.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals.
    completion_summary: Added the release execution authorization gate and Simplified Chinese translation, recorded `ADR-0100`, and added repository check coverage through `runtime.release_execution_authorization_gate`. The gate defines final go/no-go criteria, required verification state, release identifier review, artifact authorization boundaries, maintainer approval requirements, authorization outcome, stop conditions, redaction expectations, and a blocked next `release_execution_maintainer_decision` direction while preserving pre-alpha state, release publishing, release execution, release identifier selection, release artifact, hosted deployment, runtime, protocol, generated output, migration, dependency, operations/admin, authentication/session, broad product module, and direct compatibility deferrals.

  - id: W-0193
    milestone: M-121
    title: Confirm release execution maintainer decision
    status: completed
    change: changes/2026-05-21-confirm-release-execution-maintainer-decision
    commits:
      - e889537
    depends_on:
      - W-0192
    expected_change_id: confirm-release-execution-maintainer-decision
    tier: tier_2_developer_experience
    selected_direction: release_execution_maintainer_decision
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0101
      - ADR-0100
      - ADR-0099
      - ADR-0098
      - ADR-0097
      - ADR-0096
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Publishing v0.1 alpha, choosing or creating release identifiers or tags, creating release binaries, archives, containers, packages, checksums, provenance files, hosted deployments, GitHub release records, adding runtime behavior, changing protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, broadening operations/admin behavior, changing authentication/session behavior, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Record an explicit maintainer go/no-go release execution decision.
      - If go, record the approved release identifier, artifact families, publication surface, verification state, known warnings, command boundaries, and stop conditions before any execution command runs.
      - If no-go, record the reason and next bounded remediation or deferral.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Recorded the maintainer decision as `go_to_release_identifier_artifact_plan`, added `docs/release-execution-maintainer-decision.md` and its Simplified Chinese translation, accepted `ADR-0101`, and added `runtime.release_execution_maintainer_decision` check coverage. The decision allows the release execution path to continue to `W-0194 Define release identifier and artifact plan`, accepts the existing `runtime.identity_boundary` warning for planning, and preserves deferrals for release identifier approval, tag creation, artifact creation, GitHub release records, hosted deployments, publication commands, runtime behavior, protocol routes, generated output, migrations, dependencies, operations/admin behavior, authentication/session behavior, broad product modules, and direct compatibility.

  - id: W-0194
    milestone: M-122
    title: Define release identifier and artifact plan
    status: completed
    change: changes/2026-05-21-define-release-identifier-artifact-plan
    commits:
      - ef93cd5
    depends_on:
      - W-0193
    expected_change_id: define-release-identifier-artifact-plan
    tier: tier_2_developer_experience
    selected_direction: release_identifier_artifact_plan
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0102
      - ADR-0101
      - ADR-0100
      - ADR-0099
      - ADR-0098
      - ADR-0097
      - ADR-0096
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Creating or pushing release tags, publishing v0.1 alpha, creating release binaries, archives, containers, packages, checksums, provenance files, hosted deployments, GitHub release records, running release publication commands, adding runtime behavior, changing protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, broadening operations/admin behavior, changing authentication/session behavior, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define the proposed release identifier and artifact/publication plan without creating release artifacts.
      - Check and record whether the proposed identifier conflicts with existing repository tags or release records without creating or pushing anything.
      - Record approved, deferred, and forbidden artifact families, command boundaries, verification requirements, known warnings, redaction rules, and stop conditions for any later execution step.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined the proposed release identifier as `v0.1.0-alpha.1`, recorded no local tag, remote tag, or GitHub release-record conflict as of 2026-05-21, and planned the smallest later release surface as a Git tag plus GitHub release record/source archive only after final maintainer authorization. Binaries, packages, containers, checksums, provenance, hosted deployments, publication commands, runtime behavior, protocol routes, generated output, migrations, dependencies, operations/admin behavior, authentication/session changes, broad product modules, and direct Nakama/Pitaya compatibility remain deferred.

  - id: W-0195
    milestone: M-123
    title: Confirm release execution final authorization
    status: completed
    change: changes/2026-05-21-confirm-release-execution-final-authorization
    commits:
      - 30e8688
    depends_on:
      - W-0194
    expected_change_id: confirm-release-execution-final-authorization
    tier: tier_2_developer_experience
    selected_direction: release_execution_final_authorization
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0103
      - ADR-0102
      - ADR-0101
      - ADR-0100
      - ADR-0099
      - ADR-0098
      - ADR-0097
      - ADR-0096
      - ADR-0095
      - ADR-0094
      - ADR-0093
      - ADR-0092
      - ADR-0086
      - ADR-0082
    ask_first:
      - Creating or pushing release tags, publishing v0.1 alpha, creating release binaries, archives, containers, packages, checksums, provenance files, hosted deployments, GitHub release records, running release publication commands, adding runtime behavior, changing protocol routes, changing Protobuf sources or generated output, adding migrations, adding dependencies, broadening operations/admin behavior, changing authentication/session behavior, selecting direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Record a final maintainer go/no-go decision for executing the planned release commands.
      - If go, record the exact authorized release identifier, tag action, GitHub release record action, artifact families, command boundaries, verification state, known warnings, and stop conditions before running any release-producing command.
      - If no-go, record the reason and next bounded remediation or deferral.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Recorded final maintainer authorization as `go` for `v0.1.0-alpha.1`, authorized creating and pushing Git tag `v0.1.0-alpha.1`, authorized creating GitHub Release `v0.1.0-alpha.1`, limited release artifacts to the GitHub source archive, accepted the existing `runtime.identity_boundary` warning for release execution, refreshed README to make the source alpha more attractive for developers to try, and preserved deferrals for binaries, packages, containers, checksums, signing/provenance, hosted deployments, install scripts, registry publication, announcements beyond the GitHub release record, runtime behavior, protocol routes, generated output, migrations, dependencies, operations/admin behavior, authentication/session behavior, broad product modules, and direct Nakama/Pitaya API compatibility.

  - id: W-0196
    milestone: M-124
    title: Define first alpha user discovery loop
    status: completed
    change: changes/2026-05-21-define-first-alpha-user-discovery-loop
    commits:
      - ef925f8
    depends_on:
      - W-0195
    expected_change_id: define-first-alpha-user-discovery-loop
    tier: tier_2_developer_experience
    selected_direction: first_alpha_user_discovery
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0103
      - ADR-0102
      - ADR-0097
      - ADR-0086
      - ADR-0082
    ask_first:
      - Public announcements beyond the GitHub release record, paid promotion, hosted deployments, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, runtime behavior changes, protocol route changes, Protobuf source or generated output changes, migrations, dependencies, broad operations/admin behavior, authentication/session behavior changes, direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define the first alpha user discovery loop after the source-first release.
      - Record target developer segments, outreach surfaces, feedback capture, success signals, and stop conditions.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, authentication/session, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined the first alpha user discovery loop after `v0.1.0-alpha.1`, recorded the initial developer segments, allowed and deferred outreach surfaces, feedback capture fields, review questions, success signals, and stop conditions, accepted `ADR-0104`, added `runtime.first_alpha_user_discovery_loop` check coverage, and opened `W-0197 Prepare first alpha feedback intake surfaces` as the next-ready work item without executing announcements, adding hosted deployments, expanding release artifacts, or changing runtime scope.

  - id: W-0197
    milestone: M-125
    title: Prepare first alpha feedback intake surfaces
    status: completed
    change: changes/2026-05-22-prepare-first-alpha-feedback-intake-surfaces
    commits:
      - 2d4a952
    depends_on:
      - W-0196
    expected_change_id: prepare-first-alpha-feedback-intake-surfaces
    tier: tier_2_developer_experience
    selected_direction: first_alpha_feedback_intake_surfaces
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0104
      - ADR-0103
      - ADR-0097
      - ADR-0086
      - ADR-0082
    ask_first:
      - Public announcements beyond the GitHub release record, paid promotion, hosted deployments, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, runtime behavior changes, protocol route changes, Protobuf source or generated output changes, migrations, dependencies, broad operations/admin behavior, authentication/session behavior changes, direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Prepare the first repository-owned feedback intake surface for early alpha users.
      - Record the intake format, feedback fields, redaction expectations, labels or templates, and triage posture.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, release artifact, public announcement, paid promotion, authentication/session, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Prepared `.github/ISSUE_TEMPLATE/alpha-feedback.yml` as the first repository-owned alpha feedback intake surface, recorded intake format, redaction expectations, triage labels, product maturity buckets, and stop conditions in `docs/first-alpha-feedback-intake-surfaces.md`, recorded source-first alpha, prototype-ready foundation, single-node production-candidate foundation, and Nakama/Pitaya-class product stages in `docs/product-maturity-milestones.md`, accepted `ADR-0105`, added `runtime.first_alpha_feedback_intake_surfaces` check coverage, and opened `W-0198 Define prototype-ready foundation execution plan` without executing public announcements, paid promotion, hosted deployments, expanding release artifacts, changing runtime behavior, changing protocol/generated output/migrations/dependencies, broadening operations/admin, changing authentication/session behavior, adding broad product modules, or adding direct compatibility.

  - id: W-0198
    milestone: M-126
    title: Define prototype-ready foundation execution plan
    status: completed
    change: changes/2026-05-22-define-prototype-ready-foundation-execution-plan
    commits:
      - 9e7470e
    depends_on:
      - W-0197
    expected_change_id: define-prototype-ready-foundation-execution-plan
    tier: tier_2_developer_experience
    selected_direction: prototype_ready_foundation_execution_plan
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0106
      - ADR-0105
      - ADR-0104
      - ADR-0097
      - ADR-0086
      - ADR-0082
    ask_first:
      - Public announcements beyond the GitHub release record, paid promotion, hosted deployments, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, runtime behavior changes, protocol route changes, Protobuf source or generated output changes, migrations, dependencies, broad operations/admin behavior, authentication/session behavior changes, direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define the first prototype-ready foundation execution plan after the feedback intake surface is prepared.
      - Record the recommended sequence, candidate work items, maturity-stage mapping, success criteria, and stop conditions.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, broad product module, hosted deployment, release artifact, public announcement, paid promotion, authentication/session, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined the first prototype-ready foundation execution plan, recorded the recommended Stage 2 sequence, candidate work families, maturity mapping, Nakama/Pitaya capability mapping, success criteria, and stop conditions in `docs/prototype-ready-foundation-execution-plan.md`, accepted `ADR-0106`, added `runtime.prototype_ready_foundation_execution_plan` check coverage, and selected `W-0199 Define prototype-ready local development path gate` as the next-ready work item without changing runtime behavior, protocol/generated output/migrations/dependencies, broadening operations/admin, changing authentication/session behavior, adding hosted deployments, expanding release artifacts, executing announcements, running paid promotion, adding broad product modules, or adding direct compatibility.

  - id: W-0199
    milestone: M-127
    title: Define prototype-ready local development path gate
    status: completed
    change: changes/2026-05-22-define-prototype-ready-local-development-path-gate
    commits:
      - 0f8555d
    depends_on:
      - W-0198
    expected_change_id: define-prototype-ready-local-development-path-gate
    tier: tier_2_developer_experience
    selected_direction: prototype_ready_local_development_path_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0107
      - ADR-0106
      - ADR-0105
      - ADR-0097
      - ADR-0086
      - ADR-0082
    ask_first:
      - Runtime behavior changes, protocol route changes, Protobuf source or generated output changes, migrations, dependencies, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Define the gate for the prototype-ready local development path.
      - Record supported prerequisites, startup expectations, migration expectations, configuration and secret posture, example-flow shape, allowed future write areas, verification expectations, and stop conditions.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined the prototype-ready local development path gate, recorded supported prerequisites, startup expectations, migration expectations, configuration and secret posture, example-flow shape, allowed future write areas, verification expectations, and stop conditions in `docs/prototype-ready-local-development-path-gate.md`, accepted `ADR-0107`, added `runtime.prototype_ready_local_development_path_gate` check coverage, and selected `W-0200 Implement prototype-ready local development path package` as the next-ready work item without changing runtime behavior, protocol/generated output/migrations/dependencies, broadening operations/admin, changing authentication/session behavior, adding hosted deployments, expanding release artifacts, executing announcements, running paid promotion, adding broad product modules, or adding direct compatibility.

  - id: W-0200
    milestone: M-128
    title: Implement prototype-ready local development path package
    status: completed
    change: changes/2026-05-22-implement-prototype-ready-local-development-path-package
    commits:
      - 5b78003
    depends_on:
      - W-0199
    expected_change_id: implement-prototype-ready-local-development-path-package
    tier: tier_2_developer_experience
    selected_direction: prototype_ready_local_development_path_package
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0107
      - ADR-0106
      - ADR-0105
      - ADR-0097
      - ADR-0086
      - ADR-0082
    ask_first:
      - Runtime behavior changes, protocol route changes, Protobuf source or generated output changes, migrations, dependencies, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, or broad product module expansion.
    completion:
      - Implement the prototype-ready local development path package inside the W-0199 gate.
      - Package setup, startup, migration, configuration, secret redaction, example-flow, and verification ergonomics using docs, scripts, examples, static checks, or focused verification of existing behavior.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Implemented the prototype-ready local development path package, recorded the fast source checkout path, supported prerequisites, redacted local configuration template, `.gitignore` secret guardrails, migration expectations, startup guidance, request-loop proof, verification commands, and stop conditions in `docs/prototype-ready-local-development-path-package.md`, accepted `ADR-0108`, added `examples/README.md`, `examples/README.zh-CN.md`, and `examples/local.prototype.env.example`, added `runtime.prototype_ready_local_development_path_package` check coverage, and selected `W-0201 Define storage objects behavior gate` as the next-ready work item without changing runtime behavior, protocol/generated output/migrations/dependencies, automatic startup migrations, broadening operations/admin, changing authentication/session behavior, adding hosted deployments, expanding release artifacts, executing announcements, running paid promotion, adding broad product modules, or adding direct compatibility.

  - id: W-0201
    milestone: M-129
    title: Define storage objects behavior gate
    status: completed
    change: changes/2026-05-22-define-storage-objects-behavior-gate
    commits:
      - f170fb5
    depends_on:
      - W-0200
    expected_change_id: define-storage-objects-behavior-gate
    tier: tier_2_functional
    selected_direction: storage_objects_behavior_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0109
      - ADR-0108
      - ADR-0107
      - ADR-0106
      - ADR-0078
      - ADR-0086
      - ADR-0082
    ask_first:
      - Runtime behavior changes, protocol route changes, Protobuf source or generated output changes, migrations, dependencies, repository interface changes, storage adapter changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, or broad product module expansion beyond the storage-objects gate definition.
    completion:
      - Define the first storage objects behavior gate beyond the inventory proof slice.
      - Record ownership, scope/key posture, read/write semantics, permissions, conflict behavior, protocol/data expectations, verification expectations, and stop conditions.
      - Preserve runtime behavior, protocol, generated output, migration, dependency, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined the storage objects behavior gate, recorded the first player-owned small JSON object posture, object identity tuple, ownership, scope/key rules, read/write semantics, route-scoped permissions, optimistic conflict behavior, protocol/data expectations, verification expectations, and stop conditions in `docs/storage-objects-behavior-gate.md`, accepted `ADR-0109`, added `runtime.storage_objects_behavior_gate` check coverage, and selected `W-0202 Define storage objects persistence schema gate` as the next-ready work item without changing runtime behavior, protocol/generated output/migrations/dependencies, repository/storage adapters, operations/admin behavior, authentication/session behavior, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility.

  - id: W-0202
    milestone: M-130
    title: Define storage objects persistence schema gate
    status: completed
    change: changes/2026-05-22-define-storage-objects-persistence-schema-gate
    commits:
      - 5419556
    depends_on:
      - W-0201
    expected_change_id: define-storage-objects-persistence-schema-gate
    tier: tier_2_data
    selected_direction: storage_objects_persistence_schema_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0078
      - ADR-0086
      - ADR-0082
    ask_first:
      - SQL migration source additions, runtime behavior changes, protocol route changes, Protobuf source or generated output changes, dependencies, repository interface implementation, storage adapter implementation, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product module expansion beyond the storage-objects schema gate definition.
    completion:
      - Define the first storage objects persistence schema gate.
      - Record table candidate, owner identity representation, collection/key constraints, value representation, version representation, timestamps, uniqueness/index posture, redaction posture, migration source candidate, and future repository/adapter boundaries.
      - Preserve runtime behavior, protocol, generated output, migration source, dependency, repository implementation, adapter implementation, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined the storage objects persistence schema gate, recorded PostgreSQL first-store posture, future `storage_objects` logical table, future migration source candidate `runtime/migrations/postgres/000006_create_storage_objects.sql`, player owner identity, collection/key constraints, JSONB value posture, BIGINT version posture, timestamp and soft-delete posture, active uniqueness/index posture, redaction posture, and future repository/adapter ownership candidates in `docs/storage-objects-persistence-schema-gate.md`, accepted `ADR-0110`, added `runtime.storage_objects_persistence_schema_gate` check coverage, and selected `W-0203 Add storage objects migration source` as the next-ready work item without adding SQL migration source, runtime behavior, protocol/generated output, dependencies, repository/storage adapters, operations/admin behavior, authentication/session behavior, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility.

  - id: W-0203
    milestone: M-131
    title: Add storage objects migration source
    status: completed
    change: changes/2026-05-22-add-storage-objects-migration-source
    commits:
      - 511aa49
    depends_on:
      - W-0202
    expected_change_id: add-storage-objects-migration-source
    tier: tier_2_data
    selected_direction: storage_objects_migration_source
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0078
      - ADR-0086
      - ADR-0082
    ask_first:
      - Runtime behavior changes, protocol route changes, Protobuf source or generated output changes, dependencies, repository interface implementation, storage adapter implementation, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product module expansion beyond the storage-objects migration source.
    completion:
      - Add `runtime/migrations/postgres/000006_create_storage_objects.sql`.
      - Keep the SQL source aligned with `docs/storage-objects-persistence-schema-gate.md`.
      - Preserve repository interfaces, storage adapters, protocol routes, generated output, runtime behavior, dependencies, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Added `runtime/migrations/postgres/000006_create_storage_objects.sql` as a migration-source-only slice, following `docs/storage-objects-persistence-schema-gate.md`. The migration creates `storage_objects` with player owner identity, collection/key constraints, top-level JSONB object values, positive BIGINT versions, created/updated/deleted timestamps, active logical identity uniqueness, owner/collection lookup and updated-at indexes, and forbidden secret/transport/blob/S3 column exclusions. No storage object repository interfaces, storage adapters, protocol routes, generated output, runtime behavior, dependencies, operations/admin behavior, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility were added.

  - id: W-0204
    milestone: M-132
    title: Define storage objects repository boundary
    status: completed
    change: changes/2026-05-22-define-storage-objects-repository-boundary
    commits:
      - 5fc44ee
    depends_on:
      - W-0203
    expected_change_id: define-storage-objects-repository-boundary
    tier: tier_2_data
    selected_direction: storage_objects_repository_boundary
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0078
      - ADR-0086
      - ADR-0082
    ask_first:
      - Repository interface implementation, storage adapter implementation, runtime behavior changes, protocol route changes, Protobuf source or generated output changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product module expansion beyond the storage-objects repository boundary gate.
    completion:
      - Define the storage-neutral storage objects repository boundary gate.
      - Record future repository ownership, candidate value types, create/read/update/delete/conflict vocabulary, version handoff, redaction posture, PostgreSQL adapter expectations, and stop conditions.
      - Preserve repository interface implementation, storage adapters, protocol routes, generated output, runtime behavior, dependencies, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined `docs/storage-objects-repository-boundary.md` and `docs/storage-objects-repository-boundary.zh-CN.md` as a repository-boundary-only slice, accepted `ADR-0112`, recorded future storage module ownership, candidate repository interface and value type vocabulary, create/read/list/update/delete capabilities, version and conflict handoff, redaction posture, PostgreSQL adapter expectations, future implementation queue, and `runtime.storage_objects_repository_boundary` check coverage. No storage object repository interface implementation, storage adapters, protocol routes, generated output, runtime behavior, migration changes, dependencies, operations/admin behavior, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility were added.

  - id: W-0205
    milestone: M-133
    title: Implement storage-neutral storage objects repository interface
    status: completed
    change: changes/2026-05-22-implement-storage-objects-repository-interface
    depends_on:
      - W-0204
    expected_change_id: implement-storage-objects-repository-interface
    tier: tier_2_data
    selected_direction: storage_objects_repository_interface_implementation
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0112
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0078
      - ADR-0086
      - ADR-0082
    ask_first:
      - PostgreSQL storage adapter implementation, SQL execution behavior, runtime behavior changes, protocol route changes, Protobuf source or generated output changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product module expansion beyond the storage-objects repository interface implementation.
    completion:
      - Implement the storage-neutral storage objects repository interface under `runtime/internal/modules/storage`.
      - Define module-owned value types, create/read/list/update/delete input and result vocabulary, version/conflict helpers, redacted errors, and focused tests.
      - Preserve storage adapters, SQL execution, protocol routes, generated output, runtime behavior, dependencies, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Implemented `runtime/internal/modules/storage.Repository` as a storage-neutral interface with `StorageObject`, owner, identity, value, status, version, create/read/list/update/delete input and result types, optimistic conflict classes, redacted repository errors, normalization helpers, and focused tests in `runtime/internal/modules/storage/repository_test.go`. Added storage module manifest and guides, accepted `ADR-0113`, registered `runtime.storage_objects_repository_interface_implementation`, and kept PostgreSQL adapters, SQL execution, runtime behavior, protocol routes, Protobuf sources/generated output, dependencies, migration changes, authentication/session behavior, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, and direct compatibility deferred.

  - id: W-0206
    milestone: M-134
    title: Define storage objects PostgreSQL adapter gate
    status: completed
    change: changes/2026-05-22-define-storage-objects-postgresql-adapter-gate
    depends_on:
      - W-0205
    expected_change_id: define-storage-objects-postgresql-adapter-gate
    tier: tier_2_data
    selected_direction: storage_objects_postgresql_adapter_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0113
      - ADR-0112
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0078
      - ADR-0086
      - ADR-0082
    ask_first:
      - PostgreSQL storage adapter implementation, SQL execution behavior, runtime behavior changes, protocol route changes, Protobuf source or generated output changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product module expansion beyond the storage-objects PostgreSQL adapter gate.
    completion:
      - Define the storage objects PostgreSQL adapter gate after the storage-neutral repository interface.
      - Record future adapter owner, constructor and unit-of-work expectations, SQL mapping posture, transaction handoff, error mapping, test expectations, and stop conditions.
      - Preserve adapter implementation, SQL execution, protocol routes, generated output, runtime behavior, dependencies, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined the storage objects PostgreSQL adapter gate, accepted `ADR-0114`, added `docs/storage-objects-postgresql-adapter-gate.md` and its Simplified Chinese translation, registered `runtime.storage_objects_postgresql_adapter_gate`, recorded future adapter owner `runtime/internal/platform/persistence/postgres`, source candidate `runtime/internal/platform/persistence/postgres/storage_object_repository.go`, test candidate `runtime/internal/platform/persistence/postgres/storage_object_repository_test.go`, constructor and unit-of-work expectations, SQL mapping posture, transaction handoff, redacted error mapping, future tests, and stop conditions. PostgreSQL storage adapter implementation, SQL execution, runtime behavior, protocol routes, Protobuf sources/generated output, dependencies, migration changes, authentication/session behavior, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, and direct compatibility remain deferred.

  - id: W-0207
    milestone: M-135
    title: Implement storage objects PostgreSQL adapter
    status: completed
    change: changes/2026-05-22-implement-storage-objects-postgresql-adapter
    depends_on:
      - W-0206
    expected_change_id: implement-storage-objects-postgresql-adapter
    tier: tier_2_data
    selected_direction: storage_objects_postgresql_adapter_implementation
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0115
      - ADR-0114
      - ADR-0113
      - ADR-0112
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0086
      - ADR-0082
    ask_first:
      - Runtime behavior changes, protocol route changes, Protobuf source or generated output changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product module expansion beyond the storage-objects PostgreSQL adapter implementation.
    completion:
      - Implement the storage objects PostgreSQL adapter under `runtime/internal/platform/persistence/postgres`.
      - Map `runtime/internal/modules/storage.Repository` to the existing `storage_objects` table with redacted errors, version-aware conflicts, soft-delete behavior, and focused tests.
      - Preserve protocol routes, generated output, runtime behavior, dependencies, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Implemented `runtime/internal/platform/persistence/postgres/storage_object_repository.go` and `runtime/internal/platform/persistence/postgres/storage_object_repository_test.go`, mapping `runtime/internal/modules/storage.Repository` to the existing `storage_objects` table through caller-supplied unit-of-work executors. The adapter covers create/read/list/update/delete, active-row filtering, deterministic list pagination, expected-version checks, version increments, soft deletes, row scanning through storage normalizers, redacted storage repository errors, fake-executor tests, no live PostgreSQL requirement, and `UnitOfWork.NewStorageObjectRepository`. No storage object runtime handlers, protocol routes, Protobuf sources/generated output, dependencies, migration changes, authentication/session behavior, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility were added.

  - id: W-0208
    milestone: M-136
    title: Define storage objects runtime behavior gate
    status: completed
    change: changes/2026-05-22-define-storage-objects-runtime-behavior-gate
    depends_on:
      - W-0207
    expected_change_id: define-storage-objects-runtime-behavior-gate
    tier: tier_2_functional
    selected_direction: storage_objects_runtime_behavior_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0116
      - ADR-0115
      - ADR-0114
      - ADR-0113
      - ADR-0112
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0086
      - ADR-0082
    ask_first:
      - Runtime behavior implementation, protocol route changes, Protobuf source or generated output changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product module expansion beyond the storage-objects runtime behavior gate.
    completion:
      - Define the storage objects runtime behavior gate after the PostgreSQL adapter.
      - Record owner identity derivation, request identity requirements, permissions, validation, conflict semantics, route-policy expectations, service/application ownership, and stop conditions.
      - Preserve runtime implementation, protocol routes, generated output, dependencies, operations/admin, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, broad product module, object/blob storage, S3-compatible object storage, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Defined `docs/storage-objects-runtime-behavior-gate.md` and `docs/storage-objects-runtime-behavior-gate.zh-CN.md`, accepted `ADR-0116`, registered `runtime.storage_objects_runtime_behavior_gate`, and recorded future application-owned storage object service behavior under `runtime/internal/app`, candidate package `runtime/internal/app/storage`, validated request identity owner derivation, refusal to treat metadata-only `player_id` or `session_id` as proof, `request_token_required` route-policy posture, validation, permission, conflict mapping, redaction, unit-of-work repository handoff, future tests, and stop conditions. No storage object runtime behavior implementation, runtime handlers, startup wiring, protocol routes, Protobuf sources/generated output, repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility were added.

  - id: W-0209
    milestone: M-137
    title: Implement storage objects runtime behavior
    status: completed
    change: changes/2026-05-22-implement-storage-objects-runtime-behavior
    depends_on:
      - W-0208
    expected_change_id: implement-storage-objects-runtime-behavior
    tier: tier_2_functional
    selected_direction: storage_objects_runtime_behavior_implementation
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0117
      - ADR-0116
      - ADR-0115
      - ADR-0114
      - ADR-0113
      - ADR-0112
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0086
      - ADR-0082
    ask_first:
      - Protocol route changes, Protobuf source or generated output changes, repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, public ACLs, admin search, group/guild/party/room/match storage scopes, batch writes, JSON patch, merge semantics, TTL, script hooks, or broad product module expansion beyond the storage-objects runtime behavior implementation.
    completion:
      - Implement storage objects runtime behavior under the application-owned boundary ratified by `ADR-0116`.
      - Derive storage object owner identity from validated request identity and reject metadata-only `player_id` or `session_id` before repository access.
      - Compose repository operations with validation, conflict mapping, redaction, and unit-of-work handoff while preserving protocol, generated output, repository-interface, adapter, dependency, migration, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, and direct compatibility deferrals.
    completion_summary: Implemented `runtime/internal/app/storage/service.go` and `runtime/internal/app/storage/service_test.go` with application-owned own-object get/list/put/delete behavior. The service derives `player` ownership from validated `app.RequestIdentity`, rejects metadata-only `player_id` and `session_id` before unit-of-work or repository access, validates collection/key/value JSON/value size/list pagination/expected version inputs, maps storage repository conflicts to redacted public error codes, and obtains `runtime/internal/modules/storage.Repository` through unit-of-work `NewStorageObjectRepository`. No protocol routes, Protobuf sources/generated output, runtime handlers, startup wiring, repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility were added.

  - id: W-0210
    milestone: M-138
    title: Define storage objects protocol route gate
    status: completed
    change: changes/2026-05-22-define-storage-objects-protocol-route-gate
    depends_on:
      - W-0209
    expected_change_id: define-storage-objects-protocol-route-gate
    tier: tier_2_protocol
    selected_direction: storage_objects_protocol_route_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0117
      - ADR-0116
      - ADR-0115
      - ADR-0114
      - ADR-0113
      - ADR-0112
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0086
      - ADR-0082
    ask_first:
      - Protocol route implementation, Protobuf source or generated output changes, startup wiring, repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, public ACLs, admin search, group/guild/party/room/match storage scopes, batch writes, JSON patch, merge semantics, TTL, script hooks, or broad product module expansion beyond the storage-objects protocol route gate.
    completion:
      - Define the storage objects protocol route gate after application runtime behavior.
      - Record candidate route names, request/response shape expectations, protected-route policy, protocol adapter ownership, generated-output expectations, and stop conditions.
      - Preserve route implementation, Protobuf/generated output, startup wiring, repository-interface, adapter, dependency, migration, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, and direct compatibility deferrals.
    completion_summary: Defined `docs/storage-objects-protocol-route-gate.md` and `docs/storage-objects-protocol-route-gate.zh-CN.md`, accepted `ADR-0118`, registered `runtime.storage_objects_protocol_route_gate`, and recorded future routes `storage.GetOwnStorageObject`, `storage.ListOwnStorageObjects`, `storage.PutOwnStorageObject`, and `storage.DeleteOwnStorageObject`, candidate `vibit.storage.v1` Protobuf message shapes, protected-route policy `request_token_required`, authenticated-wrapper and validated request identity handoff, metadata-only `player_id` and `session_id` refusal, no client-supplied owner ids, protocol adapter/application handler/startup ownership, generated-output expectations, redaction rules, required future tests, and Nakama/Pitaya reference mapping. No route implementation, Protobuf source/generated output, startup wiring, runtime handlers, repository interface changes, PostgreSQL adapter changes, dependencies, migrations, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, broad product modules, large object/blob storage, S3-compatible object storage, or direct compatibility were added.

  - id: W-0211
    milestone: M-139
    title: Implement storage objects protocol route
    status: completed
    change: changes/2026-05-22-implement-storage-objects-protocol-route
    depends_on:
      - W-0210
    expected_change_id: implement-storage-objects-protocol-route
    tier: tier_2_protocol
    selected_direction: storage_objects_protocol_route_implementation
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0118
      - ADR-0117
      - ADR-0116
      - ADR-0115
      - ADR-0114
      - ADR-0113
      - ADR-0112
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0086
      - ADR-0082
    ask_first:
      - Repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, route-protection semantic changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, public ACLs, admin search, group/guild/party/room/match storage scopes, batch writes, JSON patch, merge semantics, TTL, script hooks, or broad product module expansion beyond the storage-objects protocol route implementation.
    completion:
      - Implement the storage objects protocol route family authorized by `ADR-0118`.
      - Add Protobuf source/generated output, protocol bridge mapping, route registration, and focused tests for own-player get/list/put/delete routes.
      - Preserve application-owned service behavior, metadata-only identity refusal, protected-route policy, redacted errors, and repository/adapter/migration/dependency/authentication/session/direct-compatibility deferrals.
    completion_summary: Implemented the own-player storage object protocol route family with route keys `storage.GetOwnStorageObject`, `storage.ListOwnStorageObjects`, `storage.PutOwnStorageObject`, and `storage.DeleteOwnStorageObject`; added `vibit.storage.v1` Protobuf source and generated Go output through Buf; added protocol bridge mapping, payload registry integration, application bootstrap route handlers, PostgreSQL startup route registration, and transaction bypass for storage writes because the storage service owns its unit of work. Focused tests cover get/list/put/delete payload mapping, optional expected versions, response mapping, redacted handler errors, request identity handoff, protected-route authentication wrapper enforcement, and startup object-id generation. No repository interface changes, PostgreSQL adapter changes, migration changes, dependency additions, authentication/session behavior changes, hosted deployments, release artifacts, public announcements, paid promotion, public ACLs, cross-owner access, admin search, group/guild/party/room/match scopes, batch writes, JSON patch, merge semantics, TTL, script hooks, large object/blob storage, S3-compatible object storage, or direct Nakama/Pitaya API compatibility were added.

  - id: W-0212
    milestone: M-140
    title: Prove storage objects protocol route in local alpha request flow
    status: completed
    change: changes/2026-05-22-prove-storage-objects-protocol-route-local-flow
    depends_on:
      - W-0211
    expected_change_id: prove-storage-objects-protocol-route-local-flow
    tier: tier_2_protocol
    selected_direction: storage_objects_protocol_route_local_proof
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0119
      - ADR-0118
      - ADR-0117
      - ADR-0116
      - ADR-0115
      - ADR-0114
      - ADR-0113
      - ADR-0112
      - ADR-0111
      - ADR-0110
      - ADR-0109
      - ADR-0108
      - ADR-0086
      - ADR-0082
    ask_first:
      - New protocol messages or routes, storage service behavior changes, repository interface changes, PostgreSQL adapter changes, dependencies, migration changes, automatic startup migration behavior, broad operations/admin behavior, authentication/session behavior changes, route-protection semantic changes, hosted deployments, public announcements beyond the GitHub release record, paid promotion, release binaries, packages, containers, checksums, provenance files, signing artifacts, install scripts, registry publication, SDK packages, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, public ACLs, admin search, group/guild/party/room/match storage scopes, batch writes, JSON patch, merge semantics, TTL, script hooks, or broad product module expansion beyond proving the storage-objects protocol route in the local alpha request flow.
    completion:
      - Add a narrow local proof for the completed storage objects protocol route family using the existing WebSocket/Protobuf request flow.
      - Record exact prerequisites, commands, expected request/response shape, redaction expectations, and Nakama/Pitaya alignment notes for own-player storage object put/get/list/delete.
      - Preserve protocol shape, storage service behavior, repository/adapter/migration/dependency/authentication/session/direct-compatibility deferrals, and avoid broad product module expansion.
    completion_summary: Added a local alpha proof for own-player storage object put/get/list/delete using the existing WebSocket/Protobuf `FrameHandler` path. `TestStorageObjectsProtocolRouteLocalAlphaFlow` performs local onboarding, device credential login, first-message connection binding, authenticated `storage.PutOwnStorageObject`, `storage.GetOwnStorageObject`, `storage.ListOwnStorageObjects`, `storage.DeleteOwnStorageObject`, and post-delete not-found verification through the existing authenticated request wrapper. The proof uses a test-only storage repository inside `_test.go`, updates `examples/local-alpha-request-loop.sh` to run both focused E2E proofs, documents prerequisites/request-flow/redaction/Nakama/Pitaya alignment in examples docs, accepts `ADR-0120`, and registers `runtime.storage_objects_protocol_route_local_proof`. No protocol messages, routes, generated output, storage service behavior, repository interface, PostgreSQL adapter, migration, dependency, authentication/session behavior, hosted deployment, release artifact, public announcement, paid promotion, broad product module, blob/S3 storage, production memory storage, public ACL, cross-owner access, admin search, group/guild/party/room/match scope, batch write, JSON patch, merge, TTL, script hook, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0213
    milestone: M-141
    title: Confirm next alpha direction after storage objects local proof
    status: completed
    change: changes/2026-05-23-confirm-next-alpha-direction-after-storage-objects-local-proof
    depends_on:
      - W-0212
    expected_change_id: confirm-next-alpha-direction-after-storage-objects-local-proof
    tier: tier_2_planning
    selected_direction: confirm_next_alpha_direction_after_storage_objects_local_proof
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0120
      - ADR-0119
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing a new module or runtime subsystem, adding protocol messages or routes, changing storage object service behavior, changing repository interfaces or PostgreSQL adapters, adding migrations, adding dependencies, changing authentication/session behavior, adding hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product expansion beyond confirming the next alpha direction.
    completion:
      - Select exactly one next bounded alpha direction after storage objects local proof.
      - Record Nakama/Pitaya reference alignment and why the selected direction is product-useful.
      - Preserve current implementation, protocol, storage, authentication/session, dependency, migration, hosted deployment, release artifact, public announcement, paid promotion, blob/S3, and direct compatibility deferrals.
    completion_summary: Selected `define_first_server_push_realtime_messaging_gate` as the next bounded alpha direction after storage objects local proof. The selection records Nakama alignment for outbound realtime vocabulary after durable storage objects and Pitaya alignment for preserving transport/session/protocol-adapter/application/backend separation before push, broadcast, groups, cluster, or RPC expansion. `M-142/W-0214 Define first server push and realtime messaging gate` is now next-ready. No implementation, protocol, generated output, storage, repository, adapter, migration, dependency, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, blob/S3, matchmaking, match runtime, distributed runtime, broad social module, or direct Nakama/Pitaya API compatibility change was added.

  - id: W-0214
    milestone: M-142
    title: Define first server push and realtime messaging gate
    status: completed
    change: changes/2026-05-23-define-first-server-push-realtime-messaging-gate
    depends_on:
      - W-0213
    expected_change_id: define-first-server-push-realtime-messaging-gate
    tier: tier_2_planning
    selected_direction: define_first_server_push_realtime_messaging_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0121
      - ADR-0120
      - ADR-0106
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing server push, broadcast, streams, chat, notifications, realtime messaging, adding protocol messages or routes, changing Protobuf source or generated output, changing storage object service behavior, changing repository interfaces or PostgreSQL adapters, adding migrations, adding dependencies, changing authentication/session behavior, changing route protection, adding hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, matchmaking, match runtime, distributed runtime, RPC, service discovery, or broad product expansion beyond defining the first server push and realtime messaging gate.
    completion:
      - Define the first server push and realtime messaging gate.
      - Record ownership, vocabulary, future allowed files, verification expectations, and stop conditions.
      - Record Nakama/Pitaya reference alignment while preserving implementation, protocol, generated output, storage, authentication/session, dependency, migration, hosted deployment, release artifact, public announcement, paid promotion, blob/S3, matchmaking, match runtime, distributed runtime, and direct compatibility deferrals.
    completion_summary: Defined the first server push and realtime messaging gate, accepted `ADR-0122`, recorded future ownership under `runtime/internal/app/realtime`, future protocol/bridge/handler/transport candidates, first message-intent vocabulary, conservative sender authority and identity rules, Nakama/Pitaya reference mapping, and stop conditions. `M-143/W-0215 Implement first server push and realtime messaging runtime slice` is now next-ready. No runtime behavior, transport delivery, protocol routes, Protobuf source, generated output, startup wiring, persistence, migrations, dependencies, authentication/session behavior changes, hosted deployment, release artifact, public announcement, paid promotion, matchmaking, match runtime, distributed runtime, broad social module, blob/S3 storage, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0215
    milestone: M-143
    title: Implement first server push and realtime messaging runtime slice
    status: completed
    change: changes/2026-05-23-implement-first-server-push-realtime-messaging-runtime-slice
    depends_on:
      - W-0214
    expected_change_id: implement-first-server-push-realtime-messaging-runtime-slice
    tier: tier_2_runtime
    selected_direction: first_server_push_realtime_messaging_runtime_slice
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0122
      - ADR-0121
      - ADR-0120
      - ADR-0106
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding Protobuf source or generated output, adding public client publish routes, adding stream subscription persistence, adding offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, backpressure mechanisms, distributed fanout, frontend/backend split, RPC, service discovery, cluster groups, broad chat, channels, groups, parties, rooms, matches, matchmaking, match runtime, storage object behavior changes, repository interface changes, PostgreSQL adapter changes, migrations, dependencies, authentication/session behavior changes, route-protection changes, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product expansion beyond the first runtime slice.
    completion:
      - Implement the smallest first server push and realtime messaging runtime slice authorized by ADR-0122.
      - Add focused tests for recipient validation, metadata-only identity refusal, redacted delivery outcomes, and single-process layering.
      - Preserve protocol/generated-output, persistence, migration, dependency, authentication/session, hosted deployment, release artifact, public announcement, paid promotion, blob/S3, matchmaking, match runtime, distributed runtime, broad social, and direct compatibility deferrals unless explicitly authorized.
    completion_summary: Implemented `runtime/internal/app/realtime/service.go` and `runtime/internal/app/realtime/service_test.go` as the first server-authored outbound realtime application service. The service accepts validated service/admin intent only, rejects metadata-only or player-authored publish attempts before registry access, resolves active bound connection and player-current-connection targets through the single-process connection registry, keeps stream subscriber delivery future-only, returns redacted copied delivery intents, and avoids socket writes. `ADR-0123` records the Nakama/Pitaya mapping: Nakama informs the notifications/streams/chat/presence-adjacent capability family, while Pitaya informs acceptor/session/handler/protocol/backend/push/group/broadcast separation. No WebSocket outbound delivery, Protobuf source, generated output, protocol bridge, application bootstrap handler, startup wiring, persistence, migration, dependency, authentication/session change, route-protection change, delivery guarantee, distributed fanout, broad social module, matchmaking, match runtime, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0216
    milestone: M-144
    title: Confirm next alpha direction after realtime runtime slice
    status: completed
    change: changes/2026-05-23-confirm-next-alpha-direction-after-realtime-runtime-slice
    depends_on:
      - W-0215
    expected_change_id: confirm-next-alpha-direction-after-realtime-runtime-slice
    tier: tier_2_planning
    selected_direction: confirm_next_alpha_direction_after_realtime_runtime_slice
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0123
      - ADR-0122
      - ADR-0106
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing WebSocket outbound delivery, adding Protobuf source or generated output, adding protocol routes, wiring startup, adding stream subscriptions, chat, groups, broadcast fanout, offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, backpressure mechanisms, distributed fanout, frontend/backend split, RPC, service discovery, cluster groups, matchmaking, match runtime, persistence, migrations, dependencies, authentication/session behavior changes, route-protection changes, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product expansion beyond choosing the next direction.
    completion:
      - Select exactly one next alpha direction after the realtime runtime slice.
      - Record Nakama/Pitaya reference alignment and preserve direct compatibility deferrals.
      - Open exactly one follow-up work item as next-ready.
    completion_summary: Selected `define_realtime_protocol_websocket_outbound_delivery_gate` as the next bounded alpha direction after the first server push and realtime messaging runtime slice. `ADR-0124` records Nakama alignment for advancing toward notifications, streams, chat, and presence-adjacent outbound protocol delivery, and Pitaya alignment for preserving acceptor/session/handler/protocol/backend/push separation before any concrete socket writes or fanout. Opened `M-145/W-0217 Define realtime protocol and WebSocket outbound delivery gate` as next-ready. No WebSocket outbound delivery, Protobuf source, generated output, protocol bridge, application bootstrap handler, startup wiring, persistence, migration, dependency, authentication/session behavior change, route-protection change, delivery guarantee, distributed fanout, broad social module, matchmaking, match runtime, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0217
    milestone: M-145
    title: Define realtime protocol and WebSocket outbound delivery gate
    status: completed
    change: changes/2026-05-23-define-realtime-protocol-websocket-outbound-delivery-gate
    depends_on:
      - W-0216
    expected_change_id: define-realtime-protocol-websocket-outbound-delivery-gate
    tier: tier_2_planning
    selected_direction: define_realtime_protocol_websocket_outbound_delivery_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0125
      - ADR-0124
      - ADR-0123
      - ADR-0122
      - ADR-0106
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing WebSocket outbound delivery, adding concrete socket writes, adding Protobuf source or generated output, adding protocol routes or bridge code, wiring application bootstrap handlers, wiring startup, adding stream subscriptions, chat, groups, broadcast fanout, offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, backpressure mechanisms, distributed fanout, frontend/backend split, RPC, service discovery, cluster groups, matchmaking, match runtime, persistence, migrations, dependencies, authentication/session behavior changes, route-protection changes, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product expansion beyond defining the gate.
    completion:
      - Define the realtime protocol and WebSocket outbound delivery gate.
      - Record protocol, generated-output, application handler, transport outbound delivery, verification, and stop-condition expectations.
      - Record Nakama/Pitaya reference alignment while preserving implementation, persistence, migration, dependency, authentication/session, route-protection, hosted deployment, release artifact, public announcement, paid promotion, blob/S3, matchmaking, match runtime, distributed runtime, broad social, delivery guarantee, and direct compatibility deferrals.
    completion_summary: Defined the realtime protocol and WebSocket outbound delivery gate, accepted `ADR-0125`, recorded future ownership for application realtime policy, Protobuf payload/envelope mapping, and WebSocket encoded-frame delivery, registered `runtime.realtime_protocol_websocket_outbound_delivery_gate`, and opened `M-146/W-0218 Implement realtime protocol and WebSocket outbound delivery slice` as next-ready. No WebSocket outbound delivery, concrete socket writes, realtime Protobuf source, generated output, protocol bridge behavior, application bootstrap handler, startup wiring, persistence, migration, dependency, authentication/session behavior change, route-protection change, stream subscription persistence, delivery guarantee, distributed fanout, broad social module, matchmaking, match runtime, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0218
    milestone: M-146
    title: Implement realtime protocol and WebSocket outbound delivery slice
    status: completed
    change: changes/2026-05-23-implement-realtime-protocol-websocket-outbound-delivery-slice
    commits: []
    depends_on:
      - W-0217
    expected_change_id: implement-realtime-protocol-websocket-outbound-delivery-slice
    tier: tier_2_runtime
    selected_direction: realtime_protocol_websocket_outbound_delivery_implementation_slice
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0125
      - ADR-0124
      - ADR-0123
      - ADR-0122
      - ADR-0106
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding stream subscriptions, chat semantics, groups, broadcast fanout, offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, backpressure mechanisms, distributed fanout, frontend/backend split, RPC, service discovery, cluster groups, matchmaking, match runtime, persistence, migrations, dependencies, authentication/session behavior changes, route-protection changes, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product expansion beyond the smallest protocol and WebSocket outbound delivery slice authorized by ADR-0125.
    completion:
      - Implement only the smallest realtime protocol and WebSocket outbound delivery slice authorized by ADR-0125.
      - Preserve application policy, protocol mapping, and transport write ownership boundaries.
      - Verify server-observed connection id and epoch targeting, redacted delivery failures, generated-output traceability, and no direct Nakama/Pitaya API compatibility.
    completion_summary: Implemented the bounded realtime protocol and WebSocket outbound delivery slice, accepted `ADR-0126`, registered `runtime.realtime_protocol_websocket_outbound_delivery_implementation`, added vibit-native `ServerNotice`, `DomainEventPush`, and `PresenceSignal` payloads with generated output, mapped accepted realtime delivery intents into existing envelopes and encoded frames, registered realtime generated payload types, added WebSocket `DeliverBinaryFrame` with server-observed connection id and epoch targeting, serialized per-socket writes, and opened `M-147/W-0219 Confirm next alpha direction after realtime outbound delivery slice` as next-ready. No startup wiring, realtime bootstrap handler, stream subscription, chat room, group, broadcast fanout, offline inbox, acknowledgement, retry, ordering guarantee, durable offset, backpressure, persistence, migration, dependency, authentication/session behavior change, route-protection change, matchmaking, match runtime, distributed runtime, hosted deployment, release artifact, public announcement, paid promotion, blob/S3 storage, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0219
    milestone: M-147
    title: Confirm next alpha direction after realtime outbound delivery slice
    status: completed
    change: changes/2026-05-23-confirm-next-alpha-direction-after-realtime-outbound-delivery-slice
    depends_on:
      - W-0218
    expected_change_id: confirm-next-alpha-direction-after-realtime-outbound-delivery-slice
    tier: tier_2_planning
    selected_direction: define_agent_native_feature_request_test_workflow
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0127
      - ADR-0126
      - ADR-0125
      - ADR-0124
      - ADR-0123
      - ADR-0122
      - ADR-0106
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing startup wiring, realtime bootstrap handlers, stream subscriptions, chat semantics, groups, broadcast fanout, offline inboxes, acknowledgements, retries, ordering guarantees, durable offsets, backpressure mechanisms, distributed fanout, frontend/backend split, RPC, service discovery, cluster groups, matchmaking, match runtime, persistence, migrations, dependencies, authentication/session behavior changes, route-protection changes, hosted deployments, release artifacts, public announcements beyond the GitHub release record, paid promotion, direct Nakama/Pitaya API compatibility, large object/blob storage, S3-compatible object storage, or broad product expansion beyond choosing the next direction.
    completion:
      - Select the next bounded alpha direction after realtime outbound delivery implementation.
      - Record the selection in ADR, change spec, conversation log, and manifests.
      - Make Nakama the primary product capability reference while deferring Pitaya as a future distributed architecture reference.
      - Record AI-native development and AI-native testing as the product purpose.
      - Preserve startup wiring, stream subscription, chat, group, broadcast fanout, persistence, delivery guarantee, distributed runtime, matchmaking, match runtime, hosted/release, public announcement, paid promotion, blob/S3, and direct compatibility deferrals.
    next_milestone: M-148
    next_ready_work_item: W-0220
    completion_summary: Selected `define_agent_native_feature_request_test_workflow` as the next alpha direction, accepted `ADR-0127`, recorded Nakama-first product capability planning, deferred Pitaya to future distributed architecture review, centered AI-native development and AI-native testing around user requirement to spec, acceptance criteria, test plan, tests, implementation, verification, and durable memory, and opened `M-148/W-0220` as next-ready. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, distributed runtime, broad product module, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0220
    milestone: M-148
    title: Define agent-native feature request and test workflow
    status: completed
    change: changes/2026-05-24-define-agent-native-feature-request-test-workflow
    depends_on:
      - W-0219
    expected_change_id: define-agent-native-feature-request-test-workflow
    tier: tier_2_standard
    selected_direction: define_agent_native_feature_request_test_workflow
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0128
      - ADR-0127
      - ADR-0126
      - ADR-0106
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing runtime behavior, protocol changes, generated output, migrations, dependencies, broad product modules, Pitaya-style distributed architecture, hosted deployment, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility.
    completion:
      - Define the AI-native feature request and test workflow standard.
      - Define required change artifacts such as bounded requirement specs, acceptance criteria, test plans, tests, implementation notes, verification records, and durable memory updates.
      - Register repository checks or a future check posture for the workflow standard.
    next_milestone: M-149
    next_ready_work_item: W-0221
    completion_summary: Defined the agent-native feature request and test workflow standard in `docs/agent-native-feature-request-test-workflow.md`, accepted `ADR-0128`, registered `runtime.agent_native_feature_request_test_workflow`, required user requirement capture, bounded specs, Nakama capability mapping, acceptance criteria, test plans, tests or explicit not-applicable rationale, implementation boundaries, verification, and durable memory updates for non-trivial user-facing feature work, and opened `M-149/W-0221 Pilot Nakama-aligned feature request workflow` as next-ready. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, broad product module, Pitaya-style distributed architecture, hosted deployment, release artifact, public announcement, paid promotion, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0221
    milestone: M-149
    title: Pilot Nakama-aligned feature request workflow
    status: completed
    change: changes/2026-05-24-pilot-nakama-aligned-feature-request-workflow
    depends_on:
      - W-0220
    expected_change_id: pilot-nakama-aligned-feature-request-workflow
    tier: tier_2_planning
    selected_direction: pilot_nakama_aligned_feature_request_workflow
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0129
      - ADR-0128
      - ADR-0127
      - ADR-0106
      - ADR-0086
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing broad runtime behavior, protocol changes, generated output, migrations, dependencies, broad product modules, Pitaya-style distributed architecture, hosted deployment, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of piloting the workflow and selecting a bounded follow-up slice.
    completion:
      - Apply the agent-native feature request and test workflow to a concrete Nakama-aligned product capability request.
      - Record the request, Nakama capability family, acceptance criteria, test plan, implementation boundaries, verification plan, and durable memory expectations.
      - Select or open one bounded follow-up work item without adding unbounded runtime/product scope.
    selected_nakama_capability_family: presence_status_and_notifications
    next_milestone: M-150
    next_ready_work_item: W-0222
    completion_summary: Piloted the agent-native feature request workflow on a concrete Nakama-style self-presence/status local alpha requirement, accepted `ADR-0129`, selected `presence_status_and_notifications` as the capability family, recorded acceptance criteria, test plan, implementation boundaries, verification, and durable memory expectations, and opened `W-0222 Harden presence status local proof through close and offline cases` as the next-ready bounded follow-up. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, broad product module, Pitaya-style distributed architecture, hosted deployment, release artifact, public announcement, paid promotion, or direct Nakama/Pitaya API compatibility was added.

  - id: W-0222
    milestone: M-150
    title: Harden presence status local proof through close and offline cases
    status: completed
    change: changes/2026-05-24-harden-presence-status-local-proof
    depends_on:
      - W-0221
    expected_change_id: harden-presence-status-local-proof
    tier: tier_2_runtime
    selected_direction: presence_status_local_proof_hardening
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0129
      - ADR-0128
      - ADR-0087
      - ADR-0085
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding new protocol routes or Protobuf messages, generated output, migrations, dependencies, persistence, presence subscriptions, status broadcast fanout, chat, groups, parties, matchmaking, match runtime, operations/admin behavior, SDKs, distributed runtime, startup wiring, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of hardening the bounded local presence/status proof.
    completion:
      - Add or strengthen focused tests that prove existing self-presence reports online from active bound connections and offline after close or invalidation.
      - Add or strengthen local alpha request-flow proof only where it uses existing protocol and runtime surfaces.
      - Record verification and memory updates without broadening presence/status into subscriptions, broadcasts, chat, social, persistence, distributed runtime, or direct compatibility.
    decision: ADR-0130
    check_rule: runtime.presence_status_local_proof_hardening
    next_milestone: M-151
    next_ready_work_item: W-0223
    completion_summary: Hardened the existing self-presence/status proof with `TestGetPlayerPresenceReportsOfflineAfterCloseAndInvalidation` at the application boundary and `TestPresenceStatusLocalAlphaFlowReportsOfflineAfterCloseAndInvalidation` through the authenticated local alpha Protobuf request flow. The slice proves online active bound connection behavior, offline after transport close, offline after close-policy invalidation, and response redaction while preserving protocol/generated/migration/dependency/persistence/startup/direct compatibility deferrals.

  - id: W-0223
    milestone: M-151
    title: Strengthen authenticated gameplay failure-path verification
    status: completed
    change: changes/2026-05-24-strengthen-authenticated-gameplay-failure-path-verification
    depends_on:
      - W-0222
    expected_change_id: strengthen-authenticated-gameplay-failure-path-verification
    tier: tier_2_runtime
    selected_direction: authenticated_gameplay_failure_path_verification
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0130
      - ADR-0129
      - ADR-0128
      - ADR-0127
      - ADR-0087
      - ADR-0085
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding new protocol routes, Protobuf messages, generated output, migrations, dependencies, persistence, startup wiring, logout behavior changes, token refresh, cleanup jobs, broad product modules, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of strengthening existing authenticated gameplay failure-path verification.
    completion:
      - Add or strengthen focused tests for authenticated local alpha gameplay failure paths using existing runtime and protocol surfaces.
      - Prove missing, malformed, invalid, expired, revoked, route-protection, or redaction behavior at the lowest existing boundary that accurately exercises the current implementation.
      - Record verification and memory updates without broadening authentication, session, protocol, persistence, social, multiplayer, distributed runtime, or direct compatibility scope.
    decision: ADR-0131
    check_rule: runtime.authenticated_gameplay_failure_path_verification
    next_milestone: M-152
    next_ready_work_item: W-0224
    completion_summary: Added `TestAuthenticatedGameplayFailurePathsLocalAlphaFlow` to the authenticated local alpha Protobuf E2E proof. The slice covers protected inventory missing wrapper, malformed wrapper, malformed token, unknown well-formed token, expired token, revoked token after logout, protected presence missing wrapper, and response redaction while preserving protocol/generated/migration/dependency/persistence/startup/authentication-session/direct compatibility deferrals.

  - id: W-0224
    milestone: M-152
    title: Select next Nakama prototype-ready capability after authenticated failure-path proof
    status: completed
    change: changes/2026-05-24-select-next-nakama-prototype-ready-capability-after-authenticated-failure-path-proof
    depends_on:
      - W-0223
    expected_change_id: select-next-nakama-prototype-ready-capability-after-authenticated-failure-path-proof
    tier: tier_1_planning
    selected_direction: select_next_nakama_prototype_ready_capability_after_authenticated_failure_path_proof
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0132
      - ADR-0131
      - ADR-0130
      - ADR-0129
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding runtime behavior, new protocol routes, Protobuf messages, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, token refresh, cleanup jobs, broad product modules, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of selecting the next bounded Nakama prototype-ready capability.
    completion:
      - Review the Nakama-first roadmap and current alpha foundation after authenticated failure-path proof.
      - Select one next bounded prototype-ready capability or foundation gap with explicit rationale and deferrals.
      - Open a follow-up work item that can apply the agent-native requirement/test workflow without broadening scope prematurely.
    decision: ADR-0132
    check_rule: runtime.next_nakama_prototype_ready_capability_selection
    next_milestone: M-153
    next_ready_work_item: W-0225
    completion_summary: Selected `client_sdks_examples_and_developer_experience` as the next bounded Nakama prototype-ready capability family and `define_local_alpha_example_client_path_gate` as the follow-up direction. The selection records that the current source-first alpha needs a clearer client-like or example-app path before broadening into chat, social, matchmaking, match runtime, operations, SDK publication, hosted surfaces, or distributed runtime. No implementation, runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, release artifact, hosted deployment, or direct compatibility scope was added.

  - id: W-0225
    milestone: M-153
    title: Define local alpha example client path gate
    status: completed
    change: changes/2026-05-24-define-local-alpha-example-client-path-gate
    depends_on:
      - W-0224
    expected_change_id: define-local-alpha-example-client-path-gate
    tier: tier_2_developer_experience
    selected_direction: define_local_alpha_example_client_path_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0132
      - ADR-0131
      - ADR-0128
      - ADR-0127
      - ADR-0108
      - ADR-0097
      - ADR-0094
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing an example client or example app, publishing SDKs, generating client libraries, adding dependencies, adding runtime behavior, new protocol routes, Protobuf messages, generated output, migrations, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of defining the local alpha example client path gate.
    completion:
      - Define the allowed source-first example client or example app shape for the local alpha path.
      - Record file ownership, redaction requirements, accepted runtime/protocol surfaces, test plan, verification commands, and stop conditions for the future implementation slice.
      - Open a follow-up implementation work item only if the gate can keep scope bounded to existing behavior and existing protocol surfaces.
    decision: ADR-0133
    check_rule: runtime.local_alpha_example_client_path_gate
    next_milestone: M-154
    next_ready_work_item: W-0226
    completion_summary: Defined the local alpha example client path gate as a source-first repository-local path. Accepted `ADR-0133`, added `docs/local-alpha-example-client-path-gate.md` and its Simplified Chinese translation, registered `runtime.local_alpha_example_client_path_gate`, and opened `M-154/W-0226 Implement local alpha example client path` as the next-ready bounded follow-up. No example implementation, SDK publication, generated client library, runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, hosted deployment, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0226
    milestone: M-154
    title: Implement local alpha example client path
    status: completed
    change: changes/2026-05-24-implement-local-alpha-example-client-path
    depends_on:
      - W-0225
    expected_change_id: implement-local-alpha-example-client-path
    tier: tier_2_developer_experience
    selected_direction: implement_local_alpha_example_client_path
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0133
      - ADR-0132
      - ADR-0131
      - ADR-0128
      - ADR-0127
      - ADR-0108
      - ADR-0097
      - ADR-0094
      - ADR-0082
      - ADR-0078
    ask_first:
      - Publishing SDKs, generating client libraries, adding dependencies, adding runtime behavior, adding public onboarding routes, adding new protocol routes, Protobuf messages, generated output, migrations, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of implementing the source-first local alpha example client path.
    completion:
      - Add a clear repository-local example path under `examples/` for the existing local alpha loop.
      - Keep raw credentials, raw access tokens, digests, verifier keys, DSNs, and transport metadata redacted.
      - Reuse existing runtime and protocol behavior without adding routes, Protobuf source, generated output, dependencies, migrations, persistence, startup wiring, SDK publication, hosted surfaces, release artifacts, distributed runtime, or direct compatibility.
      - Verify the example path and update durable memory.
    decision: ADR-0134
    check_rule: runtime.local_alpha_example_client_path_implementation
    next_milestone: M-155
    next_ready_work_item: W-0227
    completion_summary: Implemented the source-first local alpha example client path with `examples/local-alpha-client/README.md`, `examples/local-alpha-client/README.zh-CN.md`, and `examples/local-alpha-example-client.sh`; kept `examples/local-alpha-request-loop.sh` as a compatibility wrapper; reused existing focused local alpha Protobuf E2E proofs for onboarding, login, connection binding, protected inventory, presence, storage objects, logout, rejected post-logout requests, failure-path handling, and redaction checks; and opened `M-155/W-0227 Select next Nakama prototype-ready capability after local alpha example client path` as the next-ready follow-up. No SDK, generated client library, runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, hosted deployment, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0227
    milestone: M-155
    title: Select next Nakama prototype-ready capability after local alpha example client path
    status: completed
    change: changes/2026-05-24-select-next-nakama-prototype-ready-capability-after-local-alpha-example-client-path
    depends_on:
      - W-0226
    expected_change_id: select-next-nakama-prototype-ready-capability-after-local-alpha-example-client-path
    tier: tier_1_planning
    selected_direction: select_next_nakama_prototype_ready_capability_after_local_alpha_example_client_path
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0134
      - ADR-0133
      - ADR-0132
      - ADR-0128
      - ADR-0127
      - ADR-0108
      - ADR-0097
      - ADR-0094
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding runtime behavior, new protocol routes, Protobuf messages, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of selecting the next bounded Nakama prototype-ready capability.
    completion:
      - Review the Nakama-first roadmap and current alpha foundation after the source-first example client path implementation.
      - Select one next bounded prototype-ready capability or foundation gap with explicit rationale and deferrals.
      - Open a follow-up work item that can apply the agent-native requirement/test workflow without broadening scope prematurely.
    decision: ADR-0135
    check_rule: runtime.agent_native_feature_request_scaffolding_selection
    next_milestone: M-156
    next_ready_work_item: W-0228
    completion_summary: Selected `agent_native_requirement_test_implementation_workflow` as the next bounded Nakama prototype-ready capability family and `define_agent_native_feature_request_scaffolding_gate` as the follow-up direction. The selection records that the current source-first alpha now has a readable example path, so the next highest-leverage product gap is making the AI-native user requirement to tests and implementation workflow scaffoldable before broad chat, social, matchmaking, match runtime, operations/admin, SDK publication, hosted surfaces, distributed runtime, or direct compatibility work. No scaffolding implementation, runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, SDK, hosted deployment, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0228
    milestone: M-156
    title: Define agent-native feature request scaffolding gate
    status: completed
    change: changes/2026-05-24-define-agent-native-feature-request-scaffolding-gate
    depends_on:
      - W-0227
    expected_change_id: define-agent-native-feature-request-scaffolding-gate
    tier: tier_2_standard
    selected_direction: define_agent_native_feature_request_scaffolding_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0135
      - ADR-0134
      - ADR-0133
      - ADR-0132
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Implementing scaffolding, changing runtime behavior, adding new protocol routes, Protobuf messages, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of defining the scaffolding gate.
    completion:
      - Define the accepted feature request scaffolding posture for future user requirement intake.
      - Record required artifacts, template/tool candidates, redaction expectations, check coverage, test plan, verification commands, and stop conditions.
      - Open a follow-up implementation work item only if it can stay bounded to docs, templates, tooling, and checks without runtime, protocol, migration, dependency, hosted, SDK, distributed, or direct compatibility scope.
    decision: ADR-0136
    check_rule: runtime.agent_native_feature_request_scaffolding_gate
    next_milestone: M-157
    next_ready_work_item: W-0229
    completion_summary: Defined the agent-native feature request scaffolding gate as a source-first change artifact scaffolding posture. Accepted `ADR-0136`, added `docs/agent-native-feature-request-scaffolding-gate.md` and its Simplified Chinese translation, registered `runtime.agent_native_feature_request_scaffolding_gate`, required future scaffold artifacts `request.md`, `spec.yaml`, `impact.md`, `plan.md`, `checklist.md`, and `verification.md`, recorded future candidates `tools/vibit scaffold feature` and `changes/_template/feature-request/`, and opened `M-157/W-0229 Implement agent-native feature request scaffolding` as the next-ready bounded follow-up. No scaffolding implementation, templates, scaffold command, runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, persistence, startup wiring, hosted deployment, SDK, distributed runtime, or direct compatibility scope was added.

  - id: W-0229
    milestone: M-157
    title: Implement agent-native feature request scaffolding
    status: completed
    change: changes/2026-05-24-implement-agent-native-feature-request-scaffolding
    depends_on:
      - W-0228
    expected_change_id: implement-agent-native-feature-request-scaffolding
    tier: tier_2_tooling
    selected_direction: implement_agent_native_feature_request_scaffolding
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0136
      - ADR-0135
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding runtime behavior, new protocol routes, Protobuf messages, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of implementing source-first feature request scaffolding.
    completion:
      - Add source-first feature request scaffolding under `changes/_template/feature-request/` and/or a narrow `tools/vibit scaffold feature` command.
      - Ensure scaffold output or templates cover `request.md`, `spec.yaml`, `impact.md`, `plan.md`, `checklist.md`, and `verification.md`.
      - Include Nakama capability mapping, Pitaya deferral, acceptance criteria, test planning, implementation boundaries, redaction reminders, verification commands, and durable memory expectations.
      - Add focused checks or tests for the scaffold/template shape.
      - Preserve runtime, protocol, generated output, migration, dependency, hosted, SDK, distributed runtime, and direct compatibility deferrals.
    decision: ADR-0137
    check_rule: runtime.agent_native_feature_request_scaffolding_implementation
    next_milestone: M-158
    next_ready_work_item: W-0230
    completion_summary: Implemented source-first feature request scaffolding with `changes/_template/feature-request/`, the six required template artifacts, `docs/agent-native-feature-request-scaffolding.md` and its Simplified Chinese translation, a narrow `tools/vibit scaffold feature` command, `ADR-0137`, and `runtime.agent_native_feature_request_scaffolding_implementation` check coverage. The command supports `--request`, `--summary`, `--date`, and `--dry-run`, refuses to overwrite existing change directories, and creates deterministic text change artifacts. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, hosted deployment, SDK, distributed runtime, or direct compatibility scope was added.

  - id: W-0230
    milestone: M-158
    title: Pilot scaffolded Nakama feature request intake
    status: completed
    change: changes/2026-05-24-pilot-scaffolded-nakama-feature-request-intake
    depends_on:
      - W-0229
    expected_change_id: pilot-scaffolded-nakama-feature-request-intake
    tier: tier_2_standard
    selected_direction: pilot_scaffolded_nakama_feature_request_intake
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0138
      - ADR-0137
      - ADR-0136
      - ADR-0135
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding runtime behavior, new protocol routes, Protobuf messages, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of piloting scaffolded feature request intake.
    completion:
      - Use `tools/vibit scaffold feature` to create the pilot change directory.
      - Fill the scaffolded request with Nakama capability mapping, Pitaya deferral, acceptance criteria, test plan, implementation boundaries, verification, and durable memory expectations.
      - Select a bounded next implementation or gate follow-up only if it preserves current ask-first boundaries.
      - Preserve runtime, protocol, generated output, migration, dependency, hosted, SDK, distributed runtime, and direct compatibility deferrals unless the pilot explicitly opens a later authorized work item.
    decision: ADR-0138
    check_rule: runtime.scaffolded_nakama_feature_request_intake_pilot
    next_milestone: M-159
    next_ready_work_item: W-0231
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: define_friends_relationship_lifecycle_gate
    completion_summary: Used `tools/vibit scaffold feature` to create and fill the pilot change directory, selected a concrete player friendship relationship lifecycle request under the Nakama `friends_groups_and_parties` capability family, recorded acceptance criteria, future test plan, implementation boundaries, verification, and durable memory, accepted `ADR-0138`, registered `runtime.scaffolded_nakama_feature_request_intake_pilot`, and opened `M-159/W-0231 Define friends relationship lifecycle gate` as the next-ready bounded follow-up. No runtime behavior, protocol route, Protobuf source, generated output, migration, dependency, hosted deployment, SDK, distributed runtime, or direct compatibility scope was added.

  - id: W-0231
    milestone: M-159
    title: Define friends relationship lifecycle gate
    status: completed
    change: changes/2026-05-24-define-friends-relationship-lifecycle-gate
    depends_on:
      - W-0230
    expected_change_id: define-friends-relationship-lifecycle-gate
    tier: tier_2_standard
    selected_direction: define_friends_relationship_lifecycle_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0139
      - ADR-0138
      - ADR-0137
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding runtime behavior, new protocol routes, Protobuf messages, generated output, migrations, dependencies, persistence, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of defining the friends relationship lifecycle gate.
    completion:
      - Define the friendship relationship lifecycle semantic boundary before implementation.
      - Record Nakama friends/social mapping, Pitaya deferral, acceptance criteria, tests, ownership, identity, permissions, errors, events, invariants, data posture, protocol posture, generated output posture, redaction, verification, and durable memory expectations.
      - Open a bounded follow-up only if it preserves current ask-first boundaries.
      - Preserve runtime, protocol, generated output, migration, dependency, hosted, SDK, distributed runtime, and direct compatibility deferrals unless the gate explicitly opens a later authorized work item.
    decision: ADR-0139
    check_rule: runtime.friends_relationship_lifecycle_gate
    next_milestone: M-160
    next_ready_work_item: W-0232
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: define_friends_relationship_persistence_schema_gate
    completion_summary: Defined the friends relationship lifecycle semantic gate, added the English standard and Simplified Chinese translation, accepted `ADR-0139`, registered `runtime.friends_relationship_lifecycle_gate`, recorded future friend request, accept, reject, remove, block, unblock, list, and status-read scope, future command/query/event/error/permission vocabulary, actor-relative relationship states, validated-player-identity requirement, metadata-only identity rejection, lifecycle invariants, redaction, and future tests, and opened `M-160/W-0232 Define friends relationship persistence schema gate` as the next-ready follow-up. No runtime behavior, protocol route, Protobuf source, generated output, migration, repository interface, PostgreSQL adapter, dependency, startup wiring, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0232
    milestone: M-160
    title: Define friends relationship persistence schema gate
    status: completed
    change: changes/2026-05-24-define-friends-relationship-persistence-schema-gate
    depends_on:
      - W-0231
    expected_change_id: define-friends-relationship-persistence-schema-gate
    tier: tier_2_data
    selected_direction: define_friends_relationship_persistence_schema_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0137
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding migration source, relationship tables, repository interface implementation, PostgreSQL adapter implementation, runtime behavior, protocol routes, Protobuf messages, generated output, dependencies, persistence execution, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, or direct Nakama/Pitaya API compatibility instead of defining the friends relationship persistence schema gate.
    completion:
      - Define future friends relationship table candidates, pair identity, state representation, block representation, indexes, uniqueness, timestamps, event/audit posture, and redaction expectations before SQL.
      - Record future migration source candidate, repository owner, PostgreSQL adapter owner, transaction posture, test expectations, and stop conditions.
      - Preserve migration source, repository interface, adapter implementation, runtime behavior, protocol routes, generated output, dependencies, hosted, SDK, distributed runtime, and direct compatibility deferrals unless a later bounded work item explicitly authorizes them.
    decision: ADR-0140
    check_rule: runtime.friends_relationship_persistence_schema_gate
    next_milestone: M-161
    next_ready_work_item: W-0233
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: add_friends_relationship_migration_source
    completion_summary: Defined the friends relationship persistence schema gate, added the English standard and Simplified Chinese translation, accepted `ADR-0140`, registered `runtime.friends_relationship_persistence_schema_gate`, recorded PostgreSQL as the selected first store, `friend_relationships` as the future current-state table, `runtime/migrations/postgres/000007_create_friend_relationships.sql` as the migration source candidate, canonical unordered player pair identity, lifecycle state, actor-specific block timestamps, relationship versioning, timestamp checks, uniqueness/index posture, event/audit deferral, redaction, and future repository/adapter owners, and opened `M-161/W-0233 Add friends relationship migration source` as the next-ready follow-up. No migration source, relationship table, repository interface, PostgreSQL adapter, runtime behavior, protocol route, Protobuf source, generated output, dependency, startup wiring, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, event/audit table, or direct compatibility scope was added.

  - id: W-0233
    milestone: M-161
    title: Add friends relationship migration source
    status: completed
    change: changes/2026-05-24-add-friends-relationship-migration-source
    depends_on:
      - W-0232
    expected_change_id: add-friends-relationship-migration-source
    tier: tier_2_data
    selected_direction: add_friends_relationship_migration_source
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding repository interfaces, PostgreSQL adapter implementation, runtime behavior, protocol routes, Protobuf messages, generated output, dependencies, persistence execution beyond SQL source, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of adding only the friends relationship migration source.
    completion:
      - Add `runtime/migrations/postgres/000007_create_friend_relationships.sql` with goose up/down markers.
      - Create only the `friend_relationships` current-state table with pair identity, lifecycle state, block timestamps, relationship version, timestamps, uniqueness, indexes, and checks from ADR-0140.
      - Add or update static checks for the migration shape and forbidden columns.
      - Preserve repository interfaces, PostgreSQL adapter behavior, runtime behavior, protocol routes, Protobuf source, generated output, dependencies, event/audit tables, hosted, SDK, distributed runtime, and direct compatibility deferrals.
    decision: ADR-0141
    check_rule: runtime.friends_relationship_migration_source
    next_milestone: M-162
    next_ready_work_item: W-0234
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: define_friends_relationship_repository_boundary
    completion_summary: Added `runtime/migrations/postgres/000007_create_friend_relationships.sql`, accepted `ADR-0141`, registered `runtime.friends_relationship_migration_source`, created only the `friend_relationships` current-state table with canonical unordered pair identity, lifecycle state, pair-member actor columns, actor-specific block timestamps, relationship version, timestamp checks, canonical pair uniqueness, pair-member/lifecycle-state indexes, and updated-at index, and opened `M-162/W-0234 Define friends relationship repository boundary` as the next-ready follow-up. No repository interface, PostgreSQL adapter behavior, runtime friendship behavior, protocol route, Protobuf source, generated output, dependency, startup wiring, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0234
    milestone: M-162
    title: Define friends relationship repository boundary
    status: completed
    change: changes/2026-05-25-define-friends-relationship-repository-boundary
    depends_on:
      - W-0233
    expected_change_id: define-friends-relationship-repository-boundary
    tier: tier_2_data
    selected_direction: define_friends_relationship_repository_boundary
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0141
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding repository interface implementation, PostgreSQL adapter implementation, runtime behavior, protocol routes, Protobuf messages, generated output, dependencies, persistence execution beyond existing SQL source, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of defining only the friends relationship repository boundary.
    completion:
      - Define the future friends relationship repository owner and interface candidate before implementation.
      - Record future value types, create/read/list/update/delete or lifecycle method vocabulary, conflict classes, transaction handoff, redaction, PostgreSQL adapter expectations, tests, and stop conditions.
      - Preserve repository implementation, PostgreSQL adapter behavior, runtime behavior, protocol routes, generated output, dependencies, event/audit tables, hosted, SDK, distributed runtime, and direct compatibility deferrals unless a later bounded work item explicitly authorizes them.
    decision: ADR-0142
    check_rule: runtime.friends_relationship_repository_boundary
    next_milestone: M-163
    next_ready_work_item: W-0235
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: implement_friends_relationship_repository_interface
    completion_summary: Defined the friends relationship repository boundary, added the English standard and Simplified Chinese translation, accepted `ADR-0142`, registered `runtime.friends_relationship_repository_boundary`, recorded future repository owner `runtime/internal/modules/friends`, future interface candidate `runtime/internal/modules/friends.Repository`, candidate value types, lifecycle repository capabilities, canonical unordered pair identity, validated request identity handoff, version and conflict posture, redaction posture, PostgreSQL adapter expectations, and opened `M-163/W-0235 Implement storage-neutral friends relationship repository interface` as the next-ready follow-up. No repository interface implementation, PostgreSQL adapter behavior, runtime friendship behavior, protocol route, Protobuf source, generated output, migration, dependency, startup wiring, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0235
    milestone: M-163
    title: Implement storage-neutral friends relationship repository interface
    status: completed
    change: changes/2026-05-25-implement-friends-relationship-repository-interface
    depends_on:
      - W-0234
    expected_change_id: implement-friends-relationship-repository-interface
    tier: tier_2_data
    selected_direction: implement_friends_relationship_repository_interface
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0143
      - ADR-0142
      - ADR-0141
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding PostgreSQL adapter implementation, SQL execution, runtime behavior, protocol routes, Protobuf messages, generated output, dependencies, persistence execution beyond existing SQL source, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of implementing only the storage-neutral friends relationship repository interface.
    completion:
      - Add `runtime/internal/modules/friends` with storage-neutral repository interface and value types.
      - Preserve canonical pair identity, lifecycle state, actor handoff, version conflict, redaction, and repository error semantics from ADR-0142.
      - Add focused Go tests for normalization, conflict vocabulary, redacted errors, copying, and forbidden dependency posture.
      - Preserve PostgreSQL adapter behavior, runtime behavior, protocol routes, generated output, dependencies, event/audit tables, hosted, SDK, distributed runtime, and direct compatibility deferrals unless a later bounded work item explicitly authorizes them.
    decision: ADR-0143
    check_rule: runtime.friends_relationship_repository_interface_implementation
    next_milestone: M-164
    next_ready_work_item: W-0236
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: define_friends_relationship_postgresql_adapter_gate
    completion_summary: Implemented `runtime/internal/modules/friends.Repository`, value types, lifecycle/status/conflict vocabularies, canonical unordered pair and actor normalizers, list-result copying, redacted repository errors, focused tests, and `modules/friends` manifest and guides; accepted `ADR-0143`; registered `runtime.friends_relationship_repository_interface_implementation`; and opened `M-164/W-0236 Define friends relationship PostgreSQL adapter gate` as the next-ready follow-up. No PostgreSQL adapter behavior, SQL execution, runtime friendship behavior, protocol route, Protobuf source, generated output, migration, dependency, startup wiring, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0236
    milestone: M-164
    title: Define friends relationship PostgreSQL adapter gate
    status: completed
    change: changes/2026-05-26-define-friends-relationship-postgresql-adapter-gate
    depends_on:
      - W-0235
    expected_change_id: define-friends-relationship-postgresql-adapter-gate
    tier: tier_2_data
    selected_direction: define_friends_relationship_postgresql_adapter_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0143
      - ADR-0142
      - ADR-0141
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding PostgreSQL adapter implementation, SQL execution behavior, runtime behavior, protocol routes, Protobuf messages, generated output, dependencies, persistence execution beyond existing SQL source, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of defining only the friends relationship PostgreSQL adapter gate.
    completion:
      - Define the future friends relationship PostgreSQL adapter owner, source and test candidates, transaction handoff, SQL mapping posture, conflict mapping, redaction posture, and stop conditions.
      - Preserve adapter implementation, SQL execution behavior, runtime behavior, protocol routes, generated output, dependencies, event/audit tables, hosted, SDK, distributed runtime, and direct compatibility deferrals unless a later bounded work item explicitly authorizes them.
    decision: ADR-0144
    check_rule: runtime.friends_relationship_postgresql_adapter_gate
    next_milestone: M-165
    next_ready_work_item: W-0237
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: implement_friends_relationship_postgresql_adapter
    completion_summary: Defined the friends relationship PostgreSQL adapter gate, added the English standard and Simplified Chinese translation, accepted `ADR-0144`, registered `runtime.friends_relationship_postgresql_adapter_gate`, recorded the future adapter owner, source/test candidates, constructor posture, unit-of-work handoff, SQL mapping posture, conflict mapping, redaction posture, test expectations, and opened `M-165/W-0237 Implement friends relationship PostgreSQL adapter` as the next-ready follow-up. No PostgreSQL adapter implementation, SQL execution behavior, runtime friendship behavior, protocol route, Protobuf source, generated output, migration, dependency, startup wiring, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0237
    milestone: M-165
    title: Implement friends relationship PostgreSQL adapter
    status: completed
    change: changes/2026-05-26-implement-friends-relationship-postgresql-adapter
    depends_on:
      - W-0236
    expected_change_id: implement-friends-relationship-postgresql-adapter
    tier: tier_2_data
    selected_direction: implement_friends_relationship_postgresql_adapter
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0144
      - ADR-0143
      - ADR-0142
      - ADR-0141
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding runtime friendship behavior, protocol routes, Protobuf messages, generated output, dependencies, migration changes, startup wiring beyond the bounded adapter handoff, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of implementing only the friends relationship PostgreSQL adapter.
    completion:
      - Implement the PostgreSQL adapter under `runtime/internal/platform/persistence/postgres` for `runtime/internal/modules/friends.Repository`.
      - Use the existing `friend_relationships` migration source and caller-supplied unit-of-work executor handoff.
      - Preserve SQL mapping, conflict mapping, redaction, transaction-control, and test expectations from `ADR-0144`.
      - Preserve runtime behavior, protocol routes, Protobuf source, generated output, dependencies, event/audit tables, hosted, SDK, distributed runtime, and direct compatibility deferrals unless a later bounded work item explicitly authorizes them.
    decision: ADR-0145
    check_rule: runtime.friends_relationship_postgresql_adapter_implementation
    next_milestone: M-166
    next_ready_work_item: W-0238
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: define_friends_relationship_runtime_behavior_gate
    completion_summary: Implemented `runtime/internal/platform/persistence/postgres/friend_relationship_repository.go` and `runtime/internal/platform/persistence/postgres/friend_relationship_repository_test.go`, mapping `runtime/internal/modules/friends.Repository` to the existing `friend_relationships` table through caller-supplied unit-of-work executors. The adapter covers create/update request, pair lookup, player-scoped list with status filtering and one-row-overflow pagination, accept/reject/remove lifecycle transitions with optional expected-version checks, actor-specific block/unblock columns without implicit friendship restoration, canonical pair normalization, relationship version increments, row scanning through friends normalizers, redacted friends repository errors, fake-executor tests, no live PostgreSQL requirement, and `UnitOfWork.NewFriendRelationshipRepository`. No runtime friendship handlers, protocol routes, Protobuf sources/generated output, dependencies, migration changes, authentication/session behavior changes, event/audit tables, hosted deployments, release artifacts, public announcements, paid promotion, broader social features, distributed runtime, or direct compatibility were added.

  - id: W-0238
    milestone: M-166
    title: Define friends relationship runtime behavior gate
    status: completed
    change: changes/2026-05-26-define-friends-relationship-runtime-behavior-gate
    depends_on:
      - W-0237
    expected_change_id: define-friends-relationship-runtime-behavior-gate
    tier: tier_2_functional
    selected_direction: define_friends_relationship_runtime_behavior_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0145
      - ADR-0144
      - ADR-0143
      - ADR-0142
      - ADR-0141
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding runtime friendship behavior implementation, protocol routes, Protobuf messages, generated output, dependencies, migration changes, startup wiring beyond gate metadata, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of defining only the friends relationship runtime behavior gate.
    completion:
      - Define the future friends relationship runtime behavior owner, validated request identity derivation, actor-relative behavior, permission and route-policy posture, repository/unit-of-work handoff, conflict mapping, redaction posture, test expectations, and stop conditions.
      - Preserve runtime behavior implementation, protocol routes, generated output, dependencies, event/audit tables, hosted, SDK, distributed runtime, and direct compatibility deferrals unless a later bounded work item explicitly authorizes them.
    decision: ADR-0146
    check_rule: runtime.friends_relationship_runtime_behavior_gate
    next_milestone: M-167
    next_ready_work_item: W-0239
    selected_nakama_capability_family: friends_groups_and_parties
    selected_follow_up_direction: implement_friends_relationship_runtime_behavior
    completion_summary: Defined the friends relationship runtime behavior gate, added the English standard and Simplified Chinese translation, accepted `ADR-0146`, registered `runtime.friends_relationship_runtime_behavior_gate`, recorded future application owner `runtime/internal/app`, future package/source/test candidates under `runtime/internal/app/friends`, validated request identity actor derivation, metadata-only `player_id` and `session_id` proof refusal, actor-relative public relationship status behavior, conservative `request_token_required` route-policy posture, repository/unit-of-work handoff, conflict mapping, redaction posture, focused implementation test expectations, and opened `M-167/W-0239 Implement friends relationship runtime behavior` as the next-ready follow-up. No runtime friendship behavior implementation, runtime handler, startup wiring, protocol route, Protobuf source, generated output, repository interface change, PostgreSQL adapter change, migration, dependency, event/audit table, broader social feature, hosted deployment, SDK, release artifact, distributed runtime, or direct compatibility scope was added.

  - id: W-0239
    milestone: M-167
    title: Implement friends relationship runtime behavior
    status: completed
    change: changes/2026-05-26-implement-friends-relationship-runtime-behavior
    depends_on:
      - W-0238
    expected_change_id: implement-friends-relationship-runtime-behavior
    tier: tier_2_functional
    selected_direction: implement_friends_relationship_runtime_behavior
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0146
      - ADR-0145
      - ADR-0144
      - ADR-0143
      - ADR-0142
      - ADR-0141
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding protocol routes, Protobuf messages, generated output, dependencies, migration changes, startup wiring beyond application-owned service behavior, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of implementing only the friends relationship runtime behavior service.
    completion:
      - Implement the application-owned friends relationship runtime behavior service using validated request identity and existing friends repository/unit-of-work boundaries.
      - Preserve actor-relative public status, metadata-only identity rejection, lifecycle conflict mapping, redaction, and test expectations from `ADR-0146`.
      - Preserve protocol routes, Protobuf source, generated output, dependencies, migrations, event/audit tables, hosted, SDK, distributed runtime, and direct compatibility deferrals unless a later bounded work item explicitly authorizes them.
    decision: ADR-0147
    check_rule: runtime.friends_relationship_runtime_behavior_implementation
    next_milestone: M-168
    next_ready_work_item: W-0240
    selected_follow_up_direction: define_friends_relationship_protocol_route_gate
    completion_summary: Implemented the application-owned friends relationship runtime behavior service under `runtime/internal/app/friends`, added focused service tests, accepted `ADR-0147`, registered `runtime.friends_relationship_runtime_behavior_implementation`, enforced validated player request identity and metadata-only identity refusal before id generation or repository access, used `NewFriendRelationshipRepository` through unit-of-work handoff, generated server-owned relationship ids, implemented send, incoming-only accept/reject, remove, block, unblock, list, and status behavior, computed actor-relative public status, mapped repository conflicts to redacted public errors, and kept protocol routes, Protobuf source, generated output, dependencies, migrations, startup wiring beyond service tests, event/audit tables, broad social features, distributed runtime, hosted/SDK/release scope, and direct compatibility deferred.

  - id: W-0240
    milestone: M-168
    title: Define friends relationship protocol route gate
    status: completed
    change: changes/2026-05-26-define-friends-relationship-protocol-route-gate
    depends_on:
      - W-0239
    expected_change_id: define-friends-relationship-protocol-route-gate
    tier: tier_2_functional
    selected_direction: define_friends_relationship_protocol_route_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0147
      - ADR-0146
      - ADR-0145
      - ADR-0144
      - ADR-0143
      - ADR-0142
      - ADR-0141
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Adding protocol route implementation, Protobuf messages, generated output, dependencies, migration changes, startup wiring, authentication/session behavior changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of defining only the friends relationship protocol route gate.
    completion:
      - Define the future friends relationship protected route family, route policy, request/response shape expectations, service handoff, public error mapping, generated output posture, verification expectations, and stop conditions.
      - Preserve protocol route implementation, Protobuf source, generated output, dependencies, event/audit tables, hosted, SDK, distributed runtime, and direct compatibility deferrals unless a later bounded work item explicitly authorizes them.
    decision: ADR-0148
    check_rule: runtime.friends_relationship_protocol_route_gate
    next_milestone: M-169
    next_ready_work_item: W-0241
    selected_follow_up_direction: implement_friends_relationship_protocol_route
    completion_summary: Defined `docs/friends-relationship-protocol-route-gate.md` and `docs/friends-relationship-protocol-route-gate.zh-CN.md`, added `ADR-0148`, registered `runtime.friends_relationship_protocol_route_gate`, recorded future protected routes `friends.SendFriendRequest`, `friends.AcceptFriendRequest`, `friends.RejectFriendRequest`, `friends.RemoveFriend`, `friends.BlockPlayer`, `friends.UnblockPlayer`, `friends.ListFriendRelationships`, and `friends.GetFriendRelationshipStatus`, candidate `vibit.friends.v1` Protobuf shapes, request-token protection, authenticated wrapper handoff, validated request identity as actor proof, metadata-only identity refusal, no client-supplied actor id proof, public `FRIENDSHIP_*` error mapping, generated-output expectations, Nakama/Pitaya reference mapping, required future tests, and stop conditions. No route implementation, Protobuf source/generated output, dependencies, migrations, startup wiring, authentication/session changes, event/audit tables, broad social features, distributed runtime, hosted/SDK/release scope, or direct compatibility were added.

  - id: W-0241
    milestone: M-169
    title: Implement friends relationship protocol route
    status: completed
    change: changes/2026-05-26-implement-friends-relationship-protocol-route
    depends_on:
      - W-0240
    expected_change_id: implement-friends-relationship-protocol-route
    tier: tier_2_protocol
    selected_direction: implement_friends_relationship_protocol_route
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0149
      - ADR-0148
      - ADR-0147
      - ADR-0146
      - ADR-0145
      - ADR-0144
      - ADR-0143
      - ADR-0142
      - ADR-0141
      - ADR-0140
      - ADR-0139
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0082
      - ADR-0078
    ask_first:
      - Repository interface changes, PostgreSQL adapter changes, migration changes, dependencies, automatic startup migration behavior, authentication/session behavior changes, route-protection semantic changes, delivery guarantees, stream subscriptions, chat rooms, groups, parties, broadcast fanout, matchmaking, match runtime, operations/admin behavior, SDK publication, generated client libraries, Pitaya-style distributed architecture, hosted deployments, release artifacts, public announcements, paid promotion, event/audit tables, or direct Nakama/Pitaya API compatibility instead of implementing only the friends relationship protocol route family authorized by ADR-0148.
    completion:
      - Implement the friends relationship protocol route family authorized by `ADR-0148`.
      - Add Protobuf source/generated output, protocol bridge mapping, route registration, and focused tests for the eight protected friends relationship routes.
      - Preserve application-owned service behavior, metadata-only identity refusal, protected-route policy, redacted errors, and repository/adapter/migration/dependency/authentication/session/event/direct-compatibility deferrals.
    check_rule: runtime.friends_relationship_protocol_route_implementation
    next_milestone: M-170
    next_ready_work_item: W-0242
    selected_follow_up_direction: prove_friends_relationship_protocol_route_local_flow
    completion_summary: Implemented `vibit.friends.v1` Protobuf source/generated output, friends route keys, protocol bridge mapping, bootstrap handlers, PostgreSQL startup registration, command transaction bypass, and focused tests for all eight protected friends relationship routes. Preserved application-owned service behavior, validated request identity actor proof, metadata-only identity refusal, redacted public errors, and repository/adapter/migration/dependency/authentication/session/event/direct-compatibility deferrals.

  - id: W-0242
    milestone: M-170
    title: Prove friends relationship protocol route in local alpha request flow
    status: completed
    change: changes/2026-05-26-prove-friends-relationship-protocol-route-local-flow
    depends_on:
      - W-0241
    expected_change_id: prove-friends-relationship-protocol-route-local-flow
    tier: tier_2_protocol
    selected_direction: prove_friends_relationship_protocol_route_local_flow
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0149
      - ADR-0148
      - ADR-0147
      - ADR-0145
      - ADR-0128
      - ADR-0078
    ask_first:
      - New protocol messages or routes, generated output changes, friends service behavior changes, repository interface changes, PostgreSQL adapter changes, migration changes, dependencies, authentication/session behavior changes, groups, parties, chat, stream subscriptions, event/audit tables, matchmaking, match runtime, SDK publication, hosted deployments, release artifacts, public announcements, paid promotion, distributed runtime, or direct Nakama/Pitaya API compatibility instead of proving only the completed friends relationship route family through a local alpha request flow.
    completion:
      - Prove the completed friends relationship protocol route through the existing protected WebSocket/Protobuf request flow.
      - Keep proof support local and test-focused.
      - Preserve protocol shape, generated output, service behavior, repository/adapter/migration/dependency/authentication/session/event/direct-compatibility deferrals.
    decision: ADR-0150
    check_rule: runtime.friends_relationship_protocol_route_local_proof
    next_milestone: M-171
    next_ready_work_item: W-0243
    selected_follow_up_direction: select_next_nakama_prototype_ready_capability_after_friends_relationship_route_proof
    completion_summary: Added `TestFriendsRelationshipProtocolRouteLocalAlphaFlow` to the existing authenticated local alpha Protobuf E2E fixture, registered the completed friends route family in that proof path, added a test-only friends relationship repository and multi-player test ID/material support, exercised send/status/accept/list/remove/block/unblock/resend/reject through authenticated WebSocket/Protobuf frame handling, updated source-first local alpha example scripts and docs, accepted `ADR-0150`, and registered `runtime.friends_relationship_protocol_route_local_proof`. Preserved protocol shape, generated output, application service behavior, repository interfaces, PostgreSQL adapter, migrations, dependencies, authentication/session behavior, event/audit tables, broad social features, hosted/SDK/release scope, distributed runtime, and direct compatibility deferrals.

  - id: W-0243
    milestone: M-171
    title: Select next Nakama prototype-ready capability after friends relationship route proof
    status: completed
    change: changes/2026-05-26-select-next-nakama-prototype-ready-capability-after-friends-route-proof
    depends_on:
      - W-0242
    expected_change_id: select-next-nakama-prototype-ready-capability-after-friends-route-proof
    tier: tier_2_direction_selection
    selected_direction: define_minimum_operations_inspection_surface_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0150
      - ADR-0151
      - ADR-0149
      - ADR-0148
      - ADR-0147
      - ADR-0138
      - ADR-0128
      - ADR-0127
      - ADR-0078
    ask_first:
      - Implementing runtime behavior, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, groups, parties, chat, stream subscriptions, event/audit tables, matchmaking, match runtime, SDK publication, hosted deployments, release artifacts, public announcements, paid promotion, distributed runtime, or direct Nakama/Pitaya API compatibility instead of selecting only the next bounded prototype-ready capability direction.
    completion:
      - Select and record the next bounded Nakama prototype-ready capability after the friends relationship route local proof.
      - Keep the selection slice documentation-only and checkable.
      - Preserve implementation/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed/direct-compatibility deferrals.
    decision: ADR-0151
    check_rule: runtime.next_nakama_prototype_ready_capability_after_friends_route_proof
    next_milestone: M-172
    next_ready_work_item: W-0244
    selected_follow_up_direction: define_minimum_operations_inspection_surface_gate
    selected_nakama_capability_family: admin_console_metrics_observability_and_operations
    completion_summary: Selected `define_minimum_operations_inspection_surface_gate` as the next bounded Nakama-first prototype-ready capability after friends relationship route local proof, accepted `ADR-0151`, registered `runtime.next_nakama_prototype_ready_capability_after_friends_route_proof`, and opened `M-172/W-0244 Define minimum operations inspection surface gate`. Preserved runtime behavior, operations/admin implementation, protocol routes, generated output, repository/adapter/migration/dependency/authentication/session/event/direct-compatibility deferrals, hosted/SDK/release scope, and distributed runtime deferrals.

  - id: W-0244
    milestone: M-172
    title: Define minimum operations inspection surface gate
    status: completed
    change: changes/2026-05-26-define-minimum-operations-inspection-surface-gate
    depends_on:
      - W-0243
    expected_change_id: define-minimum-operations-inspection-surface-gate
    tier: tier_2_operations_gate
    selected_direction: define_minimum_operations_inspection_surface_gate
    gate_density_strategy: ADR-0082
    related_decisions:
      - ADR-0151
      - ADR-0150
      - ADR-0128
      - ADR-0127
      - ADR-0105
      - ADR-0078
    ask_first:
      - Implementing operations/admin endpoint behavior, metrics endpoints, observability pipelines, dashboards, runtime behavior, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, groups, parties, chat, stream subscriptions, event/audit tables, matchmaking, match runtime, SDK publication, hosted deployments, release artifacts, public announcements, paid promotion, distributed runtime, or direct Nakama/Pitaya API compatibility instead of defining only the minimum operations inspection surface gate.
    completion:
      - Define the minimum source-first operations inspection surface gate.
      - Record inspectable state categories, redaction posture, ownership, tests, and stop conditions.
      - Preserve implementation/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed/direct-compatibility deferrals.
    decision: ADR-0152
    check_rule: runtime.minimum_operations_inspection_surface_gate
    next_milestone: M-173
    next_ready_work_item: W-0245
    selected_follow_up_direction: implement_minimum_operations_inspection_source_first_surface
    selected_nakama_capability_family: admin_console_metrics_observability_and_operations
    completion_summary: Defined `source_first_local_operations_inspection` as the minimum operations inspection gate, accepted `ADR-0152`, registered `runtime.minimum_operations_inspection_surface_gate`, and opened `M-173/W-0245 Implement minimum operations inspection source-first surface`. Preserved operations/admin endpoint implementation, metrics endpoints, observability pipelines, dashboards, runtime behavior, protocol routes, generated output, repository/adapter/migration/dependency/authentication/session/event/direct-compatibility deferrals, hosted/SDK/release scope, and distributed runtime deferrals.

  - id: W-0245
    milestone: M-173
    title: Implement minimum operations inspection source-first surface
    status: completed
    change: changes/2026-05-26-implement-minimum-operations-inspection-source-first-surface
    depends_on:
      - W-0244
    expected_change_id: implement-minimum-operations-inspection-source-first-surface
    tier: tier_2_operations_implementation
    selected_direction: implement_minimum_operations_inspection_source_first_surface
    gate_decision: ADR-0152
    related_decisions:
      - ADR-0152
      - ADR-0151
      - ADR-0095
      - ADR-0078
    ask_first:
      - Implementing operations/admin endpoint behavior, metrics endpoints, observability pipelines, dashboards, runtime behavior, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, groups, parties, chat, stream subscriptions, event/audit tables, matchmaking, match runtime, SDK publication, hosted deployments, release artifacts, public announcements, paid promotion, distributed runtime, or direct Nakama/Pitaya API compatibility instead of implementing only the source-first operations inspection surface authorized by ADR-0152.
    completion:
      - Implement a source-first operations inspection surface for accepted local alpha categories.
      - Prove redaction for credentials, tokens, digests, verifier keys, DSNs, transport metadata, sensitive identifiers, and database payloads.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed/direct-compatibility deferrals.
    decision: ADR-0153
    check_rule: runtime.minimum_operations_inspection_source_first_surface_implementation
    next_milestone: M-174
    next_ready_work_item: W-0246
    selected_follow_up_direction: define_pitaya_aligned_distributed_runtime_vocabulary_reactivation_gate
    completion_summary: Implemented `node tools/vibit inspect operations --json` as a source-first repository inspection command for accepted local alpha operations categories, redaction posture, existing route families, local alpha flow, verification posture, and Pitaya deferred architecture mapping. Preserved operations/admin endpoint behavior, metrics endpoints, observability pipelines, dashboards, runtime behavior, protocol routes, Protobuf source, generated output, repository/adapter/migration/dependency/authentication/session/event/direct-compatibility deferrals, hosted/SDK/release scope, and distributed runtime implementation deferrals.

  - id: W-0246
    milestone: M-174
    title: Define Pitaya-aligned distributed runtime vocabulary reactivation gate
    status: completed
    change: changes/2026-05-26-define-pitaya-aligned-distributed-runtime-vocabulary-reactivation-gate
    depends_on:
      - W-0245
    expected_change_id: define-pitaya-aligned-distributed-runtime-vocabulary-reactivation-gate
    tier: tier_2_architecture_gate
    selected_direction: define_pitaya_aligned_distributed_runtime_vocabulary_reactivation_gate
    gate_decision: ADR-0153
    related_decisions:
      - ADR-0153
      - ADR-0152
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing distributed runtime behavior, frontend/backend server roles, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of defining only a Pitaya-aligned distributed runtime vocabulary reactivation gate.
    completion:
      - Define a Pitaya-aligned vocabulary gate for future distributed runtime planning.
      - Map current single-process acceptor, session binding, route handling, and group/broadcast deferrals to future architecture vocabulary.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0154
    check_rule: runtime.pitaya_aligned_distributed_runtime_vocabulary_reactivation_gate
    next_milestone: M-175
    next_ready_work_item: W-0247
    selected_follow_up_direction: implement_pitaya_aligned_distributed_runtime_vocabulary_source_first_map
    completion_summary: Defined the Pitaya-aligned distributed runtime vocabulary reactivation gate, accepted `ADR-0154`, and recorded allowed vocabulary for acceptors, frontend/backend roles, route handlers, session binding, server-to-server RPC, remote calls, service discovery, distributed groups, room broadcast, and cluster-safe session routing. Preserved distributed runtime implementation, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0247
    milestone: M-175
    title: Implement Pitaya-aligned distributed runtime vocabulary source-first map
    status: completed
    change: changes/2026-05-26-implement-pitaya-aligned-distributed-runtime-vocabulary-source-first-map
    depends_on:
      - W-0246
    expected_change_id: implement-pitaya-aligned-distributed-runtime-vocabulary-source-first-map
    tier: tier_2_architecture_implementation
    selected_direction: implement_pitaya_aligned_distributed_runtime_vocabulary_source_first_map
    gate_decision: ADR-0154
    related_decisions:
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing distributed runtime behavior, frontend/backend server roles, server-to-server RPC, service discovery, distributed groups, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of implementing only a source-first Pitaya-aligned distributed runtime vocabulary map.
    completion:
      - Implement a source-first repository inspection map for Pitaya-aligned vocabulary.
      - Summarize current single-process mapping and future distributed deferrals.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0155
    check_rule: runtime.pitaya_aligned_distributed_runtime_vocabulary_source_first_map
    next_milestone: M-176
    next_ready_work_item: W-0248
    selected_follow_up_direction: define_pitaya_aligned_frontend_backend_role_boundary_gate
    completion_summary: Implemented `node tools/vibit inspect pitaya-vocabulary --json` as a source-first repository inspection map for Pitaya-aligned distributed runtime vocabulary, current single-process mapping, source surfaces, deferrals, and redaction flags. Preserved distributed runtime implementation, frontend/backend role implementation, server-to-server RPC, remote calls, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0248
    milestone: M-176
    title: Define Pitaya-aligned frontend/backend role boundary gate
    status: completed
    change: changes/2026-05-26-define-pitaya-aligned-frontend-backend-role-boundary-gate
    depends_on:
      - W-0247
    expected_change_id: define-pitaya-aligned-frontend-backend-role-boundary-gate
    tier: tier_2_architecture_gate
    selected_direction: define_pitaya_aligned_frontend_backend_role_boundary_gate
    gate_decision: ADR-0155
    related_decisions:
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing frontend/backend server roles, distributed runtime behavior, server-to-server RPC, remote calls, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of defining only a frontend/backend role boundary gate.
    completion:
      - Define a Pitaya-aligned frontend/backend role boundary gate.
      - Map current single-process acceptor, protocol bridge, application dispatch, and module handler responsibilities to deferred role vocabulary.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0156
    check_rule: runtime.pitaya_aligned_frontend_backend_role_boundary_gate
    next_milestone: M-177
    next_ready_work_item: W-0249
    selected_follow_up_direction: implement_pitaya_aligned_frontend_backend_role_source_first_map
    completion_summary: Defined a gate-only Pitaya-aligned frontend/backend role boundary for `frontend_server` and `backend_server`, mapped current single-process acceptor, session binding, protocol bridge, application dispatch, and module handler responsibilities to deferred role vocabulary, and preserved frontend/backend role implementation, distributed runtime implementation, RPC, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0249
    milestone: M-177
    title: Implement Pitaya-aligned frontend/backend role source-first map
    status: completed
    change: changes/2026-05-26-implement-pitaya-aligned-frontend-backend-role-source-first-map
    depends_on:
      - W-0248
    expected_change_id: implement-pitaya-aligned-frontend-backend-role-source-first-map
    tier: tier_2_architecture_implementation
    selected_direction: implement_pitaya_aligned_frontend_backend_role_source_first_map
    gate_decision: ADR-0156
    related_decisions:
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing frontend/backend server roles, distributed runtime behavior, server-to-server RPC, remote calls, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of implementing only a source-first frontend/backend role map.
    completion:
      - Implement a source-first repository inspection map for frontend/backend role vocabulary.
      - Summarize current single-process role mapping and future role deferrals.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0157
    check_rule: runtime.pitaya_aligned_frontend_backend_role_source_first_map
    next_milestone: M-178
    next_ready_work_item: W-0250
    selected_follow_up_direction: define_pitaya_aligned_server_to_server_rpc_boundary_gate
    completion_summary: Implemented `node tools/vibit inspect pitaya-roles --json` as a source-first repository inspection map for Pitaya-aligned frontend/backend role vocabulary, current single-process role mapping, source surfaces, deferrals, and redaction flags. Preserved frontend/backend role implementation, distributed runtime implementation, server-to-server RPC, remote calls, service discovery, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0250
    milestone: M-178
    title: Define Pitaya-aligned server-to-server RPC boundary gate
    status: completed
    change: changes/2026-05-26-define-pitaya-aligned-server-to-server-rpc-boundary-gate
    depends_on:
      - W-0249
    expected_change_id: define-pitaya-aligned-server-to-server-rpc-boundary-gate
    tier: tier_2_architecture_gate
    selected_direction: define_pitaya_aligned_server_to_server_rpc_boundary_gate
    gate_decision: ADR-0157
    related_decisions:
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing server-to-server RPC, remote call behavior, service discovery, frontend/backend server roles, distributed runtime behavior, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of defining only a server-to-server RPC boundary gate.
    completion:
      - Define a Pitaya-aligned server-to-server RPC boundary gate.
      - Map current vibit application dispatch and module handler boundaries to deferred RPC/remote-call vocabulary.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0158
    check_rule: runtime.pitaya_aligned_server_to_server_rpc_boundary_gate
    next_milestone: M-179
    next_ready_work_item: W-0251
    selected_follow_up_direction: implement_pitaya_aligned_server_to_server_rpc_source_first_map
    completion_summary: Defined a gate-only Pitaya-aligned server-to-server RPC boundary for `server_to_server_rpc` and `remote_call`, mapped current single-process application dispatch, protocol bridge, route handler, module handler, and absent service-discovery responsibilities to deferred RPC vocabulary, and preserved RPC implementation, remote calls, service discovery, frontend/backend role implementation, distributed runtime implementation, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0251
    milestone: M-179
    title: Implement Pitaya-aligned server-to-server RPC source-first map
    status: completed
    change: changes/2026-05-26-implement-pitaya-aligned-server-to-server-rpc-source-first-map
    depends_on:
      - W-0250
    expected_change_id: implement-pitaya-aligned-server-to-server-rpc-source-first-map
    tier: tier_2_architecture_implementation
    selected_direction: implement_pitaya_aligned_server_to_server_rpc_source_first_map
    gate_decision: ADR-0158
    related_decisions:
      - ADR-0158
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing server-to-server RPC, remote call behavior, service discovery, frontend/backend server roles, distributed runtime behavior, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of implementing only a source-first server-to-server RPC map.
    completion:
      - Implement a source-first repository inspection map for server-to-server RPC and remote-call vocabulary.
      - Summarize current single-process dispatch, route handler, module handler, and service discovery deferrals.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0159
    check_rule: runtime.pitaya_aligned_server_to_server_rpc_source_first_map
    next_milestone: M-180
    next_ready_work_item: W-0252
    selected_follow_up_direction: define_pitaya_aligned_service_discovery_boundary_gate
    completion_summary: Implemented `node tools/vibit inspect pitaya-rpc --json` as a source-first repository inspection map for Pitaya-aligned server-to-server RPC vocabulary, current single-process RPC mapping, source surfaces, deferrals, and redaction flags. Preserved RPC implementation, remote calls, service discovery, frontend/backend role implementation, distributed runtime implementation, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0252
    milestone: M-180
    title: Define Pitaya-aligned service discovery boundary gate
    status: completed
    change: changes/2026-05-26-define-pitaya-aligned-service-discovery-boundary-gate
    depends_on:
      - W-0251
    expected_change_id: define-pitaya-aligned-service-discovery-boundary-gate
    tier: tier_2_architecture_gate
    selected_direction: define_pitaya_aligned_service_discovery_boundary_gate
    gate_decision: ADR-0159
    related_decisions:
      - ADR-0159
      - ADR-0158
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing service discovery, server-to-server RPC, remote call behavior, frontend/backend server roles, distributed runtime behavior, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of defining only a service discovery boundary gate.
    completion:
      - Define a Pitaya-aligned service discovery boundary gate.
      - Map current static single-process composition to deferred service discovery vocabulary.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0160
    check_rule: runtime.pitaya_aligned_service_discovery_boundary_gate
    next_milestone: M-181
    next_ready_work_item: W-0253
    selected_follow_up_direction: implement_pitaya_aligned_service_discovery_source_first_map
    completion_summary: Defined a gate-only Pitaya-aligned service discovery boundary for `service_discovery`, `service_registry`, `service_instance`, and `service_selector`, mapped current static startup composition, direct dispatch, route handlers, module handlers, and absent registry/selector behavior to deferred service discovery vocabulary, and preserved service discovery implementation, registries, selectors, node identity, RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0253
    milestone: M-181
    title: Implement Pitaya-aligned service discovery source-first map
    status: completed
    change: changes/2026-05-26-implement-pitaya-aligned-service-discovery-source-first-map
    depends_on:
      - W-0252
    expected_change_id: implement-pitaya-aligned-service-discovery-source-first-map
    tier: tier_2_architecture_implementation
    selected_direction: implement_pitaya_aligned_service_discovery_source_first_map
    gate_decision: ADR-0160
    related_decisions:
      - ADR-0160
      - ADR-0159
      - ADR-0158
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing service discovery, service registries, service selectors, node identity, server-to-server RPC, remote call behavior, frontend/backend server roles, distributed runtime behavior, distributed groups, room broadcast fanout, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of implementing only a source-first service discovery map.
    completion:
      - Implement a source-first repository inspection map for service discovery vocabulary.
      - Summarize current static single-process composition, direct dispatch, route handler, module handler, and registry/selector deferrals.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0161
    check_rule: runtime.pitaya_aligned_service_discovery_source_first_map
    next_milestone: M-182
    next_ready_work_item: W-0254
    selected_follow_up_direction: define_pitaya_aligned_distributed_group_broadcast_boundary_gate
    completion_summary: Implemented `node tools/vibit inspect pitaya-discovery --json` as a source-first repository inspection map for Pitaya-aligned service discovery vocabulary, current static single-process composition, direct dispatch, route-handler and module-handler mapping, registry/selector deferrals, source surfaces, deferrals, and redaction flags. Preserved service discovery implementation, registries, selectors, node identity, RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0254
    milestone: M-182
    title: Define Pitaya-aligned distributed group and broadcast boundary gate
    status: completed
    change: changes/2026-05-26-define-pitaya-aligned-distributed-group-broadcast-boundary-gate
    depends_on:
      - W-0253
    expected_change_id: define-pitaya-aligned-distributed-group-broadcast-boundary-gate
    tier: tier_2_architecture_gate
    selected_direction: define_pitaya_aligned_distributed_group_broadcast_boundary_gate
    gate_decision: ADR-0161
    related_decisions:
      - ADR-0161
      - ADR-0160
      - ADR-0159
      - ADR-0158
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing distributed groups, room broadcast fanout, delivery guarantees, stream subscriptions, groups, parties, chat, service discovery, service registries, service selectors, node identity, server-to-server RPC, remote call behavior, frontend/backend server roles, distributed runtime behavior, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of defining only a distributed group and broadcast boundary gate.
    completion:
      - Define a Pitaya-aligned distributed group and room broadcast boundary gate.
      - Map current single-process target scope and server-push intent surfaces to deferred distributed group and broadcast vocabulary.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0162
    check_rule: runtime.pitaya_aligned_distributed_group_broadcast_boundary_gate
    next_milestone: M-183
    next_ready_work_item: W-0255
    selected_follow_up_direction: implement_pitaya_aligned_distributed_group_broadcast_source_first_map
    completion_summary: Defined a gate-only Pitaya-aligned distributed group and broadcast boundary for `distributed_group`, `room_broadcast`, `broadcast_target`, `group_membership`, and `broadcast_fanout`, mapped current target-scope metadata, server-push intent, absent distributed group state, absent group membership registry, and absent room broadcast fanout to deferred vocabulary, and preserved distributed group implementation, room broadcast fanout, delivery guarantees, stream subscriptions, service discovery implementation, RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0255
    milestone: M-183
    title: Implement Pitaya-aligned distributed group and broadcast source-first map
    status: completed
    change: changes/2026-05-26-implement-pitaya-aligned-distributed-group-broadcast-source-first-map
    depends_on:
      - W-0254
    expected_change_id: implement-pitaya-aligned-distributed-group-broadcast-source-first-map
    tier: tier_2_architecture_implementation
    selected_direction: implement_pitaya_aligned_distributed_group_broadcast_source_first_map
    gate_decision: ADR-0162
    related_decisions:
      - ADR-0162
      - ADR-0161
      - ADR-0160
      - ADR-0159
      - ADR-0158
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing distributed groups, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, groups, parties, chat rooms, matchmaking, match runtime, service discovery, service registries, service selectors, node identity, server-to-server RPC, remote call behavior, frontend/backend server roles, distributed runtime behavior, cluster-safe session routing, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of implementing only a source-first distributed group and broadcast map.
    completion:
      - Implement a source-first repository inspection map for distributed group and broadcast vocabulary.
      - Summarize current target-scope metadata, server-push intent, single-process delivery, and group/broadcast deferrals.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0163
    check_rule: runtime.pitaya_aligned_distributed_group_broadcast_source_first_map
    next_milestone: M-184
    next_ready_work_item: W-0256
    selected_follow_up_direction: define_pitaya_aligned_cluster_safe_session_routing_boundary_gate
    completion_summary: Implemented `node tools/vibit inspect pitaya-groups --json` as a source-first repository inspection map for Pitaya-aligned distributed group and broadcast vocabulary, current target-scope metadata, server-push intent, single-process delivery, absent distributed group state, absent group membership registry, absent room broadcast fanout, source surfaces, deferrals, and redaction flags. Preserved distributed group implementation, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, service discovery implementation, RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, cluster-safe session routing, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0256
    milestone: M-184
    title: Define Pitaya-aligned cluster-safe session routing boundary gate
    status: completed
    change: changes/2026-05-26-define-pitaya-aligned-cluster-safe-session-routing-boundary-gate
    depends_on:
      - W-0255
    expected_change_id: define-pitaya-aligned-cluster-safe-session-routing-boundary-gate
    tier: tier_2_architecture_gate
    selected_direction: define_pitaya_aligned_cluster_safe_session_routing_boundary_gate
    gate_decision: ADR-0163
    related_decisions:
      - ADR-0163
      - ADR-0162
      - ADR-0161
      - ADR-0160
      - ADR-0159
      - ADR-0158
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing cluster-safe session routing, distributed runtime behavior, distributed groups, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, service discovery, service registries, service selectors, node identity, server-to-server RPC, remote call behavior, frontend/backend server roles, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of defining only a cluster-safe session routing boundary gate.
    completion:
      - Define a Pitaya-aligned cluster-safe session routing boundary gate.
      - Map current single-process connection/session binding surfaces to deferred cluster-safe routing vocabulary.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0164
    check_rule: runtime.pitaya_aligned_cluster_safe_session_routing_boundary_gate
    next_milestone: M-185
    next_ready_work_item: W-0257
    selected_follow_up_direction: implement_pitaya_aligned_cluster_safe_session_routing_source_first_map
    completion_summary: Defined a gate-only Pitaya-aligned cluster-safe session routing boundary for `cluster_safe_session_routing`, `session_location`, `connection_owner_node`, `routing_epoch`, `session_route_target`, `connection_handoff`, and `reconnect_route`, mapped current single-process connection binding, active connection registry, runtime session validation, and request identity surfaces to deferred vocabulary, and preserved cluster-safe session routing behavior, session location registries, connection owner node registries, routing epoch behavior, remote handoff, reconnect routing, distributed runtime implementation, service discovery implementation, RPC implementation, remote calls, frontend/backend role implementation, distributed groups, room broadcast fanout, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0257
    milestone: M-185
    title: Implement Pitaya-aligned cluster-safe session routing source-first map
    status: completed
    change: changes/2026-05-26-implement-pitaya-aligned-cluster-safe-session-routing-source-first-map
    depends_on:
      - W-0256
    expected_change_id: implement-pitaya-aligned-cluster-safe-session-routing-source-first-map
    tier: tier_2_architecture_implementation
    selected_direction: implement_pitaya_aligned_cluster_safe_session_routing_source_first_map
    gate_decision: ADR-0164
    related_decisions:
      - ADR-0164
      - ADR-0163
      - ADR-0162
      - ADR-0161
      - ADR-0160
      - ADR-0159
      - ADR-0158
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing cluster-safe session routing behavior, session location registries, connection owner node registries, routing epoch behavior, remote connection handoff, reconnect routing, distributed session routing, distributed runtime behavior, distributed groups, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, service discovery, service registries, service selectors, node identity, server-to-server RPC, remote call behavior, frontend/backend server roles, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of implementing only a source-first cluster-safe session routing map.
    completion:
      - Implement a source-first repository inspection map for cluster-safe session routing vocabulary.
      - Summarize current connection id, connection epoch, first-message binding, active connection registry, runtime session validation, bound identity, and request identity mappings.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
    decision: ADR-0165
    check_rule: runtime.pitaya_aligned_cluster_safe_session_routing_source_first_map
    next_milestone: M-186
    next_ready_work_item: W-0258
    selected_follow_up_direction: select_next_pitaya_aligned_direction_after_cluster_safe_session_routing_map
    completion_summary: Implemented `node tools/vibit inspect pitaya-sessions --json` as a source-first repository inspection map for Pitaya-aligned cluster-safe session routing vocabulary, current connection/session mapping, source surfaces, deferrals, and redaction flags. Preserved cluster-safe session routing behavior, session location registries, connection owner node registries, routing epoch behavior, remote handoff, reconnect routing, distributed session routing, service discovery implementation, RPC implementation, remote calls, frontend/backend role implementation, distributed runtime implementation, protocol shape, generated output, persistence, dependencies, hosted surfaces, SDKs, and direct compatibility deferrals.

  - id: W-0258
    milestone: M-186
    title: Select next Pitaya-aligned direction after cluster-safe session routing map
    status: next_ready
    change: changes/2026-05-26-select-next-pitaya-aligned-direction-after-cluster-safe-session-routing-map
    depends_on:
      - W-0257
    expected_change_id: select-next-pitaya-aligned-direction-after-cluster-safe-session-routing-map
    tier: tier_2_direction_selection
    selected_direction: select_next_pitaya_aligned_direction_after_cluster_safe_session_routing_map
    gate_decision: ADR-0165
    related_decisions:
      - ADR-0165
      - ADR-0164
      - ADR-0163
      - ADR-0162
      - ADR-0161
      - ADR-0160
      - ADR-0159
      - ADR-0158
      - ADR-0157
      - ADR-0156
      - ADR-0155
      - ADR-0154
      - ADR-0153
      - ADR-0127
      - ADR-0078
      - ADR-0019
    ask_first:
      - Implementing cluster-safe session routing behavior, session location registries, connection owner node registries, routing epoch behavior, remote connection handoff, reconnect routing, distributed session routing, distributed runtime behavior, distributed groups, room broadcast fanout, delivery guarantees, stream subscriptions, group membership registries, service discovery, service registries, service selectors, node identity, server-to-server RPC, remote call behavior, frontend/backend server roles, runtime endpoint behavior, metrics endpoints, observability pipelines, dashboards, protocol messages or routes, generated output, repository interfaces, PostgreSQL adapters, migrations, dependencies, authentication/session behavior changes, SDK publication, hosted deployments, release artifacts, or direct Nakama/Pitaya API compatibility instead of selecting only the next Pitaya-aligned direction.
    completion:
      - Select the next Pitaya-aligned direction after the cluster-safe session routing source-first map.
      - Record candidate directions and why implementation remains deferred.
      - Preserve runtime/protocol/generated/persistence/dependency/authentication/session/hosted/SDK/distributed implementation/direct-compatibility deferrals.
