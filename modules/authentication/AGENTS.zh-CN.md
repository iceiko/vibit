# authentication Module Agent Guide 中文版

状态：Draft v0.1
说明：本文件是 `modules/authentication/AGENTS.md` 的简体中文译本。英文版本是权威版本。

## 何时使用本模块

当需求定义第一版已选姿态的 storage-neutral authentication persistence boundaries 时，使用本模块：

- `device_credential_login`
- opaque high-entropy access tokens
- PostgreSQL-backed credential and token verifier records

当前 module-owned runtime boundary 是：

```text
runtime/internal/modules/authentication/repository.go
```

它可以定义 future persistence adapters 所需的 credential record structs、token verifier record structs、mutation/query shapes、repository interfaces 和 validation helpers。

## 何时不要使用本模块

不要用本模块实现：

- Runtime login handlers。
- Token generation、parsing、issuance、validation、refresh 或 bearer-token acceptance。
- 超出 storage-neutral revocation mutation shapes 的 logout behavior。
- 超出 storage-neutral cleanup query shapes 的 cleanup jobs。
- 超出单独授权的 `M-015` platform boundary 的 PostgreSQL adapters。
- WebSocket routes、proof carriers 或 handshake authentication。
- Protobuf messages，或 generated authentication shape paths 下的 handwritten authentication behavior。
- Password hashing、JWT、OAuth、OIDC、provider SDKs、key-management、Redis-like token stores、S3 或 MinIO dependencies。
- Player account lifecycle storage。

如果需求涉及上述 surface，应先创建或更新单独 ratified change，再添加代码。

## Extension Points

- Authentication repository interface：`runtime/internal/modules/authentication/repository.go`。
- Authentication repository boundary tests：`runtime/internal/modules/authentication/repository_test.go`。
- Authentication PostgreSQL adapter source：`runtime/internal/platform/persistence/postgres/authentication_repository.go`。
- Authentication PostgreSQL adapter tests：`runtime/internal/platform/persistence/postgres/authentication_repository_test.go`。
- 已存在的 credential migration source：`runtime/migrations/postgres/000003_create_authentication_device_credentials.sql`。
- 已存在的 token verifier migration source：`runtime/migrations/postgres/000004_create_authentication_access_tokens.sql`。

Repository interface 是 storage-neutral domain code。它不得导入 PostgreSQL、WebSocket、Protobuf、OAuth、OIDC、JWT、password-hashing、Redis-like、S3 或 MinIO packages。

Repository interface 可以 normalize identifiers、digest byte slices、statuses、timestamps 和 storage mutation/query shapes。它不得创建 credential material、generate tokens、compare verifiers、parse bearer tokens、validate access tokens、open transactions、execute SQL、publish events 或调用 transport/protocol code。

已实现的 PostgreSQL adapter boundary 是 `runtime/internal/platform/persistence/postgres/authentication_repository.go`，focused tests 位于 `runtime/internal/platform/persistence/postgres/authentication_repository_test.go`。它使用 `NewAuthenticationRepositoryForUnitOfWork(executor)` 并实现 `authentication.Repository`；`UnitOfWork.NewAuthenticationRepository` 会从 caller-owned executor 创建它。`M-015` 只授权 platform-owned persistence adapter work；它仍不授权 runtime authentication behavior。

Runtime authentication implementation boundary planning 记录在 `docs/runtime-authentication-implementation-boundary.md` 和 `decisions/ADR-0036-runtime-authentication-implementation-boundary.md`。未来 runtime authentication 必须由 `runtime/internal/app` 下的 application boundary 拥有，必须通过 application unit-of-work boundary 使用本模块的 `authentication.Repository`，并在 domain dispatch 前把 validated proof 转换为 `RequestIdentity`。本模块不得吸收 token generation、verifier comparison、login execution、access-token validation、logout execution、cleanup jobs、Protobuf messages、WebSocket proof carriers、generated authentication shape paths 下的 handwritten logic 或 authentication dependencies。

`M-019 Token And Credential Verifier Algorithm Redaction Boundary` 已完成。`W-0091` 已在 `docs/token-credential-verifier-algorithm-redaction-boundary.md` 和 `ADR-0040` 中定义第一版 planned high-entropy verifier posture，且没有添加 service code 或 runtime authentication behavior。更早的 application authentication service boundary 仍记录在 `docs/application-authentication-service-interface-boundary.md` 和 `ADR-0039`。第一版 planned verifier algorithm family 是 `vibit_hmac_sha256_v1`；未来 first-posture code 在后续 code gate 授权后可以使用 Go standard library packages `crypto/hmac`、`crypto/sha256`、`crypto/subtle`、`crypto/rand` 和 `encoding/base64`。本模块的 `authentication.Repository` 只能存取 already-computed digest material；它不得计算 HMAC、生成 token 或 credential material、比较 verifiers、加载 secret keys 或决定 authentication outcomes。

`M-020 Secret Configuration And Verifier Key Loading Boundary` 已完成。`W-0092` 已在 `docs/secret-configuration-verifier-key-loading-boundary.md` 和 `ADR-0041` 中定义 future key loading posture，且没有添加 service code 或 runtime authentication behavior。Future verifier key loading 由 `runtime/internal/app` 下的 application 拥有；第一版 local implementation 可在后续 code gate 授权后使用 process environment configuration 或 explicit runtime secret input；external KMS 或 secret-manager integration 仍然需要后续 dependency 和 operations gates。必须使用四个 separated logical verifier keys，`verifier_key_id` 默认不是 log-safe，production key configuration 无效时必须 fail closed，committed production-like secret values 仍然禁止。本模块不得 load verifier keys、parse environment variables、choose secret managers、rotate keys、compute HMACs、generate token 或 credential material、compare verifiers，或决定 authentication outcomes。

`M-021 Token And Credential Material Generation Boundary` 已完成。`W-0093` 已在 `docs/token-credential-material-generation-boundary.md` 和 `ADR-0042` 中定义 future raw device credential 与 opaque access-token material generation posture，且没有添加 service code 或 runtime authentication behavior。Future material generation 由 `runtime/internal/app` 下的 application 拥有；第一版 device credential 与 access token 都是 server-issued 且 application-generated；raw material 必须是 32 cryptographically random bytes，至少 256 bits entropy；text presentation 使用 URL-safe unpadded Base64 或等价 encoding；raw material 只能 one-time client-visible，且不得存储。本模块的 `authentication.Repository` 只能存取 already-computed digest material；不得 generate raw token 或 credential material、encode raw material、接收 raw material 用于 storage、compute digests、compare verifiers，或决定 authentication outcomes。`runtime.token_credential_material_generation_boundary` 是该 boundary 的 repository check rule。

`M-022 Verifier Digest Computation And Comparison Boundary` 已完成。`W-0094` 已在 `docs/verifier-digest-computation-comparison-boundary.md` 和 `ADR-0043` 中定义 future verifier digest computation 与 constant-time comparison posture，且没有添加 service code 或 runtime authentication behavior。Future digest computation and comparison 由 `runtime/internal/app` 下的 application 拥有；lookup digest equality 只能选择 candidate record；verifier digest comparison 必须 constant-time；invalid lookup、mismatch、unknown key id、unsupported algorithm、malformed proof 以及 expired 或 revoked proof 必须收敛到同一个 public invalid-proof class。本模块的 `authentication.Repository` 只能存取 already-computed digest material；不得 compute HMACs、choose verifier key sets、compare verifier digests、disclose lookup misses，或决定 authentication outcomes。`runtime.verifier_digest_computation_comparison_boundary` 是该 boundary 的 repository check rule。

`M-023 Authentication Service Implementation Readiness Gate` 已完成。`W-0095` 已在 `docs/authentication-service-implementation-readiness-gate.md` 和 `ADR-0044` 中定义 readiness gate，且没有添加 service code 或 runtime authentication behavior。Future service implementation 仍由 `runtime/internal/app` 下的 application 拥有，package candidate 是 `runtime/internal/app/authentication`。本模块仍是 storage-neutral repository boundary。它不得吸收 service orchestration、secret loading、material generation、digest computation、verifier comparison、login execution、token validation、protocol behavior、WebSocket behavior 或 production authentication decisions。`runtime.authentication_service_implementation_readiness_gate` 是该 gate 的 repository check rule。

`M-024 Local Verifier Key Configuration Loading Gate` 已完成。`W-0096` 已在 `docs/local-verifier-key-configuration-loading-gate.md` 和 `ADR-0045` 中定义该 gate，且没有添加 service code 或 runtime authentication behavior。`W-0097` 已在 `runtime/internal/app/authentication` 下实现 explicit in-memory verifier key set validation，不属于本模块。本模块仍是 storage-neutral，只能存储 already-computed verifier metadata。它不得 load verifier keys、parse environment variables、decode key text、hold key material、validate key sets、rotate keys、compute digests、compare verifiers，或决定 authentication outcomes。`runtime.local_verifier_key_configuration_loading_gate` 是该 gate 的 repository check rule。

`M-026 Environment Verifier Key Loader Gate` 已完成。`W-0098` 已在 `docs/environment-verifier-key-loader-gate.md` 和 `ADR-0046` 中定义该 gate，且没有添加 service code 或 runtime authentication behavior。Future process environment verifier key loading 属于 `runtime/internal/app/authentication`，不属于本模块，并且必须在该 package 调用 `NewVerifierKeySet`。本模块仍是 storage-neutral，只能存储 already-computed verifier metadata。它不得 parse environment variables、decode key text、hold key material、validate key sets、读取 local secret files、parse `.env` files、rotate keys、compute digests、compare verifiers，或决定 authentication outcomes。`runtime.environment_verifier_key_loader_gate` 是该 gate 的 repository check rule。

`M-027 Environment Verifier Key Loader Implementation` 已完成。`W-0099` 已在 `runtime/internal/app/authentication` 下实现 process environment verifier key loading，而不是在本模块中实现。本 authentication module 仍是 storage-neutral，不得 parse environment variables、decode key text、hold key material、validate key sets、读取 local secret files、parse `.env` files、rotate keys、compute digests、compare verifiers、wire startup 或决定 authentication outcomes。

`M-028 Token And Credential Material Generation Implementation Gate` 已完成。`W-0100` 已在 `docs/token-credential-material-generation-implementation-gate.md` 和 `ADR-0047` 中定义该 gate，且没有添加 service code 或 runtime authentication behavior。Future token and credential material helpers 属于 `runtime/internal/app/authentication`，不属于本模块。本模块仍是 storage-neutral，只能存储 already-computed digest material。它不得 generate raw token 或 credential material、encode raw material、hold raw material、accept raw material for storage、compute digests、compare verifiers、wire startup 或决定 authentication outcomes。`runtime.token_credential_material_generation_implementation_gate` 是该 gate 的 repository check rule。

`M-029 Token And Credential Material Generation Helper Implementation` 已完成。`W-0101` 已在 `runtime/internal/app/authentication` 下实现 token and credential material helpers，而不是在本模块中实现。本模块仍是 storage-neutral，只能存储 already-computed digest material。它不得 generate raw token 或 credential material、encode raw material、hold raw material、accept raw material for storage、compute digests、compare verifiers、wire startup 或决定 authentication outcomes。

`M-031 Verifier Digest Computation Helper Implementation` 已完成。`W-0103` 已在 `runtime/internal/app/authentication` 下实现 lookup and verifier digest computation helpers，而不是在本模块中实现。本模块仍是 storage-neutral，只能存储 already-computed digest material。它不得 compute HMACs、choose verifier key sets、compare verifier digests、generate raw token 或 credential material、hold raw material、wire startup 或决定 authentication outcomes。

`M-032 Verifier Digest Comparison Helper Gate` 已完成。`W-0104` 已在 `docs/verifier-digest-comparison-helper-gate.md` 和 `ADR-0049` 中定义 verifier digest comparison helper gate，且没有添加 service code 或 runtime authentication behavior。Future comparison helpers 属于 `runtime/internal/app/authentication`，不属于本模块，并应使用 `verifier_comparison.go` 和 `verifier_comparison_test.go`。本模块仍是 storage-neutral，只能存储 already-computed digest material。它不得 compare verifier digests、compare raw material、把 lookup digests comparison 当作 authentication proof、把 database-only equality 当作最终 proof、暴露 lookup misses、从 repository code 调用 comparison helpers，或决定 authentication outcomes。`runtime.verifier_digest_comparison_helper_gate` 是该 gate 的 repository check rule。

`M-033 Verifier Digest Comparison Helper Implementation` 已完成。`W-0105` 已在 `runtime/internal/app/authentication` 下实现 verifier comparison helpers，而不是在本模块中实现。本模块仍是 storage-neutral，只能存储 already-computed digest material。它不得 compare verifier digests、从 repository code 调用 comparison helpers、决定 mismatch/public failure mapping、执行 login、validate tokens、wire startup 或决定 authentication outcomes。

`M-034 Authentication Service Behavior Implementation Gate` 已完成。`W-0106` 已在 `docs/authentication-service-behavior-implementation-gate.md` 和 `ADR-0050` 中定义 authentication service behavior implementation gate，且没有添加 service code 或 runtime authentication behavior。Future service behavior 属于 `runtime/internal/app/authentication`，不属于本模块，并且必须只通过 application unit-of-work boundary 使用本模块的 repository。

`M-035 Authentication Service Behavior Skeleton` 已完成。`W-0107` 在 `runtime/internal/app/authentication` 下添加了 skeleton service files，不在本模块内。该 skeleton 预留 service vocabulary 和 fail-closed public errors，但不调用 repositories、不决定 proof validity、不构造 validated `RequestIdentity`、不执行 login、不 validate tokens、不 wire startup，也不决定 authentication outcomes。本模块仍是 storage-neutral，只能存储和读取 already-computed digest material。`runtime.authentication_service_behavior_implementation_gate` 是该 skeleton boundary 的 repository check rule。

`M-036 Device Credential Login Service Behavior Gate` 已完成。`W-0108` 已在 `docs/device-credential-login-service-behavior-gate.md` 和 `ADR-0051` 中定义 future login behavior gate，且没有改变本模块。Future login behavior 只能通过 application unit-of-work boundary 调用本模块现有的 `FindCredentialByLookupDigest` 和 `StoreToken` methods。本模块不得 decode proof、compute digests、compare verifiers、generate access-token material、decide player account state、collapse public failures、return raw tokens、wire startup 或拥有 authentication outcomes。`runtime.device_credential_login_service_behavior_gate` 是该 gate 的 repository check rule。

`M-037 Device Credential Login Service Behavior Implementation` 已完成。`W-0109` 已在 application service 中实现 device credential login，而不是在本模块中实现。Service 可以通过 unit-of-work capabilities 调用本模块现有的 `FindCredentialByLookupDigest` 和 `StoreToken` methods。本模块仍然只拥有 storage-neutral credential 和 token records；它不得 decode proof、compute digests、compare verifiers、generate access-token material、decide player account state、collapse public failures、return raw tokens、wire startup 或拥有 authentication outcomes。`runtime.device_credential_login_service_behavior_implementation` 是该 slice 的 repository check rule。

`M-038 Access Token Validation Service Behavior Gate` 已完成。`W-0110` 已在 `docs/access-token-validation-service-behavior-gate.md` 和 `ADR-0052` 中定义 future access-token validation behavior gate，且没有改变本模块。Future validation behavior 只能通过 application unit-of-work boundary 调用本模块现有的 `FindTokenByLookupDigest` 和 `GetPlayerAccount` methods。本模块不得 decode token proof、compute digests、compare verifiers、决定 token lifecycle state、决定 player account state、collapse public failures、construct `RequestIdentity`、return validated identity、wire startup 或拥有 authentication outcomes。`runtime.access_token_validation_service_behavior_gate` 是该 gate 的 repository check rule。

`M-039 Access Token Validation Service Behavior Implementation` 已完成。`W-0111` 在 application service 中实现 access-token validation，而不是在本模块中实现。该 service 可以通过 unit-of-work capabilities 调用本模块现有的 `FindTokenByLookupDigest` method。本模块仍然只拥有 storage-neutral token records；不得 decode token proof、compute digests、compare verifiers、决定 player account state、collapse public failures、construct `RequestIdentity`、return validated identity、wire startup 或拥有 authentication outcomes。`runtime.access_token_validation_service_behavior_implementation` 是该 slice 的 repository check rule。

`M-041 Access Token Protocol Carrier And Route Protection Gate` 已完成。`W-0113` 在 `docs/access-token-protocol-carrier-route-protection-gate.md` 和 `ADR-0053` 中定义了未来 request-level Protobuf payload wrapper 与 route-policy gate，而不是在本模块中实现。Wrapper candidate 是 `vibit.authentication.v1.AuthenticatedRequest`。随后 `M-042/W-0114` 在本模块之外实现了 wrapper source、generated output、Protobuf adapter behavior、application route policy 和 application validation handoff。本模块仍然是 storage-neutral；不得 parse protocol carriers、持有 raw token proof、construct route policy、为 routes validate access tokens，或拥有 request identity outcomes。`runtime.access_token_protocol_carrier_route_protection_gate` 是该 gate 的 repository check rule。Work queue 现在 blocked 在 `M-043/W-0115`，即 next-direction confirmation gate。

`M-044 Runtime Authentication Startup Composition Gate` 已完成。`W-0116` 在本模块之外定义了 `docs/runtime-authentication-startup-composition-gate.md` 和 `ADR-0054`。随后 `M-045/W-0117` 只在 PostgreSQL runtime path 中把 existing application authentication service wire 到 `runtime/cmd/vibit-server`。本模块仍然是 storage-neutral；不得 load verifier keys、parse environment variables、拥有 startup composition、parse protocol carriers、为 routes validate access tokens、拥有 route policy、import PostgreSQL 或 WebSocket packages，或决定 authentication outcomes。`runtime.authentication_startup_composition_gate` 是该 gate 和 startup composition slice 的 repository check rule。

`M-047 Authentication Command Protocol And Login Route Gate` 已完成。`W-0119` 在本模块之外定义了 `docs/authentication-command-protocol-login-route-gate.md` 和 `ADR-0055`。随后 `M-048/W-0120` 在本模块之外添加了 public `runtime.authentication.AuthenticateWithDeviceCredential` Protobuf command source、generated output、protocol bridge、application bootstrap handler、PostgreSQL startup route registration 和 transaction-wrapper bypass。本模块仍然是 storage-neutral；不得 parse login protocol payloads、持有 raw credential proof 或 access-token text、拥有 route handlers、拥有 startup composition、import Protobuf 或 WebSocket packages、决定 authentication outcomes，或提供 direct Nakama/Pitaya API compatibility。`runtime.authentication_command_protocol_login_route_gate` 是该 gate 和 implementation slice 的 repository check rule。

`M-050 Session Persistence And WebSocket Handshake Ratification` 已完成。`W-0122` 在本模块之外定义了 `docs/session-persistence-websocket-handshake-ratification.md` 和 `ADR-0056`。Ratified current path 仍然是通过 `vibit.authentication.v1.AuthenticatedRequest` 做 request-level opaque access-token validation；WebSocket transport 继续 credential-neutral；现有 Protobuf envelope 不变；未来 connection-level identity deferred 到 first-message protocol/application binding gate；未来 durable session persistence deferred 到 PostgreSQL-first schema/repository/migration gate。本模块仍然是 storage-neutral；不得拥有 WebSocket sessions、connection binding、session persistence、session repositories、session migrations、transport credential carriers、reconnect/epoch behavior、active-connection invalidation 或 direct Nakama/Pitaya API compatibility。`runtime.session_persistence_websocket_handshake_ratification` 是该 ratification 的 repository check rule。

`M-052 First Message Connection Binding Gate` 已完成。`W-0124` 在本模块之外定义了 `docs/first-message-connection-binding-gate.md` 和 `ADR-0057`。该 gate 选择 future `runtime.authentication.BindConnection` system route，以及 `vibit.authentication.v1.BindConnectionRequest` / `vibit.authentication.v1.BindConnectionResponse` payload candidates，但不添加 protocol source 或 runtime behavior。本模块仍然是 storage-neutral；不得拥有 connection binding、binding registries、WebSocket sessions、route-policy bound identity、session persistence、session repositories、session migrations、transport credential carriers、reconnect/epoch behavior、active-connection invalidation 或 direct Nakama/Pitaya API compatibility。`runtime.first_message_connection_binding_gate` 是该 gate 的 repository check rule。

`M-054 First Message Connection Binding Implementation Gate` 已完成。`W-0126` 在本模块之外定义了 `docs/first-message-connection-binding-implementation-gate.md` 和 `ADR-0058`。该 gate 定义 future `runtime.authentication.BindConnection` bounded implementation slice，包括计划中的 protocol messages、generated output、protocol adapter behavior、application binding behavior、PostgreSQL startup composition、public error mapping 和 tests。本模块仍然是 storage-neutral；仍然不得拥有 connection binding、binding registries、WebSocket sessions、route-policy bound identity、session persistence、session repositories、session migrations、transport credential carriers、reconnect/epoch behavior、active-connection invalidation 或 direct Nakama/Pitaya API compatibility。`runtime.first_message_connection_binding_implementation_gate` 是该 gate 的 repository check rule。

`M-056 First Message Connection Binding Implementation` 已完成。`W-0128` 在本模块之外通过 Protobuf source/generated output、application-owned binding、Protobuf adapter handling、WebSocket connection metadata handoff 和 PostgreSQL startup composition 实现了 `runtime.authentication.BindConnection`。本模块仍然是 storage-neutral；仍然不得拥有 connection binding execution、WebSocket sessions、route policy、raw access-token proof、validated identity construction、session persistence、session repositories、session migrations、transport credential carriers、reconnect/epoch behavior、active-connection invalidation 或 direct Nakama/Pitaya API compatibility。普通 protected routes 仍然依赖 request-level `AuthenticatedRequest` path；bound connection identity 不是本模块 repository concern。`runtime.first_message_connection_binding_implementation` 是该 slice 的 repository check rule。

`M-058 PostgreSQL Session Persistence Schema Gate` 已完成。`W-0130` 在本模块之外定义了 `docs/postgres-session-persistence-schema-gate.md` 和 `ADR-0059`。该 gate 选择 PostgreSQL 作为 first durable runtime session target，选择 `runtime_sessions` 作为 future logical table candidate，并选择 `runtime/migrations/postgres/000005_create_runtime_sessions.sql` 作为 future migration source candidate。本模块不拥有 `runtime_sessions`；不得把 raw access-token text、raw credentials、token digests 或 credential digests 复制进 future session records。本模块也不得添加 session repositories、session migrations、PostgreSQL session adapters、runtime session validation、route-policy use of sessions or bound identity、WebSocket handshake authentication、transport credential carriers、logout/revocation active-connection invalidation、reconnect/epoch behavior、dependencies、memory durable session behavior 或 direct Nakama/Pitaya API compatibility。`runtime.postgres_session_persistence_schema_gate` 是 repository check rule。Work queue 现在 blocked 在 `M-059/W-0131`，即 next-direction confirmation gate。

`M-060 Runtime Sessions Migration Source` 已完成。`W-0132` 在本模块所有权之外添加了 `runtime/migrations/postgres/000005_create_runtime_sessions.sql` 和 `ADR-0060`。新的 `runtime_sessions` table 由 `runtime.session` 拥有，不由 `authentication` 拥有。本模块只可以通过 `authentication_access_tokens(token_record_id)` 保持可选 record reference；仍然不得把 raw access-token text、raw credentials、token lookup digests、token verifier digests、credential lookup digests 或 credential verifier digests 复制进 session records。这个 slice 不授权 authentication module repository changes、PostgreSQL session adapters、runtime session validation、login-created sessions、BindConnection-created sessions、logout/revocation active-connection invalidation、WebSocket handshake authentication、route-policy use of persisted sessions or bound identity、reconnect/epoch behavior、dependencies、memory durable session behavior 或 direct Nakama/Pitaya API compatibility。`runtime.runtime_sessions_migration_source` 是 repository check rule。Work queue 现在 blocked 在 `M-061/W-0133`，即 next-direction confirmation gate。

`M-062 Session Repository Boundary Gate` 已完成。`W-0134` 在本模块所有权之外定义了 `docs/session-repository-boundary.md` 和 `ADR-0061`。Future session repository owner candidate 是 `runtime/internal/app/session`，不是 `modules/authentication`；future PostgreSQL adapter owner 是 `runtime/internal/platform/persistence/postgres`。本模块只能通过 optional token record linkage 与 runtime sessions 保持关系，不得拥有 session repository interfaces、session adapter behavior、runtime session validation、session creation、session revocation、route-policy session identity、WebSocket handshake authentication、transport credential carriers、reconnect/epoch behavior 或 direct Nakama/Pitaya API compatibility。Token validation 仍由 application authentication service 和 route access-token validator 拥有，不由 future session repository 拥有。`runtime.session_repository_boundary` 是 repository check rule。Work queue 现在 blocked 在 `M-063/W-0135`，即 next-direction confirmation gate。

`M-064 Session Repository Interface Implementation` 已完成。`W-0136` 在本模块所有权之外添加了 `runtime/internal/app/session/repository.go`、`runtime/internal/app/session/repository_test.go` 和 `ADR-0062`。Storage-neutral session repository interface 由 `runtime/internal/app/session` 拥有，不由 `modules/authentication` 拥有；本模块仍然只通过 optional access-token record linkage 与 runtime sessions 关联。它不得拥有 session adapter behavior、SQL query execution、unit-of-work factory wiring、runtime session validation、session creation、session revocation、route-policy session identity、WebSocket handshake authentication、transport credential carriers、reconnect/epoch behavior、logout active-connection behavior 或 direct Nakama/Pitaya API compatibility。`runtime.session_repository_interface_implementation` 是 repository check rule。Work queue 现在 blocked 在 `M-065/W-0137`，即 next-direction confirmation gate。

`M-066 Session PostgreSQL Adapter Gate` 已完成。`W-0138` 在本模块所有权之外定义了 `docs/session-postgresql-adapter-gate.md` 和 `ADR-0063`。Future PostgreSQL session adapter owner 是 `runtime/internal/platform/persistence/postgres`，不是 `modules/authentication`；本模块仍然只通过 optional access-token record linkage 与 runtime sessions 关联。它不得拥有 session adapter behavior、SQL query execution、unit-of-work factory wiring、runtime session validation、session creation、session revocation、route-policy session identity、WebSocket handshake authentication、transport credential carriers、reconnect/epoch behavior、logout active-connection behavior 或 direct Nakama/Pitaya API compatibility。`runtime.session_postgresql_adapter_gate` 是 repository check rule。Work queue 现在 blocked 在 `M-067/W-0139`，即 next-direction confirmation gate。

`M-068 Session PostgreSQL Adapter Implementation` 已完成。`W-0140` 在本模块所有权之外添加了 PostgreSQL runtime session adapter：`runtime/internal/platform/persistence/postgres/session_repository.go` 和 `ADR-0064`。该 adapter 只能存储和返回 `access_token_record_id` linkage；authentication 仍然拥有 access-token proof validation 和 token lifecycle behavior。本模块不得接管 runtime session validation、session creation、session revocation execution、route-policy session identity、WebSocket handshake authentication、transport credential carriers、reconnect/epoch behavior、logout active-connection behavior 或 direct Nakama/Pitaya API compatibility。`runtime.session_postgresql_adapter_implementation` 是 repository check rule。Work queue 现在 blocked 在 `M-069/W-0141`，即 next-direction confirmation gate。

`M-070 Runtime Session Validation Gate` 已完成。`W-0142` 在本模块所有权之外定义了 `docs/runtime-session-validation-gate.md` 和 `ADR-0065`。Future runtime session validation 由 `runtime/internal/app` 下的 application 拥有，不由 `modules/authentication` 拥有；authentication 可以通过现有 access-token validation path 提供 already-validated actor identity，但不拥有 persisted session validation 或 route-policy session identity。本模块不得接管 runtime session validation、session creation、session revocation execution、route-policy session identity、WebSocket handshake authentication、transport credential carriers、reconnect/epoch behavior、logout active-connection behavior 或 direct Nakama/Pitaya API compatibility。`runtime.runtime_session_validation_gate` 是 repository check rule。Work queue 现在 blocked 在 `M-071/W-0143`，即 next-direction confirmation gate。

`M-072 Runtime Session Validation Implementation` 已完成。`W-0144` 在本模块所有权之外添加了 application-owned persistent runtime session validator：`runtime/internal/app/runtime_session_validator.go`，并记录 `ADR-0066`。Authentication 仍然拥有 access-token proof validation，并可以提供 already validated player identity，但不拥有 persisted session validation、session creation、route-policy session identity、WebSocket handshake authentication、logout active-connection behavior、reconnect/epoch behavior 或 direct Nakama/Pitaya API compatibility。该 validator 只能在 durable session validation 成功后设置 `RequestIdentity.SessionValidated = true`。本模块不得添加 session validator code、session repositories、session adapters、session creation、session revocation execution、route-policy session identity、WebSocket handshake authentication、transport credential carriers、reconnect/epoch behavior、logout active-connection behavior 或 direct Nakama/Pitaya API compatibility。`runtime.runtime_session_validation_implementation` 是 repository check rule。Work queue 现在 blocked 在 `M-073/W-0145`，即 next-direction confirmation gate。

`M-074 Session Creation Composition Gate` 已完成。`W-0146` 在本模块所有权之外定义了 `docs/session-creation-composition-gate.md` 和 `ADR-0067`。Future login-time durable session creation 后续可以在 application layer 通过 `AuthenticateWithDeviceCredential` 和 `session.Repository.CreateRuntimeSession` 组合，但本模块仍不拥有 runtime session persistence 或 route-policy session identity。`access_token_record_id` linkage 保持 private server metadata，不是 client proof。本模块不得实现 session creation、生成 session ids、在 login 或 BindConnection 创建 sessions、为 session creation 改动 authentication service behavior、添加 session repositories 或 adapters、暴露 session protocol carriers、改变 WebSocket handshake authentication、添加 logout active-connection behavior、添加 reconnect/epoch behavior 或 direct Nakama/Pitaya API compatibility。`runtime.session_creation_composition_gate` 是 repository check rule。Work queue 现在 blocked 在 `M-075/W-0147`，即 next-direction confirmation gate。

`M-076 Session Creation Composition Implementation` 已完成。`W-0148` 在 `runtime/internal/app/authentication/service.go` 和 `ADR-0068` 中实现了 application-layer login-time durable runtime session creation。Authentication service behavior 只能因为 device-credential login 是已选择的 token issuance entry point，才可以通过 application unit-of-work 组合 `session.Repository.CreateRuntimeSession`；authentication module 仍不拥有 runtime session persistence、route-policy session identity、WebSocket handshake authentication、logout active-connection behavior、reconnect/epoch behavior、protocol session carriers 或 direct Nakama/Pitaya API compatibility。Runtime sessions 以 private server metadata 形式链接 `access_token_record_id`，并且不存储 raw proof 或 digest material。`runtime.session_creation_composition_implementation` 是 repository check rule。Work queue 现在 blocked 在 `M-077/W-0149`，即 next-direction confirmation gate。

`M-078 Bound Identity Route Policy Gate` 已完成。`W-0150` 在本模块所有权之外定义了 `docs/bound-identity-route-policy-gate.md` 和 `ADR-0069`。Future route-policy 使用 request-token、bound-connection、session-validated、bound-session identity 时，由 `runtime/internal/app` 下的 application 拥有；authentication 可以继续通过 service boundary 验证 access-token proof，但本模块不拥有 route policy、bound connection identity、persisted runtime session validation、WebSocket handshake authentication、protocol session carriers、logout active-connection behavior、reconnect/epoch behavior 或 direct Nakama/Pitaya API compatibility。该 gate 推荐普通 protected domain routes 继续使用 request-level access-token proof，直到后续 implementation slice 显式重新分类 routes。`runtime.bound_identity_route_policy_gate` 是 repository check rule。Work queue 现在 blocked 在 `M-079/W-0151`，即 next-direction confirmation gate。

`M-080 Bound Identity Route Policy Implementation` 已完成。`W-0152` 在本模块所有权之外，于 `runtime/internal/app/route_authentication.go` 和 `ADR-0070` 实现了显式 route policy families。Authentication 继续通过 `RouteAccessTokenValidator` 验证 access-token proof，但不拥有 route classification、bound connection identity、persisted session validation、protocol session carriers、WebSocket handshake authentication、logout active-connection behavior、reconnect/epoch behavior 或 direct Nakama/Pitaya API compatibility。普通 protected routes 仍默认使用 request-level access-token proof；bound/session identities 只能满足显式分类的 routes。`runtime.bound_identity_route_policy_implementation` 是 repository check rule。Work queue 现在 blocked 在 `M-081/W-0153`，即 next-direction confirmation gate。

`M-082 Logout Revocation Active Connection Gate` 已完成。`W-0154` 在本模块所有权之外定义了 `docs/logout-revocation-active-connection-gate.md` 和 `ADR-0071`。Future logout/revocation active-connection policy 由 `runtime/internal/app` 下的 application 拥有；token revocation、runtime session revocation 和 active socket invalidation 仍然是分离的 future decisions。本模块只有在后续 gate 授权后，才可以拥有 storage-neutral token record shapes 和 future token revocation mutation vocabulary；它不拥有 active connection invalidation、connection registries、WebSocket close policy、runtime session revocation policy、Protobuf logout routes、protocol session carriers、reconnect/epoch behavior 或 direct Nakama/Pitaya API compatibility。该 gate 不实现 logout 或 revocation execution。`runtime.logout_revocation_active_connection_gate` 是 repository check rule。Work queue 现在 blocked 在 `M-083/W-0155`，即 next-direction confirmation gate。

`M-084 Logout Access Token Behavior Gate` 已完成。`W-0156` 在本模块所有权之外定义了 `docs/logout-access-token-behavior-gate.md` 和 `ADR-0072`。`M-086 Logout Access Token Behavior Implementation` 已完成。`W-0158` 在 `runtime/internal/app/authentication` 实现了 `LogoutAccessToken`；本模块角色仍然只限于通过 repository interfaces 提供 storage-neutral token record mutation vocabulary。Application service 负责解析 raw access tokens、计算 lookup 和 verifier digests、比较 verifiers、决定 logout proof validity，并且只在 verifier comparison 之后请求 `RevokeToken`。本模块不得拥有 runtime session revocation、active connection invalidation、connection registries、WebSocket close policy、Protobuf logout routes、protocol session carriers、refresh/logout-all/admin revocation behavior、reconnect/epoch behavior 或 direct Nakama/Pitaya API compatibility。`runtime.logout_access_token_behavior_gate` 和 `runtime.logout_access_token_behavior_implementation` 是 repository check rules。Work queue 随后 blocked 在 `M-087/W-0159`，即 next-direction confirmation gate。

`M-088 Active Connection Registry Gate` 已完成。`W-0160` 在本模块所有权之外定义了 `docs/active-connection-registry-gate.md` 和 `ADR-0074`。Future active connection registry behavior 由 `runtime/internal/app/connection` 下的 application 拥有，不由 `modules/authentication` 拥有。Authentication 可以继续拥有 token lifecycle service behavior 和 storage-neutral token record mutation vocabulary，但不拥有 active connection registries、WebSocket close policy、kick/disconnect behavior、reconnect/epoch behavior、protocol carriers、durable/distributed registry storage 或 direct Nakama/Pitaya API compatibility。`runtime.active_connection_registry_gate` 是 repository check rule。Work queue 现在 blocked 在 `M-089/W-0161`，即 next-direction confirmation gate。

`M-090 Active Connection Registry Single Process Implementation` 已完成。`W-0162` 在本模块所有权之外实现了 `runtime/internal/app/connection/registry.go` 和 `ADR-0075`。该 registry 由 runtime application 拥有，第一版是 single-process、in-memory、non-durable。Authentication 可以继续拥有 token lifecycle service behavior 和 storage-neutral token record mutation vocabulary，但不拥有 registry state、socket close policy、connection invalidation policy、kick/disconnect behavior、runtime session revocation、reconnect/epoch behavior、protocol carriers、durable/distributed registry storage 或 direct Nakama/Pitaya API compatibility。`runtime.active_connection_registry_single_process_implementation` 是 repository check rule。Work queue 现在 blocked 在 `M-091/W-0163`，即 next-direction confirmation gate。

`M-092 WebSocket Close Policy Gate` 已完成。`W-0164` 在本模块所有权之外定义了 `docs/websocket-close-policy-gate.md` 和 `ADR-0076`。Future WebSocket close policy 由 `runtime/internal/app` 下的 runtime application 拥有，不由 `modules/authentication` 拥有；active connection registry 仍只是 target state，WebSocket transport 也只是未来 narrow close handoff owner。Authentication 可以继续拥有 token lifecycle service behavior 和 storage-neutral token record mutation vocabulary，但不拥有 socket close policy、concrete transport close handoff、close codes、close reason text、kick/disconnect behavior、logout-triggered socket close、runtime session revocation policy、reconnect/epoch behavior、protocol carriers、durable/distributed registry storage 或 direct Nakama/Pitaya compatibility。`runtime.websocket_close_policy_gate` 是 repository check rule。

`M-094 WebSocket Close Policy Single Process Implementation` 已完成。`W-0166` 在本模块所有权之外实现了 `runtime/internal/app/connection/close_policy.go` 和 `ADR-0077`。该 policy 会 invalidates active bound registry records，并产出带有 `mark_invalidated_only` 的 redacted `CloseIntent`；authentication 不调用它，不拥有 registry invalidation policy，本 slice 也不改变 `LogoutAccessToken` behavior。Authentication 仍然不能拥有 concrete socket close、transport close handoff、close codes、close reason text、protocol close messages、runtime session revocation close behavior、reconnect/epoch behavior、protocol carriers、durable/distributed registry storage 或 direct Nakama/Pitaya compatibility。`runtime.websocket_close_policy_single_process_implementation` 是 repository check rule。

`M-096 Nakama Pitaya Product Parity Roadmap` 已完成。`W-0168` 添加了 `docs/nakama-pitaya-product-parity-roadmap.md` 和 `ADR-0078`，把 Nakama/Pitaya-class common capability coverage 设为产品目标，但不添加 direct API compatibility。当前 next ready work item 是 `M-097/W-0169 define_protocol_logout_route_gate`。Authentication 可以通过已有 `LogoutAccessToken` service behavior 参与，但本模块仍不拥有 protocol route registration、Protobuf message generation、concrete WebSocket close handoff、reconnect/epoch behavior、protocol session carriers、presence、chat、social modules、matchmaking 或 match runtime。`runtime.reference_product_parity_roadmap` 是 repository check rule。

`M-097 Protocol Logout Route Gate` 已完成。`W-0169` 添加了 `docs/protocol-logout-route-gate.md` 和 `ADR-0079`。Authentication 通过已有 `LogoutAccessToken` service behavior 参与，但 route registration 属于 application bootstrap，Protobuf mapping 属于 protocol adapter。该 gate 不授权 socket close、runtime session revocation、active connection invalidation、reconnect/epoch behavior、protocol session carriers、dependencies、memory durable logout behavior 或 direct Nakama/Pitaya API compatibility。`runtime.protocol_logout_route_gate` 是 repository check rule。

`M-098 Protocol Logout Route Implementation` 已完成。`W-0170` 通过 protocol pipeline 暴露了已有 `LogoutAccessToken` service behavior，包括 Protobuf request/response messages、通过 Buf 生成 output、protocol bridge mapping、application bootstrap route handling、PostgreSQL startup registration、transaction bypass 和 focused tests。Authentication 仍只通过 application service boundary 拥有 token proof validation 和 token revocation；本模块不拥有 route policy、WebSocket transport behavior、socket close handoff、runtime session revocation、reconnect behavior、protocol session carriers、memory durable logout behavior 或 direct Nakama/Pitaya API compatibility。`runtime.protocol_logout_route_implementation` 是 repository check rule。

`M-099 Next Direction Confirmation After Protocol Logout Route Implementation` 已完成。`W-0171` 选择了 `define_transport_close_handoff_gate`，它仍在本模块所有权之外。当前 next ready work item 是 `M-100/W-0172 define_transport_close_handoff_gate`。Authentication 不得拥有 concrete socket close、close codes、close reason text、logout-triggered close、runtime session revocation、reconnect/epoch behavior、protocol session carriers、operations/admin disconnect、dependencies 或 direct Nakama/Pitaya API compatibility。

`M-100 Transport Close Handoff Gate` 已完成。`W-0172` 在本模块所有权之外添加了 `docs/transport-close-handoff-gate.md` 和 `ADR-0080`。Authentication 仍不得调用 WebSocket transport，不得拥有 concrete socket close handoff，不得在 logout 后关闭 sockets，不得选择 close codes 或 close reason text，不得 revoke runtime sessions，不得添加 reconnect/epoch behavior、protocol session carriers 或 direct Nakama/Pitaya API compatibility。`runtime.transport_close_handoff_gate` 是 repository check rule。当前 next ready work item 是 `M-101/W-0173 confirm_next_direction_after_transport_close_handoff_gate`。

`M-101 Next Direction Confirmation After Transport Close Handoff Gate` 已完成。`W-0173` 选择了 `implement_transport_close_handoff_single_process`，它仍在本模块所有权之外。当前 next ready work item 是 `M-102/W-0174 implement_transport_close_handoff_single_process`。Authentication 仍不得调用 WebSocket transport，不得拥有 concrete socket close handoff，不得在 logout 后关闭 sockets，不得选择 close codes 或 close reason text，不得 revoke runtime sessions，不得添加 reconnect/epoch behavior、protocol session carriers 或 direct Nakama/Pitaya API compatibility。

`M-102 Transport Close Handoff Single Process Implementation` 已在本模块所有权之外完成。`W-0174` 添加了 WebSocket transport-owned、按 server-observed `connection_id + epoch` 定位的 single-process close handoff；`ADR-0081` 记录该实现。Authentication 仍只是 token lifecycle service logic。Authentication 不得调用 WebSocket transport，不得拥有 concrete socket close handoff，不得在 logout 后关闭 sockets，不得选择 close codes 或 close reason text，不得 revoke runtime sessions，不得添加 reconnect/epoch behavior、protocol session carriers 或 direct Nakama/Pitaya API compatibility。当前 next ready work item 是 `M-103/W-0175 confirm_next_direction_after_transport_close_handoff_single_process_implementation`。

## Forbidden Shortcuts

- 不要存储 raw credential 或 token material。
- 不要在本模块中计算 HMAC、生成 verifier digests、加载 verifier keys 或比较 verifier material。
- 不要添加 `AuthService`、`Authenticator`、`TokenValidator`、`TokenIssuer`、`TokenVerifier`、`TokenRepository` 或 `CredentialRepository` implementation types。
- 不要在本模块下添加 PostgreSQL adapter files。
- 不要导入 `pgx`、`goose`、WebSocket、Protobuf、JWT、OAuth、OIDC、bcrypt、argon2、provider SDKs、Redis-like clients、S3 SDKs 或 MinIO clients。
- 编辑 repository interface boundary 时，不要添加 runtime authentication behavior。
- 未经单独 change spec 和 decision，不要改变已 ratified 的 credential 或 token verifier migration schemas。
- 不要让 player account lifecycle tables 拥有 authentication state。

## Required Tests

参见 `module.yaml` 中的 `tests.required`。

当前 module 状态下，focused tests 只限于 repository interface shape、closed status sets、required-field normalization、digest copying、timestamp UTC normalization，以及 storage-neutral mutation/query validation。PostgreSQL adapter tests 在 `M-015` 期间属于 `runtime/internal/platform/persistence/postgres/`；runtime authentication、WebSocket、Protobuf 和 generated-shape tests 只有在它们各自的 boundary 被单独 ratify 后才会成为必需。
