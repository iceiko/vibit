# Request

Define Pitaya-aligned cluster observability boundary gate.

Register `runtime.pitaya_aligned_cluster_observability_boundary_gate` and define `docs/pitaya-aligned-cluster-observability-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
