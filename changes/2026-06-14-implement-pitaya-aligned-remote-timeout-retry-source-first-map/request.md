# Request

Implement Pitaya-aligned remote timeout and retry source-first map.

Register `runtime.pitaya_aligned_remote_timeout_retry_source_first_map` and implement `node tools/vibit inspect pitaya-remote-timeout-retry --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
