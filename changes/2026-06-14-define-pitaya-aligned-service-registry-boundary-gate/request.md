# Request

Define Pitaya-aligned service registry boundary gate.

Register `runtime.pitaya_aligned_service_registry_boundary_gate` and define `docs/pitaya-aligned-service-registry-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
