# Request

Define Pitaya-aligned node identity boundary gate.

Register `runtime.pitaya_aligned_node_identity_boundary_gate` and define `docs/pitaya-aligned-node-identity-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
