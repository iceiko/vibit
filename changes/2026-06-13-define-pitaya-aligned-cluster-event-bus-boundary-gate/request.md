# Request

Define `W-0311 Pitaya-aligned cluster event bus boundary gate`.

Register `runtime.pitaya_aligned_cluster_event_bus_boundary_gate` and define `docs/pitaya-aligned-cluster-event-bus-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
