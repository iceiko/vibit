# Request

Define the gate-only boundary for future active WebSocket connection registry behavior after logout access-token behavior implementation.

The work should strongly reference Nakama and Pitaya, but preserve vibit-native ownership and avoid direct compatibility claims.
