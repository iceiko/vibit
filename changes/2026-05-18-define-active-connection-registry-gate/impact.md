# Impact

This change adds a standard and ADR for future active connection registry behavior.

It does not add Go runtime registry code, WebSocket close behavior, kick/disconnect behavior, reconnect/epoch behavior, Protobuf logout routes, protocol session carriers, migrations, generated output, dependencies, durable or distributed connection storage, or direct Nakama/Pitaya API compatibility.
