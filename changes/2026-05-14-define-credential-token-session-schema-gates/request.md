# Request

Define credential, token, external identity, and session schema gates for the selected first login/token posture.

The change must preserve the current player account lifecycle schema and must not add migrations, repository interfaces, adapters, runtime lookup behavior, handlers, routes, Protobuf changes, WebSocket behavior, or authentication implementation.
