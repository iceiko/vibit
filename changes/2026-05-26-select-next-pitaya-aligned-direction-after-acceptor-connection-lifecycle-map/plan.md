# Plan

1. Record ADR-0175 for the selected follow-up direction.
2. Add change artifacts and conversation memory for W-0267.
3. Update `.arch` manifests, module manifests, AGENTS guides, README, alpha docs, and roadmap docs to show W-0268 as next-ready.
4. Register the runtime check rule in `rules/check-rules.json` and `tools/vibit`.
5. Verify `node tools/vibit inspect next --json`, the new rule, work/runtime/memory/schema/all checks, and diff hygiene.
