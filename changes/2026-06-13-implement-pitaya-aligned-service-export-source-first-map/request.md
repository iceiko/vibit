# Request

Implement `W-0304 Pitaya-aligned service export source-first map`.

Register `runtime.pitaya_aligned_service_export_source_first_map` and expose `node tools/vibit inspect pitaya-service-export --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
