# Plan

1. Record `W-0308` in `.arch/work-items.yaml`.
2. Update architecture manifests and continuation docs.
3. Add `node tools/vibit inspect pitaya-frontend-forwarding --json`.
4. Register `runtime.pitaya_aligned_frontend_message_forwarding_source_first_map` in `tools/vibit` and `rules/check-rules.json`.
5. Add `ADR-0216`, conversation memory, and change artifacts.
6. Run targeted and full verification.
