# Request

Implement `W-0308 Pitaya-aligned frontend message forwarding source-first map`.

Register `runtime.pitaya_aligned_frontend_message_forwarding_source_first_map` and expose `node tools/vibit inspect pitaya-frontend-forwarding --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
