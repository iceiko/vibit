# Impact

This change records source-first Pitaya-aligned cross-node error mapping planning only.

Runtime behavior, protocol shape, generated output, persistence, dependencies, distributed runtime behavior, and direct compatibility are unchanged.
