# Request

Define Pitaya-aligned cross-node error mapping boundary gate.

Register `runtime.pitaya_aligned_cross_node_error_mapping_boundary_gate` and define `docs/pitaya-aligned-cross-node-error-mapping-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
