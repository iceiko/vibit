# Plan

1. Add runtime contract inspection support to `tools/vibit`.
2. Preserve existing domain module contract inspection behavior.
3. Add work item trace.
4. Verify runtime and domain contract inspection commands.
5. Run repository checks.

## Boundaries

Do not change runtime behavior, public protocol shape, generated output, authentication, token, credential, persistence, or session storage behavior.
