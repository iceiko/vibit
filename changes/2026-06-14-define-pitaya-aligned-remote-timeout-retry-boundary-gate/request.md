# Request

Define Pitaya-aligned remote timeout and retry boundary gate.

Register `runtime.pitaya_aligned_remote_timeout_retry_boundary_gate` and define `docs/pitaya-aligned-remote-timeout-retry-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
