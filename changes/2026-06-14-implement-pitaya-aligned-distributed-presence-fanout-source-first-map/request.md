# Request

Implement Pitaya-aligned distributed presence fanout source-first map.

Register `runtime.pitaya_aligned_distributed_presence_fanout_source_first_map` and implement `node tools/vibit inspect pitaya-presence-fanout --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
