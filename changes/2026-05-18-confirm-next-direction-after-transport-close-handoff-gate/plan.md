# Plan

1. Mark `M-101/W-0173` completed with selected direction `implement_transport_close_handoff_single_process`.
2. Create `M-102/W-0174` as the next bounded implementation work item.
3. Keep the confirmation step free of concrete socket close code.
4. Preserve close code mapping, close reason text, logout-triggered close, runtime session revocation, reconnect/epoch behavior, protocol session carriers, presence, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, and broad product modules behind later gates.
5. Record the direction in the change spec and conversation memory.
6. Verify the work queue and change spec.
