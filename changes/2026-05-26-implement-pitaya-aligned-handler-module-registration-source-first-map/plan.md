# Plan

1. Capture RED checks for the missing W-0284 rule, missing inspect command, and missing change directory.
2. Add the `pitaya-handler-modules` inspection builder and command in `tools/vibit`.
3. Add the W-0284 runtime check, completed-state helper, dynamic next-work helpers, and runtime check dispatch.
4. Add the rule catalog entry for `runtime.pitaya_aligned_handler_module_registration_source_first_map`.
5. Add ADR-0192, conversation log, and change artifacts.
6. Update architecture manifests, module manifests, README files, AGENTS guides, and roadmap documents to complete W-0284 and open W-0285.
7. Run focused checks, full repository verification, whitespace checks, staged secret scan, commit, and push.
