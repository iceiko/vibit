# Request

Define Pitaya-aligned distributed session ownership boundary gate.

Register `runtime.pitaya_aligned_distributed_session_ownership_boundary_gate` and define `docs/pitaya-aligned-distributed-session-ownership-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
