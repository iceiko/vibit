# Request

Implement Pitaya-aligned service selector source-first map.

Register `runtime.pitaya_aligned_service_selector_source_first_map` and implement `node tools/vibit inspect pitaya-service-selector --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
