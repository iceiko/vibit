# Request

Continue advancing toward Nakama/Pitaya-class server capability while reducing confirmation-gate overhead.

Implement the first bounded presence lifecycle behavior after protocol-visible runtime session metadata exists.

This slice must stay Tier 2 under `ADR-0082`: the boundary belongs in this change spec and the implementation may proceed directly when it remains small, checkable, and reversible.

