# Plan

1. Add the application-owned persistent session validator source.
2. Add fake-repository tests for success, missing or conflicting session id, metadata-only identity, unvalidated identity, lookup failures, repository failures, record mismatches, nil repository, and no mutations.
3. Add ADR and conversation memory.
4. Update manifests, module guides, AGENTS guides, rule catalog, and runtime checks.
5. Run Go tests and repository checks.
