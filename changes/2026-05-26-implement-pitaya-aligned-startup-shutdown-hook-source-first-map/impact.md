# Impact

- Add `node tools/vibit inspect pitaya-startup-shutdown --json`.
- Register `runtime.pitaya_aligned_startup_shutdown_hook_source_first_map` in `tools/vibit` and `rules/check-rules.json`.
- Add `ADR-0198`.
- Complete `M-218/W-0290` and open `M-219/W-0291 Select next Pitaya-aligned direction after startup and shutdown hook map`.
- Update architecture manifests, module next-work metadata, README/AGENTS guidance, alpha docs, and roadmap docs.

No runtime behavior, protocol shape, generated output, persistence, dependency, hosted surface, SDK, release artifact, distributed runtime behavior, or direct Nakama/Pitaya API compatibility is added.
