# Plan

1. Add the `vibit.storage.v1` Protobuf source for own-player get/list/put/delete storage object requests and responses.
2. Regenerate Go Protobuf output with `buf generate`.
3. Add storage route keys under `runtime/internal/app/storage`.
4. Add application bootstrap route handlers that validate payload types, inject `request.Identity`, and map redacted public errors.
5. Add Protobuf bridge mapping for storage requests and results.
6. Register the storage generated package in the payload registry and dispatch bridge.
7. Register storage routes in PostgreSQL startup composition and bypass the outer transaction wrapper for service-owned write routes.
8. Add focused tests for route handlers, bridge mapping, route protection, and startup id generation.
9. Record `ADR-0119`, the conversation log, change metadata, manifests, and check-rule updates.
10. Run static checks, focused Go tests, full Go tests, and diff hygiene checks.

## Generated Artifacts

- `runtime/internal/generated/proto/vibit/storage/v1/storage.pb.go`

This file is generated by `buf generate` from:

- `proto/vibit/storage/v1/storage.proto`

## Handwritten Logic

- `runtime/internal/app/storage/routes.go`
- `runtime/internal/app/bootstrap/storage.go`
- `runtime/internal/platform/protocol/protobuf/storage_bridge.go`
- startup composition in `runtime/cmd/vibit-server/main.go`
- focused tests

## Verification Commands

- `buf generate`
- `cd runtime && go test ./internal/modules/storage ./internal/app/storage ./internal/app/bootstrap ./internal/platform/protocol/protobuf ./cmd/vibit-server`
- `cd runtime && go test ./...`
- `node -c tools/vibit`
- `node tools/vibit inspect next --json`
- `node tools/vibit inspect rule runtime.storage_objects_protocol_route_implementation`
- `node tools/vibit check change implement-storage-objects-protocol-route --json`
- `node tools/vibit check module storage --json`
- `node tools/vibit check work --json`
- `node tools/vibit check protocol --json`
- `node tools/vibit check generated --json`
- `node tools/vibit check runtime --json`
- `node tools/vibit check memory --json`
- `node tools/vibit check schemas --json`
- `node tools/vibit check all --json`
- `git diff --check`

## Rollback Or Migration Notes

Reversal means removing the storage Protobuf source/generated output, route keys, bootstrap handlers, bridge mapping, startup registration, tests, ADR-0119, check-rule updates, and W-0211 manifest state, then reopening `M-139/W-0211`.

No database rollback is required because this change adds no migrations and does not change the `storage_objects` table.
