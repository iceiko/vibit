# Verification

Status: Verified

Commands:

```bash
node tools/vibit check work --json
node tools/vibit check change confirm-next-direction-after-bound-identity-route-policy-implementation --json
node tools/vibit check all --json
```
