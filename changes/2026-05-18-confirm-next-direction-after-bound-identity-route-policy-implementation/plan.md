# Plan

1. Mark `M-081/W-0153` completed with selected direction `define_logout_revocation_active_connection_gate`.
2. Add `M-082/W-0154` for the gate-only logout/revocation active-connection boundary.
3. Add `M-083/W-0155` as the next confirmation gate.
4. Record the decision in `ADR-0071`.
5. Keep Nakama and Pitaya as reference baselines without copying public APIs.
6. Verify the work queue and repository checks.
