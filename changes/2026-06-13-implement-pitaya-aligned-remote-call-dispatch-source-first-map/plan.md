# Plan

1. Record `W-0306` in `.arch/work-items.yaml`.
2. Update architecture manifests and continuation docs.
3. Add `node tools/vibit inspect pitaya-remote-call-dispatch --json`.
4. Register `runtime.pitaya_aligned_remote_call_dispatch_source_first_map` in `tools/vibit` and `rules/check-rules.json`.
5. Add `ADR-0214`, conversation memory, and change artifacts.
6. Run targeted and full verification.
