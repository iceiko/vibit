# Request

Implement `W-0306 Pitaya-aligned remote call dispatch source-first map`.

Register `runtime.pitaya_aligned_remote_call_dispatch_source_first_map` and expose `node tools/vibit inspect pitaya-remote-call-dispatch --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
