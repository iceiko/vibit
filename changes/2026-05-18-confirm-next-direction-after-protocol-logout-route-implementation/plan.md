# Plan

1. Mark `M-099/W-0171` completed with selected direction `define_transport_close_handoff_gate`.
2. Create `M-100/W-0172` as a bounded gate-only work item for transport close handoff.
3. Keep the next gate focused on application-to-transport handoff shape, not implementation.
4. Preserve reconnect/epoch, protocol session carriers, presence lifecycle, operations/admin disconnect, dependencies, direct Nakama/Pitaya API compatibility, and broad product modules behind later gates.
5. Record the decision in the change spec and conversation memory.
6. Verify the work queue and change spec.
