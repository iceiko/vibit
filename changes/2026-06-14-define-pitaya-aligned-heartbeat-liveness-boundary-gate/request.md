# Request

Define Pitaya-aligned heartbeat and liveness boundary gate.

Register `runtime.pitaya_aligned_heartbeat_liveness_boundary_gate` and define `docs/pitaya-aligned-heartbeat-liveness-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
