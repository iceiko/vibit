# Request

Implement Pitaya-aligned cluster observability source-first map.

Register `runtime.pitaya_aligned_cluster_observability_source_first_map` and implement `node tools/vibit inspect pitaya-cluster-observability --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
