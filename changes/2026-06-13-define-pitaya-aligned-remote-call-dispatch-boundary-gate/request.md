# Request

Define `W-0305 Pitaya-aligned remote call dispatch boundary gate`.

Register `runtime.pitaya_aligned_remote_call_dispatch_boundary_gate` and define `docs/pitaya-aligned-remote-call-dispatch-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
