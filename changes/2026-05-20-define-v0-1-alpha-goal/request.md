# Request

Refine the project goal and record it in repository documents so a future Codex session can continue directly from the repository directory without depending on this conversation.

The short-term target is a `v0.1 alpha` that real developers can download, run, try, and use as a contribution entry point.

The long-term target is an AI-era Pitaya or Nakama: Nakama/Pitaya-class server capability adapted around vibit's agent-native maintainability model, not direct public API compatibility.
