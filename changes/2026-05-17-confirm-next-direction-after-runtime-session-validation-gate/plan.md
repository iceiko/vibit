# Plan

1. Close `M-071/W-0143` with `implement_runtime_session_validation`.
2. Create the bounded runtime session validation implementation milestone.
3. Preserve ask-first boundaries for session creation, route policy, logout/revocation, reconnect/epoch, operations, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior.
4. Run work and change checks.
