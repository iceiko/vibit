# Request

Implement Pitaya-aligned node identity source-first map.

Register `runtime.pitaya_aligned_node_identity_source_first_map` and implement `node tools/vibit inspect pitaya-node-identity --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
