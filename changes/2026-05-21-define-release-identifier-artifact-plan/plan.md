# Plan

1. Record the proposed release identifier as `v0.1.0-alpha.1`.
2. Check local tags, remote origin tags, and GitHub release tag records for conflicts without creating or pushing anything.
3. Define the smallest future artifact/publication surface as tag, GitHub release record, release notes, and hosting-platform source archive.
4. Defer binaries, packages, containers, checksums, provenance, hosted deployments, package registries, SDK packages, install scripts, and public announcements beyond the release record.
5. Update manifests, docs, AGENTS guides, and checks so the next work item is blocked final authorization.
6. Verify repository checks.
