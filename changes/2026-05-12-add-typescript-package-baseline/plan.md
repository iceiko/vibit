# Plan

## Files To Create

- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `conversations/2026-05-12-add-typescript-package-baseline.md`

## Files To Edit

- `tools/vibit`
- `.arch/runtime.yaml`
- `README.md`
- `README.zh-CN.md`
- `AGENTS.md`
- `AGENTS.zh-CN.md`
- `changes/2026-05-12-add-typescript-package-baseline/*`

## Generated Artifacts

- `package-lock.json` generated by npm.

## Handwritten Logic

- Add npm scripts for typechecking, tests, and full checks.
- Add a no-emit TypeScript config for current runtime files.
- Update `node tools/vibit check runtime` to run `npm run typecheck -- --pretty false` before runtime tests when the TypeScript package baseline exists.

## Tests

- `npm run typecheck`
- `npm test`
- `npm run check`
- `node tools/vibit check runtime`
- `node tools/vibit check runtime --json`
- `node tools/vibit check all --json`
- Secret scan.
- Diff check.

## Verification Commands

- `npm install`
- `npm run typecheck`
- `npm test`
- `npm run check`
- `node tools/vibit check runtime`
- `node tools/vibit check runtime --json`
- `node tools/vibit check schemas`
- `node tools/vibit check all --json`
- `rg -n -o "ghp_[A-Za-z0-9]+|github_pat_[A-Za-z0-9_]+" --hidden --glob '!.git/**' --glob '!.vibit.local.env' .`
- `git diff --check`

## Rollback Or Migration Notes

Remove `package.json`, `package-lock.json`, `tsconfig.json`, and the runtime check typecheck step if the package baseline is replaced before other tooling depends on it.
