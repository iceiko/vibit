# Plan

1. Record `W-0310` in `.arch/work-items.yaml`.
2. Update architecture manifests and continuation docs.
3. Add `node tools/vibit inspect pitaya-backend-service-routes --json`.
4. Register `runtime.pitaya_aligned_backend_service_route_ownership_source_first_map` in `tools/vibit` and `rules/check-rules.json`.
5. Add `ADR-0218`, conversation memory, and change artifacts.
6. Run targeted and full verification.
