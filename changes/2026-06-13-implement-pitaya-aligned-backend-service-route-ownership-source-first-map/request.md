# Request

Implement `W-0310 Pitaya-aligned backend service route ownership source-first map`.

Register `runtime.pitaya_aligned_backend_service_route_ownership_source_first_map` and expose `node tools/vibit inspect pitaya-backend-service-routes --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
