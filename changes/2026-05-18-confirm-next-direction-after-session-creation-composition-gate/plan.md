# Plan

1. Close `M-075/W-0147` with `implement_session_creation_composition`.
2. Create the bounded session creation composition implementation milestone.
3. Preserve ask-first boundaries for route policy, WebSocket handshake authentication, Protobuf carriers, logout/revocation, reconnect/epoch, operations, memory durable session behavior, direct Nakama/Pitaya API compatibility, and broader game backend behavior.
4. Run work and change checks.
