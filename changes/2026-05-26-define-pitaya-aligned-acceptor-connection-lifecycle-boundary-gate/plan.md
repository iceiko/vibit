# Plan

1. Record the acceptor and connection lifecycle boundary standard and Chinese translation.
2. Add `ADR-0173`, change artifacts, and conversation memory.
3. Update `.arch` manifests, module manifests, AGENTS guides, README, alpha docs, and roadmap docs to show `W-0266` as next-ready.
4. Register `runtime.pitaya_aligned_acceptor_connection_lifecycle_boundary_gate` in `rules/check-rules.json` and `tools/vibit`.
5. Verify focused checks, runtime/memory/schema checks, full `check all`, and diff hygiene.
