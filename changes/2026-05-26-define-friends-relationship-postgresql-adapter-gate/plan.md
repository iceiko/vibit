# Plan

1. Read the repository constitution, module instructions, friends lifecycle/persistence/migration/repository artifacts, and current next work item.
2. Add the friends relationship PostgreSQL adapter gate standard and Simplified Chinese translation.
3. Add ADR-0144, change spec artifacts, and conversation memory.
4. Register `runtime.friends_relationship_postgresql_adapter_gate` and add a focused static check.
5. Advance `.arch/work-items.yaml` from `W-0236` to `W-0237 Implement friends relationship PostgreSQL adapter`.
6. Update architecture manifests, module manifests, README, alpha docs, roadmap docs, and AGENTS guidance.
7. Preserve PostgreSQL adapter implementation, SQL execution, runtime behavior, protocol routes, Protobuf source, generated output, migrations, dependencies, event/audit tables, SDK, hosted, distributed runtime, and direct compatibility out of scope.
8. Run focused and full verification.
9. Commit and push if verification passes.

