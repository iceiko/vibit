# Request

Implement `W-0312 Pitaya-aligned cluster event bus source-first map`.

Register `runtime.pitaya_aligned_cluster_event_bus_source_first_map` and expose `node tools/vibit inspect pitaya-cluster-event-bus --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
