# Request

Implement Pitaya-aligned route targeting source-first map.

Register `runtime.pitaya_aligned_route_targeting_source_first_map` and implement `node tools/vibit inspect pitaya-route-targeting --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
