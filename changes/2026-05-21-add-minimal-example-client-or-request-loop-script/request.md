# Request

Continue the project by advancing `W-0186 Add minimal example client or request-loop script`.

The work should add the smallest local developer-facing example or request-loop script that exercises the documented alpha path without adding runtime behavior, startup semantics, protocol routes, Protobuf sources, generated output, migrations, dependencies, release artifacts, production signup, broad product modules, or direct Nakama/Pitaya API compatibility.
