# Plan

1. Add `examples/local-alpha-request-loop.sh` as the minimal local alpha request-loop entry point.
2. Update the runtime runbook and README pointers to reference the script.
3. Record `ADR-0094` and the conversation log for the developer-experience slice.
4. Update architecture manifests and work queue state so `W-0186` is completed and `W-0187` is next ready.
5. Add repository check coverage for `runtime.minimal_example_client_or_request_loop`.
6. Run the script and repository verification commands.
