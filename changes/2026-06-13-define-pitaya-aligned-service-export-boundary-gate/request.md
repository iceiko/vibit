# Request

Define `W-0303 Pitaya-aligned service export boundary gate`.

Register `runtime.pitaya_aligned_service_export_boundary_gate` and define `docs/pitaya-aligned-service-export-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
