# Request

Implement Pitaya-aligned heartbeat and liveness source-first map.

Register `runtime.pitaya_aligned_heartbeat_liveness_source_first_map` and implement `node tools/vibit inspect pitaya-heartbeat-liveness --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
