# Request: Define Currency Wallet Runtime Behavior Gate

## User Request

```text
继续20步
```

## Interpreted Work Item

Advance `W-0299 Define currency wallet runtime behavior gate`.

## Scope

Define the future currency wallet runtime behavior gate after the PostgreSQL adapter implementation.

## Non-Goals

- Do not implement currency wallet runtime behavior.
- Do not add runtime handlers, route dispatch, startup wiring, protocol routes, Protobuf source, generated output, dependencies, migrations, authentication/session behavior, reward integration, inventory integration, purchase behavior, catalog/event tables, payment behavior, hosted surfaces, SDKs, release artifacts, distributed runtime, or direct Nakama/Pitaya API compatibility.
