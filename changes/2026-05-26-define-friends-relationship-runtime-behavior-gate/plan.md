# Plan

1. Read the repository constitution, module instructions, friends lifecycle/repository/adapter artifacts, route-policy/session boundaries, and current next work item.
2. Add the friends relationship runtime behavior gate standard and Simplified Chinese translation.
3. Add ADR-0146, change spec artifacts, and conversation memory.
4. Register `runtime.friends_relationship_runtime_behavior_gate` and add a focused static check.
5. Advance `.arch/work-items.yaml` from `W-0238` to `W-0239 Implement friends relationship runtime behavior`.
6. Update architecture manifests, module manifests, README, alpha docs, roadmap docs, and AGENTS guidance.
7. Preserve runtime behavior implementation, handlers, startup wiring, protocol routes, Protobuf source, generated output, repository/adapter changes, migrations, dependencies, event/audit tables, SDK, hosted, distributed runtime, and direct compatibility out of scope.
8. Run focused and full verification.

