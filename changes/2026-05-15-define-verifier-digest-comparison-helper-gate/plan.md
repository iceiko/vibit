# Plan

1. Add the English and Simplified Chinese verifier digest comparison helper gate standard.
2. Add `ADR-0049` for the gate decision.
3. Add this change spec and conversation log.
4. Update architecture manifests and module manifest with gate markers.
5. Update AGENTS guides with the new boundary.
6. Add `runtime.verifier_digest_comparison_helper_gate` to the rule catalog.
7. Extend `tools/vibit` runtime checks for this gate.
8. Mark `W-0104` and `M-032` complete, then create `M-033` and `W-0105` as the next implementation slice.
9. Run repository verification.
10. Commit and push.
