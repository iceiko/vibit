# Plan

1. Confirm the current blocked work item is `W-0159`.
2. Select `define_active_connection_registry_gate`.
3. Record the selection in manifests and memory.
4. Create `M-088/W-0160` as the next bounded gate-only work item.
5. Preserve ask-first boundaries for implementation, socket close, reconnect, protocol carriers, operations, memory durable session behavior, broader game modules, and direct Nakama/Pitaya compatibility.
