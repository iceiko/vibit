# Request

Implement Pitaya-aligned service registry source-first map.

Register `runtime.pitaya_aligned_service_registry_source_first_map` and implement `node tools/vibit inspect pitaya-service-registry --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
