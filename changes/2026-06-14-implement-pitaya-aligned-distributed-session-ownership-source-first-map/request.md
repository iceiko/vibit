# Request

Implement Pitaya-aligned distributed session ownership source-first map.

Register `runtime.pitaya_aligned_distributed_session_ownership_source_first_map` and implement `node tools/vibit inspect pitaya-distributed-session-ownership --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
