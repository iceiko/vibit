# Plan

1. Stop after `W-0164`.
2. Present candidate next directions.
3. Wait for maintainer selection before adding WebSocket close implementation, protocol logout routes, reconnect/epoch behavior, protocol session carriers, operations posture, broader modules, or direct compatibility.
