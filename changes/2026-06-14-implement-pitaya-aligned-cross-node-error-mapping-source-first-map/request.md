# Request

Implement Pitaya-aligned cross-node error mapping source-first map.

Register `runtime.pitaya_aligned_cross_node_error_mapping_source_first_map` and implement `node tools/vibit inspect pitaya-cross-node-errors --json`.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
