# Request

Define Pitaya-aligned route targeting boundary gate.

Register `runtime.pitaya_aligned_route_targeting_boundary_gate` and define `docs/pitaya-aligned-route-targeting-boundary-gate.md` plus its Simplified Chinese translation.

Do not add runtime behavior, protocol changes, generated output, persistence, dependencies, distributed runtime behavior, or direct compatibility.
